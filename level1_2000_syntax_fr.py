# Syntax documentation
#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : min, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → s = ...  # stored in memory
#   → print(min(...))
"""
Problème 1501 :
Utilisez `default=` avec `min()` à avoid crash on une liste vide set.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → nums = ...  # stored in memory
#   → print(next(...))
"""
Problème 1502 :
Utilisez `default=` avec `next()` à check pour existence de un élément dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → scores = ...  # stored in memory
#   → print(max(...))
"""
Problème 1503 :
Utilisez `default=` dans `max()` à retourner fallback lorsque list est vide.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → letters = ...  # stored in memory
#   → print(next(...))
"""
Problème 1504 :
Utilisez `default=` avec `next()` à retourner un default si no élément satisfies la condition.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → strings = ...  # stored in memory
#   → print(max(...))
"""
Problème 1505 :
Utilisez `default=` dans `max()` avec un custom clé fonction et vide entrée.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → users = ...  # stored in memory
#   → print(next(...))
"""
Problème 1506 :
Utilisez `default=` avec `next()` à safely iterate une condition on une liste de dictionnaires.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → print(min(...))
"""
Problème 1507 :
Utilisez `default=` avec `min()` à handle une liste qui peut be vide.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → scores = ...  # stored in memory
#   → print(max(...))
"""
Problème 1508 :
Utilisez `default=` dans `max()` à get plus haut score ou "No données".
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : iter, next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → print(next(...))
"""
Problème 1509 :
Utilisez `default=` dans `next()` à avoid StopIteration on vide entrée.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → print(max(...))
"""
Problème 1510 :
Utilisez `default=` avec `max()` à trouver max chaîne ou retourner fallback.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1511 :
Utilisez `default=` avec `sorted()` à handle vide iterables et retourner un default liste triée.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → min_age_dict = ...  # stored in memory
#   → print(min_age_dict)
#   → people = ...  # stored in memory
#   → min_age_dict = ...  # stored in memory
#   → print(min_age_dict)
#   → people = ...  # stored in memory
#   → min_age_dict = ...  # stored in memory
#   → print(min_age_dict)
"""
Problème 1512 :
Utilisez `default=` avec `min()` à trouver le minimum valeur dans une liste de dictionnaires by une clé, ou retourner un default.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → max_word = ...  # stored in memory
#   → print(max_word)
#   → words = ...  # stored in memory
#   → max_word = ...  # stored in memory
#   → print(max_word)
#   → words = ...  # stored in memory
#   → max_word = ...  # stored in memory
#   → print(max_word)
"""
Problème 1513 :
Utilisez `default=` avec `max()` à trouver le maximum longueur chaîne dans une liste, ou retourner'No éléments'si vide.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
#   → data = ...  # stored in memory
#   → name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → print(name, city)
"""
Problème 1514 :
Utilisez `dict.get()` avec une valeur par défaut à safely access un dictionnaire imbriqué clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → counter = ...  # stored in memory
#   → for item in items  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from items, runs body, repeats until done
"""
Problème 1515 :
Utilisez `collections.defaultdict` avec `int` as default factory à count occurrences d'éléments dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(data)
"""
Problème 1516 :
Utilisez `dict.setdefault()` à initialize une liste valeur dans un dictionnaire si la clé doesn't exist.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
"""
Problème 1517 :
Utilisez `default=` avec `next()` à get le premier élément de un filtered iterator, ou retourner None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for (category, item) in items  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from items, runs body, repeats until done
"""
Problème 1518 :
Utilisez `collections.defaultdict` avec `list` as default factory à group éléments by category.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → user_info = ...  # stored in memory
#   → name = ...  # stored in memory
#   → print(name)
#   → data = ...  # stored in memory
#   → user_info = ...  # stored in memory
#   → name = ...  # stored in memory
#   → print(name)
#   → data = ...  # stored in memory
#   → user_info = ...  # stored in memory
#   → name = ...  # stored in memory
#   → print(name)
"""
Problème 1519 :
Utilisez `dict.get()` avec un default vide dictionnaire à safely access dictionnaire valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → shortest = ...  # stored in memory
#   → print(shortest)
#   → words = ...  # stored in memory
#   → shortest = ...  # stored in memory
#   → print(shortest)
#   → words = ...  # stored in memory
#   → shortest = ...  # stored in memory
#   → print(shortest)
"""
Problème 1520 :
Utilisez `default=` avec `min()` et une clé fonction à trouver le plus court chaîne, ou retourner une liste vide chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
"""
Problème 1521 :
Utilisez `dict.setdefault()` à créer un dictionnaire imbriqué structure avec default valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → tuples = ...  # stored in memory
#   → max_tuple = ...  # stored in memory
#   → print(max_tuple)
#   → tuples = ...  # stored in memory
#   → max_tuple = ...  # stored in memory
#   → print(max_tuple)
#   → tuples = ...  # stored in memory
#   → max_tuple = ...  # stored in memory
#   → print(max_tuple)
"""
Problème 1522 :
Utilisez `default=` avec `max()` à trouver le maximum valeur dans une liste de tuples by index, ou retourner (0, 0).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for (key, value) in items  # runs body once per item
#   → print(?)
#
# Loop: takes one item from items, runs body, repeats until done
"""
Problème 1523 :
Utilisez `collections.defaultdict` avec `set` as default factory à collect les valeurs uniques per clé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + |
# Fonctions intégrées : set, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
"""
Problème 1524 :
Utilisez `dict.get()` avec une valeur par défaut de 0 à safely somme valeurs de multiple dictionnaires.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
#   → numbers = ...  # stored in memory
#   → first_even = ...  # stored in memory
#   → print(first_even)
"""
Problème 1525 :
Utilisez `default=` avec `next()` à get le premier même nombre de an iterator, ou retourner -1.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(?)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(?)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → print(?)
"""
Problème 1526 :
Utilisez `dict.setdefault()` à append éléments à une liste valeur, creating la liste si it doesn't exist.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → min_age = ...  # stored in memory
#   → print(min_age)
#   → people = ...  # stored in memory
#   → min_age = ...  # stored in memory
#   → print(min_age)
#   → people = ...  # stored in memory
#   → min_age = ...  # stored in memory
#   → print(min_age)
"""
Problème 1527 :
Utilisez `default=` avec `min()` à trouver le minimum age de une liste de personne dictionnaires, ou retourner 0.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → grouped = ...  # stored in memory
#   → for word in words  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from words, runs body, repeats until done
"""
Problème 1528 :
Utilisez `collections.defaultdict` avec `lambda: []` à group strings by leur premier caractère.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → items = ...  # stored in memory
#   → missing = ...  # stored in memory
#   → print(items, missing)
#   → data = ...  # stored in memory
#   → items = ...  # stored in memory
#   → missing = ...  # stored in memory
#   → print(items, missing)
#   → data = ...  # stored in memory
#   → items = ...  # stored in memory
#   → missing = ...  # stored in memory
#   → print(items, missing)
"""
Problème 1529 :
Utilisez `dict.get()` avec un default liste vide à safely access list valeurs de un dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → longest = ...  # stored in memory
#   → print(longest)
#   → words = ...  # stored in memory
#   → longest = ...  # stored in memory
#   → print(longest)
#   → words = ...  # stored in memory
#   → longest = ...  # stored in memory
#   → print(longest)
"""
Problème 1530 :
Utilisez `default=` avec `max()` et une clé fonction à trouver le plus long mot, ou retourner'N/A'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → counter = ...  # stored in memory
#   → counter.setdefault('count', 0)
#   → print(counter)
#   → counter = ...  # stored in memory
#   → counter.setdefault('count', 0)
#   → print(counter)
#   → counter = ...  # stored in memory
#   → counter.setdefault('count', 0)
#   → print(counter)
"""
Problème 1531 :
Utilisez `dict.setdefault()` à initialize un counter dictionnaire avec la valeur par défaut 0.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + > != ** and
# Fonctions intégrées : all, int, next, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n > 1 and all((n % i != 0 for i in range(2, int(n ** 0.5) + 1)))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_prime is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_prime = ...  # stored in memory
#   → print(first_prime)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_prime(...))  # is_prime() runs first → value goes to print()
"""
Problème 1532 :
Utilisez `default=` avec `next()` à get le premier un nombre premier de an iterator, ou retourner None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → char_count = ...  # stored in memory
#   → for char in text  # runs body once per item
#   → print(dict(...))
#
# Loop: takes one item from text, runs body, repeats until done
"""
Problème 1533 :
Utilisez `collections.defaultdict` avec `int` à créer une fréquence counter pour caractères dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + |
# Fonctions intégrées : set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → merged = ...  # stored in memory
#   → print(merged)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → merged = ...  # stored in memory
#   → print(merged)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → merged = ...  # stored in memory
#   → print(merged)
"""
Problème 1534 :
Utilisez `dict.get()` avec une valeur par défaut à safely merge deux dictionnaires, handling manquant clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → students = ...  # stored in memory
#   → min_score = ...  # stored in memory
#   → print(min_score)
#   → students = ...  # stored in memory
#   → min_score = ...  # stored in memory
#   → print(min_score)
#   → students = ...  # stored in memory
#   → min_score = ...  # stored in memory
#   → print(min_score)
"""
Problème 1535 :
Utilisez `default=` avec `min()` à trouver le minimum score de une liste de student records, ou retourner 0.0.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → data.setdefault(...).append('carrot')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → data.setdefault(...).append('carrot')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).append('apple')
#   → data.setdefault(...).append('banana')
#   → data.setdefault(...).append('carrot')
#   → print(data)
"""
Problème 1536 :
Utilisez `dict.setdefault()` à créer un dictionnaire de listes, appending valeurs à existing listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → products = ...  # stored in memory
#   → max_price = ...  # stored in memory
#   → print(max_price)
#   → products = ...  # stored in memory
#   → max_price = ...  # stored in memory
#   → print(max_price)
#   → products = ...  # stored in memory
#   → max_price = ...  # stored in memory
#   → print(max_price)
"""
Problème 1537 :
Utilisez `default=` avec `max()` à trouver le maximum price de une liste de products, ou retourner'No products'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → nested = ...  # stored in memory
#   → for (key1, key2) in data  # runs body once per item
#   → print(?)
#
# Loop: takes one item from data, runs body, repeats until done
"""
Problème 1538 :
Utilisez `collections.defaultdict` avec `lambda: defaultdict(int)` à créer imbriqué counters.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → full_name = ...  # stored in memory
#   → print(full_name)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → full_name = ...  # stored in memory
#   → print(full_name)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → full_name = ...  # stored in memory
#   → print(full_name)
"""
Problème 1539 :
Utilisez `dict.get()` avec un default vide chaîne à safely concatenate valeurs de multiple dictionnaires.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_greater_than_5 = ...  # stored in memory
#   → print(first_greater_than_5)
#   → numbers = ...  # stored in memory
#   → first_greater_than_5 = ...  # stored in memory
#   → print(first_greater_than_5)
#   → numbers = ...  # stored in memory
#   → first_greater_than_5 = ...  # stored in memory
#   → print(first_greater_than_5)
"""
Problème 1540 :
Utilisez `default=` avec `next()` à get le premier élément matching une condition, ou retourner un default objet.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → data.setdefault(...).add('python')
#   → data.setdefault(...).add('programming')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).add('python')
#   → data.setdefault(...).add('programming')
#   → print(data)
#   → data = ...  # stored in memory
#   → data.setdefault(...).add('python')
#   → data.setdefault(...).add('programming')
#   → print(data)
"""
Problème 1541 :
Utilisez `dict.setdefault()` à initialize un ensemble valeur dans un dictionnaire, adding éléments à le set.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → oldest = ...  # stored in memory
#   → print(oldest)
#   → people = ...  # stored in memory
#   → oldest = ...  # stored in memory
#   → print(oldest)
#   → people = ...  # stored in memory
#   → oldest = ...  # stored in memory
#   → print(oldest)
"""
Problème 1542 :
Utilisez `default=` avec `min()` et un custom clé à trouver le oldest personne, ou retourner un default personne dict.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: for
# Operators: +
# Fonctions intégrées : defaultdict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → words = ...  # stored in memory
#   → result = ...  # stored in memory
#   → for (key, value) in words  # runs body once per item
#   → print(?)
#
# Loop: takes one item from words, runs body, repeats until done
"""
Problème 1543 :
Utilisez `collections.defaultdict` avec `str` as default factory à build une chaîne concatenation dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → existing = ...  # stored in memory
#   → existing.extend(?)
#   → print(existing)
#   → data = ...  # stored in memory
#   → existing = ...  # stored in memory
#   → existing.extend(?)
#   → print(existing)
#   → data = ...  # stored in memory
#   → existing = ...  # stored in memory
#   → existing.extend(?)
#   → print(existing)
"""
Problème 1544 :
Utilisez `dict.get()` avec une valeur par défaut de [] à safely extend une liste de dictionnaire valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : datetime, max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → events = ...  # stored in memory
#   → max_date = ...  # stored in memory
#   → print(max_date)
#   → events = ...  # stored in memory
#   → max_date = ...  # stored in memory
#   → print(max_date)
#   → events = ...  # stored in memory
#   → max_date = ...  # stored in memory
#   → print(max_date)
"""
Problème 1545 :
Utilisez `default=` avec `max()` à trouver le maximum timestamp de une liste de events, ou retourner datetime.min.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
#   → data = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(data)
"""
Problème 1546 :
Utilisez `dict.setdefault()` à créer un dictionnaire de dictionnaires avec imbriqué default valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : iter, next()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# evaluates expression
# evaluates expression
# evaluates expression
# evaluates expression
# evaluates expression
# evaluates expression
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — number_gen, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → gen = ...  # stored in memory
#   → first = ...  # stored in memory
#   → empty_gen = ...  # stored in memory
#   → empty_result = ...  # stored in memory
#   → print(first, empty_result)
"""
Problème 1547 :
Utilisez `default=` avec `next()` à safely get le premier élément de un generator, ou retourner une valeur sentinelle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : defaultdict, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → stats = ...  # stored in memory
#   → print(dict(...))
"""
Problème 1548 :
Utilisez `collections.defaultdict` avec `lambda: {'count': 0,'total': 0}` à créer statistics dictionnaires.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# if ... → only one branch executes based on condition
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → if condition  # evaluates → runs the True branch
#   → items = ...  # stored in memory
#   → items.append('apple')
#   → print(data)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1549 :
Utilisez `dict.get()` avec un default l'appel de fonction à lazily initialize dictionnaire valeurs seulement lorsque accessed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
"""
Problème 1550 :
Write un Pythonic façon à Utilisez `functools.reduce()` à calculate le produit de tous nombres dans une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : multiply_by_5, partial()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → multiply_by_5 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → multiply_by_5 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → multiply_by_5 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1551 :
Write un Pythonic façon à Utilisez `functools.partial()` à créer une fonction qui multiplie un nombre by un fixed valeur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: + - <
# Fonctions intégrées : lru_cache()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return n
#   → return n
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only fibonacci is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(fibonacci(...))  # fibonacci() runs first → value goes to print()
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(fibonacci(...))  # fibonacci() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1552 :
Write un Pythonic façon à Utilisez `functools.lru_cache()` à memoize un recursive Fibonacci fonction.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, permutations()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → perms = ...  # stored in memory
#   → print(perms)
#   → items = ...  # stored in memory
#   → perms = ...  # stored in memory
#   → print(perms)
#   → items = ...  # stored in memory
#   → perms = ...  # stored in memory
#   → print(perms)
"""
Problème 1553 :
Write un Pythonic façon à Utilisez `itertools.permutations()` à générer tous permutations de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, product()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → cartesian = ...  # stored in memory
#   → print(cartesian)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → cartesian = ...  # stored in memory
#   → print(cartesian)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → cartesian = ...  # stored in memory
#   → print(cartesian)
"""
Problème 1554 :
Write un Pythonic façon à Utilisez `itertools.product()` à générer le Cartesian produit des deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : cycle, next, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → colors = ...  # stored in memory
#   → color_cycle = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → colors = ...  # stored in memory
#   → color_cycle = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → colors = ...  # stored in memory
#   → color_cycle = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
"""
Problème 1555 :
Write un Pythonic façon à Utilisez `itertools.cycle()` à cycle à travers une liste indefinitely.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, repeat()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → pass
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → repeated = ...  # stored in memory
#   → print(repeated)
#   → main()  # jumps into main(), runs body, returns
"""
Problème 1556 :
Write un Pythonic façon à Utilisez `itertools.repeat()` à créer an iterator qui repeats une valeur un spécifié nombre de fois.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : islice, list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → numbers = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
#   → numbers = ...  # stored in memory
#   → first_5 = ...  # stored in memory
#   → print(first_5)
"""
Problème 1557 :
Write un Pythonic façon à Utilisez `itertools.islice()` à get les n premiers éléments de an iterator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : iter, list, tee()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(list(...), list(...))
"""
Problème 1558 :
Write un Pythonic façon à Utilisez `itertools.tee()` à split an iterator dans multiple independent iterators.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : accumulate, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → cumulative = ...  # stored in memory
#   → print(cumulative)
#   → numbers = ...  # stored in memory
#   → cumulative = ...  # stored in memory
#   → print(cumulative)
#   → numbers = ...  # stored in memory
#   → cumulative = ...  # stored in memory
#   → print(cumulative)
"""
Problème 1559 :
Write un Pythonic façon à Utilisez `itertools.accumulate()` à calculate cumulative sums de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : Person()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → person = ...  # stored in memory
#   → print(person)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))
"""
Problème 1560 :
Écrivez un programme Python en utilisant `dataclasses` à créer un simple personne class avec nom et age fields.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : Student, field()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Student are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → student = ...  # stored in memory
#   → print(student)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Student(...))
"""
Problème 1561 :
Écrivez un programme Python en utilisant `dataclasses.field()` à set default valeurs dans un dataclass.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : Point, dataclass()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Point are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → point = ...  # stored in memory
#   → print(point)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Point(...))
"""
Problème 1562 :
Écrivez un programme Python en utilisant `@dataclass(frozen=True)` à créer an immutable dataclass.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : reduce, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → from functools import reduce
#   → import operator
#   → return reduce(operator.add, numbers, 0)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_numbers(...))  # sum_numbers() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done
"""
Problème 1563 :
Write un Python fonction avec tapez hints en utilisant `typing.List` à specify une fonction qui prend une liste d'entiers.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: is
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Hello, Guest!'
#   → return 'Hello, Guest!'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only greet is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(greet(...), greet(...))  # greet() runs first → value goes to print()
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(greet(...))  # greet() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1564 :
Write un Python fonction avec tapez hints en utilisant `typing.Optional` à indicate un paramètre qui peut be None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(value)
#   → return str(value)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only process_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(process_value(...), process_value(...))  # process_value() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process_value(...))  # process_value() runs first → value goes to print()
"""
Problème 1565 :
Write un Python fonction avec tapez hints en utilisant `typing.Union` à accept multiple tapez.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: >
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → numbers = ...  # stored in memory
#   → numbers = ...  # stored in memory
#
# Loop: check condition → if True run body → check again
"""
Problème 1566 :
Write un Pythonic façon à Utilisez le walrus operator (`:=`) à assign et check une valeur dans un tandis que loop condition.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: * >
# Fonctions intégrées : pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pow(x, 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only expensive_function is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → x = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(expensive_function(...))  # expensive_function() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1567 :
Write un Pythonic façon à Utilisez le walrus operator (`:=`) dans une liste comprehension à avoid repeated fonction appelle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Starting...'
#   → return 'Starting...'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only handle_command is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(handle_command(...))  # handle_command() runs first → value goes to print()
#   → cmd = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(handle_command(...))  # handle_command() runs first → value goes to print()
"""
Problème 1568 :
Écrivez un programme Python en utilisant `match/case` statements à handle different chaîne motifs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Origin'
#   → return 'Origin'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only process_point is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(process_point(...))  # process_point() runs first → value goes to print()
#   → point = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process_point(...))  # process_point() runs first → value goes to print()
"""
Problème 1569 :
Écrivez un programme Python en utilisant `match/case` avec motif matching à destructure un tuple.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Operators: * **
# Fonctions intégrées : Circle()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Circle are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → circle = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Circle(...))
"""
Problème 1570 :
Write un Python class en utilisant `@property` decorator à créer un computed attribute.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Keywords: if/else
# Operators: <
# Fonctions intégrées : Temperature, ValueError()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Temperature are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → temp = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Temperature(...))
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1571 :
Write un Python class en utilisant `@property.setter` à validate et set un property valeur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Book, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Book are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → book = ...  # stored in memory
#   → print(str(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Book(...))
"""
Problème 1572 :
Write un Python class implementing `__str__` magic méthode à customize chaîne representation.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Point, repr()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Point are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → point = ...  # stored in memory
#   → print(repr(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Point(...))
"""
Problème 1573 :
Write un Python class implementing `__repr__` magic méthode à provide un developer-friendly representation.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Keywords: if/else
# Operators: == and
# Fonctions intégrées : Person, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1574 :
Write un Python class implementing `__eq__` magic méthode à define equality comparison.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: == and
# Fonctions intégrées : Point, hash, isinstance, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Point are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → point = ...  # stored in memory
#   → points_set = ...  # stored in memory
#   → print(len(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Point(...))
"""
Problème 1575 :
Write un Python class implementing `__hash__` magic méthode à make un objet hashable.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Stack, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Stack are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → stack = ...  # stored in memory
#   → stack.push(1)
#   → stack.push(2)
#   → print(len(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Stack(...))
"""
Problème 1576 :
Write un Python class implementing `__len__` magic méthode à make un objet work avec `len()`.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : MyList()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyList are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyList(...))
"""
Problème 1577 :
Write un Python class implementing `__getitem__` magic méthode à enable indexing.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Operators: -
# Fonctions intégrées : Timer, range, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Timer are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Timer(...))
"""
Problème 1578 :
Écrivez un programme Python en utilisant un custom context manager class avec `__enter__` et `__exit__` méthodes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : ExitStack, Resource()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Resource are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Resource(...))
"""
Problème 1579 :
Write un Pythonic façon à Utilisez `contextlib.ExitStack` à manage multiple context managers.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → path = ...  # stored in memory
#   → exists = ...  # stored in memory
#   → print(?)
"""
Problème 1580 :
Write un Pythonic façon à Utilisez `pathlib.Path` à vérifier si un fichier existe.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → path = ...  # stored in memory
#   → parent = ...  # stored in memory
#   → print(parent)
#   → path = ...  # stored in memory
#   → parent = ...  # stored in memory
#   → print(parent)
#   → path = ...  # stored in memory
#   → parent = ...  # stored in memory
#   → print(parent)
"""
Problème 1581 :
Write un Pythonic façon à Utilisez `pathlib.Path` à get le parent directory de un fichier path.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: /
# Fonctions intégrées : Path()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → base = ...  # stored in memory
#   → full_path = ...  # stored in memory
#   → print(full_path)
#   → base = ...  # stored in memory
#   → full_path = ...  # stored in memory
#   → print(full_path)
#   → base = ...  # stored in memory
#   → full_path = ...  # stored in memory
#   → print(full_path)
"""
Problème 1582 :
Write un Pythonic façon à Utilisez `pathlib.Path` à join path components.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → data = ...  # stored in memory
#   → json_string = ...  # stored in memory
#   → print(json_string)
#   → data = ...  # stored in memory
#   → json_string = ...  # stored in memory
#   → print(json_string)
#   → data = ...  # stored in memory
#   → json_string = ...  # stored in memory
#   → print(json_string)
"""
Problème 1583 :
Écrivez un programme Python en utilisant `json.dumps()` à convert un Python dictionnaire à un JSON chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1584 :
Écrivez un programme Python en utilisant `json.loads()` à parse un JSON chaîne dans un Python dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → new_text = ...  # stored in memory
#   → print(new_text)
#   → text = ...  # stored in memory
#   → new_text = ...  # stored in memory
#   → print(new_text)
#   → text = ...  # stored in memory
#   → new_text = ...  # stored in memory
#   → print(new_text)
"""
Problème 1585 :
Écrivez un programme Python en utilisant `re.sub()` à replace tous occurrences de un motif dans une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
"""
Problème 1586 :
Écrivez un programme Python en utilisant `re.finditer()` à trouver tous correspond de un motif et get leur positions.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Keywords: if/else
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# if ... → only one branch executes based on condition
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → match = ...  # stored in memory
#   → if condition  # evaluates → runs the True branch
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1587 :
Écrivez un programme Python en utilisant nommée groups dans regex à extraire spécifique parts de une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : OrderedDict, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → od = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(list(...))
"""
Problème 1588 :
Write un Pythonic façon à Utilisez `collections.OrderedDict` à maintain insertion order dans un dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : ChainMap, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → chain = ...  # stored in memory
#   → print(dict(...))
"""
Problème 1589 :
Write un Pythonic façon à Utilisez `collections.ChainMap` à combiner multiple dictionnaires dans un seul mapping.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Point, namedtuple()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → Point = ...  # stored in memory
#   → p = ...  # stored in memory
#   → print(?, ?)
#   → Point = ...  # stored in memory
#   → p = ...  # stored in memory
#   → print(?, ?)
#   → Point = ...  # stored in memory
#   → p = ...  # stored in memory
#   → print(?, ?)
"""
Problème 1590 :
Write un Pythonic façon à Utilisez `collections.namedtuple` à créer un simple données structure avec nommée fields.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → matrix = ...  # stored in memory
#   → print(matrix)
#   → matrix = ...  # stored in memory
#   → print(matrix)
#   → matrix = ...  # stored in memory
#   → print(matrix)
"""
Problème 1591 :
Write un Pythonic façon à Utilisez une liste imbriquée comprehensions à créer un matrix.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % == and
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
#   → numbers = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
#   → numbers = ...  # stored in memory
#   → filtered = ...  # stored in memory
#   → print(filtered)
"""
Problème 1592 :
Write un Pythonic façon à Utilisez une liste comprehension avec multiple conditions à filtrer éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : Person, attrgetter, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → sorted_people = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))
"""
Problème 1593 :
Write un Pythonic façon à Utilisez `operator.attrgetter()` à trier une liste de objets by an attribute.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : Person, list, map, methodcaller()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → people = ...  # stored in memory
#   → greetings = ...  # stored in memory
#   → print(greetings)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))
"""
Problème 1594 :
Write un Pythonic façon à Utilisez `operator.methodcaller()` à call une méthode on multiple objets.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1595 :
Write un Pythonic façon à Utilisez `str.removeprefix()` à remove un prefix de une chaîne si it existe.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1596 :
Write un Pythonic façon à Utilisez `str.removesuffix()` à remove un suffix de une chaîne si it existe.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
"""
Problème 1597 :
Write un Pythonic façon à Utilisez `str.rsplit()` à split une chaîne de le right, limiting le nombre de divise.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → original = ...  # stored in memory
#   → copied = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(original, copied)
#   → original = ...  # stored in memory
#   → copied = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(original, copied)
#   → original = ...  # stored in memory
#   → copied = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(original, copied)
"""
Problème 1598 :
Write un Pythonic façon à Utilisez `list.copy()` à créer un shallow copy de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → my_list.clear()
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.clear()
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.clear()
#   → print(my_list)
"""
Problème 1599 :
Write un Pythonic façon à Utilisez `list.clear()` à remove tous éléments de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → item = ...  # stored in memory
#   → print(item, d)
#   → d = ...  # stored in memory
#   → item = ...  # stored in memory
#   → print(item, d)
#   → d = ...  # stored in memory
#   → item = ...  # stored in memory
#   → print(item, d)
"""
Problème 1600 :
Write un Pythonic façon à Utilisez `dict.popitem()` à remove et retourner le dernier la paire clé-valeur de un dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → print(dict1)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → print(dict1)
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → dict1.update(dict2)
#   → print(dict1)
"""
Problème 1601 :
Write un Pythonic façon à Utilisez `dict.update()` à merge un autre dictionnaire dans an existing l'une.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → keys = ...  # stored in memory
#   → print(keys)
#   → d = ...  # stored in memory
#   → keys = ...  # stored in memory
#   → print(keys)
#   → d = ...  # stored in memory
#   → keys = ...  # stored in memory
#   → print(keys)
"""
Problème 1602 :
Write un Pythonic façon à Utilisez `dict.keys()` à iterate over dictionnaire clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → values = ...  # stored in memory
#   → print(values)
#   → d = ...  # stored in memory
#   → values = ...  # stored in memory
#   → print(values)
#   → d = ...  # stored in memory
#   → values = ...  # stored in memory
#   → print(values)
"""
Problème 1603 :
Write un Pythonic façon à Utilisez `dict.values()` à get tous valeurs de un dictionnaire as un view.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → d = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)
#   → d = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)
#   → d = ...  # stored in memory
#   → items = ...  # stored in memory
#   → print(items)
"""
Problème 1604 :
Write un Pythonic façon à Utilisez `dict.items()` à iterate over les paires clé-valeur dans un dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Keywords: try/except
# Fonctions intégrées : CustomError()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes CustomError are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(CustomError(...))
"""
Problème 1605 :
Écrivez un programme Python à define un custom exception class qui inherits de Exception.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
# Fonctions intégrées : TypeError, int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
"""
Problème 1606 :
Écrivez un programme Python en utilisant exception chaining avec `raise...de` à preserve le original exception.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1607 :
Write un Pythonic façon à Utilisez `contextlib.redirect_stdout` à capture print sortie à une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1608 :
Write un Pythonic façon à Utilisez `functools.wraps` à preserve fonction metadata dans un decorator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : accumulate, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → pass
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — main, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → cumulative_products = ...  # stored in memory
#   → print(cumulative_products)
#   → main()  # jumps into main(), runs body, returns
"""
Problème 1609 :
Write un Pythonic façon à Utilisez `itertools.accumulate()` avec un custom fonction à calculate cumulative products.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : combinations_with_replacement, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → items = ...  # stored in memory
#   → combs = ...  # stored in memory
#   → print(combs)
#   → items = ...  # stored in memory
#   → combs = ...  # stored in memory
#   → print(combs)
#   → items = ...  # stored in memory
#   → combs = ...  # stored in memory
#   → print(combs)
"""
Problème 1610 :
Write un Pythonic façon à Utilisez `itertools.combinations_with_replacement()` à générer combinations allowing repeated éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
# Fonctions intégrées : dropwhile, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1611 :
Write un Pythonic façon à Utilisez `itertools.dropwhile()` à skip éléments jusqu'à une condition est false.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
# Fonctions intégrées : list, takewhile()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1612 :
Write un Pythonic façon à Utilisez `itertools.takewhile()` à take éléments de an iterator tandis que une condition est vrai.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, starmap()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → pairs = ...  # stored in memory
#   → products = ...  # stored in memory
#   → print(products)
#   → pairs = ...  # stored in memory
#   → products = ...  # stored in memory
#   → print(products)
#   → pairs = ...  # stored in memory
#   → products = ...  # stored in memory
#   → print(products)
"""
Problème 1613 :
Write un Pythonic façon à Utilisez `itertools.starmap()` à apply une fonction à arguments unpacked de tuples.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + -
# Fonctions intégrées : cmp_to_key, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.sub(y, x)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only compare is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → sorted_nums = ...  # stored in memory
#   → print(sorted_nums)
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(compare(...))  # compare() runs first → value goes to print()
"""
Problem 1614:
def compare(x, y):  # defines a function with parameters x, y
    return y - x  # return sends the result back to the caller
compare(x, y)  # arguments evaluated first, then function body runs
print(compare(3, 14))  # compare() runs first, then print() outputs the result
y - x  # y → then x → then -
y + ?  # y → then ? → then +
from functools import cmp_to_key
def compare(x, y):  # stored in memory, not executed yet
numbers = ...  # assigns a value to the variable
sorted_nums = ...  # assigns a value to the variable
print(sorted_nums)
def compare(x, y):  # stored in memory, not executed yet
def compare(x, y):  # stored in memory, not executed yet
x = ...  # assigns a value to the variable
y = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(compare(...))  # compare() runs with x=3, y=14


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'Unknown type: {type(value).__name__}'
#   → return f'Unknown type: {type(value).__name__}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — _, process are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(process(...), process(...))  # process() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process(...))  # process() runs first → value goes to print()
"""
Problème 1615 :
Write un Pythonic façon à Utilisez `functools.singledispatch` à créer une fonction avec méthode overloading based on argument tapez.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum(scores.values())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only process_scores is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → scores = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(process_scores(...))  # process_scores() runs first → value goes to print()
"""
Problem 1616:
def process_scores(scores):  # defines a function with parameters scores
    return sum(scores.values())  # return sends the result back to the caller
process_scores(?)  # arguments evaluated first, then function body runs
process_scores(scores)  # arguments evaluated first, then function body runs
from typing import Dict
def process_scores(scores):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def process_scores(scores):  # stored in memory, not executed yet
def process_scores(scores):  # stored in memory, not executed yet
scores = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(process_scores(...))  # process_scores() runs with scores=[4, 1, 8, 6, 3]


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_name_age is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → ? = ...  # stored in memory
#   → print(?)
#   → result = ...  # stored in memory
#   → print(result)
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1617 :
Écrivez un programme Python en utilisant `typing.Tuple` à specify une fonction qui retourne un tuple de spécifique tapez.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : op()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return op(x, y)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only apply_operation is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → x = ...  # stored in memory
#   → y = ...  # stored in memory
#   → op = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(apply_operation(...))  # apply_operation() runs first → value goes to print()
"""
Problem 1618:
def apply_operation(x, y, op):  # defines a function with parameters x, y, op
    return op(x, y)  # return sends the result back to the caller
apply_operation(5, 3, ?)  # arguments evaluated first, then function body runs
apply_operation(x, y, op)  # arguments evaluated first, then function body runs
print(apply_operation(3, 14, 25))  # apply_operation() runs first, then print() outputs the result
a * b  # a → then b → then *
from typing import Callable
def apply_operation(x, y, op):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def apply_operation(x, y, op):  # stored in memory, not executed yet
def apply_operation(x, y, op):  # stored in memory, not executed yet
x = ...  # assigns a value to the variable
y = ...  # assigns a value to the variable
op = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(apply_operation(...))  # apply_operation() runs with x=3, y=14, op=25


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: < > ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Negative'
#   → return 'Negative'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only categorize is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(categorize(...), categorize(...), categorize(...))  # categorize() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(categorize(...))  # categorize() runs first → value goes to print()
"""
Problème 1619 :
Écrivez un programme Python en utilisant `match/case` avec guards (si conditions) à add additional motif matching logic.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'OK'
#   → return 'OK'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only handle_status is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(handle_status(...), handle_status(...))  # handle_status() runs first → value goes to print()
#   → code = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(handle_status(...))  # handle_status() runs first → value goes to print()
"""
Problème 1620 :
Écrivez un programme Python en utilisant `match/case` avec `_` as un catch-tous motif à handle unmatched cases.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : Person()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes Person are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → p = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(Person(...))
"""
Problème 1621 :
Write un Python class en utilisant `@property.deleter` à define behavior lorsque un property est deleted.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : MyDict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyDict are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_dict = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyDict(...))
"""
Problème 1622 :
Write un Python class implementing `__setitem__` magic méthode à enable élément assignment.\
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Fonctions intégrées : MyList()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyList are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyList(...))
"""
Problème 1623 :
Write un Python class implementing `__delitem__` magic méthode à enable élément deletion.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# Operators: in
# Fonctions intégrées : MyContainer()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes MyContainer are defined — methods stored in memory
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → container = ...  # stored in memory
#   → print(?)
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(MyContainer(...))
"""
Problème 1624 :
Write un Python class implementing `__contains__` magic méthode à enable `in` operator support.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: + <=
# Fonctions intégrées : CountDown, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# Classes CountDown are defined — methods stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → counter = ...  # stored in memory
#   → print(list(...))
#   → obj = ...  # stored in memory
#   → print(obj)
#   → print(CountDown(...))
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1625 :
Write un Python class implementing `__iter__` et `__next__` magic méthodes à créer un custom iterator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# evaluates expression
# evaluates expression
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only my_context is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Entering context')
#   → print('Exiting context')
#   → print('Entering context')
#   → print('Exiting context')
"""
Problème 1626 :
Écrivez un programme Python en utilisant `contextlib.contextmanager` decorator à créer un context manager de un generator fonction.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1627 :
Write un Pythonic façon à Utilisez `pathlib.Path.read_text()` à read le entier contents de un fichier texte.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1628 :
Write un Pythonic façon à Utilisez `pathlib.Path.write_text()` à write text à un fichier.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1629 :
Write un Pythonic façon à Utilisez `pathlib.Path.glob()` à trouver tous files matching un motif.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1630 :
Écrivez un programme Python en utilisant `json.dump()` à write un Python objet à un fichier JSON.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1631 :
Écrivez un programme Python en utilisant `json.load()` à read un fichier JSON et parse it dans un Python objet.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → pattern = ...  # stored in memory
#   → text = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → pattern = ...  # stored in memory
#   → text = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
#   → pattern = ...  # stored in memory
#   → text = ...  # stored in memory
#   → matches = ...  # stored in memory
#   → print(matches)
"""
Problème 1632 :
Écrivez un programme Python en utilisant `re.compile()` à créer un compiled regex motif pour repeated Utilisez.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
#   → text = ...  # stored in memory
#   → parts = ...  # stored in memory
#   → print(parts)
"""
Problème 1633 :
Écrivez un programme Python en utilisant `re.split()` à split une chaîne by un regex motif.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : OrderedDict, list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → od = ...  # stored in memory
#   → od.move_to_end('a')
#   → print(list(...))
"""
Problème 1634 :
Write un Pythonic façon à Utilisez `collections.OrderedDict.move_to_end()` à move une clé à le end de an ordered dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : ChainMap, dict()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → dict1 = ...  # stored in memory
#   → dict2 = ...  # stored in memory
#   → chain = ...  # stored in memory
#   → new_chain = ...  # stored in memory
#   → print(dict(...))
"""
Problème 1635 :
Write un Pythonic façon à Utilisez `collections.ChainMap.new_child()` à add un nouveau dictionnaire à le début de un ChainMap.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : Point, namedtuple()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → Point = ...  # stored in memory
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(p1, p2)
#   → Point = ...  # stored in memory
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(p1, p2)
#   → Point = ...  # stored in memory
#   → p1 = ...  # stored in memory
#   → p2 = ...  # stored in memory
#   → print(p1, p2)
"""
Problème 1636 :
Write un Pythonic façon à Utilisez `collections.namedtuple._replace()` à créer une nouvelle namedtuple avec updated fields.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → table = ...  # stored in memory
#   → print(table)
#   → table = ...  # stored in memory
#   → print(table)
#   → table = ...  # stored in memory
#   → print(table)
"""
Problème 1637 :
Write un Pythonic façon à Utilisez un dictionnaire comprehension avec imbriqué loops à créer un multiplication table dictionnaire.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → strings = ...  # stored in memory
#   → unique_chars = ...  # stored in memory
#   → print(sorted(...))
"""
Problème 1638 :
Write un Pythonic façon à Utilisez un ensemble comprehension à créer un ensemble de unique caractères de multiple strings.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → numbers = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
#   → numbers = ...  # stored in memory
#   → total = ...  # stored in memory
#   → print(total)
"""
Problème 1639 :
Write un Pythonic façon à Utilisez `operator.add` avec `functools.reduce()` à somme une liste de nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : reduce()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
#   → numbers = ...  # stored in memory
#   → product = ...  # stored in memory
#   → print(product)
"""
Problème 1640 :
Write un Pythonic façon à Utilisez `operator.mul` avec `functools.reduce()` à calculate le produit de une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
#
# Execution starts at the top-level code (outside def/class):
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → print(?)
"""
Problème 1641 :
Écrivez un programme Python en utilisant `str.casefold()` à perform cas-insensitive chaîne comparison.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → encoded = ...  # stored in memory
#   → print(encoded)
#   → text = ...  # stored in memory
#   → encoded = ...  # stored in memory
#   → print(encoded)
#   → text = ...  # stored in memory
#   → encoded = ...  # stored in memory
#   → print(encoded)
"""
Problème 1642 :
Écrivez un programme Python en utilisant `str.encode()` à convert une chaîne à bytes en utilisant UTF-8 encoding.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → bytes_data = ...  # stored in memory
#   → decoded = ...  # stored in memory
#   → print(decoded)
#   → bytes_data = ...  # stored in memory
#   → decoded = ...  # stored in memory
#   → print(decoded)
#   → bytes_data = ...  # stored in memory
#   → decoded = ...  # stored in memory
#   → print(decoded)
"""
Problème 1643 :
Écrivez un programme Python en utilisant `bytes.decode()` à convert bytes back à une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → my_list.extend(?)
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.extend(?)
#   → print(my_list)
#   → my_list = ...  # stored in memory
#   → my_list.extend(?)
#   → print(my_list)
"""
Problème 1644 :
Write un Pythonic façon à Utilisez `list.extend()` à add multiple éléments de an iterable à une liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → print(index)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → print(index)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → print(index)
"""
Problème 1645 :
Write un Pythonic façon à Utilisez `list.index()` à trouver le index de le premier occurrence de un élément.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → keys = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
#   → keys = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
#   → keys = ...  # stored in memory
#   → d = ...  # stored in memory
#   → print(d)
"""
Problème 1646 :
Write un Pythonic façon à Utilisez `dict.fromkeys()` à créer un dictionnaire avec clés de une liste et une valeur par défaut.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
# Operators: /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
"""
Problème 1647 :
Écrivez un programme Python en utilisant un try-except-else block où le else clause exécute seulement si no exception occurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
# Operators: /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
"""
Problème 1648 :
Écrivez un programme Python en utilisant un try-except-finally block où finally always exécute regardless de exceptions.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1649 :
Write un Pythonic façon à Utilisez `contextlib.suppress()` avec multiple exception tapez à ignore multiple tapez de errors.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + -
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.sub(a, b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only subtract_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(subtract_numbers(...))  # subtract_numbers() runs first → value goes to print()
"""
Problème 1650 :
Écrivez une fonction Python appelée `subtract_numbers` qui prend deux nombres comme paramètres et retourne leur différence.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * /
# Fonctions intégrées : Fraction, float()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → from fractions import Fraction
#   → return float(Fraction(a, b))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divide_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divide_numbers(...))  # divide_numbers() runs first → value goes to print()
"""
Problème 1651 :
Écrivez une fonction Python appelée `divide_numbers` qui prend deux nombres comme paramètres et retourne leur quotient.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % * - //
# Fonctions intégrées : divmod()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return divmod(a, b)[1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_remainder is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_remainder(...))  # calculate_remainder() runs first → value goes to print()
"""
Problème 1652 :
Écrivez une fonction Python appelée `calculate_remainder` qui prend deux nombres comme paramètres et retourne le reste lorsque le premier nombre est divided by le deuxième.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : pow()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import math
#   → return math.pow(base, exponent)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_power is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → base = ...  # stored in memory
#   → exponent = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_power(...))  # calculate_power() runs first → value goes to print()
"""
Problème 1653 :
Écrivez une fonction Python appelée `calculate_power` qui prend un base et exponent comme paramètres et retourne le base raised à le exponent.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 3.14 * radius * radius
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_circle_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → radius = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_circle_area(...))  # calculate_circle_area() runs first → value goes to print()
"""
Problème 1654 :
Écrivez une fonction Python appelée `calculate_circle_area` qui prend le radius comme paramètre et retourne le area de un circle (Utilisez 3.14 pour pi).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: *
# Fonctions intégrées : pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pow(length, width)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_rectangle_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_rectangle_area(...))  # calculate_rectangle_area() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1655 :
Écrivez une fonction Python appelée `calculate_rectangle_area` qui prend longueur et width comme paramètres et retourne le area de un rectangle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 0.5 * base * height
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_triangle_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → base = ...  # stored in memory
#   → height = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_triangle_area(...))  # calculate_triangle_area() runs first → value goes to print()
"""
Problème 1656 :
Écrivez une fonction Python appelée `calculate_triangle_area` qui prend base et height comme paramètres et retourne le area de un triangle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * + /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return celsius * 9 / 5 + 32
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_celsius_to_fahrenheit is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → celsius = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_celsius_to_fahrenheit(...))  # convert_celsius_to_fahrenheit() runs first → value goes to print()
"""
Problème 1657 :
Écrivez une fonction Python appelée `convert_celsius_to_fahrenheit` qui prend un temperature dans Celsius et retourne it converted à Fahrenheit.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * - /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (fahrenheit - 32) * 5 / 9
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_fahrenheit_to_celsius is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → fahrenheit = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_fahrenheit_to_celsius(...))  # convert_fahrenheit_to_celsius() runs first → value goes to print()
"""
Problème 1658 :
Écrivez une fonction Python appelée `convert_fahrenheit_to_celsius` qui prend un temperature dans Fahrenheit et retourne it converted à Celsius.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: > <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not number <= 0
#   → return not number <= 0
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_positive is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_positive(...), is_positive(...))  # is_positive() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_positive(...))  # is_positive() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1659 :
Écrivez une fonction Python appelée `is_positive` qui prend un nombre en entrée et retourne vrai si le nombre est positif, et False sinon.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: < >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not number >= 0
#   → return not number >= 0
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only is_negative is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(is_negative(...), is_negative(...))  # is_negative() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(is_negative(...))  # is_negative() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1660 :
Écrivez une fonction Python appelée `is_negative` qui prend un nombre en entrée et retourne vrai si le nombre est négatif, et False sinon.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text[0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_char(...))  # get_first_char() runs first → value goes to print()
"""
Problème 1661 :
Écrivez une fonction Python appelée `get_first_char` qui prend une chaîne en entrée et retourne le premier caractère de la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text[-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_char(...))  # get_last_char() runs first → value goes to print()
"""
Problème 1662 :
Écrivez une fonction Python appelée `get_last_char` qui prend une chaîne en entrée et retourne le dernier caractère de la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text[len(text) // 2]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_char(...))  # get_middle_char() runs first → value goes to print()
"""
Problème 1663 :
Écrivez une fonction Python appelée `get_middle_char` qui prend une chaîne en entrée et retourne le middle caractère. si la chaîne un même longueur, retourner le caractère at index longueur//2.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: *
# Fonctions intégrées : pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pow(text, n)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only repeat_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(repeat_string(...))  # repeat_string() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1664 :
Écrivez une fonction Python appelée `repeat_string` qui prend une chaîne et un nombre n, et retourne la chaîne repeated n fois.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: + <=
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_first_last is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_first_last(...))  # swap_first_last() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1665 :
Écrivez une fonction Python appelée `swap_first_last` qui prend une chaîne en entrée et retourne une nouvelle chaîne avec le premier et dernier caractères swapped.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text[1:]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_first_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_first_char(...))  # remove_first_char() runs first → value goes to print()
"""
Problème 1666 :
Écrivez une fonction Python appelée `remove_first_char` qui prend une chaîne en entrée et retourne la chaîne avec le premier caractère removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text[:-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_last_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_last_char(...))  # remove_last_char() runs first → value goes to print()
"""
Problème 1667 :
Écrivez une fonction Python appelée `remove_last_char` qui prend une chaîne en entrée et retourne la chaîne avec le dernier caractère removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.split()[0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_word(...))  # get_first_word() runs first → value goes to print()
"""
Problème 1668 :
Écrivez une fonction Python appelée `get_first_word` qui prend une chaîne de mots séparés by spaces et retourne le premier mot.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.split()[-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_word(...))  # get_last_word() runs first → value goes to print()
"""
Problème 1669 :
Écrivez une fonction Python appelée `get_last_word` qui prend une chaîne de mots séparés by spaces et retourne le dernier mot.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return len(text.replace(' ', ''))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_characters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_characters(...))  # count_characters() runs first → value goes to print()
"""
Problème 1670 :
Écrivez une fonction Python appelée `count_characters` qui prend une chaîne en entrée et retourne le nombre de caractères dans la chaîne (excluding spaces).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return new_list
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_to_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_to_list(...))  # add_to_list() runs first → value goes to print()
"""
Problème 1671 :
Écrivez une fonction Python appelée `add_to_list` qui prend une liste et un élément, et retourne une nouvelle liste avec le élément added at le end.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_element(...))  # get_first_element() runs first → value goes to print()
"""
Problème 1672 :
Écrivez une fonction Python appelée `get_first_element` qui prend une liste et retourne le premier élément de la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_element(...))  # get_last_element() runs first → value goes to print()
"""
Problème 1673 :
Écrivez une fonction Python appelée `get_last_element` qui prend une liste et retourne le dernier élément de la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[len(my_list) // 2]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_element(...))  # get_middle_element() runs first → value goes to print()
"""
Problème 1674 :
Écrivez une fonction Python appelée `get_middle_element` qui prend une liste et retourne le middle élément. si la liste un même longueur, retourner le élément at index longueur//2.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[1:]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_first_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_first_element(...))  # remove_first_element() runs first → value goes to print()
"""
Problème 1675 :
Écrivez une fonction Python appelée `remove_first_element` qui prend une liste et retourne une nouvelle liste avec le premier élément removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[:-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_last_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_last_element(...))  # remove_last_element() runs first → value goes to print()
"""
Problème 1676 :
Écrivez une fonction Python appelée `remove_last_element` qui prend une liste et retourne une nouvelle liste avec le dernier élément removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[start:end]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_list_slice is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_list_slice(...))  # get_list_slice() runs first → value goes to print()
"""
Problème 1677 :
Écrivez une fonction Python appelée `get_list_slice` qui prend une liste, start index, et end index, et retourne un slice de la liste de start à end.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.add(list1, list2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only combine_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(combine_lists(...))  # combine_lists() runs first → value goes to print()
"""
Problème 1678 :
Écrivez une fonction Python appelée `combine_lists` qui prend deux listes et retourne une nouvelle liste contenant tous éléments de les deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x * 2 for x in numbers]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply_list(...))  # multiply_list() runs first → value goes to print()
"""
Problème 1679 :
Écrivez une fonction Python appelée `multiply_list` qui prend une liste de nombres et retourne une nouvelle liste où chaque nombre est multiplied by 2.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x + 1 for x in numbers]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_one_to_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_one_to_all(...))  # add_one_to_all() runs first → value goes to print()
"""
Problème 1680 :
Écrivez une fonction Python appelée `add_one_to_all` qui prend une liste de nombres et retourne une nouvelle liste où 1 est added à chaque nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1681 :
Écrivez un programme Python à print nombres de 1 à 10 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1682 :
Écrivez un programme Python à print tous les nombres pairs de 1 à 20 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % !=
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1683 :
Écrivez un programme Python à print tous les nombres impairs de 1 à 20 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: *
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1684 :
Écrivez un programme Python à print le multiplication table de 5 de 1 à 10 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → text = ...  # stored in memory
#   → for char in text  # runs body once per item
#   → text = ...  # stored in memory
#   → for char in text  # runs body once per item
#   → text = ...  # stored in memory
#   → for char in text  # runs body once per item
#
# Loop: takes one item from text, runs body, repeats until done
"""
Problème 1685 :
Écrivez un programme Python à print chaque caractère de une chaîne on un separate line en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → my_list = ...  # stored in memory
#   → for element in my_list  # runs body once per item
#   → my_list = ...  # stored in memory
#   → for element in my_list  # runs body once per item
#   → my_list = ...  # stored in memory
#   → for element in my_list  # runs body once per item
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1686 :
Écrivez un programme Python à print chaque élément de une liste on un separate line en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → total = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(total)
#   → total = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(total)
#   → total = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(total)
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1687 :
Écrivez un programme Python à calculate le somme de nombres de 1 à 10 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → product = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(product)
#   → product = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(product)
#   → product = ...  # stored in memory
#   → for i in range(...)  # runs body once per item
#   → print(product)
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1688 :
Écrivez un programme Python à calculate le produit de nombres de 1 à 5 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1689 :
Écrivez un programme Python à nombre de 10 down à 1 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#   → for i in range(...)  # runs body once per item
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1690 :
Écrivez un programme Python à print nombres de 0 à 20 qui sont divisible by 3 en utilisant un pour loop.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#
# Loop: check condition → if True run body → check again
"""
Problème 1691 :
Écrivez un programme Python en utilisant un tandis que loop à print nombres de 1 à 5.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → total = ...  # stored in memory
#   → i = ...  # stored in memory
#   → print(total)
#   → total = ...  # stored in memory
#   → i = ...  # stored in memory
#   → print(total)
#   → total = ...  # stored in memory
#   → i = ...  # stored in memory
#   → print(total)
#
# Loop: check condition → if True run body → check again
"""
Problème 1692 :
Écrivez un programme Python en utilisant un tandis que loop à calculate le somme de nombres de 1 à 10.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#
# Loop: check condition → if True run body → check again
"""
Problème 1693 :
Écrivez un programme Python en utilisant un tandis que loop à print tous les nombres pairs de 2 à 20.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
# while ... → repeats as long as condition is true
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only solve is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#   → i = ...  # stored in memory
#
# Loop: check condition → if True run body → check again
"""
Problème 1694 :
Écrivez un programme Python en utilisant un tandis que loop à count down de 10 à 1.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: > <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not a <= b
#   → return not a <= b
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_greater is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_greater(...), check_greater(...))  # check_greater() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_greater(...))  # check_greater() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1695 :
Écrivez une fonction Python appelée `check_greater` qui prend deux nombres et retourne vrai si le premier nombre est greater que le deuxième, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: < >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not a >= b
#   → return not a >= b
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_less is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_less(...), check_less(...))  # check_less() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_less(...))  # check_less() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1696 :
Écrivez une fonction Python appelée `check_less` qui prend deux nombres et retourne vrai si le premier nombre est less que le deuxième, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return a == b
#   → return a == b
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_equal(...), check_equal(...))  # check_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_equal(...))  # check_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1697 :
Écrivez une fonction Python appelée `check_equal` qui prend deux valeurs et retourne vrai si they sont equal, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: != ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not a == b
#   → return not a == b
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_not_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_not_equal(...), check_not_equal(...))  # check_not_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_not_equal(...))  # check_not_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1698 :
Écrivez une fonction Python appelée `check_not_equal` qui prend deux valeurs et retourne vrai si they sont pas equal, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: < >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not a < b
#   → return not a < b
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_greater_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_greater_equal(...), check_greater_equal(...))  # check_greater_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_greater_equal(...))  # check_greater_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1699 :
Écrivez une fonction Python appelée `check_greater_equal` qui prend deux nombres et retourne vrai si le premier nombre est greater que ou equal à le deuxième, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: > <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return not a > b
#   → return not a > b
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_less_equal is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_less_equal(...), check_less_equal(...))  # check_less_equal() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_less_equal(...))  # check_less_equal() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1700 :
Écrivez une fonction Python appelée `check_less_equal` qui prend deux nombres et retourne vrai si le premier nombre est less que ou equal à le deuxième, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : abs()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return abs(number)
#   → return abs(number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_absolute_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_absolute_value(...), get_absolute_value(...))  # get_absolute_value() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_absolute_value(...))  # get_absolute_value() runs first → value goes to print()
"""
Problème 1701 :
Écrivez une fonction Python appelée `get_absolute_value` qui prend un nombre et retourne ses absolute valeur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return round(number)
#   → return round(number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only round_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(round_number(...), round_number(...))  # round_number() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(round_number(...))  # round_number() runs first → value goes to print()
"""
Problème 1702 :
Écrivez une fonction Python appelée `round_number` qui prend un nombre et retourne it rounded à le nearest integer.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(a, b, c)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_maximum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_maximum(...))  # get_maximum() runs first → value goes to print()
"""
Problème 1703 :
Écrivez une fonction Python appelée `get_maximum` qui prend trois nombres et retourne le plus grand l'une.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return min(a, b, c)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_minimum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_minimum(...))  # get_minimum() runs first → value goes to print()
"""
Problème 1704 :
Écrivez une fonction Python appelée `get_minimum` qui prend trois nombres et retourne le plus petit l'une.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return a % b == 0
#   → return a % b == 0
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_divisible is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_divisible(...), check_divisible(...))  # check_divisible() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_divisible(...))  # check_divisible() runs first → value goes to print()
"""
Problème 1705 :
Écrivez une fonction Python appelée `check_divisible` qui prend deux nombres et retourne vrai si le premier nombre est divisible by le deuxième, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result, type(...))
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_string(...))  # convert_to_string() runs first → value goes to print()
"""
Problème 1706 :
Écrivez une fonction Python appelée `convert_to_string` qui prend un nombre et retourne le en une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : int, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return int(text)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_integer is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result, type(...))
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_integer(...))  # convert_to_integer() runs first → value goes to print()
"""
Problème 1707 :
Écrivez une fonction Python appelée `convert_to_integer` qui prend une chaîne contenant un nombre et retourne le en un entier.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : float, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return float(text)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_to_float is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result, type(...))
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_to_float(...))  # convert_to_float() runs first → value goes to print()
"""
Problème 1708 :
Écrivez une fonction Python appelée `convert_to_float` qui prend une chaîne contenant un nombre et retourne le en un flottant.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str, type()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(type(value).__name__)
#   → return str(type(value).__name__)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_type is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_type(...), get_type(...))  # get_type() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_type(...))  # get_type() runs first → value goes to print()
"""
Problème 1709 :
Écrivez une fonction Python appelée `get_type` qui prend une valeur et retourne ses données tapez as une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return isinstance(value, int)
#   → return isinstance(value, int)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_is_integer is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_is_integer(...), check_is_integer(...))  # check_is_integer() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_is_integer(...))  # check_is_integer() runs first → value goes to print()
"""
Problème 1710 :
Écrivez une fonction Python appelée `check_is_integer` qui prend une valeur et retourne vrai si c'est une entier, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return isinstance(value, str)
#   → return isinstance(value, str)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_is_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_is_string(...), check_is_string(...))  # check_is_string() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_is_string(...))  # check_is_string() runs first → value goes to print()
"""
Problème 1711 :
Écrivez une fonction Python appelée `check_is_string` qui prend une valeur et retourne vrai si c'est une chaîne, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return isinstance(value, list)
#   → return isinstance(value, list)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_is_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_is_list(...), check_is_list(...))  # check_is_list() runs first → value goes to print()
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_is_list(...))  # check_is_list() runs first → value goes to print()
"""
Problème 1712 :
Écrivez une fonction Python appelée `check_is_list` qui prend une valeur et retourne vrai si c'est une liste, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Hello, %s!' % name
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_greeting is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_greeting(...))  # format_greeting() runs first → value goes to print()
"""
Problème 1713 :
Écrivez une fonction Python appelée `format_greeting` qui prend un nom et retourne un message de salutation message en utilisant an f-chaîne:'Hello, {nom}!'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '%s is %s years old.' % (name, age)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_age_message is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → age = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_age_message(...))  # format_age_message() runs first → value goes to print()
"""
Problème 1714 :
Écrivez une fonction Python appelée `format_age_message` qui prend un nom et age, et retourne un message en utilisant an f-chaîne:'{nom} est {age} years old.'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'{num1} + {num2} = {num1 + num2}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_calculation is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → num1 = ...  # stored in memory
#   → num2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_calculation(...))  # format_calculation() runs first → value goes to print()
"""
Problème 1715 :
Écrivez une fonction Python appelée `format_calculation` qui prend deux nombres et retourne un formatted chaîne showing leur somme:'{num1} + {num2} = {somme}'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 2 * (length + width)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_perimeter_rectangle is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_perimeter_rectangle(...))  # calculate_perimeter_rectangle() runs first → value goes to print()
"""
Problème 1716 :
Écrivez une fonction Python appelée `calculate_perimeter_rectangle` qui prend longueur et width et retourne le perimeter de un rectangle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: *
# Fonctions intégrées : pow, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pow(4, side)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_perimeter_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → side = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_perimeter_square(...))  # calculate_perimeter_square() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1717 :
Écrivez une fonction Python appelée `calculate_perimeter_square` qui prend le side longueur et retourne le perimeter de un carré.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 2 * 3.14 * radius
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_circle_circumference is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → radius = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_circle_circumference(...))  # calculate_circle_circumference() runs first → value goes to print()
"""
Problème 1718 :
Écrivez une fonction Python appelée `calculate_circle_circumference` qui prend le radius et retourne le circumference de un circle (Utilisez 3.14 pour pi).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return km * 0.621371
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_km_to_miles is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → km = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_km_to_miles(...))  # convert_km_to_miles() runs first → value goes to print()
"""
Problème 1719 :
Écrivez une fonction Python appelée `convert_km_to_miles` qui prend un distance dans kilometers et retourne it converted à miles (1 km = 0.621371 miles).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: /
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return miles / 0.621371
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_miles_to_km is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → miles = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_miles_to_km(...))  # convert_miles_to_km() runs first → value goes to print()
"""
Problème 1720 :
Écrivez une fonction Python appelée `convert_miles_to_km` qui prend un distance dans miles et retourne it converted à kilometers.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return kg * 2.20462
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_kg_to_pounds is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → kg = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_kg_to_pounds(...))  # convert_kg_to_pounds() runs first → value goes to print()
"""
Problème 1721 :
Écrivez une fonction Python appelée `convert_kg_to_pounds` qui prend un weight dans kilograms et retourne it converted à pounds (1 kg = 2.20462 pounds).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: /
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pounds / 2.20462
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only convert_pounds_to_kg is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → pounds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(convert_pounds_to_kg(...))  # convert_pounds_to_kg() runs first → value goes to print()
"""
Problème 1722 :
Écrivez une fonction Python appelée `convert_pounds_to_kg` qui prend un weight dans pounds et retourne it converted à kilograms.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return char.lower() in 'aeiou'
#   → return char.lower() in 'aeiou'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_vowel is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_vowel(...), check_vowel(...))  # check_vowel() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_vowel(...))  # check_vowel() runs first → value goes to print()
"""
Problème 1723 :
Écrivez une fonction Python appelée `check_vowel` qui prend un caractère et retourne vrai si c'est une voyelle (a, e, i, o, u), otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: and not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return char.isalpha() and char.lower() not in 'aeiou'
#   → return char.isalpha() and char.lower() not in 'aeiou'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_consonant is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_consonant(...), check_consonant(...))  # check_consonant() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_consonant(...))  # check_consonant() runs first → value goes to print()
"""
Problème 1724 :
Écrivez une fonction Python appelée `check_consonant` qui prend un caractère et retourne vrai si c'est une consonne, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return char.isdigit()
#   → return char.isdigit()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_digit is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_digit(...), check_digit(...))  # check_digit() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_digit(...))  # check_digit() runs first → value goes to print()
"""
Problème 1725 :
Écrivez une fonction Python appelée `check_digit` qui prend un caractère et retourne vrai si c'est une chiffre (0-9), otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return char.isalpha()
#   → return char.isalpha()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_letter(...), check_letter(...))  # check_letter() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_letter(...))  # check_letter() runs first → value goes to print()
"""
Problème 1726 :
Écrivez une fonction Python appelée `check_letter` qui prend un caractère et retourne vrai si c'est une lettre (a-z ou A-Z), otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return char.isupper()
#   → return char.isupper()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_uppercase is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_uppercase(...), check_uppercase(...))  # check_uppercase() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_uppercase(...))  # check_uppercase() runs first → value goes to print()
"""
Problème 1727 :
Écrivez une fonction Python appelée `check_uppercase` qui prend un caractère et retourne vrai si c'est uppercase, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return char.islower()
#   → return char.islower()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_lowercase is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_lowercase(...), check_lowercase(...))  # check_lowercase() runs first → value goes to print()
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_lowercase(...))  # check_lowercase() runs first → value goes to print()
"""
Problème 1728 :
Écrivez une fonction Python appelée `check_lowercase` qui prend un caractère et retourne vrai si c'est lowercase, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.find(char)
#   → return text.find(char)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_char_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(find_char_index(...), find_char_index(...))  # find_char_index() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_char_index(...))  # find_char_index() runs first → value goes to print()
"""
Problème 1729 :
Écrivez une fonction Python appelée `find_char_index` qui prend une chaîne et un caractère, et retourne le index de le premier occurrence de le caractère dans la chaîne, ou -1 si pas trouvé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ==
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → count = 0
#   → for c in text:
#   →     if c == char:
#   →         count += 1
#   → return count
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_char_in_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_char_in_string(...))  # count_char_in_string() runs first → value goes to print()
#
# Loop: takes one item from text, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1730 :
Écrivez une fonction Python appelée `count_char_in_string` qui prend une chaîne et un caractère, et retourne combien de fois caractère apparaît dans la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.startswith(prefix)
#   → return text.startswith(prefix)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_starts_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_starts_with(...), check_starts_with(...))  # check_starts_with() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → prefix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_starts_with(...))  # check_starts_with() runs first → value goes to print()
"""
Problème 1731 :
Écrivez une fonction Python appelée `check_starts_with` qui prend une chaîne et un prefix, et retourne vrai si la chaîne commence par le prefix, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.endswith(suffix)
#   → return text.endswith(suffix)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_ends_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_ends_with(...), check_ends_with(...))  # check_ends_with() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → suffix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_ends_with(...))  # check_ends_with() runs first → value goes to print()
"""
Problème 1732 :
Écrivez une fonction Python appelée `check_ends_with` qui prend une chaîne et un suffix, et retourne vrai si la chaîne termine avec le suffix, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.replace(old_char, new_char)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only replace_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → old_char = ...  # stored in memory
#   → new_char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(replace_char(...))  # replace_char() runs first → value goes to print()
"""
Problème 1733 :
Écrivez une fonction Python appelée `replace_char` qui prend une chaîne, old caractère, et new caractère, et retourne une nouvelle chaîne avec tous occurrences de old caractère replaced avec new caractère.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.replace(char, '')
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_char is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → char = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_char(...))  # remove_char() runs first → value goes to print()
"""
Problème 1734 :
Écrivez une fonction Python appelée `remove_char` qui prend une chaîne et un caractère, et retourne une nouvelle chaîne avec tous occurrences de cette caractère removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text[start:end]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_substring is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_substring(...))  # get_substring() runs first → value goes to print()
"""
Problème 1735 :
Écrivez une fonction Python appelée `get_substring` qui prend une chaîne, start index, et end index, et retourne le substring de start à end (pas y compris end).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : reversed()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → result = ''
#   → for char in my_list:
#   →     result = char + result
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_list(...))  # reverse_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1736 :
Écrivez une fonction Python appelée `reverse_list` qui prend une liste et retourne une nouvelle liste avec éléments en ordre inverse order.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((1 for _ in my_list))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_list_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_list_length(...))  # get_list_length() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1737 :
Écrivez une fonction Python appelée `get_list_length` qui prend une liste et retourne le nombre d'éléments dans la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: == in
# Fonctions intégrées : any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return any((element == element for element in my_list))
#   → return any((element == element for element in my_list))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_element_in_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_element_in_list(...), check_element_in_list(...))  # check_element_in_list() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_element_in_list(...))  # check_element_in_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1738 :
Écrivez une fonction Python appelée `check_element_in_list` qui prend une liste et un élément, et retourne vrai si le élément est dans la liste, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ==
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → count = 0
#   → for c in my_list:
#   →     if c == element:
#   →         count += 1
#   → return count
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_element_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_element_count(...))  # get_element_count() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1739 :
Écrivez une fonction Python appelée `get_element_count` qui prend une liste et un élément, et retourne combien de fois élément apparaît dans la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list.index(element)
#   → return my_list.index(element)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_element_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_element_index(...), get_element_index(...))  # get_element_index() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_element_index(...))  # get_element_index() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1740 :
Écrivez une fonction Python appelée `get_element_index` qui prend une liste et un élément, et retourne le index de le premier occurrence de le élément, ou -1 si pas trouvé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x % 2 == 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_numbers(...))  # get_even_numbers() runs first → value goes to print()
"""
Problème 1741 :
Écrivez une fonction Python appelée `get_even_numbers` qui prend une liste de nombres et retourne une nouvelle liste contenant seulement les nombres pairs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % !=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x % 2 != 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_odd_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_odd_numbers(...))  # get_odd_numbers() runs first → value goes to print()
"""
Problème 1742 :
Écrivez une fonction Python appelée `get_odd_numbers` qui prend une liste de nombres et retourne une nouvelle liste contenant seulement les nombres impairs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x > 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_positive_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_positive_numbers(...))  # get_positive_numbers() runs first → value goes to print()
"""
Problème 1743 :
Écrivez une fonction Python appelée `get_positive_numbers` qui prend une liste de nombres et retourne une nouvelle liste contenant seulement les nombres positifs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x < 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_negative_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_negative_numbers(...))  # get_negative_numbers() runs first → value goes to print()
"""
Problème 1744 :
Écrivez une fonction Python appelée `get_negative_numbers` qui prend une liste de nombres et retourne une nouvelle liste contenant seulement les nombres négatifs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x > threshold]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_greater_than is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → threshold = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_greater_than(...))  # get_numbers_greater_than() runs first → value goes to print()
"""
Problème 1745 :
Écrivez une fonction Python appelée `get_numbers_greater_than` qui prend une liste de nombres et une seuil, et retourne une nouvelle liste contenant seulement nombres greater que le seuil.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x < threshold]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_less_than is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → threshold = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_less_than(...))  # get_numbers_less_than() runs first → value goes to print()
"""
Problème 1746 :
Écrivez une fonction Python appelée `get_numbers_less_than` qui prend une liste de nombres et une seuil, et retourne une nouvelle liste contenant seulement nombres less que le seuil.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : reduce, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → from functools import reduce
#   → import operator
#   → return reduce(operator.add, numbers, 0)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_sum_of_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_sum_of_list(...))  # calculate_sum_of_list() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done
"""
Problème 1747 :
Écrivez une fonction Python appelée `calculate_sum_of_list` qui prend une liste de nombres et retourne le somme de tous nombres dans la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return product
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_product_of_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_product_of_list(...))  # calculate_product_of_list() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done
"""
Problème 1748 :
Écrivez une fonction Python appelée `calculate_product_of_list` qui prend une liste de nombres et retourne le produit de tous nombres dans la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: /
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → total = 0
#   → count = 0
#   → for item in numbers:
#   →     total += item
#   →     count += 1
#   → return total / count if count else 0
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_average_of_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_average_of_list(...))  # get_average_of_list() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done
"""
Problème 1749 :
Écrivez une fonction Python appelée `get_average_of_list` qui prend une liste de nombres et retourne le moyenne (mean) de tous nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : dict, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return dict(zip(keys, values))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_dictionary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → keys = ...  # stored in memory
#   → values = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_dictionary(...))  # create_dictionary() runs first → value goes to print()
"""
Problème 1750 :
Écrivez une fonction Python appelée `create_dictionary` qui prend deux listes (clés et valeurs) et retourne un dictionnaire created de them.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return d.get(key)
#   → return d.get(key)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_dictionary_value is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_dictionary_value(...), get_dictionary_value(...))  # get_dictionary_value() runs first → value goes to print()
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_dictionary_value(...))  # get_dictionary_value() runs first → value goes to print()
"""
Problème 1751 :
Écrivez une fonction Python appelée `get_dictionary_value` qui prend un dictionnaire et une clé, et retourne le valeur associated avec qui clé, ou None si la clé doesn't exist.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(d.keys())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_dictionary_keys is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_dictionary_keys(...))  # get_dictionary_keys() runs first → value goes to print()
"""
Problème 1752 :
Écrivez une fonction Python appelée `get_dictionary_keys` qui prend un dictionnaire et retourne une liste de tous clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(d.values())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_dictionary_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_dictionary_values(...))  # get_dictionary_values() runs first → value goes to print()
"""
Problème 1753 :
Écrivez une fonction Python appelée `get_dictionary_values` qui prend un dictionnaire et retourne une liste de tous valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: == in
# Fonctions intégrées : any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return any((element == key for element in d))
#   → return any((element == key for element in d))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_key_in_dictionary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_key_in_dictionary(...), check_key_in_dictionary(...))  # check_key_in_dictionary() runs first → value goes to print()
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_key_in_dictionary(...))  # check_key_in_dictionary() runs first → value goes to print()
#
# Loop: takes one item from d, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1754 :
Écrivez une fonction Python appelée `check_key_in_dictionary` qui prend un dictionnaire et une clé, et retourne vrai si la clé existe dans le dictionnaire, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return new_dict
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_to_dictionary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → d = ...  # stored in memory
#   → key = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_to_dictionary(...))  # add_to_dictionary() runs first → value goes to print()
"""
Problème 1755 :
Écrivez une fonction Python appelée `add_to_dictionary` qui prend un dictionnaire, une clé, et une valeur, et retourne un nouveau dictionnaire avec la paire clé-valeur added.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : tuple()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return tuple(args)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_tuple is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_tuple(...))  # create_tuple() runs first → value goes to print()
"""
Problème 1756 :
Écrivez une fonction Python appelée `create_tuple` qui prend un nombre de arguments et retourne un tuple contenant ceux arguments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_tuple[index]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_tuple_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_tuple = ...  # stored in memory
#   → index = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_tuple_element(...))  # get_tuple_element() runs first → value goes to print()
"""
Problème 1757 :
Écrivez une fonction Python appelée `get_tuple_element` qui prend un tuple et un indice, et retourne le élément at qui index.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (a, b)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only unpack_tuple is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_tuple = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(unpack_tuple(...))  # unpack_tuple() runs first → value goes to print()
"""
Problème 1758 :
Écrivez une fonction Python appelée `unpack_tuple` qui prend un tuple des deux éléments et retourne them as separate valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return set(my_list)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only create_set is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(create_set(...))  # create_set() runs first → value goes to print()
"""
Problème 1759 :
Écrivez une fonction Python appelée `create_set` qui prend une liste et retourne un ensemble contenant le unique éléments de la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: == in
# Fonctions intégrées : any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return any((element == element for element in my_set))
#   → return any((element == element for element in my_set))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_element_in_set is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_element_in_set(...), check_element_in_set(...))  # check_element_in_set() runs first → value goes to print()
#   → my_set = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_element_in_set(...))  # check_element_in_set() runs first → value goes to print()
#
# Loop: takes one item from my_set, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1760 :
Écrivez une fonction Python appelée `check_element_in_set` qui prend un ensemble et un élément, et retourne vrai si le élément est dans le set, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: |
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return set1 | set2
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only union_sets is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → set1 = ...  # stored in memory
#   → set2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(union_sets(...))  # union_sets() runs first → value goes to print()
"""
Problème 1761 :
Écrivez une fonction Python appelée `union_sets` qui prend deux sets et retourne une nouvelle set contenant tous éléments de les deux sets.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: &
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return set1 & set2
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only intersection_sets is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → set1 = ...  # stored in memory
#   → set2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(intersection_sets(...))  # intersection_sets() runs first → value goes to print()
"""
Problème 1762 :
Écrivez une fonction Python appelée `intersection_sets` qui prend deux sets et retourne une nouvelle set contenant seulement éléments qui sont dans les deux sets.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + -
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.sub(set1, set2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only difference_sets is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → set1 = ...  # stored in memory
#   → set2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(difference_sets(...))  # difference_sets() runs first → value goes to print()
"""
Problème 1763 :
Écrivez une fonction Python appelée `difference_sets` qui prend deux sets et retourne une nouvelle set contenant éléments qui sont dans le premier set but pas dans le deuxième.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f.read()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only read_file_content is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Function defined: read_file_content(filename)')
#   → filename = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(read_file_content(...))  # read_file_content() runs first → value goes to print()
"""
Problème 1764 :
Écrivez une fonction Python appelée `read_file_content` qui prend un filename et retourne le content de le file as une chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → with open(filename, 'w') as f:
#   →     f.write(content)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — solve, write_to_file are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Function defined: write_to_file(filename, content)')
#   → filename = ...  # stored in memory
#   → content = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(write_to_file(...))  # write_to_file() runs first → value goes to print()
"""
Problème 1765 :
Écrivez une fonction Python appelée `write_to_file` qui prend un filename et content, et écrit le content à le file.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : open()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → with open(filename, 'a') as f:
#   →     f.write(content)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — append_to_file, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print('Function defined: append_to_file(filename, content)')
#   → filename = ...  # stored in memory
#   → content = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(append_to_file(...))  # append_to_file() runs first → value goes to print()
"""
Problème 1766 :
Écrivez une fonction Python appelée `append_to_file` qui prend un filename et content, et ajoute le content à le end de le file.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
# Operators: * /
# Fonctions intégrées : Fraction, float()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → from fractions import Fraction
#   → return float(Fraction(a, b))
#   → from fractions import Fraction
#   → return float(Fraction(a, b))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only safe_divide is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(safe_divide(...), safe_divide(...))  # safe_divide() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(safe_divide(...))  # safe_divide() runs first → value goes to print()
"""
Problème 1767 :
Écrivez une fonction Python appelée `safe_divide` qui prend deux nombres et retourne leur division résultat. si division by zero occurs, retourner None instead de raising une erreur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
# Fonctions intégrées : int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return int(text)
#   → return int(text)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only safe_convert_to_int is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(safe_convert_to_int(...), safe_convert_to_int(...))  # safe_convert_to_int() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(safe_convert_to_int(...))  # safe_convert_to_int() runs first → value goes to print()
"""
Problème 1768 :
Écrivez une fonction Python appelée `safe_convert_to_int` qui prend une chaîne et tries à convert it à un entier. si conversion fails, retourner None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: try/except
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[index]
#   → return my_list[index]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only safe_get_list_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(safe_get_list_element(...), safe_get_list_element(...))  # safe_get_list_element() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(safe_get_list_element(...))  # safe_get_list_element() runs first → value goes to print()
"""
Problème 1769 :
Écrivez une fonction Python appelée `safe_get_list_element` qui prend une liste et un indice, et retourne le élément at qui index. si index est out de range, retourner None.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return separator.join(strings)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only join_strings is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → separator = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(join_strings(...))  # join_strings() runs first → value goes to print()
"""
Problème 1770 :
Écrivez une fonction Python appelée `join_strings` qui prend une liste de chaînes et un separator, et retourne un seul chaîne avec tous strings joined by le separator.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.split(delimiter)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only split_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → delimiter = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(split_string(...))  # split_string() runs first → value goes to print()
"""
Problème 1771 :
Écrivez une fonction Python appelée `split_string` qui prend une chaîne et un delimiter, et retourne une liste de chaînes split by le delimiter.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.strip()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only strip_whitespace is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(strip_whitespace(...))  # strip_whitespace() runs first → value goes to print()
"""
Problème 1772 :
Écrivez une fonction Python appelée `strip_whitespace` qui prend une chaîne et retourne une nouvelle chaîne avec leading et trailing les espaces removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(text).capitalize()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only capitalize_first_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(capitalize_first_letter(...))  # capitalize_first_letter() runs first → value goes to print()
"""
Problème 1773 :
Écrivez une fonction Python appelée `capitalize_first_letter` qui prend une chaîne et retourne une nouvelle chaîne avec seulement le premier lettre capitalized.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(text).swapcase()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_case is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_case(...))  # swap_case() runs first → value goes to print()
"""
Problème 1774 :
Écrivez une fonction Python appelée `swap_case` qui prend une chaîne et retourne une nouvelle chaîne avec uppercase lettre converted à lowercase et vice versa.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.isdigit()
#   → return text.isdigit()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_digits(...), check_all_digits(...))  # check_all_digits() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_digits(...))  # check_all_digits() runs first → value goes to print()
"""
Problème 1775 :
Écrivez une fonction Python appelée `check_all_digits` qui prend une chaîne et retourne vrai si tous caractères dans la chaîne sont les chiffres, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.isalpha()
#   → return text.isalpha()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_letters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_letters(...), check_all_letters(...))  # check_all_letters() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_letters(...))  # check_all_letters() runs first → value goes to print()
"""
Problème 1776 :
Écrivez une fonction Python appelée `check_all_letters` qui prend une chaîne et retourne vrai si tous caractères dans la chaîne sont lettre, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text.isalnum()
#   → return text.isalnum()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_alphanumeric is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_alphanumeric(...), check_all_alphanumeric(...))  # check_all_alphanumeric() runs first → value goes to print()
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_alphanumeric(...))  # check_all_alphanumeric() runs first → value goes to print()
"""
Problème 1777 :
Écrivez une fonction Python appelée `check_all_alphanumeric` qui prend une chaîne et retourne vrai si tous caractères sont lettre ou les chiffres, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x ** 2 for x in numbers]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_squared_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_squared_numbers(...))  # get_squared_numbers() runs first → value goes to print()
"""
Problème 1778 :
Écrivez une fonction Python appelée `get_squared_numbers` qui prend une liste de nombres et retourne une nouvelle liste avec chaque nombre au carré, en utilisant une liste comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x * 2 for x in numbers]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_doubled_numbers is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_doubled_numbers(...))  # get_doubled_numbers() runs first → value goes to print()
"""
Problème 1779 :
Écrivez une fonction Python appelée `get_doubled_numbers` qui prend une liste de nombres et retourne une nouvelle liste avec chaque nombre doubled, en utilisant une liste comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x % 2 == 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_numbers_comprehension is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_numbers_comprehension(...))  # get_even_numbers_comprehension() runs first → value goes to print()
"""
Problème 1780 :
Écrivez une fonction Python appelée `get_even_numbers_comprehension` qui prend une liste de nombres et retourne une nouvelle liste contenant seulement les nombres pairs, en utilisant une liste comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [s.upper() for s in strings]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_uppercase_strings is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_uppercase_strings(...))  # get_uppercase_strings() runs first → value goes to print()
"""
Problème 1781 :
Écrivez une fonction Python appelée `get_uppercase_strings` qui prend une liste de chaînes et retourne une nouvelle liste avec tous strings converted à uppercase, en utilisant une liste comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [len(s) for s in strings]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_string_lengths is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_string_lengths(...))  # get_string_lengths() runs first → value goes to print()
"""
Problème 1782 :
Écrivez une fonction Python appelée `get_string_lengths` qui prend une liste de chaînes et retourne une nouvelle liste avec la longueur de chaque chaîne, en utilisant une liste comprehension.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n + 1):
#   →     for j in range(1, i + 1):
#   →         print(j, end=' ')
#   →     print()
#   → for i in range(1, n + 1):
#   →     for j in range(1, i + 1):
#   →         print(j, end=' ')
#   →     print()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_number_pattern, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_number_pattern(5)  # jumps into print_number_pattern(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_number_pattern(...))  # print_number_pattern() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1783 :
Écrivez une fonction Python appelée `print_number_pattern` qui prend un nombre n et affiche un motif de nombres de 1 à n, où chaque line contient nombres de 1 à le line nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: * +
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n + 1):
#   →     print('*' * i)
#   → for i in range(1, n + 1):
#   →     print('*' * i)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_star_pattern, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_star_pattern(5)  # jumps into print_star_pattern(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_star_pattern(...))  # print_star_pattern() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1784 :
Écrivez une fonction Python appelée `print_star_pattern` qui prend un nombre n et affiche n lines de stars, où le premier line un 1 star, deuxième un 2 stars, et so on.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: *
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(n, 0, -1):
#   →     print('*' * i)
#   → for i in range(n, 0, -1):
#   →     print('*' * i)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — print_reverse_star_pattern, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_reverse_star_pattern(5)  # jumps into print_reverse_star_pattern(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_reverse_star_pattern(...))  # print_reverse_star_pattern() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1785 :
Écrivez une fonction Python appelée `print_reverse_star_pattern` qui prend un nombre n et affiche n lines de stars en ordre inverse order, où le premier line un n stars, deuxième un n-1 stars, et so on.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: * +
# Fonctions intégrées : range, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → for i in range(1, n * n + 1, n):
#   →     row = [str(j) for j in range(i, i + n)]
#   → result = ' '.join(row)
#   → return result
#   → for i in range(1, n * n + 1, n):
#   →     row = [str(j) for j in range(i, i + n)]
#   → result = ' '.join(row)
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only print_number_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print_number_square(3)  # jumps into print_number_square(), runs body, returns
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(print_number_square(...))  # print_number_square() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1786 :
Écrivez une fonction Python appelée `print_number_square` qui prend un nombre n et affiche an n by n carré de nombres de 1 à n*n.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_factorial is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_factorial(...))  # calculate_factorial() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1787 :
Écrivez une fonction Python appelée `calculate_factorial` qui prend un nombre n et retourne la factorielle de n (n! = n * (n-1) *... * 1).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : range, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum(range(start, end + 1))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_sum_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_sum_range(...))  # calculate_sum_range() runs first → value goes to print()
"""
Problème 1788 :
Écrivez une fonction Python appelée `calculate_sum_range` qui prend deux nombres start et end, et retourne le somme de tous nombres de start à end (inclusive).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return product
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_product_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_product_range(...))  # calculate_product_range() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1789 :
Écrivez une fonction Python appelée `calculate_product_range` qui prend deux nombres start et end, et retourne le produit de tous nombres de start à end (inclusive).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: + - <= ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return []
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_fibonacci_sequence is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_fibonacci_sequence(...))  # get_fibonacci_sequence() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1790 :
Écrivez une fonction Python appelée `get_fibonacci_sequence` qui prend un nombre n et retourne une liste contenant les n premiers nombres de la suite de Fibonacci.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(number) == str(number)[::-1]
#   → return str(number) == str(number)[::-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_palindrome_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_palindrome_number(...), check_palindrome_number(...))  # check_palindrome_number() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_palindrome_number(...))  # check_palindrome_number() runs first → value goes to print()
"""
Problème 1791 :
Écrivez une fonction Python appelée `check_palindrome_number` qui prend un nombre et retourne vrai si le nombre est un palindrome (lit le même forwards et backwards), otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : int, str, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((int(digit) for digit in str(number)))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sum_of_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sum_of_digits(...))  # sum_of_digits() runs first → value goes to print()
"""
Problème 1792 :
Écrivez une fonction Python appelée `sum_of_digits` qui prend un nombre et retourne le somme de tous ses les chiffres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Fonctions intégrées : int, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return product
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only product_of_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(product_of_digits(...))  # product_of_digits() runs first → value goes to print()
#
# Loop: takes one item from str(...), runs body, repeats until done
"""
Problème 1793 :
Écrivez une fonction Python appelée `product_of_digits` qui prend un nombre et retourne le produit de tous ses les chiffres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return len(str(number))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_digits is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_digits(...))  # count_digits() runs first → value goes to print()
"""
Problème 1794 :
Écrivez une fonction Python appelée `count_digits` qui prend un nombre et retourne le nombre de les chiffres dans le nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : int, str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return int(str(number)[::-1])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_number(...))  # reverse_number() runs first → value goes to print()
"""
Problème 1795 :
Écrivez une fonction Python appelée `reverse_number` qui prend un nombre et retourne le nombre avec ses les chiffres inversé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[:n]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_first_n_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_first_n_elements(...))  # get_first_n_elements() runs first → value goes to print()
"""
Problème 1796 :
Écrivez une fonction Python appelée `get_first_n_elements` qui prend une liste et un nombre n, et retourne les n premiers éléments de la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[-n:]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_last_n_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_last_n_elements(...))  # get_last_n_elements() runs first → value goes to print()
"""
Problème 1797 :
Écrivez une fonction Python appelée `get_last_n_elements` qui prend une liste et un nombre n, et retourne le dernier n éléments de la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda item: item, my_list))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_duplicates_from_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_duplicates_from_list(...))  # remove_duplicates_from_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1798 :
Écrivez une fonction Python appelée `remove_duplicates_from_list` qui prend une liste et retourne une nouvelle liste avec en double éléments removed, preserving order.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: <
# Fonctions intégrées : len, max, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_two_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(merge_two_lists(...))  # merge_two_lists() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1799 :
Écrivez une fonction Python appelée `merge_two_lists` qui prend deux listes et retourne une nouvelle liste contenant tous éléments de les deux listes, alternating entre them.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in list1 if x in list2]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_common_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_common_elements(...))  # get_common_elements() runs first → value goes to print()
"""
Problème 1800 :
Écrivez une fonction Python appelée `get_common_elements` qui prend deux listes et retourne une nouvelle liste contenant éléments qui appear dans les deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda item: item, list1))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_elements(...))  # get_unique_elements() runs first → value goes to print()
#
# Loop: takes one item from list1, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1801 :
Écrivez une fonction Python appelée `get_unique_elements` qui prend deux listes et retourne une nouvelle liste contenant éléments qui appear dans seulement l'une de le listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(numbers, reverse=False)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_list_ascending is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_list_ascending(...))  # sort_list_ascending() runs first → value goes to print()
"""
Problème 1802 :
Écrivez une fonction Python appelée `sort_list_ascending` qui prend une liste de nombres et retourne une nouvelle liste trié dans l'ordre croissant.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(numbers, reverse=True)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_list_descending is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_list_descending(...))  # sort_list_descending() runs first → value goes to print()
"""
Problème 1803 :
Écrivez une fonction Python appelée `sort_list_descending` qui prend une liste de nombres et retourne une nouvelle liste trié dans l'ordre décroissant.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : len, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return unique_numbers[1] if len(unique_numbers) > 1 else None
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_second_largest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_second_largest(...))  # get_second_largest() runs first → value goes to print()
"""
Problème 1804 :
Écrivez une fonction Python appelée `get_second_largest` qui prend une liste de nombres et retourne le deuxième plus grand nombre dans la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : len, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return unique_numbers[1] if len(unique_numbers) > 1 else None
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_second_smallest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_second_smallest(...))  # get_second_smallest() runs first → value goes to print()
"""
Problème 1805 :
Écrivez une fonction Python appelée `get_second_smallest` qui prend une liste de nombres et retourne le deuxième plus petit nombre dans la liste.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return numbers == sorted(numbers)
#   → return numbers == sorted(numbers)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_sorted is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_sorted(...), check_sorted(...))  # check_sorted() runs first → value goes to print()
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_sorted(...))  # check_sorted() runs first → value goes to print()
"""
Problème 1806 :
Écrivez une fonction Python appelée `check_sorted` qui prend une liste de nombres et retourne vrai si la liste est trié dans l'ordre croissant, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return counts
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_occurrences_in_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_occurrences_in_list(...))  # count_occurrences_in_list() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1807 :
Écrivez une fonction Python appelée `count_occurrences_in_list` qui prend une liste et retourne un dictionnaire où clés sont éléments et valeurs sont leur compte.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(counts, key=counts.get)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_most_frequent is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_most_frequent(...))  # get_most_frequent() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1808 :
Écrivez une fonction Python appelée `get_most_frequent` qui prend une liste et retourne le élément qui apparaît la plupart frequently.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return min(counts, key=counts.get)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_least_frequent is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_least_frequent(...))  # get_least_frequent() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1809 :
Écrivez une fonction Python appelée `get_least_frequent` qui prend une liste et retourne le élément qui apparaît moins frequently.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only flatten_nested_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → nested_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(flatten_nested_list(...))  # flatten_nested_list() runs first → value goes to print()
#
# Loop: takes one item from nested_list, runs body, repeats until done
"""
Problème 1810 :
Écrivez une fonction Python appelée `flatten_nested_list` qui prend une liste de listes et retourne un seul list contenant tous éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return groups
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only group_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(group_by_length(...))  # group_by_length() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1811 :
Écrivez une fonction Python appelée `group_by_length` qui prend une liste de chaînes et retourne un dictionnaire où clés sont lengths et valeurs sont listes de chaînes avec qui longueur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >=
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [s for s in strings if len(s) >= min_length]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → min_length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(filter_by_length(...))  # filter_by_length() runs first → value goes to print()
"""
Problème 1812 :
Écrivez une fonction Python appelée `filter_by_length` qui prend une liste de chaînes et un minimum longueur, et retourne une nouvelle liste contenant seulement strings avec longueur greater que ou equal à le minimum.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(strings, key=len)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_longest_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_longest_string(...))  # get_longest_string() runs first → value goes to print()
"""
Problème 1813 :
Écrivez une fonction Python appelée `get_longest_string` qui prend une liste de chaînes et retourne le plus long chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return min(strings, key=len)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_shortest_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_shortest_string(...))  # get_shortest_string() runs first → value goes to print()
"""
Problème 1814 :
Écrivez une fonction Python appelée `get_shortest_string` qui prend une liste de chaînes et retourne le plus court chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ' '.join((word.capitalize() for word in text.split()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only capitalize_all_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(capitalize_all_words(...))  # capitalize_all_words() runs first → value goes to print()
"""
Problème 1815 :
Écrivez une fonction Python appelée `capitalize_all_words` qui prend une chaîne de mots séparés by spaces et retourne une nouvelle chaîne avec le premier lettre de chaque mot capitalized.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : reversed()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ' '.join(reversed(words))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only reverse_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(reverse_words(...))  # reverse_words() runs first → value goes to print()
"""
Problème 1816 :
Écrivez une fonction Python appelée `reverse_words` qui prend une chaîne de mots séparés by spaces et retourne une nouvelle chaîne avec le order de mots inversé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return len(text.split())
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words(...))  # count_words() runs first → value goes to print()
"""
Problème 1817 :
Écrivez une fonction Python appelée `count_words` qui prend une chaîne et retourne le nombre de mots dans la chaîne (words sont séparés by spaces).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(words, key=len)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_longest_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_longest_word(...))  # get_longest_word() runs first → value goes to print()
"""
Problème 1818 :
Écrivez une fonction Python appelée `get_longest_word` qui prend une chaîne de mots séparés by spaces et retourne le plus long mot.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return min(words, key=len)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_shortest_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_shortest_word(...))  # get_shortest_word() runs first → value goes to print()
"""
Problème 1819 :
Écrivez une fonction Python appelée `get_shortest_word` qui prend une chaîne de mots séparés by spaces et retourne le plus court mot.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ''.join((char for char in text if char not in vowels))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_vowels_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_vowels_from_string(...))  # remove_vowels_from_string() runs first → value goes to print()
"""
Problème 1820 :
Écrivez une fonction Python appelée `remove_vowels_from_string` qui prend une chaîne et retourne une nouvelle chaîne avec tous voyelles (a, e, i, o, u) removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in or
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ''.join((char for char in text if char in vowels or not char.isalpha()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_consonants_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_consonants_from_string(...))  # remove_consonants_from_string() runs first → value goes to print()
"""
Problème 1821 :
Écrivez une fonction Python appelée `remove_consonants_from_string` qui prend une chaîne et retourne une nouvelle chaîne avec tous consonnes removed, keeping seulement voyelles et autre caractères.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((1 for char in text if char in vowels))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_vowels_in_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_vowels_in_string(...))  # count_vowels_in_string() runs first → value goes to print()
"""
Problème 1822 :
Écrivez une fonction Python appelée `count_vowels_in_string` qui prend une chaîne et retourne le nombre de voyelles (a, e, i, o, u) dans la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: <
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return text
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_first_last_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_first_last_words(...))  # swap_first_last_words() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1824 :
Écrivez une fonction Python appelée `swap_first_last_words` qui prend une chaîne de mots séparés by spaces et retourne une nouvelle chaîne avec le premier et dernier words swapped.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: //
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return words[len(words) // 2]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_word is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_word(...))  # get_middle_word() runs first → value goes to print()
"""
Problème 1825 :
Écrivez une fonction Python appelée `get_middle_word` qui prend une chaîne de mots séparés by spaces et retourne le middle mot. si il y un même nombre de mots, retourner le mot at index longueur//2.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return all((x == my_list[0] for x in my_list)) if my_list else True
#   → return all((x == my_list[0] for x in my_list)) if my_list else True
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_same is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_same(...), check_all_same(...))  # check_all_same() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_same(...))  # check_all_same() runs first → value goes to print()
"""
Problème 1826 :
Écrivez une fonction Python appelée `check_all_same` qui prend une liste et retourne vrai si tous éléments dans la liste sont le même, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : len, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return len(my_list) == len(set(my_list))
#   → return len(my_list) == len(set(my_list))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_different is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_different(...), check_all_different(...))  # check_all_different() runs first → value goes to print()
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_different(...))  # check_all_different() runs first → value goes to print()
"""
Problème 1827 :
Écrivez une fonction Python appelée `check_all_different` qui prend une liste et retourne vrai si tous éléments dans la liste sont different, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : list, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(range(start, end + 1))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_range_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_range_list(...))  # get_range_list() runs first → value goes to print()
"""
Problème 1828 :
Écrivez une fonction Python appelée `get_range_list` qui prend deux nombres start et end, et retourne une liste contenant tous nombres de start à end (inclusive).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in range(start, end + 1) if x % 2 == 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_range(...))  # get_even_range() runs first → value goes to print()
"""
Problème 1829 :
Écrivez une fonction Python appelée `get_even_range` qui prend deux nombres start et end, et retourne une liste contenant tous les nombres pairs de start à end (inclusive).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + !=
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in range(start, end + 1) if x % 2 != 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_odd_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_odd_range(...))  # get_odd_range() runs first → value goes to print()
"""
Problème 1830 :
Écrivez une fonction Python appelée `get_odd_range` qui prend deux nombres start et end, et retourne une liste contenant tous les nombres impairs de start à end (inclusive).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [a * b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply_lists(...))  # multiply_lists() runs first → value goes to print()
"""
Problème 1831 :
Écrivez une fonction Python appelée `multiply_lists` qui prend deux listes de nombres de le même longueur et retourne une nouvelle liste où chaque élément est le produit de corresponding éléments de les deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [a + b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only add_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(add_lists(...))  # add_lists() runs first → value goes to print()
"""
Problème 1832 :
Écrivez une fonction Python appelée `add_lists` qui prend deux listes de nombres de le même longueur et retourne une nouvelle liste où chaque élément est le somme de corresponding éléments de les deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: -
# Fonctions intégrées : zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [a - b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only subtract_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(subtract_lists(...))  # subtract_lists() runs first → value goes to print()
"""
Problème 1833 :
Écrivez une fonction Python appelée `subtract_lists` qui prend deux listes de nombres de le même longueur et retourne une nouvelle liste où chaque élément est le différence de corresponding éléments de les deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return numbers.index(max(numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_index_of_max is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_index_of_max(...))  # get_index_of_max() runs first → value goes to print()
"""
Problème 1834 :
Écrivez une fonction Python appelée `get_index_of_max` qui prend une liste de nombres et retourne le index de le maximum valeur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return numbers.index(min(numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_index_of_min is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_index_of_min(...))  # get_index_of_min() runs first → value goes to print()
"""
Problème 1835 :
Écrivez une fonction Python appelée `get_index_of_min` qui prend une liste de nombres et retourne le index de le minimum valeur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % +
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[n:] + my_list[:n]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only rotate_list_left is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(rotate_list_left(...))  # rotate_list_left() runs first → value goes to print()
"""
Problème 1836 :
Écrivez une fonction Python appelée `rotate_list_left` qui prend une liste et un nombre n, et retourne une nouvelle liste rotated n positions à le left.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % +
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[-n:] + my_list[:-n]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only rotate_list_right is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(rotate_list_right(...))  # rotate_list_right() runs first → value goes to print()
"""
Problème 1837 :
Écrivez une fonction Python appelée `rotate_list_right` qui prend une liste et un nombre n, et retourne une nouvelle liste rotated n positions à le right.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [my_list[i:i + n] for i in range(0, len(my_list), n)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only chunk_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(chunk_list(...))  # chunk_list() runs first → value goes to print()
"""
Problème 1838 :
Écrivez une fonction Python appelée `chunk_list` qui prend une liste et un nombre n, et retourne une liste de listes où chaque inner list contient n éléments (except possibly le dernier l'une).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: <
# Fonctions intégrées : len, max, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only interleave_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(interleave_lists(...))  # interleave_lists() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1839 :
Écrivez une fonction Python appelée `interleave_lists` qui prend deux listes et retourne une nouvelle liste avec éléments de les deux listes interleaved (alternating).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(zip(list1, list2))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only zip_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(zip_lists(...))  # zip_lists() runs first → value goes to print()
"""
Problème 1840 :
Écrivez une fonction Python appelée `zip_lists` qui prend deux listes et retourne une liste de tuples, où chaque tuple contient corresponding éléments de les deux listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ([], [])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only unzip_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → tuples_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(unzip_list(...))  # unzip_list() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1841 :
Écrivez une fonction Python appelée `unzip_list` qui prend une liste de tuples et retourne deux listes: l'une avec tous premier éléments et l'une avec tous deuxième éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((1 for char in text if char.isupper()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_uppercase_letters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_uppercase_letters(...))  # count_uppercase_letters() runs first → value goes to print()
"""
Problème 1842 :
Écrivez une fonction Python appelée `count_uppercase_letters` qui prend une chaîne et retourne le nombre de uppercase lettre dans la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((1 for char in text if char.islower()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_lowercase_letters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_lowercase_letters(...))  # count_lowercase_letters() runs first → value goes to print()
"""
Problème 1843 :
Écrivez une fonction Python appelée `count_lowercase_letters` qui prend une chaîne et retourne le nombre de lowercase lettre dans la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return str(text).swapcase()
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_uppercase_lowercase is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_uppercase_lowercase(...))  # swap_uppercase_lowercase() runs first → value goes to print()
"""
Problème 1844 :
Écrivez une fonction Python appelée `swap_uppercase_lowercase` qui prend une chaîne et retourne une nouvelle chaîne où uppercase lettre become lowercase et lowercase lettre become uppercase.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ''.join((char for char in text if not char.isdigit()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_numbers_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_numbers_from_string(...))  # remove_numbers_from_string() runs first → value goes to print()
"""
Problème 1845 :
Écrivez une fonction Python appelée `remove_numbers_from_string` qui prend une chaîne et retourne une nouvelle chaîne avec tous les chiffres removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return ''.join((char for char in text if not char.isalpha()))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_letters_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_letters_from_string(...))  # remove_letters_from_string() runs first → value goes to print()
"""
Problème 1846 :
Écrivez une fonction Python appelée `remove_letters_from_string` qui prend une chaîne et retourne une nouvelle chaîne avec tous lettre removed, keeping seulement les chiffres et autre caractères.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [char for char in text if char.isdigit()]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only extract_numbers_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(extract_numbers_from_string(...))  # extract_numbers_from_string() runs first → value goes to print()
"""
Problème 1847 :
Écrivez une fonction Python appelée `extract_numbers_from_string` qui prend une chaîne et retourne une liste de tous nombres trouvé dans la chaîne (as strings).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [char for char in text if char.isalpha()]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only extract_letters_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(extract_letters_from_string(...))  # extract_letters_from_string() runs first → value goes to print()
"""
Problème 1848 :
Écrivez une fonction Python appelée `extract_letters_from_string` qui prend une chaîne et retourne une liste de tous lettre trouvé dans la chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'({digits[0:3]}) {digits[3:6]}-{digits[6:10]}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_phone_number is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → digits = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_phone_number(...))  # format_phone_number() runs first → value goes to print()
"""
Problème 1849 :
Écrivez une fonction Python appelée `format_phone_number` qui prend une chaîne de 10 les chiffres et retourne it formatted as (XXX) XXX-XXXX.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return length * width * height
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_volume_box is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → height = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_volume_box(...))  # calculate_volume_box() runs first → value goes to print()
"""
Problème 1850 :
Écrivez une fonction Python appelée `calculate_volume_box` qui prend longueur, width, et height comme paramètres et retourne le volume de un rectangular box.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + > and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return a + b > c and a + c > b and (b + c > a)
#   → return a + b > c and a + c > b and (b + c > a)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_triangle_valid is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_triangle_valid(...), check_triangle_valid(...))  # check_triangle_valid() runs first → value goes to print()
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_triangle_valid(...))  # check_triangle_valid() runs first → value goes to print()
"""
Problème 1851 :
Écrivez une fonction Python appelée `check_triangle_valid` qui prend trois nombres representing side lengths et retourne vrai si they peut form un valid triangle, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: >=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'A'
#   → return 'A'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_grade is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(find_grade(...), find_grade(...), find_grade(...))  # find_grade() runs first → value goes to print()
#   → score = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_grade(...))  # find_grade() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1852 :
Écrivez une fonction Python appelée `find_grade` qui prend un score et retourne une lettre grade:'A'pour 90+,'B'pour 80-89,'C'pour 70-79,'D'pour 60-69,'F'pour en dessous de 60.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Winter'
#   → return 'Winter'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_season is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(get_season(...), get_season(...), get_season(...), get_season(...))  # get_season() runs first → value goes to print()
#   → month = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_season(...))  # get_season() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1853 :
Écrivez une fonction Python appelée `get_season` qui prend un month nombre (1-12) et retourne le season:'Winter'pour 12,1,2;'Spring'pour 3,4,5;'Summer'pour 6,7,8;'Fall'pour 9,10,11.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * - /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return price * (1 - discount_percent / 100)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_discount is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → price = ...  # stored in memory
#   → discount_percent = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_discount(...))  # calculate_discount() runs first → value goes to print()
"""
Problème 1854 :
Écrivez une fonction Python appelée `calculate_discount` qui prend un price et discount percentage, et retourne le final price après applying le discount.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * + /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (tip, total)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_tip is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → bill_amount = ...  # stored in memory
#   → tip_percent = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_tip(...))  # calculate_tip() runs first → value goes to print()
"""
Problème 1855 :
Écrivez une fonction Python appelée `calculate_tip` qui prend un bill amount et tip percentage, et retourne les deux le tip amount et total amount as un tuple.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return new_list
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_two_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → index1 = ...  # stored in memory
#   → index2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_two_elements(...))  # swap_two_elements() runs first → value goes to print()
"""
Problème 1856 :
Écrivez une fonction Python appelée `swap_two_elements` qui prend une liste et deux les indices, et retourne une nouvelle liste avec le éléments at ceux les indices swapped.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return new_list
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only insert_at_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → value = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(insert_at_index(...))  # insert_at_index() runs first → value goes to print()
"""
Problème 1857 :
Écrivez une fonction Python appelée `insert_at_index` qui prend une liste, un indice, et une valeur, et retourne une nouvelle liste avec le valeur inserted at qui index.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return new_list
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_at_index is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → index = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_at_index(...))  # remove_at_index() runs first → value goes to print()
"""
Problème 1858 :
Écrivez une fonction Python appelée `remove_at_index` qui prend une liste et un indice, et retourne une nouvelle liste avec le élément at qui index removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : max, min()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (min(numbers), max(numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_min_max is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_min_max(...))  # get_min_max() runs first → value goes to print()
"""
Problème 1860 :
Écrivez une fonction Python appelée `get_min_max` qui prend une liste de nombres et retourne un tuple contenant le minimum et maximum valeurs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % > == and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return number > 0 and number % 2 == 0 and (number % 3 == 0)
#   → return number > 0 and number % 2 == 0 and (number % 3 == 0)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_conditions is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_all_conditions(...), check_all_conditions(...))  # check_all_conditions() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_conditions(...))  # check_all_conditions() runs first → value goes to print()
"""
Problème 1861 :
Écrivez une fonction Python appelée `check_all_conditions` qui prend un nombre et retourne vrai si c'est positif, même, et divisible by 3, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '%s %s %s' % (first, middle, last)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_full_name is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → first = ...  # stored in memory
#   → middle = ...  # stored in memory
#   → last = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_full_name(...))  # format_full_name() runs first → value goes to print()
"""
Problème 1862 :
Écrivez une fonction Python appelée `format_full_name` qui prend premier nom, middle nom, et dernier nom, et retourne un formatted full nom chaîne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: / **
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return round(bmi, 1)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_bmi is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → weight_kg = ...  # stored in memory
#   → height_m = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_bmi(...))  # calculate_bmi() runs first → value goes to print()
"""
Problème 1863 :
Écrivez une fonction Python appelée `calculate_bmi` qui prend weight dans kg et height dans meters, et retourne le BMI valeur rounded à l'une decimal place.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: / < **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Underweight'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_bmi_category is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → weight_kg = ...  # stored in memory
#   → height_m = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_bmi_category(...))  # check_bmi_category() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1864 :
Écrivez une fonction Python appelée `check_bmi_category` qui prend weight et height, calcule BMI, et retourne le category:'Underweight'(<18.5),'Normal'(18.5-24.9),'Overweight'(25-29.9),'Obese'(30+).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in list1 if x in list2 and x in list3]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_common_elements_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_common_elements_three(...))  # find_common_elements_three() runs first → value goes to print()
"""
Problème 1865 :
Écrivez une fonction Python appelée `find_common_elements_three` qui prend trois listes et retourne une liste d'éléments qui appear dans tous trois listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list1 + list2 + list3
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only merge_three_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(merge_three_lists(...))  # merge_three_lists() runs first → value goes to print()
"""
Problème 1866 :
Écrivez une fonction Python appelée `merge_three_lists` qui prend trois listes et retourne une nouvelle liste contenant tous éléments de tous trois listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [my_list[i] for i in indices]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_at_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → indices = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_at_indices(...))  # get_elements_at_indices() runs first → value goes to print()
"""
Problème 1867 :
Écrivez une fonction Python appelée `get_elements_at_indices` qui prend une liste et une liste de les indices, et retourne une liste d'éléments at ceux les indices.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only replace_multiple_chars is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → replacements = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(replace_multiple_chars(...))  # replace_multiple_chars() runs first → value goes to print()
#
# Loop: takes one item from replacements.items(...), runs body, repeats until done
"""
Problème 1868 :
Écrivez une fonction Python appelée `replace_multiple_chars` qui prend une chaîne et un dictionnaire mapping old caractères à new caractères, et retourne une chaîne avec tous replacements made.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return counts
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words_by_length(...))  # count_words_by_length() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
"""
Problème 1869 :
Écrivez une fonction Python appelée `count_words_by_length` qui prend une chaîne et retourne un dictionnaire où clés sont mot lengths et valeurs sont compte de mots avec qui longueur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % * ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x * 2 for x in numbers if x % 2 == 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only filter_and_transform is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(filter_and_transform(...))  # filter_and_transform() runs first → value goes to print()
"""
Problème 1870 :
Écrivez une fonction Python appelée `filter_and_transform` qui prend une liste de nombres et retourne une nouvelle liste contenant seulement les nombres pairs, chaque multiplied by 2.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [s for s in strings if s.startswith(prefix)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_starting_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → prefix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_starting_with(...))  # get_strings_starting_with() runs first → value goes to print()
"""
Problème 1871 :
Écrivez une fonction Python appelée `get_strings_starting_with` qui prend une liste de chaînes et un prefix, et retourne une nouvelle liste contenant seulement strings qui start avec qui prefix.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [s for s in strings if s.endswith(suffix)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_ending_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → suffix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_ending_with(...))  # get_strings_ending_with() runs first → value goes to print()
"""
Problème 1872 :
Écrivez une fonction Python appelée `get_strings_ending_with` qui prend une liste de chaînes et un suffix, et retourne une nouvelle liste contenant seulement strings qui end avec qui suffix.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * + /
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.add(subtotal, tax)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_total_cost is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → prices = ...  # stored in memory
#   → tax_rate = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_total_cost(...))  # calculate_total_cost() runs first → value goes to print()
"""
Problème 1873 :
Écrivez une fonction Python appelée `calculate_total_cost` qui prend une liste de prices et un tax rate percentage, et retourne le total cost y compris tax.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : enumerate()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [i for i, x in enumerate(my_list) if x == element]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_all_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_all_indices(...))  # find_all_indices() runs first → value goes to print()
"""
Problème 1874 :
Écrivez une fonction Python appelée `find_all_indices` qui prend une liste et un élément, et retourne une liste de tous les indices où qui élément apparaît.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: +
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [my_list[i:i + chunk_size] for i in range(0, len(my_list), chunk_size)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only split_into_chunks is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → chunk_size = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(split_into_chunks(...))  # split_into_chunks() runs first → value goes to print()
"""
Problème 1875 :
Écrivez une fonction Python appelée `split_into_chunks` qui prend une liste et un chunk size, et retourne une liste de listes où chaque inner list un at la plupart chunk_size éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in my_list if x not in elements_to_remove]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_multiple_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → elements_to_remove = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_multiple_elements(...))  # remove_multiple_elements() runs first → value goes to print()
"""
Problème 1876 :
Écrivez une fonction Python appelée `remove_multiple_elements` qui prend une liste et une liste d'éléments à remove, et retourne une nouvelle liste avec tous ceux éléments removed.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {elem: my_list.count(elem) for elem in elements}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_occurrences_multiple is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → elements = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_occurrences_multiple(...))  # count_occurrences_multiple() runs first → value goes to print()
"""
Problème 1877 :
Écrivez une fonction Python appelée `count_occurrences_multiple` qui prend une liste et une liste d'éléments, et retourne un dictionnaire avec compte de chaque élément.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: + ==
# Fonctions intégrées : list, map, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda elem: elem, all_elements))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_elements_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_elements_three(...))  # get_unique_elements_three() runs first → value goes to print()
#
# Loop: takes one item from all_elements, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1878 :
Écrivez une fonction Python appelée `get_unique_elements_three` qui prend trois listes et retourne une liste d'éléments qui appear dans exactly l'une de le trois listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(people, key=lambda x: (x[1], x[2]))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only sort_by_multiple_criteria is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → people = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(sort_by_multiple_criteria(...))  # sort_by_multiple_criteria() runs first → value goes to print()
"""
Problème 1879 :
Écrivez une fonction Python appelée `sort_by_multiple_criteria` qui prend une liste de tuples (nom, age, score) et retourne la liste trié premier by age, puis by score.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: /
# Fonctions intégrées : len, max, min, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {'sum': sum(numbers), 'average': sum(numbers) / len(numbers), 'min': min(numbers), 'max': max(numbers)}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — calculate_statistics, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_statistics(...))  # calculate_statistics() runs first → value goes to print()
"""
Problème 1880 :
Écrivez une fonction Python appelée `calculate_statistics` qui prend une liste de nombres et retourne un dictionnaire avec'somme','moyenne','min', et'max'clés.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [int(match) for match in re.findall('\\d+', text)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only extract_numbers_from_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(extract_numbers_from_string(...))  # extract_numbers_from_string() runs first → value goes to print()
"""
Problème 1881 :
Écrivez une fonction Python appelée `extract_numbers_from_string` qui prend une chaîne et retourne une liste de tous nombres trouvé dans la chaîne (as integers).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >= and
# Fonctions intégrées : any, len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))
#   → return len(password) >= 8 and any((c.isalpha() for c in password)) and any((c.isdigit() for c in password))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_password_strength is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_password_strength(...), check_password_strength(...))  # check_password_strength() runs first → value goes to print()
#   → password = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_password_strength(...))  # check_password_strength() runs first → value goes to print()
"""
Problème 1883 :
Écrivez une fonction Python appelée `check_password_strength` qui prend un password chaîne et retourne vrai si it un at moins 8 caractères, contient les deux lettre et les chiffres, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [word for word in words if min_length <= len(word) <= max_length]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_by_length_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → min_length = ...  # stored in memory
#   → max_length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_by_length_range(...))  # get_words_by_length_range() runs first → value goes to print()
"""
Problème 1884 :
Écrivez une fonction Python appelée `get_words_by_length_range` qui prend une chaîne et min/max longueur, et retourne une liste de mots dans qui longueur range.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * + / **
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return principal * (1 + rate / (100 * frequency)) ** (frequency * time)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_compound_interest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → principal = ...  # stored in memory
#   → rate = ...  # stored in memory
#   → time = ...  # stored in memory
#   → frequency = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_compound_interest(...))  # calculate_compound_interest() runs first → value goes to print()
"""
Problème 1885 :
Écrivez une fonction Python appelée `calculate_compound_interest` qui prend principal, rate, time, et compounding la fréquence, et retourne le final amount.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: in
# Fonctions intégrées : list, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(duplicates)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_duplicate_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_duplicate_elements(...))  # find_duplicate_elements() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1886 :
Écrivez une fonction Python appelée `find_duplicate_elements` qui prend une liste et retourne une liste d'éléments qui appear plus que once.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return groups
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only group_by_first_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(group_by_first_letter(...))  # group_by_first_letter() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1887 :
Écrivez une fonction Python appelée `group_by_first_letter` qui prend une liste de chaînes et retourne un dictionnaire où clés sont premier lettre et valeurs sont listes de chaînes starting avec qui lettre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + - **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_distance is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → x1 = ...  # stored in memory
#   → y1 = ...  # stored in memory
#   → x2 = ...  # stored in memory
#   → y2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_distance(...))  # calculate_distance() runs first → value goes to print()
"""
Problem 1888:
def calculate_distance(x1, y1, x2, y2):  # defines a function with parameters x1, y1, x2, y2
    return math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2)  # return sends the result back to the caller
calculate_distance(0, 0, 3, 4)  # arguments evaluated first, then function body runs
calculate_distance(x1, y1, x2, y2)  # arguments evaluated first, then function body runs
print(calculate_distance(25, 25, 25, 25))  # calculate_distance() runs first, then print() outputs the result
? + ?  # ? → then ? → then +
? ** 2  # ? → then 2 → then **
x2 - x1  # x2 → then x1 → then -
y2 - y1  # y2 → then y1 → then -
import math
def calculate_distance(x1, y1, x2, y2):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_distance(x1, y1, x2, y2):  # stored in memory, not executed yet
def calculate_distance(x1, y1, x2, y2):  # stored in memory, not executed yet
x1 = ...  # assigns a value to the variable
y1 = ...  # assigns a value to the variable
x2 = ...  # assigns a value to the variable
y2 = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_distance(...))  # calculate_distance() runs with x1=25, y1=25, x2=25, y2=25


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return min_val <= number <= max_val
#   → return min_val <= number <= max_val
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_in_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_in_range(...), check_in_range(...))  # check_in_range() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_in_range(...))  # check_in_range() runs first → value goes to print()
"""
Problème 1889 :
Écrivez une fonction Python appelée `check_in_range` qui prend un nombre et un range (min, max), et retourne vrai si le nombre est dans le range (inclusive), otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + ==
# Fonctions intégrées : range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [i for i in range(1, number + 1) if number % i == 0]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_divisors is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_divisors(...))  # get_divisors() runs first → value goes to print()
"""
Problème 1890 :
Écrivez une fonction Python appelée `get_divisors` qui prend un nombre et retourne une liste de tous divisors de cette nombre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : int()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return root == int(root)
#   → return root == int(root)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_perfect_square is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_perfect_square(...), check_perfect_square(...))  # check_perfect_square() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_perfect_square(...))  # check_perfect_square() runs first → value goes to print()
"""
Problème 1891 :
Écrivez une fonction Python appelée `check_perfect_square` qui prend un nombre et retourne vrai si c'est une perfect carré, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'{hours:02d}:{minutes:02d}:{seconds:02d}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_time is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → hours = ...  # stored in memory
#   → minutes = ...  # stored in memory
#   → seconds = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_time(...))  # format_time() runs first → value goes to print()
"""
Problème 1892 :
Écrivez une fonction Python appelée `format_time` qui prend hours, minutes, et seconds, et retourne un formatted fois chaîne as HH:MM:SS.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: + - <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → import operator
#   → return operator.sub(current_year, birth_year)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_age is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → birth_year = ...  # stored in memory
#   → birth_month = ...  # stored in memory
#   → birth_day = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_age(...))  # calculate_age() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1893 :
Écrivez une fonction Python appelée `calculate_age` qui prend birth year, birth month, et birth day, et retourne le age dans years (assuming actuel date est 2024-01-01).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[1:-1]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_middle_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_middle_elements(...))  # get_middle_elements() runs first → value goes to print()
"""
Problème 1894 :
Écrivez une fonction Python appelée `get_middle_elements` qui prend une liste et retourne une nouvelle liste contenant éléments de index 1 à -1 (excluding premier et dernier).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: + -
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return new_list
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only swap_adjacent_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(swap_adjacent_elements(...))  # swap_adjacent_elements() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
"""
Problème 1895 :
Écrivez une fonction Python appelée `swap_adjacent_elements` qui prend une liste et retourne une nouvelle liste avec adjacents éléments swapped (1st avec 2nd, 3rd avec 4th, etc..).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return my_list[::n]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_every_nth_element is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_every_nth_element(...))  # get_every_nth_element() runs first → value goes to print()
"""
Problème 1896 :
Écrivez une fonction Python appelée `get_every_nth_element` qui prend une liste et un nombre n, et retourne une liste contenant chaque nth élément starting de index 0.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda item: item, my_list))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only remove_duplicates_preserve_order is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(remove_duplicates_preserve_order(...))  # remove_duplicates_preserve_order() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1898 :
Écrivez une fonction Python appelée `remove_duplicates_preserve_order` qui prend une liste et retourne une nouvelle liste avec les doublons removed tandis que preserving le order de premier occurrence.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in and
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in list1 if x in list2 and x in list3]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_intersection_three_lists is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → list3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_intersection_three_lists(...))  # get_intersection_three_lists() runs first → value goes to print()
"""
Problème 1899 :
Écrivez une fonction Python appelée `get_intersection_three_lists` qui prend trois listes et retourne une liste d'éléments qui appear dans tous trois listes.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * /
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return round(part / whole * 100, 2)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_percentage is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → part = ...  # stored in memory
#   → whole = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_percentage(...))  # calculate_percentage() runs first → value goes to print()
"""
Problème 1900 :
Écrivez une fonction Python appelée `calculate_percentage` qui prend un part et un whole, et retourne le percentage as un flottant rounded à 2 decimal places.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(numbers, reverse=True)[:3]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_largest_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_largest_three(...))  # find_largest_three() runs first → value goes to print()
"""
Problème 1901 :
Écrivez une fonction Python appelée `find_largest_three` qui prend une liste de nombres et retourne une liste contenant le trois plus grand nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(numbers)[:3]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_smallest_three is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_smallest_three(...))  # find_smallest_three() runs first → value goes to print()
"""
Problème 1902 :
Écrivez une fonction Python appelée `find_smallest_three` qui prend une liste de nombres et retourne une liste contenant le trois plus petit nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (is_palindrome, length)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_palindrome_and_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_palindrome_and_length(...))  # check_palindrome_and_length() runs first → value goes to print()
"""
Problème 1903 :
Écrivez une fonction Python appelée `check_palindrome_and_length` qui prend une chaîne et retourne un tuple contenant vrai/False pour un palindrome et la chaîne longueur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [word for word in words if word[0] in vowels]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_starting_with_vowel is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_starting_with_vowel(...))  # get_words_starting_with_vowel() runs first → value goes to print()
"""
Problème 1904 :
Écrivez une fonction Python appelée `get_words_starting_with_vowel` qui prend une chaîne et retourne une liste de mots qui start avec une voyelle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: and not in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [word for word in words if word[-1] not in vowels and word[-1].isalpha()]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_ending_with_consonant is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_ending_with_consonant(...))  # get_words_ending_with_consonant() runs first → value goes to print()
"""
Problème 1905 :
Écrivez une fonction Python appelée `get_words_ending_with_consonant` qui prend une chaîne et retourne une liste de mots qui end avec un consonne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (area, perimeter)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_area_perimeter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → length = ...  # stored in memory
#   → width = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_area_perimeter(...))  # calculate_area_perimeter() runs first → value goes to print()
"""
Problème 1906 :
Écrivez une fonction Python appelée `calculate_area_perimeter` qui prend longueur et width, et retourne un tuple contenant area et perimeter de un rectangle.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'${amount:.2f}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_currency is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → amount = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_currency(...))  # format_currency() runs first → value goes to print()
"""
Problème 1907 :
Écrivez une fonction Python appelée `format_currency` qui prend un nombre et retourne it formatted as currency avec dollar sign et deux decimal places.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [my_list[i] for i in range(0, len(my_list), 2)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_even_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_even_indices(...))  # get_even_indices() runs first → value goes to print()
"""
Problème 1908 :
Écrivez une fonction Python appelée `get_even_indices` qui prend une liste et retourne une liste contenant éléments at même les indices (0, 2, 4,...).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [my_list[i] for i in range(1, len(my_list), 2)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_odd_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_odd_indices(...))  # get_odd_indices() runs first → value goes to print()
"""
Problème 1909 :
Écrivez une fonction Python appelée `get_odd_indices` qui prend une liste et retourne une liste contenant éléments at impair les indices (1, 3, 5,...).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: *
# Fonctions intégrées : zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [a * b for a, b in zip(list1, list2)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only multiply_corresponding is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(multiply_corresponding(...))  # multiply_corresponding() runs first → value goes to print()
"""
Problème 1910 :
Écrivez une fonction Python appelée `multiply_corresponding` qui prend deux listes de nombres et retourne une nouvelle liste où chaque élément est le produit de corresponding éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: / ==
# Fonctions intégrées : zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only divide_corresponding is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(divide_corresponding(...))  # divide_corresponding() runs first → value goes to print()
#
# Loop: takes one item from zip(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1911 :
Écrivez une fonction Python appelée `divide_corresponding` qui prend deux listes de nombres et retourne une nouvelle liste où chaque élément est le division de corresponding éléments (handle division by zero by returning None).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [s for s in strings if substring in s]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_containing is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → substring = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_containing(...))  # get_strings_containing() runs first → value goes to print()
"""
Problème 1912 :
Écrivez une fonction Python appelée `get_strings_containing` qui prend une liste de chaînes et un substring, et retourne une liste de chaînes qui contien qui substring.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: and
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {'letters': sum((1 for c in text if c.isalpha())), 'digits': sum((1 for c in text if c.isdigit())), 'others': sum((1 for c in text if not c.isalnum() and (not c.isspace())))}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — count_characters_by_type, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_characters_by_type(...))  # count_characters_by_type() runs first → value goes to print()
"""
Problème 1913 :
Écrivez une fonction Python appelée `count_characters_by_type` qui prend une chaîne et retourne un dictionnaire avec'lettre','les chiffres', et'others'clés contenant leur compte.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in my_list if min_val <= x <= max_val]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_range_between is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_range_between(...))  # get_range_between() runs first → value goes to print()
"""
Problème 1914 :
Écrivez une fonction Python appelée `get_range_between` qui prend une liste et deux valeurs, et retourne une liste d'éléments qui fall entre ceux deux valeurs (inclusive).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'{year}-{month:02d}-{day:02d}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_date is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → year = ...  # stored in memory
#   → month = ...  # stored in memory
#   → day = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_date(...))  # format_date() runs first → value goes to print()
"""
Problème 1915 :
Écrivez une fonction Python appelée `format_date` qui prend year, month, et day, et retourne un formatted date chaîne as YYYY-MM-DD.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return principal * rate * time / 100
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_simple_interest is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → principal = ...  # stored in memory
#   → rate = ...  # stored in memory
#   → time = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_simple_interest(...))  # calculate_simple_interest() runs first → value goes to print()
"""
Problème 1916 :
Écrivez une fonction Python appelée `calculate_simple_interest` qui prend principal, rate, et time, et retourne le simple interest amount.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda char: char, text))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_characters is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_characters(...))  # get_unique_characters() runs first → value goes to print()
#
# Loop: takes one item from text, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1917 :
Écrivez une fonction Python appelée `get_unique_characters` qui prend une chaîne et retourne une liste de unique caractères dans le order they premier appear.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((1 for word in words if word.endswith(suffix)))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words_ending_with is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → suffix = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words_ending_with(...))  # count_words_ending_with() runs first → value goes to print()
"""
Problème 1918 :
Écrivez une fonction Python appelée `count_words_ending_with` qui prend une chaîne et un suffix, et retourne le nombre de mots qui end avec qui suffix.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if min_val <= x <= max_val]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_in_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_in_range(...))  # get_numbers_in_range() runs first → value goes to print()
"""
Problème 1919 :
Écrivez une fonction Python appelée `get_numbers_in_range` qui prend une liste de nombres et un range (min, max), et retourne une liste de nombres dans qui range.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Name: %s, Age: %s, City: %s' % (name, age, city)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_name_age_city is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → age = ...  # stored in memory
#   → city = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_name_age_city(...))  # format_name_age_city() runs first → value goes to print()
"""
Problème 1920 :
Écrivez une fonction Python appelée `format_name_age_city` qui prend nom, age, et city, et retourne un formatted chaîne:'nom: {nom}, Age: {age}, City: {city}'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return all((number % d == 0 for d in divisors))
#   → return all((number % d == 0 for d in divisors))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_multiple_of is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_multiple_of(...), check_multiple_of(...))  # check_multiple_of() runs first → value goes to print()
#   → number = ...  # stored in memory
#   → divisors = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_multiple_of(...))  # check_multiple_of() runs first → value goes to print()
"""
Problème 1921 :
Écrivez une fonction Python appelée `check_multiple_of` qui prend un nombre et une liste de divisors, et retourne vrai si le nombre est divisible by tous divisors, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: **
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((x ** 2 for x in numbers))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_sum_of_squares is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_sum_of_squares(...))  # get_sum_of_squares() runs first → value goes to print()
"""
Problème 1922 :
Écrivez une fonction Python appelée `get_sum_of_squares` qui prend une liste de nombres et retourne le somme de squares de tous nombres.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: % ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return product
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_product_of_even is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_product_of_even(...))  # get_product_of_even() runs first → value goes to print()
#
# Loop: takes one item from numbers, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1923 :
Écrivez une fonction Python appelée `get_product_of_even` qui prend une liste de nombres et retourne le produit de tous les nombres pairs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % !=
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum((x for x in numbers if x % 2 != 0))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_sum_of_odd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_sum_of_odd(...))  # get_sum_of_odd() runs first → value goes to print()
"""
Problème 1924 :
Écrivez une fonction Python appelée `get_sum_of_odd` qui prend une liste de nombres et retourne le somme de tous les nombres impairs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ==
# Fonctions intégrées : enumerate()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return i
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only find_second_occurrence is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → element = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(find_second_occurrence(...))  # find_second_occurrence() runs first → value goes to print()
#
# Loop: takes one item from enumerate(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1925 :
Écrivez une fonction Python appelée `find_second_occurrence` qui prend une liste et un élément, et retourne le index de la deuxième occurrence de cette élément, ou -1 si pas trouvé.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: / >
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x > avg]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_greater_than_average is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_greater_than_average(...))  # get_elements_greater_than_average() runs first → value goes to print()
"""
Problème 1926 :
Écrivez une fonction Python appelée `get_elements_greater_than_average` qui prend une liste de nombres et retourne une liste de nombres greater que le moyenne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: / <
# Fonctions intégrées : len, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in numbers if x < avg]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_less_than_average is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_less_than_average(...))  # get_elements_less_than_average() runs first → value goes to print()
"""
Problème 1927 :
Écrivez une fonction Python appelée `get_elements_less_than_average` qui prend une liste de nombres et retourne une liste de nombres less que le moyenne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [word for word in words if len(word) == length]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_length(...))  # get_words_with_length() runs first → value goes to print()
"""
Problème 1929 :
Écrivez une fonction Python appelée `get_words_with_length` qui prend une chaîne et un longueur, et retourne une liste de mots avec exactly qui longueur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '%s %s, %s, %s %s' % (street_num, street_name, city, state, zipcode)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_full_address is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → street_num = ...  # stored in memory
#   → street_name = ...  # stored in memory
#   → city = ...  # stored in memory
#   → state = ...  # stored in memory
#   → zipcode = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_full_address(...))  # format_full_address() runs first → value goes to print()
"""
Problème 1930 :
Écrivez une fonction Python appelée `format_full_address` qui prend street nombre, street nom, city, state, et zipcode, et retourne un complet formatted address.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * + - /
# Fonctions intégrées : round, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return total
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_total_with_discount is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → prices = ...  # stored in memory
#   → discount_percent = ...  # stored in memory
#   → tax_rate = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_total_with_discount(...))  # calculate_total_with_discount() runs first → value goes to print()
"""
Problème 1931 :
Écrivez une fonction Python appelée `calculate_total_with_discount` qui prend une liste de prices, un discount percentage, et un tax rate, et retourne le final total.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: ==
# Fonctions intégrées : len, min, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return prefix
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_common_prefix is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_common_prefix(...))  # get_common_prefix() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1932 :
Écrivez une fonction Python appelée `get_common_prefix` qui prend deux strings et retourne le plus long commun prefix they share.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: + ==
# Fonctions intégrées : len, min, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return suffix
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_common_suffix is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_common_suffix(...))  # get_common_suffix() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1933 :
Écrivez une fonction Python appelée `get_common_suffix` qui prend deux strings et retourne le plus long commun suffix they share.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return all((word[0].isupper() for word in words if word))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_all_uppercase_words is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_all_uppercase_words(...))  # check_all_uppercase_words() runs first → value goes to print()
"""
Problème 1934 :
Écrivez une fonction Python appelée `check_all_uppercase_words` qui prend une chaîne et retourne vrai si tous words start avec uppercase lettre, otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <=
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [s for s in strings if min_length <= len(s) <= max_length]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_with_length_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → min_length = ...  # stored in memory
#   → max_length = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_with_length_range(...))  # get_strings_with_length_range() runs first → value goes to print()
"""
Problème 1935 :
Écrivez une fonction Python appelée `get_strings_with_length_range` qui prend une liste de chaînes et min/max longueur, et retourne strings dans qui longueur range.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: % + - / // ==
# Fonctions intégrées : len, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return (sorted_nums[n // 2 - 1] + sorted_nums[n // 2]) / 2
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_median is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_median(...))  # calculate_median() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1936 :
Écrivez une fonction Python appelée `calculate_median` qui prend une liste de nombres et retourne la médiane valeur (middle valeur lorsque trié).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : max()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return max(counts, key=counts.get)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_mode is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_mode(...))  # get_mode() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1937 :
Écrivez une fonction Python appelée `get_mode` qui prend une liste et retourne le élément qui apparaît la plupart frequently (le mode).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % ==
# Fonctions intégrées : all()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [n for n in numbers if all((n % d == 0 for d in divisors))]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_divisible_by_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → divisors = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_divisible_by_all(...))  # get_numbers_divisible_by_all() runs first → value goes to print()
"""
Problème 1939 :
Écrivez une fonction Python appelée `get_numbers_divisible_by_all` qui prend une liste de nombres et une liste de divisors, et retourne nombres divisible by tous divisors.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: == in
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [word for word in words if sum((1 for c in word if c in vowels)) == n]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_vowel_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_vowel_count(...))  # get_words_with_vowel_count() runs first → value goes to print()
"""
Problème 1941 :
Écrivez une fonction Python appelée `get_words_with_vowel_count` qui prend une chaîne et un nombre n, et retourne words qui ont exactly n voyelles.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '%s | %s | %s' % (value1, value2, value3)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_table_row is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → value1 = ...  # stored in memory
#   → value2 = ...  # stored in memory
#   → value3 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_table_row(...))  # format_table_row() runs first → value goes to print()
"""
Problème 1942 :
Écrivez une fonction Python appelée `format_table_row` qui prend trois valeurs et retourne un formatted chaîne avec valeurs séparés by pipes:'value1 | value2 | value3'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : list, map, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda item: item, list1))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_not_in_both is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_not_in_both(...))  # get_elements_not_in_both() runs first → value goes to print()
#
# Loop: takes one item from list1, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1943 :
Écrivez une fonction Python appelée `get_elements_not_in_both` qui prend deux listes et retourne une liste d'éléments qui sont dans l'une list but pas le autre.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * /
# Fonctions intégrées : Fraction, float, sum, zip()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → from fractions import Fraction
#   → return float(Fraction(weighted_sum, total_weight))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_weighted_average is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → values = ...  # stored in memory
#   → weights = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_weighted_average(...))  # calculate_weighted_average() runs first → value goes to print()
"""
Problème 1944 :
Écrivez une fonction Python appelée `calculate_weighted_average` qui prend une liste de valeurs et une liste de weights, et retourne le weighted moyenne.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(strings, key=len)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_sorted_by_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_sorted_by_length(...))  # get_strings_sorted_by_length() runs first → value goes to print()
"""
Problème 1945 :
Écrivez une fonction Python appelée `get_strings_sorted_by_length` qui prend une liste de chaînes et retourne la liste trié by longueur (le plus court à le plus long).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [f'{n}%' for n in numbers]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_percentage_list is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_percentage_list(...))  # format_percentage_list() runs first → value goes to print()
"""
Problème 1946 :
Écrivez une fonction Python appelée `format_percentage_list` qui prend une liste de nombres et retourne une liste de chaînes, chaque formatted as un percentage avec % sign.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: + ==
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pairs
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_pairs_summing_to is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → target = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_pairs_summing_to(...))  # get_pairs_summing_to() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1947 :
Écrivez une fonction Python appelée `get_pairs_summing_to` qui prend une liste de nombres et une cible somme, et retourne une liste de tuples contenant pairs qui somme à le cible.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: in and not in
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → vowels = 'aeiouAEIOU'
#   → return {'vowels': sum((1 for c in text if c in vowels)), 'consonants': sum((1 for c in text if c.isalpha() and c not in vowels)), 'digits': sum((1 for c in text if c.isdigit())), 'spaces': sum((1 for c in text if c.isspace()))}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — count_character_types, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_character_types(...))  # count_character_types() runs first → value goes to print()
"""
Problème 1948 :
Écrivez une fonction Python appelée `count_character_types` qui prend une chaîne et retourne un dictionnaire avec'voyelles','consonnes','les chiffres', et'spaces'compte.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: in not in
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_by_vowel_count is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_by_vowel_count(...))  # get_words_by_vowel_count() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1949 :
Écrivez une fonction Python appelée `get_words_by_vowel_count` qui prend une chaîne et retourne un dictionnaire où clés sont voyelle compte et valeurs sont listes de mots avec qui count.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '%s. %s %s' % (title, first, last)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_name_with_title is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → first = ...  # stored in memory
#   → last = ...  # stored in memory
#   → title = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_name_with_title(...))  # format_name_with_title() runs first → value goes to print()
"""
Problème 1950 :
Écrivez une fonction Python appelée `format_name_with_title` qui prend premier nom, dernier nom, et title, et retourne un formatted chaîne:'Title. premier dernier'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * / **
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return angle_degrees / 360 * math.pi * radius ** 2
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_area_circle_sector is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → radius = ...  # stored in memory
#   → angle_degrees = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_area_circle_sector(...))  # calculate_area_circle_sector() runs first → value goes to print()
"""
Problème 1951 :
Écrivez une fonction Python appelée `calculate_area_circle_sector` qui prend radius et angle dans degrees, et retourne le area de le sector.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {i: my_list[i] for i in indices if i < len(my_list)}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_at_multiple_indices is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → indices = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_at_multiple_indices(...))  # get_elements_at_multiple_indices() runs first → value goes to print()
"""
Problème 1952 :
Écrivez une fonction Python appelée `get_elements_at_multiple_indices` qui prend une liste et une liste de les indices, et retourne un dictionnaire où clés sont les indices et valeurs sont éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: and
# Fonctions intégrées : any()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return {'has_uppercase': any((c.isupper() for c in text)), 'has_lowercase': any((c.islower() for c in text)), 'has_digit': any((c.isdigit() for c in text)), 'has_special': any((not c.isalnum() and (not c.isspace()) for c in text))}
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — check_string_properties, solve are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_string_properties(...))  # check_string_properties() runs first → value goes to print()
"""
Problème 1953 :
Écrivez une fonction Python appelée `check_string_properties` qui prend une chaîne et retourne un dictionnaire avec boolean valeurs pour'has_uppercase','has_lowercase','has_digit','has_special'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: + <=
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return pairs
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_numbers_summing_to_range is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → numbers = ...  # stored in memory
#   → min_sum = ...  # stored in memory
#   → max_sum = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_numbers_summing_to_range(...))  # get_numbers_summing_to_range() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1954 :
Écrivez une fonction Python appelée `get_numbers_summing_to_range` qui prend une liste de nombres et un range (min_sum, max_sum), et retourne pairs de nombres whose somme est dans qui range.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '+%s (%s) %s' % (country_code, area_code, number)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_phone_with_country is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → country_code = ...  # stored in memory
#   → area_code = ...  # stored in memory
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_phone_with_country(...))  # format_phone_with_country() runs first → value goes to print()
"""
Problème 1955 :
Écrivez une fonction Python appelée `format_phone_with_country` qui prend country code, area code, et nombre, et retourne un formatted phone nombre:'+{country} ({area}) {nombre}'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: + -
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [my_list[i:i + n] for i in range(len(my_list) - n + 1)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_consecutive_elements is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → n = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_consecutive_elements(...))  # get_consecutive_elements() runs first → value goes to print()
"""
Problème 1956 :
Écrivez une fonction Python appelée `get_consecutive_elements` qui prend une liste et un nombre n, et retourne une liste de listes où chaque inner list contient n consécutifs éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: %
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return a
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_gcd is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_gcd(...))  # calculate_gcd() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
"""
Problème 1957 :
Écrivez une fonction Python appelée `calculate_gcd` qui prend deux nombres et retourne leur greatest commun divisor en utilisant Euclidean algorithm.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: while
# Operators: % * //
# Fonctions intégrées : abs, gcd()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return x
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_lcm is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_lcm(...))  # calculate_lcm() runs first → value goes to print()
#
# Loop: check condition → if True run body → check again
"""
Problème 1958 :
Écrivez une fonction Python appelée `calculate_lcm` qui prend deux nombres et retourne leur moins commun multiple.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
# Fonctions intégrées : dict, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return dict(sorted(counts.items(), key=lambda x: x[1], reverse=True))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_frequency_dict is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_frequency_dict(...))  # get_frequency_dict() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
"""
Problème 1959 :
Écrivez une fonction Python appelée `get_frequency_dict` qui prend une liste et retourne un dictionnaire trié by la fréquence (la plupart frequent premier).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'{number:,}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_number_with_commas is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_number_with_commas(...))  # format_number_with_commas() runs first → value goes to print()
"""
Problème 1960 :
Écrivez une fonction Python appelée `format_number_with_commas` qui prend un nombre et retourne le en une chaîne avec comma separators (e.g., 1000 becomes'1,000').
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: not in
# Fonctions intégrées : len()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return result
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_same_length is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_same_length(...))  # get_words_with_same_length() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1961 :
Écrivez une fonction Python appelée `get_words_with_same_length` qui prend une liste de chaînes et retourne un dictionnaire où clés sont lengths et valeurs sont listes de chaînes avec qui longueur.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: ==
# Fonctions intégrées : sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))
#   → return sorted(str1.lower().replace(' ', '')) == sorted(str2.lower().replace(' ', ''))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only check_anagram_pair is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(check_anagram_pair(...), check_anagram_pair(...))  # check_anagram_pair() runs first → value goes to print()
#   → str1 = ...  # stored in memory
#   → str2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(check_anagram_pair(...))  # check_anagram_pair() runs first → value goes to print()
"""
Problème 1962 :
Écrivez une fonction Python appelée `check_anagram_pair` qui prend deux strings et retourne vrai si they sont anagrams (contien même caractères), otherwise False.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return sum(my_list[start:end])
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_sublist_sum is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → start = ...  # stored in memory
#   → end = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_sublist_sum(...))  # get_sublist_sum() runs first → value goes to print()
"""
Problème 1963 :
Écrivez une fonction Python appelée `get_sublist_sum` qui prend une liste et start/end les indices, et retourne le somme d'éléments dans qui sublist.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: - < ==
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return '12 AM'
#   → return '12 AM'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_time_12hour is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → print(format_time_12hour(...), format_time_12hour(...), format_time_12hour(...))  # format_time_12hour() runs first → value goes to print()
#   → hours_24 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_time_12hour(...))  # format_time_12hour() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1964 :
Écrivez une fonction Python appelée `format_time_12hour` qui prend hours (24-hour format) et retourne fois dans 12-hour format avec AM/PM.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: <
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in my_list if min_val < x < max_val]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_between_values is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → min_val = ...  # stored in memory
#   → max_val = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_between_values(...))  # get_elements_between_values() runs first → value goes to print()
"""
Problème 1965 :
Écrivez une fonction Python appelée `get_elements_between_values` qui prend une liste et deux valeurs, et retourne éléments qui sont greater que le premier valeur et less que le deuxième.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: + - ==
# Fonctions intégrées : len, range()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return count
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_substring_occurrences is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → substring = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_substring_occurrences(...))  # count_substring_occurrences() runs first → value goes to print()
#
# Loop: takes one item from range(...), runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1966 :
Écrivez une fonction Python appelée `count_substring_occurrences` qui prend une chaîne et un substring, et retourne combien de fois substring apparaît (overlapping allowed).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'[{separator.join((str(x) for x in my_list))}]'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_list_as_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → separator = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_list_as_string(...))  # format_list_as_string() runs first → value goes to print()
"""
Problème 1967 :
Écrivez une fonction Python appelée `format_list_as_string` qui prend une liste et un separator, et retourne une chaîne avec éléments joined by separator et enclosed dans brackets.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [(a, b) for a in list1 for b in list2]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_unique_combinations is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_unique_combinations(...))  # get_unique_combinations() runs first → value goes to print()
"""
Problème 1968 :
Écrivez une fonction Python appelée `get_unique_combinations` qui prend deux listes et retourne une liste de tuples contenant tous unique combinations d'éléments.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: if/else
# Operators: * + - / < **
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return None
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_quadratic_roots is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → a = ...  # stored in memory
#   → b = ...  # stored in memory
#   → c = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_quadratic_roots(...))  # calculate_quadratic_roots() runs first → value goes to print()
# Branch: condition picks which block runs — the rest are skipped
"""
Problem 1969:
def calculate_quadratic_roots(a, b, c):  # defines a function with parameters a, b, c
    discriminant = b ** 2 - 4 * a * c
    if discriminant < 0:
        return None
    root1 = (-b + math.sqrt(discriminant)) / (2 * a)
    root2 = (-b - math.sqrt(discriminant)) / (2 * a)
    return (root1, root2)  # return sends the result back to the caller
calculate_quadratic_roots(1, ?, 6)  # arguments evaluated first, then function body runs
calculate_quadratic_roots(a, b, c)  # arguments evaluated first, then function body runs
print(calculate_quadratic_roots(20, 30, 10))  # calculate_quadratic_roots() runs first, then print() outputs the result
? - ?  # ? → then ? → then -
discriminant < 0  # evaluated as True or False → picks which branch
? / ?  # ? → then ? → then /
b ** 2  # b → then 2 → then **
? * c  # ? → then c → then *
? + math.sqrt(...)  # ? → then math.sqrt(...) → then +
2 * a  # 2 → then a → then *
? - math.sqrt(...)  # ? → then math.sqrt(...) → then -
4 * a  # 4 → then a → then *
import math
def calculate_quadratic_roots(a, b, c):  # stored in memory, not executed yet
result = ...  # assigns a value to the variable
print(result)
def calculate_quadratic_roots(a, b, c):  # stored in memory, not executed yet
def calculate_quadratic_roots(a, b, c):  # stored in memory, not executed yet
a = ...  # assigns a value to the variable
b = ...  # assigns a value to the variable
c = ...  # assigns a value to the variable
result = ...  # assigns a value to the variable
print(result)
print(calculate_quadratic_roots(...))  # calculate_quadratic_roots() runs with a=20, b=30, c=10


#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: >=
# Fonctions intégrées : list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda s: s, strings))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_matching_pattern is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → pattern = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_matching_pattern(...))  # get_strings_matching_pattern() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1970 :
Écrivez une fonction Python appelée `get_strings_matching_pattern` qui prend une liste de chaînes et un motif (substring), et retourne strings qui contien le motif at moins twice.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'{first[0]}.{middle[0]}.{last[0]}.'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_name_initials is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → first = ...  # stored in memory
#   → middle = ...  # stored in memory
#   → last = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_name_initials(...))  # format_name_initials() runs first → value goes to print()
"""
Problème 1971 :
Écrivez une fonction Python appelée `format_name_initials` qui prend premier nom, middle nom, et dernier nom, et retourne initials avec periods:'F.M.L.'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: >
# Fonctions intégrées : condition_func()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return [x for x in my_list if condition_func(x)]
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_with_condition is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → condition_func = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_with_condition(...))  # get_elements_with_condition() runs first → value goes to print()
"""
Problème 1972 :
Écrivez une fonction Python appelée `get_elements_with_condition` qui prend une liste et une fonction (lambda), et retourne éléments qui satisfy la condition.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: * ** ==
# Fonctions intégrées : round()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return total
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_total_area is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(round(...))
#   → shapes = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_total_area(...))  # calculate_total_area() runs first → value goes to print()
#
# Loop: takes one item from shapes, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1973 :
Écrivez une fonction Python appelée `calculate_total_area` qui prend une liste de shapes represented as tuples (shape_type, dimensions), et retourne le total area. Shape tapez:'rectangle'(longueur, width),'circle'(radius).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: !=
# Fonctions intégrées : len, list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda word: word, words))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_words_with_repeated_chars is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_words_with_repeated_chars(...))  # get_words_with_repeated_chars() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1974 :
Écrivez une fonction Python appelée `get_words_with_repeated_chars` qui prend une chaîne et retourne une liste de mots qui contien at moins l'une repeated caractère.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: %
# Fonctions intégrées : str()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return 'Name: %s, Scores: %s' % (name, scores_str)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_multi_value_string is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → name = ...  # stored in memory
#   → scores = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_multi_value_string(...))  # format_multi_value_string() runs first → value goes to print()
"""
Problème 1975 :
Écrivez une fonction Python appelée `format_multi_value_string` qui prend un nom et une liste de scores, et retourne un formatted chaîne:'nom: {nom}, Scores: {scores}'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Fonctions intégrées : list, set, sorted()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(set1.symmetric_difference(set2))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_symmetric_difference is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(sorted(...))
#   → list1 = ...  # stored in memory
#   → list2 = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_symmetric_difference(...))  # get_symmetric_difference() runs first → value goes to print()
"""
Problème 1976 :
Écrivez une fonction Python appelée `get_symmetric_difference` qui prend deux listes et retourne une liste d'éléments qui sont dans l'une list but pas le autre (symmetric différence).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for
# Operators: +
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return counts
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only count_words_by_first_letter is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → text = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(count_words_by_first_letter(...))  # count_words_by_first_letter() runs first → value goes to print()
#
# Loop: takes one item from words, runs body, repeats until done
"""
Problème 1977 :
Écrivez une fonction Python appelée `count_words_by_first_letter` qui prend une chaîne et retourne un dictionnaire où clés sont premier lettre et valeurs sont mot compte.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return f'{number:.{decimal_places}f}'
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only format_decimal_places is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → number = ...  # stored in memory
#   → decimal_places = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(format_decimal_places(...))  # format_decimal_places() runs first → value goes to print()
"""
Problème 1978 :
Écrivez une fonction Python appelée `format_decimal_places` qui prend un nombre et decimal places, et retourne le nombre formatted à qui many decimal places.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: < >
# Fonctions intégrées : all, cond, list, map()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda item: item, my_list))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_elements_satisfying_all is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → my_list = ...  # stored in memory
#   → ? = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_elements_satisfying_all(...))  # get_elements_satisfying_all() runs first → value goes to print()
#
# Loop: takes one item from my_list, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1979 :
Écrivez une fonction Python appelée `get_elements_satisfying_all` qui prend une liste et multiple conditions (fonctions), et retourne éléments qui satisfy tous conditions.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: * + - /
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return final
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_net_price is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → original_price = ...  # stored in memory
#   → discount_percent = ...  # stored in memory
#   → tax_rate = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(calculate_net_price(...))  # calculate_net_price() runs first → value goes to print()
"""
Problème 1980 :
Écrivez une fonction Python appelée `calculate_net_price` qui prend original price, discount percentage, et tax rate, et retourne le final price après discount et tax.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Fonctions intégrées : list, map, set()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → return list(map(lambda s: s, strings))
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only get_strings_with_all_vowels is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → strings = ...  # stored in memory
#   → result = ...  # stored in memory
#   → print(result)
#   → print(get_strings_with_all_vowels(...))  # get_strings_with_all_vowels() runs first → value goes to print()
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1981 :
Écrivez une fonction Python appelée `get_strings_with_all_vowels` qui prend une liste de chaînes et retourne strings qui contien tous cinq voyelles (a, e, i, o, u).
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 1984 :
Écrivez une fonction Python appelée `calculate_geometric_mean` qui prend une liste de les nombres positifs et retourne le geometric mean.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Keywords: for | if/else
# Operators: in
# Fonctions intégrées : all, isinstance()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#   → lst = ['a', 'b', 'c']
#   → result = True
#   → for x in lst:
#   →     if not isinstance(x, str):
#   →         result = False
#   →         break
#   → print(result)
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — get_elements_matching_multiple_patterns, main are stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
#   → main()  # jumps into main(), runs body, returns
#
# Loop: takes one item from strings, runs body, repeats until done
# Branch: condition picks which block runs — the rest are skipped
"""
Problème 1997 :
Écrivez une fonction Python appelée `get_elements_matching_multiple_patterns` qui prend une liste de chaînes et multiple motifs, et retourne strings qui match tous motifs.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# Operators: % + - / //
# Fonctions intégrées : len, max, min, sorted, sum()
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# def blocks are skipped at runtime — only calculate_statistical_summary is stored in memory
#
# Execution starts at the top-level code (outside def/class):
#   → result = ...  # stored in memory
#   → print(result)
"""
Problème 1998 :
Écrivez une fonction Python appelée `calculate_statistical_summary` qui prend une liste de nombres et retourne un dictionnaire avec'mean','la médiane','min','max', et'range'.
"""

#
# SYNTAX:
# Syntax describes the Python building blocks used in this code —
# the keywords, operators, and structures that make it work.
#
#
# EVALUATION ORDER:
# Evaluation Order traces how Python reads and evaluates expressions.
# Inner expressions are evaluated first, then the outer ones.
#
# (expressions evaluate left to right, inner calls first)
#
# EXECUTION ORDER:
# Execution Order shows the sequence Python follows when running
# the code. 'def' and 'class' blocks are just stored, not run yet.
#
#
# EXECUTION FLOW:
# Flux d'Exécution tracks where Python jumps during runtime — which
# blocks are skipped and which actually run.
#
# No top-level code runs — functions must be called externally
"""
Problème 2000 :
Écrivez une fonction Python appelée `get_comprehensive_statistics` qui prend une liste de nombres et retourne un dictionnaire avec somme, moyenne, min, max, count, et l'écart-tapez (en utilisant basic formula).
"""

