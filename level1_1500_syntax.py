# Syntax documentation for level 1 (problems 1-1500)
# Auto-generated by gen_syntax_files.py

"""
Problem 1001:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to reversed_list
#   3. Run print(reversed_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to reversed_list
#   8. Run print(reversed_list)
#   9. Assign to numbers
#   10. Assign to reversed_list
#   11. Run print(reversed_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      reversed_list = ... (stored in memory)
#      print(reversed_list)
#      numbers = ... (stored in memory)
#      reversed_list = ... (stored in memory)
#      print(reversed_list)
#      numbers = ... (stored in memory)
#      reversed_list = ... (stored in memory)
#      print(reversed_list)

"""

"""
Problem 1002:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_list
#   3. Run print(sliced_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_list
#   8. Run print(sliced_list)
#   9. Assign to numbers
#   10. Assign to sliced_list
#   11. Run print(sliced_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)

"""

"""
Problem 1003:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1004:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: + - //
#
# ===== EVALUATION ORDER =====
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 2: first get middle_index, then get 2, then compute middle_index + 2
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 2: first get middle_index, then get 2, then compute middle_index + 2
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 2: first get middle_index, then get 2, then compute middle_index + 2
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 2: first get middle_index, then get 2, then compute middle_index + 2
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 2: first get middle_index, then get 2, then compute middle_index + 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to middle_index
#   3. Assign to sliced_text
#   4. Run print(sliced_text)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to text
#   8. Assign to middle_index
#   9. Assign to sliced_text
#   10. Run print(sliced_text)
#   11. Assign to text
#   12. Assign to middle_index
#   13. Assign to sliced_text
#   14. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      middle_index = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      middle_index = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      middle_index = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1005:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1006:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: //
#
# ===== EVALUATION ORDER =====
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1007:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: //
#
# ===== EVALUATION ORDER =====
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1008:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: + - //
#
# ===== EVALUATION ORDER =====
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   len(...) // 2: first get len(...), then get 2, then compute len(...) // 2
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 1: first get middle_index, then get 1, then compute middle_index + 1
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 1: first get middle_index, then get 1, then compute middle_index + 1
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 1: first get middle_index, then get 1, then compute middle_index + 1
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 1: first get middle_index, then get 1, then compute middle_index + 1
#   middle_index - 1: first get middle_index, then get 1, then compute middle_index - 1
#   middle_index + 1: first get middle_index, then get 1, then compute middle_index + 1
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to middle_index
#   3. Assign to sliced_text
#   4. Run print(sliced_text)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to text
#   8. Assign to middle_index
#   9. Assign to sliced_text
#   10. Run print(sliced_text)
#   11. Assign to text
#   12. Assign to middle_index
#   13. Assign to sliced_text
#   14. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      middle_index = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      middle_index = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      middle_index = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1009:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_list
#   3. Run print(sliced_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_list
#   8. Run print(sliced_list)
#   9. Assign to numbers
#   10. Assign to sliced_list
#   11. Run print(sliced_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)

"""

"""
Problem 1010:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_tuple
#   3. Run print(sliced_tuple)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_tuple
#   8. Run print(sliced_tuple)
#   9. Assign to numbers
#   10. Assign to sliced_tuple
#   11. Run print(sliced_tuple)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)

"""

"""
Problem 1011:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_list
#   3. Run print(sliced_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_list
#   8. Run print(sliced_list)
#   9. Assign to numbers
#   10. Assign to sliced_list
#   11. Run print(sliced_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)

"""

"""
Problem 1012:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1013:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_tuple
#   3. Run print(sliced_tuple)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_tuple
#   8. Run print(sliced_tuple)
#   9. Assign to numbers
#   10. Assign to sliced_tuple
#   11. Run print(sliced_tuple)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)

"""

"""
Problem 1014:
#
# ===== SYNTAX =====
#   def is_palindrome(s)
#   return — exits the function and sends back a value
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome('madam')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome('hello')): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome(?)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   is_palindrome('hello'): gets the literal 'hello', then runs the function body
#   is_palindrome(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(s) — stored for later
#   2. Run print(is_palindrome(...))
#       calls is_palindrome with: s='madam'
#         2.1 Return from function
#   3. Run print(is_palindrome(...))
#       calls is_palindrome with: s='hello'
#         3.1 Return from function
#   4. Define def is_palindrome(s) — stored for later
#   5. Define def is_palindrome(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_palindrome(...))
#       calls is_palindrome with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      print(is_palindrome('madam')) → function runs first, then print shows the result
#      print(is_palindrome('hello')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome(?)) → function runs first, then print shows the result

"""

"""
Problem 1015:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1016:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_list
#   3. Run print(sliced_list)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_list
#   8. Run print(sliced_list)
#   9. Assign to numbers
#   10. Assign to sliced_list
#   11. Run print(sliced_list)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)
#      numbers = ... (stored in memory)
#      sliced_list = ... (stored in memory)
#      print(sliced_list)

"""

"""
Problem 1017:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to sliced_tuple
#   3. Run print(sliced_tuple)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to sliced_tuple
#   8. Run print(sliced_tuple)
#   9. Assign to numbers
#   10. Assign to sliced_tuple
#   11. Run print(sliced_tuple)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)
#      numbers = ... (stored in memory)
#      sliced_tuple = ... (stored in memory)
#      print(sliced_tuple)

"""

"""
Problem 1018:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to sliced_text
#   3. Run print(sliced_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to sliced_text
#   8. Run print(sliced_text)
#   9. Assign to text
#   10. Assign to sliced_text
#   11. Run print(sliced_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)
#      text = ... (stored in memory)
#      sliced_text = ... (stored in memory)
#      print(sliced_text)

"""

"""
Problem 1019:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to squares
#   2. Run print(squares)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to squares
#   6. Run print(squares)
#   7. Assign to squares
#   8. Run print(squares)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      squares = ... (stored in memory)
#      print(squares)
#      squares = ... (stored in memory)
#      print(squares)
#      squares = ... (stored in memory)
#      print(squares)

"""

"""
Problem 1020:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to evens
#   2. Run print(evens)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to evens
#   6. Run print(evens)
#   7. Assign to evens
#   8. Run print(evens)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      evens = ... (stored in memory)
#      print(evens)
#      evens = ... (stored in memory)
#      print(evens)
#      evens = ... (stored in memory)
#      print(evens)

"""

"""
Problem 1021:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 5: first get x, then get 5, then compute x % 5
#   x % 5: first get x, then get 5, then compute x % 5
#   x % 5: first get x, then get 5, then compute x % 5
#   x % 5: first get x, then get 5, then compute x % 5
#   x % 5: first get x, then get 5, then compute x % 5
#
# ===== EXECUTION ORDER =====
#   1. Assign to div_by_5
#   2. Run print(div_by_5)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to div_by_5
#   6. Run print(div_by_5)
#   7. Assign to div_by_5
#   8. Run print(div_by_5)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      div_by_5 = ... (stored in memory)
#      print(div_by_5)
#      div_by_5 = ... (stored in memory)
#      print(div_by_5)
#      div_by_5 = ... (stored in memory)
#      print(div_by_5)

"""

"""
Problem 1022:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sentence
#   2. Assign to first_letters
#   3. Run print(first_letters)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to first_letters
#   8. Run print(first_letters)
#   9. Assign to sentence
#   10. Assign to first_letters
#   11. Run print(first_letters)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      first_letters = ... (stored in memory)
#      print(first_letters)
#      sentence = ... (stored in memory)
#      first_letters = ... (stored in memory)
#      print(first_letters)
#      sentence = ... (stored in memory)
#      first_letters = ... (stored in memory)
#      print(first_letters)

"""

"""
Problem 1023:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sentence
#   2. Assign to long_words
#   3. Run print(long_words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to long_words
#   8. Run print(long_words)
#   9. Assign to sentence
#   10. Assign to long_words
#   11. Run print(long_words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      long_words = ... (stored in memory)
#      print(long_words)
#      sentence = ... (stored in memory)
#      long_words = ... (stored in memory)
#      print(long_words)
#      sentence = ... (stored in memory)
#      long_words = ... (stored in memory)
#      print(long_words)

"""

"""
Problem 1024:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to squares_tuples
#   2. Run print(squares_tuples)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to squares_tuples
#   6. Run print(squares_tuples)
#   7. Assign to squares_tuples
#   8. Run print(squares_tuples)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      squares_tuples = ... (stored in memory)
#      print(squares_tuples)
#      squares_tuples = ... (stored in memory)
#      print(squares_tuples)
#      squares_tuples = ... (stored in memory)
#      print(squares_tuples)

"""

"""
Problem 1025:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: *
#
# ===== EVALUATION ORDER =====
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to doubled
#   3. Run print(doubled)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to doubled
#   8. Run print(doubled)
#   9. Assign to numbers
#   10. Assign to doubled
#   11. Run print(doubled)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      doubled = ... (stored in memory)
#      print(doubled)
#      numbers = ... (stored in memory)
#      doubled = ... (stored in memory)
#      print(doubled)
#      numbers = ... (stored in memory)
#      doubled = ... (stored in memory)
#      print(doubled)

"""

"""
Problem 1026:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 7: first get x, then get 7, then compute x % 7
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 7: first get x, then get 7, then compute x % 7
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 7: first get x, then get 7, then compute x % 7
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 7: first get x, then get 7, then compute x % 7
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 7: first get x, then get 7, then compute x % 7
#
# ===== EXECUTION ORDER =====
#   1. Assign to div_by_3_or_7
#   2. Run print(div_by_3_or_7)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to div_by_3_or_7
#   6. Run print(div_by_3_or_7)
#   7. Assign to div_by_3_or_7
#   8. Run print(div_by_3_or_7)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      div_by_3_or_7 = ... (stored in memory)
#      print(div_by_3_or_7)
#      div_by_3_or_7 = ... (stored in memory)
#      print(div_by_3_or_7)
#      div_by_3_or_7 = ... (stored in memory)
#      print(div_by_3_or_7)

"""

"""
Problem 1027:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to upper_words
#   3. Run print(upper_words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to upper_words
#   8. Run print(upper_words)
#   9. Assign to words
#   10. Assign to upper_words
#   11. Run print(upper_words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      upper_words = ... (stored in memory)
#      print(upper_words)
#      words = ... (stored in memory)
#      upper_words = ... (stored in memory)
#      print(upper_words)
#      words = ... (stored in memory)
#      upper_words = ... (stored in memory)
#      print(upper_words)

"""

"""
Problem 1028:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to nested_list
#   2. Assign to flattened
#   3. Run print(flattened)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to nested_list
#   7. Assign to flattened
#   8. Run print(flattened)
#   9. Assign to nested_list
#   10. Assign to flattened
#   11. Run print(flattened)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      nested_list = ... (stored in memory)
#      flattened = ... (stored in memory)
#      print(flattened)
#      nested_list = ... (stored in memory)
#      flattened = ... (stored in memory)
#      print(flattened)
#      nested_list = ... (stored in memory)
#      flattened = ... (stored in memory)
#      print(flattened)

"""

"""
Problem 1029:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to vowels
#   3. Run print(vowels)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to vowels
#   8. Run print(vowels)
#   9. Assign to text
#   10. Assign to vowels
#   11. Run print(vowels)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      vowels = ... (stored in memory)
#      print(vowels)
#      text = ... (stored in memory)
#      vowels = ... (stored in memory)
#      print(vowels)
#      text = ... (stored in memory)
#      vowels = ... (stored in memory)
#      print(vowels)

"""

"""
Problem 1030:
#
# ===== SYNTAX =====
#   def solve()
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to mixed_list
#   2. Assign to numbers
#   3. Run print(numbers)
#   4. Assign to result
#   5. For loop over Ellipsis
#       5.1 Run result.append(x)
#   6. Run print(result)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to mixed_list
#   10. Assign to numbers
#   11. Run print(numbers)
#   12. Assign to mixed_list
#   13. Assign to numbers
#   14. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      mixed_list = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#      mixed_list = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      mixed_list = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#
#   Loop: grabs one item from Ellipsis each round

"""

"""
Problem 1031:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: +
#
# ===== EVALUATION ORDER =====
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#
# ===== EXECUTION ORDER =====
#   1. Assign to fib
#   2. Evaluate expression
#   3. Run print(fib)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to fib
#   7. Evaluate expression
#   8. Run print(fib)
#   9. Assign to fib
#   10. Evaluate expression
#   11. Run print(fib)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      fib = ... (stored in memory)
#      top-level statement runs
#      print(fib)
#      fib = ... (stored in memory)
#      top-level statement runs
#      print(fib)
#      fib = ... (stored in memory)
#      top-level statement runs
#      print(fib)

"""

"""
Problem 1032:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   all() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: + % ** !=
#
# ===== EVALUATION ORDER =====
#   x % y: first get x, then get y, then compute x % y
#   x % y: first get x, then get y, then compute x % y
#   x % y: first get x, then get y, then compute x % y
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   x % y: first get x, then get y, then compute x % y
#   x % y: first get x, then get y, then compute x % y
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#
# ===== EXECUTION ORDER =====
#   1. Assign to primes
#   2. Run print(primes)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to primes
#   6. Run print(primes)
#   7. Assign to primes
#   8. Run print(primes)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      primes = ... (stored in memory)
#      print(primes)
#      primes = ... (stored in memory)
#      print(primes)
#      primes = ... (stored in memory)
#      print(primes)

"""

"""
Problem 1033:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sentence
#   2. Assign to vowel_ending_words
#   3. Run print(vowel_ending_words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to vowel_ending_words
#   8. Run print(vowel_ending_words)
#   9. Assign to sentence
#   10. Assign to vowel_ending_words
#   11. Run print(vowel_ending_words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      vowel_ending_words = ... (stored in memory)
#      print(vowel_ending_words)
#      sentence = ... (stored in memory)
#      vowel_ending_words = ... (stored in memory)
#      print(vowel_ending_words)
#      sentence = ... (stored in memory)
#      vowel_ending_words = ... (stored in memory)
#      print(vowel_ending_words)

"""

"""
Problem 1034:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to lower_chars
#   3. Run print(lower_chars)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to lower_chars
#   8. Run print(lower_chars)
#   9. Assign to text
#   10. Assign to lower_chars
#   11. Run print(lower_chars)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      lower_chars = ... (stored in memory)
#      print(lower_chars)
#      text = ... (stored in memory)
#      lower_chars = ... (stored in memory)
#      print(lower_chars)
#      text = ... (stored in memory)
#      lower_chars = ... (stored in memory)
#      print(lower_chars)

"""

"""
Problem 1035:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to sentence
#   2. Assign to word_lengths
#   3. Run print(word_lengths)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to word_lengths
#   8. Run print(word_lengths)
#   9. Assign to sentence
#   10. Assign to word_lengths
#   11. Run print(word_lengths)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sentence = ... (stored in memory)
#      word_lengths = ... (stored in memory)
#      print(word_lengths)
#      sentence = ... (stored in memory)
#      word_lengths = ... (stored in memory)
#      print(word_lengths)
#      sentence = ... (stored in memory)
#      word_lengths = ... (stored in memory)
#      print(word_lengths)

"""

"""
Problem 1036:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to unique_numbers
#   3. Run print(unique_numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to unique_numbers
#   8. Run print(unique_numbers)
#   9. Assign to numbers
#   10. Assign to unique_numbers
#   11. Run print(unique_numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      unique_numbers = ... (stored in memory)
#      print(unique_numbers)
#      numbers = ... (stored in memory)
#      unique_numbers = ... (stored in memory)
#      print(unique_numbers)
#      numbers = ... (stored in memory)
#      unique_numbers = ... (stored in memory)
#      print(unique_numbers)

"""

"""
Problem 1037:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to filtered_words
#   3. Run print(filtered_words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to filtered_words
#   8. Run print(filtered_words)
#   9. Assign to words
#   10. Assign to filtered_words
#   11. Run print(filtered_words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      filtered_words = ... (stored in memory)
#      print(filtered_words)
#      words = ... (stored in memory)
#      filtered_words = ... (stored in memory)
#      print(filtered_words)
#      words = ... (stored in memory)
#      filtered_words = ... (stored in memory)
#      print(filtered_words)

"""

"""
Problem 1038:
#
# ===== SYNTAX =====
#   def factorial(n)
#   return — exits the function and sends back a value
#   factorial() — built-in function call
#   operators: - * ==
#
# ===== EVALUATION ORDER =====
#   print(factorial(5)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(15): gets the literal 15, then runs the function body
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   factorial(n - 1): computes the expression first, then runs the function body
#   factorial(n - 1): computes the expression first, then runs the function body
#   factorial(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(n) — stored for later
#   2. Run print(factorial(...))
#       calls factorial with: n=5
#         2.1 Return from function
#   3. Define def factorial(n) — stored for later
#   4. Define def factorial(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(factorial(...))
#       calls factorial with: n=15
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      print(factorial(5)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result

"""

"""
Problem 1039:
#
# ===== SYNTAX =====
#   def sum_n(n)
#   return — exits the function and sends back a value
#   sum_n() — built-in function call
#   operators: + - ==
#
# ===== EVALUATION ORDER =====
#   print(sum_n(10)): the inner sum_n() executes FIRST, then print() outputs the result
#   sum_n(n): looks up variable 'n', then runs the function body
#   print(sum_n(15)): the inner sum_n() executes FIRST, then print() outputs the result
#   sum_n(10): gets the literal 10, then runs the function body
#   sum_n(15): gets the literal 15, then runs the function body
#   n + sum_n(...): first get n, then get sum_n(...), then compute n + sum_n(...)
#   n + sum_n(...): first get n, then get sum_n(...), then compute n + sum_n(...)
#   n + sum_n(...): first get n, then get sum_n(...), then compute n + sum_n(...)
#   sum_n(n - 1): computes the expression first, then runs the function body
#   sum_n(n - 1): computes the expression first, then runs the function body
#   sum_n(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_n(n) — stored for later
#   2. Run print(sum_n(...))
#       calls sum_n with: n=10
#         2.1 Return from function
#   3. Define def sum_n(n) — stored for later
#   4. Define def sum_n(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(sum_n(...))
#       calls sum_n with: n=15
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_n, sum_n, sum_n are just stored
#   Execution starts at the non-def lines below:
#      print(sum_n(10)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_n(15)) → function runs first, then print shows the result

"""

"""
Problem 1040:
#
# ===== SYNTAX =====
#   def fibonacci(n)
#   return — exits the function and sends back a value
#   fibonacci() — built-in function call
#   operators: + - <=
#
# ===== EVALUATION ORDER =====
#   print(fibonacci(6)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(n): looks up variable 'n', then runs the function body
#   print(fibonacci(15)): the inner fibonacci() executes FIRST, then print() outputs the result
#   fibonacci(6): gets the literal 6, then runs the function body
#   fibonacci(15): gets the literal 15, then runs the function body
#   fibonacci(...) + fibonacci(...): first get fibonacci(...), then get fibonacci(...), then compute fibonacci(...) + fibonacci(...)
#   fibonacci(...) + fibonacci(...): first get fibonacci(...), then get fibonacci(...), then compute fibonacci(...) + fibonacci(...)
#   fibonacci(...) + fibonacci(...): first get fibonacci(...), then get fibonacci(...), then compute fibonacci(...) + fibonacci(...)
#   fibonacci(n - 1): computes the expression first, then runs the function body
#   fibonacci(n - 2): computes the expression first, then runs the function body
#   fibonacci(n - 1): computes the expression first, then runs the function body
#   fibonacci(n - 2): computes the expression first, then runs the function body
#   fibonacci(n - 1): computes the expression first, then runs the function body
#   fibonacci(n - 2): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 2: first get n, then get 2, then compute n - 2
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 2: first get n, then get 2, then compute n - 2
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 2: first get n, then get 2, then compute n - 2
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci(n) — stored for later
#   2. Run print(fibonacci(...))
#       calls fibonacci with: n=6
#         2.1 Return from function
#   3. Define def fibonacci(n) — stored for later
#   4. Define def fibonacci(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(fibonacci(...))
#       calls fibonacci with: n=15
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci, fibonacci, fibonacci are just stored
#   Execution starts at the non-def lines below:
#      print(fibonacci(6)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci(15)) → function runs first, then print shows the result

"""

"""
Problem 1041:
#
# ===== SYNTAX =====
#   def reverse_string(s)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   reverse_string() — built-in function call
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   print(reverse_string('hello')): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string(s): looks up variable 's', then runs the function body
#   print(reverse_string(?)): the inner reverse_string() executes FIRST, then print() outputs the result
#   reverse_string('hello'): gets the literal 'hello', then runs the function body
#   reverse_string(?): resolves the value, then runs the function body
#   ? + reverse_string(...): first get ?, then get reverse_string(...), then compute ? + reverse_string(...)
#   ? + reverse_string(...): first get ?, then get reverse_string(...), then compute ? + reverse_string(...)
#   ? + reverse_string(...): first get ?, then get reverse_string(...), then compute ? + reverse_string(...)
#   reverse_string(?): resolves the value, then runs the function body
#   reverse_string(?): resolves the value, then runs the function body
#   reverse_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_string(s) — stored for later
#   2. Run print(reverse_string(...))
#       calls reverse_string with: s='hello'
#         2.1 Return from function
#   3. Define def reverse_string(s) — stored for later
#   4. Define def reverse_string(s) — stored for later
#   5. Assign to s
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(reverse_string(...))
#       calls reverse_string with: s=[4, 1, 8, 6, 3]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_string, reverse_string, reverse_string are just stored
#   Execution starts at the non-def lines below:
#      print(reverse_string('hello')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_string(?)) → function runs first, then print shows the result

"""

"""
Problem 1042:
#
# ===== SYNTAX =====
#   def count_digits(n)
#   return — exits the function and sends back a value
#   count_digits() — built-in function call
#   operators: + // <
#
# ===== EVALUATION ORDER =====
#   print(count_digits(12345)): the inner count_digits() executes FIRST, then print() outputs the result
#   count_digits(n): looks up variable 'n', then runs the function body
#   print(count_digits(15)): the inner count_digits() executes FIRST, then print() outputs the result
#   count_digits(12345): gets the literal 12345, then runs the function body
#   count_digits(15): gets the literal 15, then runs the function body
#   1 + count_digits(...): first get 1, then get count_digits(...), then compute 1 + count_digits(...)
#   1 + count_digits(...): first get 1, then get count_digits(...), then compute 1 + count_digits(...)
#   1 + count_digits(...): first get 1, then get count_digits(...), then compute 1 + count_digits(...)
#   count_digits(n // 10): computes the expression first, then runs the function body
#   count_digits(n // 10): computes the expression first, then runs the function body
#   count_digits(n // 10): computes the expression first, then runs the function body
#   n // 10: first get n, then get 10, then compute n // 10
#   n // 10: first get n, then get 10, then compute n // 10
#   n // 10: first get n, then get 10, then compute n // 10
#
# ===== EXECUTION ORDER =====
#   1. Define def count_digits(n) — stored for later
#   2. Run print(count_digits(...))
#       calls count_digits with: n=12345
#         2.1 Return from function
#   3. Define def count_digits(n) — stored for later
#   4. Define def count_digits(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(count_digits(...))
#       calls count_digits with: n=15
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_digits, count_digits, count_digits are just stored
#   Execution starts at the non-def lines below:
#      print(count_digits(12345)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_digits(15)) → function runs first, then print shows the result

"""

"""
Problem 1043:
#
# ===== SYNTAX =====
#   def gcd(a, b)
#   return — exits the function and sends back a value
#   gcd() — built-in function call
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(gcd(48, 18)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(gcd(20, 30)): the inner gcd() executes FIRST, then print() outputs the result
#   gcd(48, 18): gets the literal 48, gets the literal 18, then runs the function body
#   gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   gcd(b, a % b): looks up variable 'b', computes the expression first, then runs the function body
#   gcd(b, a % b): looks up variable 'b', computes the expression first, then runs the function body
#   gcd(b, a % b): looks up variable 'b', computes the expression first, then runs the function body
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#   a % b: first get a, then get b, then compute a % b
#
# ===== EXECUTION ORDER =====
#   1. Define def gcd(a, b) — stored for later
#   2. Run print(gcd(...))
#       calls gcd with: a=48, b=18
#         2.1 Return from function
#   3. Define def gcd(a, b) — stored for later
#   4. Define def gcd(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(gcd(...))
#       calls gcd with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions gcd, gcd, gcd are just stored
#   Execution starts at the non-def lines below:
#      print(gcd(48, 18)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(gcd(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1044:
#
# ===== SYNTAX =====
#   def list_sum(lst)
#   return — exits the function and sends back a value
#   list_sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(list_sum(?)): the inner list_sum() executes FIRST, then print() outputs the result
#   list_sum(lst): looks up variable 'lst', then runs the function body
#   print(list_sum(?)): the inner list_sum() executes FIRST, then print() outputs the result
#   list_sum(?): resolves the value, then runs the function body
#   list_sum(?): resolves the value, then runs the function body
#   ? + list_sum(...): first get ?, then get list_sum(...), then compute ? + list_sum(...)
#   ? + list_sum(...): first get ?, then get list_sum(...), then compute ? + list_sum(...)
#   ? + list_sum(...): first get ?, then get list_sum(...), then compute ? + list_sum(...)
#   list_sum(?): resolves the value, then runs the function body
#   list_sum(?): resolves the value, then runs the function body
#   list_sum(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def list_sum(lst) — stored for later
#   2. Run print(list_sum(...))
#       calls list_sum with: lst=[1, 2, 3, 4, 5]
#         2.1 Return from function
#   3. Define def list_sum(lst) — stored for later
#   4. Define def list_sum(lst) — stored for later
#   5. Assign to lst
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(list_sum(...))
#       calls list_sum with: lst=[1, 3, 5, 7]
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_sum, list_sum, list_sum are just stored
#   Execution starts at the non-def lines below:
#      print(list_sum(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_sum(?)) → function runs first, then print shows the result

"""

"""
Problem 1045:
#
# ===== SYNTAX =====
#   def is_palindrome(n, temp)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   len() — built-in function call
#   is_palindrome() — built-in function call
#   if/elif/else
#   operators: == <= and is
#
# ===== EVALUATION ORDER =====
#   print(is_palindrome(121)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   print(is_palindrome(123)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(n, temp): looks up variable 'n', looks up variable 'temp', then runs the function body
#   print(is_palindrome(15, ?)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(121): gets the literal 121, then runs the function body
#   is_palindrome(123): gets the literal 123, then runs the function body
#   is_palindrome(15, ?): gets the literal 15, resolves the value, then runs the function body
#   is_palindrome(n, ?): looks up variable 'n', resolves the value, then runs the function body
#   is_palindrome(n, ?): looks up variable 'n', resolves the value, then runs the function body
#   is_palindrome(n, ?): looks up variable 'n', resolves the value, then runs the function body
#   temp  None: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(n, temp) — stored for later
#   2. Run print(is_palindrome(...))
#       calls is_palindrome with: n=121
#         2.1 Return from function
#   3. Run print(is_palindrome(...))
#       calls is_palindrome with: n=123
#         3.1 Return from function
#   4. Define def is_palindrome(n, temp) — stored for later
#   5. Define def is_palindrome(n, temp) — stored for later
#   6. Assign to n
#   7. Assign to temp
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(is_palindrome(...))
#       calls is_palindrome with: n=15, temp=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      print(is_palindrome(121)) → function runs first, then print shows the result
#      print(is_palindrome(123)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      temp = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome(15, ?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1046:
#
# ===== SYNTAX =====
#   def power(x, n)
#   return — exits the function and sends back a value
#   power() — built-in function call
#   operators: - * ==
#
# ===== EVALUATION ORDER =====
#   print(power(2, 3)): the inner power() executes FIRST, then print() outputs the result
#   power(x, n): looks up variable 'x', looks up variable 'n', then runs the function body
#   print(power(3, 15)): the inner power() executes FIRST, then print() outputs the result
#   power(2, 3): gets the literal 2, gets the literal 3, then runs the function body
#   power(3, 15): gets the literal 3, gets the literal 15, then runs the function body
#   x * power(...): first get x, then get power(...), then compute x * power(...)
#   x * power(...): first get x, then get power(...), then compute x * power(...)
#   x * power(...): first get x, then get power(...), then compute x * power(...)
#   power(x, n - 1): looks up variable 'x', computes the expression first, then runs the function body
#   power(x, n - 1): looks up variable 'x', computes the expression first, then runs the function body
#   power(x, n - 1): looks up variable 'x', computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def power(x, n) — stored for later
#   2. Run print(power(...))
#       calls power with: x=2, n=3
#         2.1 Return from function
#   3. Define def power(x, n) — stored for later
#   4. Define def power(x, n) — stored for later
#   5. Assign to x
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(power(...))
#       calls power with: x=3, n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions power, power, power are just stored
#   Execution starts at the non-def lines below:
#      print(power(2, 3)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(power(3, 15)) → function runs first, then print shows the result

"""

"""
Problem 1047:
#
# ===== SYNTAX =====
#   def multiply(a, b)
#   return — exits the function and sends back a value
#   multiply() — built-in function call
#   operators: + - ==
#
# ===== EVALUATION ORDER =====
#   print(multiply(5, 4)): the inner multiply() executes FIRST, then print() outputs the result
#   multiply(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(multiply(20, 30)): the inner multiply() executes FIRST, then print() outputs the result
#   multiply(5, 4): gets the literal 5, gets the literal 4, then runs the function body
#   multiply(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#   a + multiply(...): first get a, then get multiply(...), then compute a + multiply(...)
#   a + multiply(...): first get a, then get multiply(...), then compute a + multiply(...)
#   a + multiply(...): first get a, then get multiply(...), then compute a + multiply(...)
#   multiply(a, b - 1): looks up variable 'a', computes the expression first, then runs the function body
#   multiply(a, b - 1): looks up variable 'a', computes the expression first, then runs the function body
#   multiply(a, b - 1): looks up variable 'a', computes the expression first, then runs the function body
#   b - 1: first get b, then get 1, then compute b - 1
#   b - 1: first get b, then get 1, then compute b - 1
#   b - 1: first get b, then get 1, then compute b - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply(a, b) — stored for later
#   2. Run print(multiply(...))
#       calls multiply with: a=5, b=4
#         2.1 Return from function
#   3. Define def multiply(a, b) — stored for later
#   4. Define def multiply(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(multiply(...))
#       calls multiply with: a=20, b=30
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply, multiply, multiply are just stored
#   Execution starts at the non-def lines below:
#      print(multiply(5, 4)) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1048:
#
# ===== SYNTAX =====
#   def print_numbers(n)
#   print_numbers() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: - >
#
# ===== EVALUATION ORDER =====
#   print_numbers(5): gets the literal 5, then runs the function body
#   print_numbers(n): looks up variable 'n', then runs the function body
#   print(print_numbers(15)): the inner print_numbers() executes FIRST, then print() outputs the result
#   print_numbers(15): gets the literal 15, then runs the function body
#   print_numbers(n - 1): computes the expression first, then runs the function body
#   print_numbers(n - 1): computes the expression first, then runs the function body
#   print_numbers(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n > 0: is this True or False? The answer picks which branch
#   n > 0: is this True or False? The answer picks which branch
#   n > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_numbers(n) — stored for later
#   2. Run print_numbers(5)
#       with: n=5
#         2.1 If statement (one branch runs)
#         2.2 Assign to result
#         2.3 Return from function
#   3. Define def print_numbers(n) — stored for later
#   4. Define def print_numbers(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_numbers(...))
#       calls print_numbers with: n=15
#         8.1 If statement (one branch runs)
#         8.2 Assign to result
#         8.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_numbers, print_numbers, print_numbers are just stored
#   Execution starts at the non-def lines below:
#      print_numbers(5) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_numbers(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1049:
#
# ===== SYNTAX =====
#   def print_reverse(n)
#   print_reverse() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: - >
#
# ===== EVALUATION ORDER =====
#   print_reverse(5): gets the literal 5, then runs the function body
#   print_reverse(n): looks up variable 'n', then runs the function body
#   print(print_reverse(15)): the inner print_reverse() executes FIRST, then print() outputs the result
#   print_reverse(5): gets the literal 5, then runs the function body
#   print_reverse(5): gets the literal 5, then runs the function body
#   print_reverse(15): gets the literal 15, then runs the function body
#   print_reverse(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   print_reverse(n - 1): computes the expression first, then runs the function body
#   print_reverse(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n > 0: is this True or False? The answer picks which branch
#   n > 0: is this True or False? The answer picks which branch
#   n > 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def print_reverse(n) — stored for later
#   2. Run print_reverse(5)
#       with: n=5
#         2.1 If statement (one branch runs)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_reverse(...))
#       calls print_reverse with: n=15
#         8.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_reverse, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print_reverse(5) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_reverse(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1050:
#
# ===== SYNTAX =====
#   def is_sorted(lst)
#   return — exits the function and sends back a value
#   len() — built-in function call
#   is_sorted() — built-in function call
#   operators: <= and
#
# ===== EVALUATION ORDER =====
#   print(is_sorted(?)): the inner is_sorted() executes FIRST, then print() outputs the result
#   print(is_sorted(?)): the inner is_sorted() executes FIRST, then print() outputs the result
#   is_sorted(lst): looks up variable 'lst', then runs the function body
#   print(is_sorted(?)): the inner is_sorted() executes FIRST, then print() outputs the result
#   is_sorted(?): resolves the value, then runs the function body
#   is_sorted(?): resolves the value, then runs the function body
#   is_sorted(?): resolves the value, then runs the function body
#   is_sorted(?): resolves the value, then runs the function body
#   is_sorted(?): resolves the value, then runs the function body
#   is_sorted(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_sorted(lst) — stored for later
#   2. Run print(is_sorted(...))
#       calls is_sorted with: lst=[1, 2, 3, 4, 5]
#         2.1 Return from function
#   3. Run print(is_sorted(...))
#       calls is_sorted with: lst=[5, 3, 1]
#         3.1 Return from function
#   4. Define def is_sorted(lst) — stored for later
#   5. Define def is_sorted(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_sorted(...))
#       calls is_sorted with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_sorted, is_sorted, is_sorted are just stored
#   Execution starts at the non-def lines below:
#      print(is_sorted(?)) → function runs first, then print shows the result
#      print(is_sorted(?)) → function runs first, then print shows the result
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_sorted(?)) → function runs first, then print shows the result

"""

"""
Problem 1051:
#
# ===== SYNTAX =====
#   def char_count(s, c)
#   return — exits the function and sends back a value
#   char_count() — built-in function call
#   operators: + ==
#
# ===== EVALUATION ORDER =====
#   print(char_count('banana', 'a')): the inner char_count() executes FIRST, then print() outputs the result
#   char_count(s, c): looks up variable 's', looks up variable 'c', then runs the function body
#   print(char_count(?, 10)): the inner char_count() executes FIRST, then print() outputs the result
#   char_count('banana', 'a'): gets the literal 'banana', gets the literal 'a', then runs the function body
#   char_count(?, 10): resolves the value, gets the literal 10, then runs the function body
#   ? + char_count(...): first get ?, then get char_count(...), then compute ? + char_count(...)
#   ? + char_count(...): first get ?, then get char_count(...), then compute ? + char_count(...)
#   ? + char_count(...): first get ?, then get char_count(...), then compute ? + char_count(...)
#   char_count(?, c): resolves the value, looks up variable 'c', then runs the function body
#   char_count(?, c): resolves the value, looks up variable 'c', then runs the function body
#   char_count(?, c): resolves the value, looks up variable 'c', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def char_count(s, c) — stored for later
#   2. Run print(char_count(...))
#       calls char_count with: s='banana', c='a'
#         2.1 Return from function
#   3. Define def char_count(s, c) — stored for later
#   4. Define def char_count(s, c) — stored for later
#   5. Assign to s
#   6. Assign to c
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(char_count(...))
#       calls char_count with: s=[4, 1, 8, 6, 3], c=10
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions char_count, char_count, char_count are just stored
#   Execution starts at the non-def lines below:
#      print(char_count('banana', 'a')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(char_count(?, 10)) → function runs first, then print shows the result

"""

"""
Problem 1052:
#
# ===== SYNTAX =====
#   def binary_strings(n, s)
#   binary_strings() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: + - ==
#
# ===== EVALUATION ORDER =====
#   binary_strings(3): gets the literal 3, then runs the function body
#   binary_strings(n, s): looks up variable 'n', looks up variable 's', then runs the function body
#   print(binary_strings(15, 25)): the inner binary_strings() executes FIRST, then print() outputs the result
#   binary_strings(3): gets the literal 3, then runs the function body
#   binary_strings(3): gets the literal 3, then runs the function body
#   binary_strings(15, 25): gets the literal 15, gets the literal 25, then runs the function body
#   binary_strings(n - 1, s + '0'): computes the expression first, computes the expression first, then runs the function body
#   binary_strings(n - 1, s + '1'): computes the expression first, computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   s + '0': first get s, then get '0', then compute s + '0'
#   n - 1: first get n, then get 1, then compute n - 1
#   s + '1': first get s, then get '1', then compute s + '1'
#   binary_strings(n - 1, s + '0'): computes the expression first, computes the expression first, then runs the function body
#   binary_strings(n - 1, s + '1'): computes the expression first, computes the expression first, then runs the function body
#   binary_strings(n - 1, s + '0'): computes the expression first, computes the expression first, then runs the function body
#   binary_strings(n - 1, s + '1'): computes the expression first, computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   s + '0': first get s, then get '0', then compute s + '0'
#   n - 1: first get n, then get 1, then compute n - 1
#   s + '1': first get s, then get '1', then compute s + '1'
#   n - 1: first get n, then get 1, then compute n - 1
#   s + '0': first get s, then get '0', then compute s + '0'
#   n - 1: first get n, then get 1, then compute n - 1
#   s + '1': first get s, then get '1', then compute s + '1'
#   n == 0: is this True or False? The answer picks which branch
#   n == 0: is this True or False? The answer picks which branch
#   n == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def binary_strings(n, s) — stored for later
#   2. Run binary_strings(3)
#       with: n=3
#         2.1 If statement (one branch runs)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(binary_strings(...))
#       calls binary_strings with: n=15, s=25
#         9.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions binary_strings, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      binary_strings(3) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(binary_strings(15, 25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1053:
#
# ===== SYNTAX =====
#   def sum_of_digits(n)
#   return — exits the function and sends back a value
#   sum_of_digits() — built-in function call
#   if/elif/else
#   operators: + // % == <
#
# ===== EVALUATION ORDER =====
#   print(sum_of_digits(string)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   print(sum_of_digits(1234)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   sum_of_digits(n): looks up variable 'n', then runs the function body
#   print(sum_of_digits(15)): the inner sum_of_digits() executes FIRST, then print() outputs the result
#   sum_of_digits(string): looks up variable 'string', then runs the function body
#   sum_of_digits(1234): gets the literal 1234, then runs the function body
#   sum_of_digits(15): gets the literal 15, then runs the function body
#   ? + sum_of_digits(...): first get ?, then get sum_of_digits(...), then compute ? + sum_of_digits(...)
#   ? + sum_of_digits(...): first get ?, then get sum_of_digits(...), then compute ? + sum_of_digits(...)
#   n % 10: first get n, then get 10, then compute n % 10
#   sum_of_digits(n // 10): computes the expression first, then runs the function body
#   n % 10: first get n, then get 10, then compute n % 10
#   sum_of_digits(n // 10): computes the expression first, then runs the function body
#   n // 10: first get n, then get 10, then compute n // 10
#   n // 10: first get n, then get 10, then compute n // 10
#   n == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_digits(n) — stored for later
#   2. Assign to string
#   3. Run print(sum_of_digits(...))
#       calls sum_of_digits with: n=string
#         3.1 Return from function
#   4. Define def sum_of_digits(n) — stored for later
#   5. Run print(sum_of_digits(...))
#       calls sum_of_digits with: n=1234
#         5.1 Return from function
#   6. Define def sum_of_digits(n) — stored for later
#   7. Define def sum_of_digits(n) — stored for later
#   8. Assign to n
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(sum_of_digits(...))
#       calls sum_of_digits with: n=15
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_digits, sum_of_digits, sum_of_digits, sum_of_digits are just stored
#   Execution starts at the non-def lines below:
#      string = ... (stored in memory)
#      print(sum_of_digits(string)) → function runs first, then print shows the result
#      print(sum_of_digits(1234)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_digits(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1054:
#
# ===== SYNTAX =====
#   def triangular_number(n)
#   return — exits the function and sends back a value
#   triangular_number() — built-in function call
#   operators: + - ==
#
# ===== EVALUATION ORDER =====
#   print(triangular_number(5)): the inner triangular_number() executes FIRST, then print() outputs the result
#   triangular_number(n): looks up variable 'n', then runs the function body
#   print(triangular_number(15)): the inner triangular_number() executes FIRST, then print() outputs the result
#   triangular_number(5): gets the literal 5, then runs the function body
#   triangular_number(15): gets the literal 15, then runs the function body
#   n + triangular_number(...): first get n, then get triangular_number(...), then compute n + triangular_number(...)
#   n + triangular_number(...): first get n, then get triangular_number(...), then compute n + triangular_number(...)
#   n + triangular_number(...): first get n, then get triangular_number(...), then compute n + triangular_number(...)
#   triangular_number(n - 1): computes the expression first, then runs the function body
#   triangular_number(n - 1): computes the expression first, then runs the function body
#   triangular_number(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def triangular_number(n) — stored for later
#   2. Run print(triangular_number(...))
#       calls triangular_number with: n=5
#         2.1 Return from function
#   3. Define def triangular_number(n) — stored for later
#   4. Define def triangular_number(n) — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(triangular_number(...))
#       calls triangular_number with: n=15
#         8.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions triangular_number, triangular_number, triangular_number are just stored
#   Execution starts at the non-def lines below:
#      print(triangular_number(5)) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(triangular_number(15)) → function runs first, then print shows the result

"""

"""
Problem 1055:
#
# ===== SYNTAX =====
#   def remove_duplicates(s, seen)
#   return — exits the function and sends back a value
#   remove_duplicates() — built-in function call
#   operators: + in
#
# ===== EVALUATION ORDER =====
#   print(remove_duplicates('banana')): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates(s, seen): looks up variable 's', looks up variable 'seen', then runs the function body
#   print(remove_duplicates(?, 25)): the inner remove_duplicates() executes FIRST, then print() outputs the result
#   remove_duplicates('banana'): gets the literal 'banana', then runs the function body
#   remove_duplicates(?, 25): resolves the value, gets the literal 25, then runs the function body
#   remove_duplicates(?, seen): resolves the value, looks up variable 'seen', then runs the function body
#   ? + remove_duplicates(...): first get ?, then get remove_duplicates(...), then compute ? + remove_duplicates(...)
#   remove_duplicates(?, seen): resolves the value, looks up variable 'seen', then runs the function body
#   ? + remove_duplicates(...): first get ?, then get remove_duplicates(...), then compute ? + remove_duplicates(...)
#   remove_duplicates(?, seen): resolves the value, looks up variable 'seen', then runs the function body
#   ? + remove_duplicates(...): first get ?, then get remove_duplicates(...), then compute ? + remove_duplicates(...)
#   remove_duplicates(?, seen + ?): resolves the value, computes the expression first, then runs the function body
#   remove_duplicates(?, seen + ?): resolves the value, computes the expression first, then runs the function body
#   remove_duplicates(?, seen + ?): resolves the value, computes the expression first, then runs the function body
#   seen + ?: first get seen, then get ?, then compute seen + ?
#   seen + ?: first get seen, then get ?, then compute seen + ?
#   seen + ?: first get seen, then get ?, then compute seen + ?
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_duplicates(s, seen) — stored for later
#   2. Run print(remove_duplicates(...))
#       calls remove_duplicates with: s='banana'
#         2.1 Return from function
#   3. Define def remove_duplicates(s, seen) — stored for later
#   4. Define def remove_duplicates(s, seen) — stored for later
#   5. Assign to s
#   6. Assign to seen
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_duplicates(...))
#       calls remove_duplicates with: s=[4, 1, 8, 6, 3], seen=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_duplicates, remove_duplicates, remove_duplicates are just stored
#   Execution starts at the non-def lines below:
#      print(remove_duplicates('banana')) → function runs first, then print shows the result
#      s = ... (stored in memory)
#      seen = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_duplicates(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1056:
#
# ===== SYNTAX =====
#   def permutations(s, prefix)
#   range() — built-in function call
#   len() — built-in function call
#   permutations() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   permutations('abc'): gets the literal 'abc', then runs the function body
#   permutations(s, prefix): looks up variable 's', looks up variable 'prefix', then runs the function body
#   print(permutations(?, 'Hi, ')): the inner permutations() executes FIRST, then print() outputs the result
#   permutations('abc'): gets the literal 'abc', then runs the function body
#   permutations('abc'): gets the literal 'abc', then runs the function body
#   permutations(?, 'Hi, '): resolves the value, gets the literal 'Hi, ', then runs the function body
#   permutations(? + ?, prefix + ?): computes the expression first, computes the expression first, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   prefix + ?: first get prefix, then get ?, then compute prefix + ?
#   permutations(? + ?, prefix + ?): computes the expression first, computes the expression first, then runs the function body
#   permutations(? + ?, prefix + ?): computes the expression first, computes the expression first, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   prefix + ?: first get prefix, then get ?, then compute prefix + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   prefix + ?: first get prefix, then get ?, then compute prefix + ?
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def permutations(s, prefix) — stored for later
#   2. Run permutations('abc')
#       with: s='abc'
#         2.1 If statement (one branch runs)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to s
#   6. Assign to prefix
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(permutations(...))
#       calls permutations with: s=[4, 1, 8, 6, 3], prefix='Hi, '
#         9.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions permutations, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      permutations('abc') → jumps into function, runs body, returns result → printed
#      s = ... (stored in memory)
#      prefix = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(permutations(?, 'Hi, ')) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1057:
#
# ===== SYNTAX =====
#   def longest_string(words)
#   return — exits the function and sends back a value
#   max() — built-in function call
#   len() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   longest_string(?): resolves the value, then runs the function body
#   longest_string(words): looks up variable 'words', then runs the function body
#   print(longest_string(?)): the inner longest_string() executes FIRST, then print() outputs the result
#   longest_string(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def longest_string(words) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def longest_string(words) — stored for later
#   5. Define def longest_string(words) — stored for later
#   6. Assign to words
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(longest_string(...))
#       calls longest_string with: words=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions longest_string, longest_string, longest_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(longest_string(?)) → function runs first, then print shows the result

"""

"""
Problem 1058:
#
# ===== SYNTAX =====
#   def solve()
#   is_even() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to is_even
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to is_even
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to is_even
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      is_even = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_even = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_even = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1059:
#
# ===== SYNTAX =====
#   def square_roots(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   square_roots(?): resolves the value, then runs the function body
#   square_roots(numbers): looks up variable 'numbers', then runs the function body
#   print(square_roots(?)): the inner square_roots() executes FIRST, then print() outputs the result
#   square_roots(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def square_roots(numbers) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def square_roots(numbers) — stored for later
#   6. Define def square_roots(numbers) — stored for later
#   7. Assign to numbers
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(square_roots(...))
#       calls square_roots with: numbers=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions square_roots, square_roots, square_roots are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_roots(?)) → function runs first, then print shows the result

"""

"""
Problem 1060:
#
# ===== SYNTAX =====
#   def solve()
#   contains_digits() — built-in function call
#   any() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to contains_digits
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to contains_digits
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to contains_digits
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      contains_digits = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      contains_digits = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      contains_digits = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1061:
#
# ===== SYNTAX =====
#   def sort_by_first_element_desc(tuples)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   sort_by_first_element_desc(?): resolves the value, then runs the function body
#   sort_by_first_element_desc(tuples): looks up variable 'tuples', then runs the function body
#   print(sort_by_first_element_desc(?)): the inner sort_by_first_element_desc() executes FIRST, then print() outputs the result
#   sort_by_first_element_desc(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_first_element_desc(tuples) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_by_first_element_desc(tuples) — stored for later
#   5. Define def sort_by_first_element_desc(tuples) — stored for later
#   6. Assign to tuples
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_by_first_element_desc(...))
#       calls sort_by_first_element_desc with: tuples=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_first_element_desc, sort_by_first_element_desc, sort_by_first_element_desc are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      tuples = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_first_element_desc(?)) → function runs first, then print shows the result

"""

"""
Problem 1062:
#
# ===== SYNTAX =====
#   def filter_short_words(words, length)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   len() — built-in function call
#   lambda
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   filter_short_words(?, 6): resolves the value, gets the literal 6, then runs the function body
#   filter_short_words(words, length): looks up variable 'words', looks up variable 'length', then runs the function body
#   print(filter_short_words('world', 25)): the inner filter_short_words() executes FIRST, then print() outputs the result
#   filter_short_words('world', 25): gets the literal 'world', gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_short_words(words, length) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def filter_short_words(words, length) — stored for later
#   5. Define def filter_short_words(words, length) — stored for later
#   6. Assign to words
#   7. Assign to length
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(filter_short_words(...))
#       calls filter_short_words with: words='world', length=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_short_words, filter_short_words, filter_short_words are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      words = ... (stored in memory)
#      length = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_short_words('world', 25)) → function runs first, then print shows the result

"""

"""
Problem 1063:
#
# ===== SYNTAX =====
#   def double_elements(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   lambda
#   operators: *
#
# ===== EVALUATION ORDER =====
#   double_elements(?): resolves the value, then runs the function body
#   double_elements(numbers): looks up variable 'numbers', then runs the function body
#   print(double_elements(?)): the inner double_elements() executes FIRST, then print() outputs the result
#   double_elements(?): resolves the value, then runs the function body
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def double_elements(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def double_elements(numbers) — stored for later
#   5. Define def double_elements(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(double_elements(...))
#       calls double_elements with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions double_elements, double_elements, double_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(double_elements(?)) → function runs first, then print shows the result

"""

"""
Problem 1064:
#
# ===== SYNTAX =====
#   def solve()
#   is_divisible_by_3() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 3: first get x, then get 3, then compute x % 3
#   x % 3: first get x, then get 3, then compute x % 3
#
# ===== EXECUTION ORDER =====
#   1. Assign to is_divisible_by_3
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to is_divisible_by_3
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to is_divisible_by_3
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      is_divisible_by_3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_divisible_by_3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_divisible_by_3 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1065:
#
# ===== SYNTAX =====
#   def solve()
#   second_largest() — built-in function call
#   sorted() — built-in function call
#   set() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to second_largest
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to second_largest
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to second_largest
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      second_largest = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      second_largest = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      second_largest = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1066:
#
# ===== SYNTAX =====
#   def cube_elements(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   lambda
#   operators: **
#
# ===== EVALUATION ORDER =====
#   cube_elements(?): resolves the value, then runs the function body
#   cube_elements(numbers): looks up variable 'numbers', then runs the function body
#   print(cube_elements(?)): the inner cube_elements() executes FIRST, then print() outputs the result
#   cube_elements(?): resolves the value, then runs the function body
#   x ** 3: first get x, then get 3, then compute x ** 3
#   x ** 3: first get x, then get 3, then compute x ** 3
#   x ** 3: first get x, then get 3, then compute x ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def cube_elements(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def cube_elements(numbers) — stored for later
#   5. Define def cube_elements(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(cube_elements(...))
#       calls cube_elements with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions cube_elements, cube_elements, cube_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(cube_elements(?)) → function runs first, then print shows the result

"""

"""
Problem 1067:
#
# ===== SYNTAX =====
#   def sort_by_length(words)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   len() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   sort_by_length(?): resolves the value, then runs the function body
#   sort_by_length(words): looks up variable 'words', then runs the function body
#   print(sort_by_length(?)): the inner sort_by_length() executes FIRST, then print() outputs the result
#   sort_by_length(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def sort_by_length(words) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sort_by_length(words) — stored for later
#   5. Define def sort_by_length(words) — stored for later
#   6. Assign to words
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sort_by_length(...))
#       calls sort_by_length with: words=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sort_by_length, sort_by_length, sort_by_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      words = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sort_by_length(?)) → function runs first, then print shows the result

"""

"""
Problem 1068:
#
# ===== SYNTAX =====
#   def solve()
#   is_odd() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: % !=
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to is_odd
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to is_odd
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to is_odd
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      is_odd = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_odd = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_odd = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1069:
#
# ===== SYNTAX =====
#   def sum_of_elements(numbers)
#   return — exits the function and sends back a value
#   reduce() — built-in function call
#   lambda
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_of_elements(?): resolves the value, then runs the function body
#   sum_of_elements(numbers): looks up variable 'numbers', then runs the function body
#   print(sum_of_elements(?)): the inner sum_of_elements() executes FIRST, then print() outputs the result
#   sum_of_elements(?): resolves the value, then runs the function body
#   x + y: first get x, then get y, then compute x + y
#   x + y: first get x, then get y, then compute x + y
#   x + y: first get x, then get y, then compute x + y
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Define def sum_of_elements(numbers) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def sum_of_elements(numbers) — stored for later
#   6. Define def sum_of_elements(numbers) — stored for later
#   7. Assign to numbers
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(sum_of_elements(...))
#       calls sum_of_elements with: numbers=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions sum_of_elements, sum_of_elements, sum_of_elements are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_elements(?)) → function runs first, then print shows the result

"""

"""
Problem 1070:
#
# ===== SYNTAX =====
#   def solve()
#   ends_with_vowel() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ends_with_vowel
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to ends_with_vowel
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to ends_with_vowel
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ends_with_vowel = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      ends_with_vowel = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      ends_with_vowel = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1071:
#
# ===== SYNTAX =====
#   def solve()
#   min() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to smallest_number
#   3. Run print('The smallest number in the list is:', smallest_number)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to smallest_number
#   8. Run print('The smallest number in the list is:', smallest_number)
#   9. Assign to numbers
#   10. Assign to smallest_number
#   11. Run print('The smallest number in the list is:', smallest_number)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      smallest_number = ... (stored in memory)
#      print('The smallest number in the list is:', smallest_number)
#      numbers = ... (stored in memory)
#      smallest_number = ... (stored in memory)
#      print('The smallest number in the list is:', smallest_number)
#      numbers = ... (stored in memory)
#      smallest_number = ... (stored in memory)
#      print('The smallest number in the list is:', smallest_number)

"""

"""
Problem 1072:
#
# ===== SYNTAX =====
#   def solve()
#   is_alpha() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to is_alpha
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to is_alpha
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to is_alpha
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      is_alpha = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_alpha = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_alpha = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1073:
#
# ===== SYNTAX =====
#   def convert_to_strings(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   str() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   convert_to_strings(?): resolves the value, then runs the function body
#   convert_to_strings(numbers): looks up variable 'numbers', then runs the function body
#   print(convert_to_strings(?)): the inner convert_to_strings() executes FIRST, then print() outputs the result
#   convert_to_strings(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_strings(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_to_strings(numbers) — stored for later
#   5. Define def convert_to_strings(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_strings(...))
#       calls convert_to_strings with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_strings, convert_to_strings, convert_to_strings are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_strings(?)) → function runs first, then print shows the result

"""

"""
Problem 1074:
#
# ===== SYNTAX =====
#   def sum_of_even(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   filter() — built-in function call
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   sum_of_even(?): resolves the value, then runs the function body
#   sum_of_even(numbers): looks up variable 'numbers', then runs the function body
#   print(sum_of_even(?)): the inner sum_of_even() executes FIRST, then print() outputs the result
#   sum_of_even(?): resolves the value, then runs the function body
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_even(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sum_of_even(numbers) — stored for later
#   5. Define def sum_of_even(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sum_of_even(...))
#       calls sum_of_even with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_even, sum_of_even, sum_of_even are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_even(?)) → function runs first, then print shows the result

"""

"""
Problem 1076:
#
# ===== SYNTAX =====
#   def solve()
#   count_even() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   filter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to count_even
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to count_even
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to count_even
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      count_even = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      count_even = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      count_even = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1077:
#
# ===== SYNTAX =====
#   def solve()
#   product_of_odds() — built-in function call
#   reduce() — built-in function call
#   filter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: * % !=
#
# ===== EVALUATION ORDER =====
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x * y: first get x, then get y, then compute x * y
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Import reduce from functools
#   2. Assign to product_of_odds
#   3. Assign to result
#   4. Run print(result)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import reduce from functools
#   8. Assign to product_of_odds
#   9. Assign to result
#   10. Run print(result)
#   11. Import reduce from functools
#   12. Assign to product_of_odds
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      product_of_odds = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      product_of_odds = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      product_of_odds = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1078:
#
# ===== SYNTAX =====
#   def solve()
#   is_prime() — built-in function call
#   all() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   lambda
#   operators: + % ** != >
#
# ===== EVALUATION ORDER =====
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#
# ===== EXECUTION ORDER =====
#   1. Assign to is_prime
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to is_prime
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to is_prime
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      is_prime = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_prime = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_prime = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1079:
#
# ===== SYNTAX =====
#   def create_square_root_tuples(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   create_square_root_tuples(?): resolves the value, then runs the function body
#   create_square_root_tuples(numbers): looks up variable 'numbers', then runs the function body
#   print(create_square_root_tuples(?)): the inner create_square_root_tuples() executes FIRST, then print() outputs the result
#   create_square_root_tuples(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def create_square_root_tuples(numbers) — stored for later
#   3. Assign to result
#   4. Run print(result)
#   5. Define def create_square_root_tuples(numbers) — stored for later
#   6. Define def create_square_root_tuples(numbers) — stored for later
#   7. Assign to numbers
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(create_square_root_tuples(...))
#       calls create_square_root_tuples with: numbers=[4, 1, 8, 6, 3]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions create_square_root_tuples, create_square_root_tuples, create_square_root_tuples are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_square_root_tuples(?)) → function runs first, then print shows the result

"""

"""
Problem 1080:
#
# ===== SYNTAX =====
#   def solve()
#   contains_uppercase() — built-in function call
#   any() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to contains_uppercase
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to contains_uppercase
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to contains_uppercase
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      contains_uppercase = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      contains_uppercase = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      contains_uppercase = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1081:
#
# ===== SYNTAX =====
#   def average_of_odds(lst)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   sum() — built-in function call
#   len() — built-in function call
#   filter() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   lambda
#   operators: * / % !=
#
# ===== EVALUATION ORDER =====
#   average_of_odds(lst): looks up variable 'lst', then runs the function body
#   average_of_odds(?): resolves the value, then runs the function body
#   average_of_odds(lst): looks up variable 'lst', then runs the function body
#   print(average_of_odds(?)): the inner average_of_odds() executes FIRST, then print() outputs the result
#   total / count: first get total, then get count, then compute total / count
#   total * ?: first get total, then get ?, then compute total * ?
#   average_of_odds(?): resolves the value, then runs the function body
#   sum(...) / len(...): first get sum(...), then get len(...), then compute sum(...) / len(...)
#   1 / count: first get 1, then get count, then compute 1 / count
#   num % 2: first get num, then get 2, then compute num % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def average_of_odds(lst) — stored for later
#   2. Assign to lst
#   3. Run average_of_odds(lst)
#       with: lst=lst
#         3.1 Import Fraction from fractions
#         3.2 Return from function
#   4. Define def average_of_odds(numbers) — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Define def average_of_odds(lst) — stored for later
#   8. Define def average_of_odds(lst) — stored for later
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(average_of_odds(...))
#       calls average_of_odds with: lst=[1, 3, 5, 7]
#         12.1 Import Fraction from fractions
#         12.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions average_of_odds, average_of_odds, average_of_odds, average_of_odds are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      average_of_odds(lst) → jumps into function, runs body, returns result → printed
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(average_of_odds(?)) → function runs first, then print shows the result

"""

"""
Problem 1082:
#
# ===== SYNTAX =====
#   def square_odd_numbers(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   filter() — built-in function call
#   lambda
#   operators: % ** !=
#
# ===== EVALUATION ORDER =====
#   square_odd_numbers(?): resolves the value, then runs the function body
#   square_odd_numbers(numbers): looks up variable 'numbers', then runs the function body
#   print(square_odd_numbers(?)): the inner square_odd_numbers() executes FIRST, then print() outputs the result
#   square_odd_numbers(?): resolves the value, then runs the function body
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_odd_numbers(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def square_odd_numbers(numbers) — stored for later
#   5. Define def square_odd_numbers(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_odd_numbers(...))
#       calls square_odd_numbers with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_odd_numbers, square_odd_numbers, square_odd_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_odd_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1083:
#
# ===== SYNTAX =====
#   def solve()
#   contains_negative() — built-in function call
#   any() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: <
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to contains_negative
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to contains_negative
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to contains_negative
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      contains_negative = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      contains_negative = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      contains_negative = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1084:
#
# ===== SYNTAX =====
#   def create_cube_tuples(numbers)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   lambda
#   operators: **
#
# ===== EVALUATION ORDER =====
#   create_cube_tuples(?): resolves the value, then runs the function body
#   create_cube_tuples(numbers): looks up variable 'numbers', then runs the function body
#   print(create_cube_tuples(?)): the inner create_cube_tuples() executes FIRST, then print() outputs the result
#   create_cube_tuples(?): resolves the value, then runs the function body
#   x ** 3: first get x, then get 3, then compute x ** 3
#   x ** 3: first get x, then get 3, then compute x ** 3
#   x ** 3: first get x, then get 3, then compute x ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def create_cube_tuples(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def create_cube_tuples(numbers) — stored for later
#   5. Define def create_cube_tuples(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(create_cube_tuples(...))
#       calls create_cube_tuples with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_cube_tuples, create_cube_tuples, create_cube_tuples are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(create_cube_tuples(?)) → function runs first, then print shows the result

"""

"""
Problem 1085:
#
# ===== SYNTAX =====
#   def filter_greater_than(numbers, value)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   lambda
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   filter_greater_than(?, 3): resolves the value, gets the literal 3, then runs the function body
#   filter_greater_than(numbers, value): looks up variable 'numbers', looks up variable 'value', then runs the function body
#   print(filter_greater_than(?, 25)): the inner filter_greater_than() executes FIRST, then print() outputs the result
#   filter_greater_than(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_greater_than(numbers, value) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def filter_greater_than(numbers, value) — stored for later
#   5. Define def filter_greater_than(numbers, value) — stored for later
#   6. Assign to numbers
#   7. Assign to value
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(filter_greater_than(...))
#       calls filter_greater_than with: numbers=[4, 1, 8, 6, 3], value=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_greater_than, filter_greater_than, filter_greater_than are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(filter_greater_than(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1086:
#
# ===== SYNTAX =====
#   def convert_to_lengths(strings)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   map() — built-in function call
#   len() — built-in function call
#   lambda
#
# ===== EVALUATION ORDER =====
#   convert_to_lengths(?): resolves the value, then runs the function body
#   convert_to_lengths(strings): looks up variable 'strings', then runs the function body
#   print(convert_to_lengths('world')): the inner convert_to_lengths() executes FIRST, then print() outputs the result
#   convert_to_lengths('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def convert_to_lengths(strings) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def convert_to_lengths(strings) — stored for later
#   5. Define def convert_to_lengths(strings) — stored for later
#   6. Assign to strings
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(convert_to_lengths(...))
#       calls convert_to_lengths with: strings='world'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions convert_to_lengths, convert_to_lengths, convert_to_lengths are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      strings = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(convert_to_lengths('world')) → function runs first, then print shows the result

"""

"""
Problem 1087:
#
# ===== SYNTAX =====
#   def solve()
#   is_perfect_square() — built-in function call
#   int() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: ** ==
#
# ===== EVALUATION ORDER =====
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#   x ** 0.5: first get x, then get 0.5, then compute x ** 0.5
#
# ===== EXECUTION ORDER =====
#   1. Assign to is_perfect_square
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to is_perfect_square
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to is_perfect_square
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      is_perfect_square = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_perfect_square = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      is_perfect_square = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1088:
#
# ===== SYNTAX =====
#   def sum_of_cubes(numbers)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   map() — built-in function call
#   lambda
#   operators: **
#
# ===== EVALUATION ORDER =====
#   sum_of_cubes(?): resolves the value, then runs the function body
#   sum_of_cubes(numbers): looks up variable 'numbers', then runs the function body
#   print(sum_of_cubes(?)): the inner sum_of_cubes() executes FIRST, then print() outputs the result
#   sum_of_cubes(?): resolves the value, then runs the function body
#   x ** 3: first get x, then get 3, then compute x ** 3
#   x ** 3: first get x, then get 3, then compute x ** 3
#   x ** 3: first get x, then get 3, then compute x ** 3
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_of_cubes(numbers) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def sum_of_cubes(numbers) — stored for later
#   5. Define def sum_of_cubes(numbers) — stored for later
#   6. Assign to numbers
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sum_of_cubes(...))
#       calls sum_of_cubes with: numbers=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_of_cubes, sum_of_cubes, sum_of_cubes are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_of_cubes(?)) → function runs first, then print shows the result

"""

"""
Problem 1089:
#
# ===== SYNTAX =====
#   def remove_elements_greater_than(numbers, value)
#   return — exits the function and sends back a value
#   list() — built-in function call
#   filter() — built-in function call
#   lambda
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   remove_elements_greater_than(?, 3): resolves the value, gets the literal 3, then runs the function body
#   remove_elements_greater_than(numbers, value): looks up variable 'numbers', looks up variable 'value', then runs the function body
#   print(remove_elements_greater_than(?, 25)): the inner remove_elements_greater_than() executes FIRST, then print() outputs the result
#   remove_elements_greater_than(?, 25): resolves the value, gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_elements_greater_than(numbers, value) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def remove_elements_greater_than(numbers, value) — stored for later
#   5. Define def remove_elements_greater_than(numbers, value) — stored for later
#   6. Assign to numbers
#   7. Assign to value
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(remove_elements_greater_than(...))
#       calls remove_elements_greater_than with: numbers=[4, 1, 8, 6, 3], value=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_elements_greater_than, remove_elements_greater_than, remove_elements_greater_than are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      numbers = ... (stored in memory)
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_elements_greater_than(?, 25)) → function runs first, then print shows the result

"""

"""
Problem 1090:
#
# ===== SYNTAX =====
#   def solve()
#   all_positive() — built-in function call
#   all() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: >
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to all_positive
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to all_positive
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to all_positive
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      all_positive = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      all_positive = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      all_positive = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1091:
#
# ===== SYNTAX =====
#   def square_generator(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   square_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: **
#
# ===== EVALUATION ORDER =====
#   square_generator(n): looks up variable 'n', then runs the function body
#   print(square_generator(15)): the inner square_generator() executes FIRST, then print() outputs the result
#   square_generator(5): gets the literal 5, then runs the function body
#   square_generator(15): gets the literal 15, then runs the function body
#   square_generator(5): gets the literal 5, then runs the function body
#   square_generator(5): gets the literal 5, then runs the function body
#   i ** 2: first get i, then get 2, then compute i ** 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_generator(...))
#       calls square_generator with: n=15
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1092:
#
# ===== SYNTAX =====
#   def fibonacci_generator(limit)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   fibonacci_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   fibonacci_generator(limit): looks up variable 'limit', then runs the function body
#   print(fibonacci_generator(25)): the inner fibonacci_generator() executes FIRST, then print() outputs the result
#   fibonacci_generator(20): gets the literal 20, then runs the function body
#   fibonacci_generator(25): gets the literal 25, then runs the function body
#   fibonacci_generator(20): gets the literal 20, then runs the function body
#   fibonacci_generator(20): gets the literal 20, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci_generator(limit) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to limit
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(fibonacci_generator(...))
#       calls fibonacci_generator with: limit=25
#         9.1 Assign to ?
#         9.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      limit = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci_generator(25)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1093:
#
# ===== SYNTAX =====
#   def char_generator(s)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   char_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   char_generator(s): looks up variable 's', then runs the function body
#   print(char_generator(?)): the inner char_generator() executes FIRST, then print() outputs the result
#   char_generator('hello'): gets the literal 'hello', then runs the function body
#   char_generator(?): resolves the value, then runs the function body
#   char_generator('hello'): gets the literal 'hello', then runs the function body
#   char_generator('hello'): gets the literal 'hello', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def char_generator(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(char_generator(...))
#       calls char_generator with: s=[4, 1, 8, 6, 3]
#         9.1 For loop over s
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions char_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(char_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round

"""

"""
Problem 1094:
#
# ===== SYNTAX =====
#   def step_generator(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   step_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   step_generator(n): looks up variable 'n', then runs the function body
#   print(step_generator(15)): the inner step_generator() executes FIRST, then print() outputs the result
#   step_generator(10): gets the literal 10, then runs the function body
#   step_generator(15): gets the literal 15, then runs the function body
#   step_generator(10): gets the literal 10, then runs the function body
#   step_generator(10): gets the literal 10, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def step_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(step_generator(...))
#       calls step_generator with: n=15
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions step_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(step_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1095:
#
# ===== SYNTAX =====
#   def even_number_generator(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   even_number_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   even_number_generator(n): looks up variable 'n', then runs the function body
#   print(even_number_generator(15)): the inner even_number_generator() executes FIRST, then print() outputs the result
#   even_number_generator(10): gets the literal 10, then runs the function body
#   even_number_generator(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   even_number_generator(10): gets the literal 10, then runs the function body
#   even_number_generator(10): gets the literal 10, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def even_number_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_number_generator(...))
#       calls even_number_generator with: n=15
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_number_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_number_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1096:
#
# ===== SYNTAX =====
#   def square_even_generator(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   square_even_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   square_even_generator(n): looks up variable 'n', then runs the function body
#   print(square_even_generator(15)): the inner square_even_generator() executes FIRST, then print() outputs the result
#   square_even_generator(10): gets the literal 10, then runs the function body
#   square_even_generator(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   square_even_generator(10): gets the literal 10, then runs the function body
#   square_even_generator(10): gets the literal 10, then runs the function body
#   i ** 2: first get i, then get 2, then compute i ** 2
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   i ** 2: first get i, then get 2, then compute i ** 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_even_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_even_generator(...))
#       calls square_even_generator with: n=15
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_even_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_even_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1097:
#
# ===== SYNTAX =====
#   def prime_generator(n)
#   len() — built-in function call
#   all() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   prime_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   if/elif/else
#   while
#   operators: % != <
#
# ===== EVALUATION ORDER =====
#   prime_generator(n): looks up variable 'n', then runs the function body
#   print(prime_generator(15)): the inner prime_generator() executes FIRST, then print() outputs the result
#   prime_generator(5): gets the literal 5, then runs the function body
#   prime_generator(15): gets the literal 15, then runs the function body
#   prime_generator(5): gets the literal 5, then runs the function body
#   prime_generator(5): gets the literal 5, then runs the function body
#   num % p: first get num, then get p, then compute num % p
#   num % p: first get num, then get p, then compute num % p
#   num % p: first get num, then get p, then compute num % p
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def prime_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(prime_generator(...))
#       calls prime_generator with: n=15
#         9.1 Assign to primes
#         9.2 Assign to num
#         9.3 While loop
#         9.4 Evaluate expression
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions prime_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(prime_generator(15)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1098:
#
# ===== SYNTAX =====
#   def consecutive_pair_sum(lst)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   consecutive_pair_sum() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   consecutive_pair_sum(lst): looks up variable 'lst', then runs the function body
#   print(consecutive_pair_sum(?)): the inner consecutive_pair_sum() executes FIRST, then print() outputs the result
#   consecutive_pair_sum(?): resolves the value, then runs the function body
#   consecutive_pair_sum(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   consecutive_pair_sum(?): resolves the value, then runs the function body
#   consecutive_pair_sum(?): resolves the value, then runs the function body
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   ? + ?: first get ?, then get ?, then compute ? + ?
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def consecutive_pair_sum(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(consecutive_pair_sum(...))
#       calls consecutive_pair_sum with: lst=[1, 3, 5, 7]
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions consecutive_pair_sum, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(consecutive_pair_sum(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1099:
#
# ===== SYNTAX =====
#   def factorial_generator(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   factorial() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   factorial_generator(n): looks up variable 'n', then runs the function body
#   print(factorial_generator(15)): the inner factorial_generator() executes FIRST, then print() outputs the result
#   factorial_generator(5): gets the literal 5, then runs the function body
#   factorial_generator(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   x + 1: first get x, then get 1, then compute x + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def factorial_generator(n) — stored for later
#   5. Define def factorial_generator(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(factorial_generator(...))
#       calls factorial_generator with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial_generator, factorial_generator, factorial_generator are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1100:
#
# ===== SYNTAX =====
#   def divisible_by_3_and_5_generator(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   divisible_by_3_and_5_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: + % == and
#
# ===== EVALUATION ORDER =====
#   divisible_by_3_and_5_generator(n): looks up variable 'n', then runs the function body
#   print(divisible_by_3_and_5_generator(15)): the inner divisible_by_3_and_5_generator() executes FIRST, then print() outputs the result
#   divisible_by_3_and_5_generator(50): gets the literal 50, then runs the function body
#   divisible_by_3_and_5_generator(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   divisible_by_3_and_5_generator(50): gets the literal 50, then runs the function body
#   divisible_by_3_and_5_generator(50): gets the literal 50, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 5: first get i, then get 5, then compute i % 5
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 5: first get i, then get 5, then compute i % 5
#   i % 3: first get i, then get 3, then compute i % 3
#   i % 5: first get i, then get 5, then compute i % 5
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_3_and_5_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(divisible_by_3_and_5_generator(...))
#       calls divisible_by_3_and_5_generator with: n=15
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_3_and_5_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_3_and_5_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1101:
#
# ===== SYNTAX =====
#   def odd_filter_generator(lst)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   odd_filter_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   odd_filter_generator(lst): looks up variable 'lst', then runs the function body
#   print(odd_filter_generator(?)): the inner odd_filter_generator() executes FIRST, then print() outputs the result
#   odd_filter_generator(?): resolves the value, then runs the function body
#   odd_filter_generator(?): resolves the value, then runs the function body
#   odd_filter_generator(?): resolves the value, then runs the function body
#   odd_filter_generator(?): resolves the value, then runs the function body
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def odd_filter_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(odd_filter_generator(...))
#       calls odd_filter_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over lst
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions odd_filter_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(odd_filter_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1102:
#
# ===== SYNTAX =====
#   def cumulative_sum_generator(lst)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   cumulative_sum_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   cumulative_sum_generator(lst): looks up variable 'lst', then runs the function body
#   print(cumulative_sum_generator(?)): the inner cumulative_sum_generator() executes FIRST, then print() outputs the result
#   cumulative_sum_generator(?): resolves the value, then runs the function body
#   cumulative_sum_generator(?): resolves the value, then runs the function body
#   cumulative_sum_generator(?): resolves the value, then runs the function body
#   cumulative_sum_generator(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def cumulative_sum_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(cumulative_sum_generator(...))
#       calls cumulative_sum_generator with: lst=[1, 3, 5, 7]
#         9.1 Assign to total
#         9.2 For loop over lst
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions cumulative_sum_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(cumulative_sum_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 1103:
#
# ===== SYNTAX =====
#   def power_generator(n, power)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   power_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   power_generator(n, power): looks up variable 'n', looks up variable 'power', then runs the function body
#   print(power_generator(15, 25)): the inner power_generator() executes FIRST, then print() outputs the result
#   power_generator(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   power_generator(15, 25): gets the literal 15, gets the literal 25, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   power_generator(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   power_generator(5, 3): gets the literal 5, gets the literal 3, then runs the function body
#   i ** power: first get i, then get power, then compute i ** power
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   i ** power: first get i, then get power, then compute i ** power
#   i ** power: first get i, then get power, then compute i ** power
#
# ===== EXECUTION ORDER =====
#   1. Define def power_generator(n, power) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to power
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(power_generator(...))
#       calls power_generator with: n=15, power=25
#         10.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions power_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      power = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(power_generator(15, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1104:
#
# ===== SYNTAX =====
#   def word_generator(sentence)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   word_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   word_generator(sentence): looks up variable 'sentence', then runs the function body
#   print(word_generator('Python is fun')): the inner word_generator() executes FIRST, then print() outputs the result
#   word_generator('This is a test sentence'): gets the literal 'This is a test sentence', then runs the function body
#   word_generator('Python is fun'): gets the literal 'Python is fun', then runs the function body
#   word_generator('This is a test sentence'): gets the literal 'This is a test sentence', then runs the function body
#   word_generator('This is a test sentence'): gets the literal 'This is a test sentence', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def word_generator(sentence) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(word_generator(...))
#       calls word_generator with: sentence='Python is fun'
#         9.1 Assign to words
#         9.2 For loop over words
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions word_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(word_generator('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round

"""

"""
Problem 1105:
#
# ===== SYNTAX =====
#   def square_number_generator(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   square_number_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + **
#
# ===== EVALUATION ORDER =====
#   square_number_generator(n): looks up variable 'n', then runs the function body
#   print(square_number_generator(15)): the inner square_number_generator() executes FIRST, then print() outputs the result
#   square_number_generator(5): gets the literal 5, then runs the function body
#   square_number_generator(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   square_number_generator(5): gets the literal 5, then runs the function body
#   square_number_generator(5): gets the literal 5, then runs the function body
#   i ** 2: first get i, then get 2, then compute i ** 2
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   i ** 2: first get i, then get 2, then compute i ** 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_number_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(square_number_generator(...))
#       calls square_number_generator with: n=15
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_number_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_number_generator(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1106:
#
# ===== SYNTAX =====
#   def main(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   main() — built-in function call
#   square_odd_number_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   for
#   if/elif/else
#   in
#   operators: + % ** !=
#
# ===== EVALUATION ORDER =====
#   main(n): looks up variable 'n', then runs the function body
#   print(main(15)): the inner main() executes FIRST, then print() outputs the result
#   main(n): looks up variable 'n', then runs the function body
#   square_odd_number_generator(10): gets the literal 10, then runs the function body
#   main(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   main(n): looks up variable 'n', then runs the function body
#   square_odd_number_generator(10): gets the literal 10, then runs the function body
#   main(n_2): looks up variable 'n_2', then runs the function body
#   square_odd_number_generator(10): gets the literal 10, then runs the function body
#   num % 2: first get num, then get 2, then compute num % 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   n_2 + 1: first get n_2, then get 1, then compute n_2 + 1
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num % 2: first get num, then get 2, then compute num % 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#   num % 2: first get num, then get 2, then compute num % 2
#   i ** 2: first get i, then get 2, then compute i ** 2
#   num ** 2: first get num, then get 2, then compute num ** 2
#   num ** 2: first get num, then get 2, then compute num ** 2
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def main(n) — stored for later
#   2. Assign to n
#   3. Assign to result
#   4. Run print(result)
#   5. Define def square_odd_number_generator(n) — stored for later
#   6. Assign to result
#   7. Run print(result)
#   8. Define def solve() — stored for later
#   9. Define def solve() — stored for later
#   10. Assign to n
#   11. Assign to result
#   12. Run print(result)
#   13. Run print(main(...))
#       calls main with: n=15
#         13.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, square_odd_number_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1107:
#
# ===== SYNTAX =====
#   def reverse_list_generator(lst)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   reverse_list_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: -
#
# ===== EVALUATION ORDER =====
#   reverse_list_generator(lst): looks up variable 'lst', then runs the function body
#   print(reverse_list_generator(?)): the inner reverse_list_generator() executes FIRST, then print() outputs the result
#   reverse_list_generator(?): resolves the value, then runs the function body
#   reverse_list_generator(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   reverse_list_generator(?): resolves the value, then runs the function body
#   reverse_list_generator(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_list_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_list_generator(...))
#       calls reverse_list_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_list_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_list_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1108:
#
# ===== SYNTAX =====
#   def multiple_generator(n, multiple)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   multiple_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   multiple_generator(n, multiple): looks up variable 'n', looks up variable 'multiple', then runs the function body
#   print(multiple_generator(15, 25)): the inner multiple_generator() executes FIRST, then print() outputs the result
#   multiple_generator(20, 3): gets the literal 20, gets the literal 3, then runs the function body
#   multiple_generator(15, 25): gets the literal 15, gets the literal 25, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   multiple_generator(20, 3): gets the literal 20, gets the literal 3, then runs the function body
#   multiple_generator(20, 3): gets the literal 20, gets the literal 3, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def multiple_generator(n, multiple) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to multiple
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(multiple_generator(...))
#       calls multiple_generator with: n=15, multiple=25
#         10.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiple_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      multiple = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiple_generator(15, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1109:
#
# ===== SYNTAX =====
#   def uppercase_generator(s)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   uppercase_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   uppercase_generator(s): looks up variable 's', then runs the function body
#   print(uppercase_generator(?)): the inner uppercase_generator() executes FIRST, then print() outputs the result
#   uppercase_generator('Hello World'): gets the literal 'Hello World', then runs the function body
#   uppercase_generator(?): resolves the value, then runs the function body
#   uppercase_generator('Hello World'): gets the literal 'Hello World', then runs the function body
#   uppercase_generator('Hello World'): gets the literal 'Hello World', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def uppercase_generator(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(uppercase_generator(...))
#       calls uppercase_generator with: s=[4, 1, 8, 6, 3]
#         9.1 For loop over s
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions uppercase_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(uppercase_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1110:
#
# ===== SYNTAX =====
#   def consecutive_diff_generator(lst)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   consecutive_diff_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   consecutive_diff_generator(lst): looks up variable 'lst', then runs the function body
#   print(consecutive_diff_generator(?)): the inner consecutive_diff_generator() executes FIRST, then print() outputs the result
#   consecutive_diff_generator(?): resolves the value, then runs the function body
#   consecutive_diff_generator(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   consecutive_diff_generator(?): resolves the value, then runs the function body
#   consecutive_diff_generator(?): resolves the value, then runs the function body
#   ? - ?: first get ?, then get ?, then compute ? - ?
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   ? - ?: first get ?, then get ?, then compute ? - ?
#   ? - ?: first get ?, then get ?, then compute ? - ?
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def consecutive_diff_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(consecutive_diff_generator(...))
#       calls consecutive_diff_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions consecutive_diff_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(consecutive_diff_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1111:
#
# ===== SYNTAX =====
#   def greater_than_generator(lst, value)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   greater_than_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: >
#
# ===== EVALUATION ORDER =====
#   greater_than_generator(lst, value): looks up variable 'lst', looks up variable 'value', then runs the function body
#   print(greater_than_generator(?, 25)): the inner greater_than_generator() executes FIRST, then print() outputs the result
#   greater_than_generator(?, 3): resolves the value, gets the literal 3, then runs the function body
#   greater_than_generator(?, 25): resolves the value, gets the literal 25, then runs the function body
#   greater_than_generator(?, 3): resolves the value, gets the literal 3, then runs the function body
#   greater_than_generator(?, 3): resolves the value, gets the literal 3, then runs the function body
#   num > value: is this True or False? The answer picks which branch
#   num > value: is this True or False? The answer picks which branch
#   num > value: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def greater_than_generator(lst, value) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to value
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(greater_than_generator(...))
#       calls greater_than_generator with: lst=[1, 3, 5, 7], value=25
#         10.1 For loop over lst
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greater_than_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      value = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greater_than_generator(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1112:
#
# ===== SYNTAX =====
#   def sum_with_index_generator(lst)
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   sum_with_index_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   sum_with_index_generator(lst): looks up variable 'lst', then runs the function body
#   print(sum_with_index_generator(?)): the inner sum_with_index_generator() executes FIRST, then print() outputs the result
#   sum_with_index_generator(?): resolves the value, then runs the function body
#   sum_with_index_generator(?): resolves the value, then runs the function body
#   sum_with_index_generator(?): resolves the value, then runs the function body
#   sum_with_index_generator(?): resolves the value, then runs the function body
#   num + index: first get num, then get index, then compute num + index
#   num + index: first get num, then get index, then compute num + index
#   num + index: first get num, then get index, then compute num + index
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_with_index_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(sum_with_index_generator(...))
#       calls sum_with_index_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over enumerate(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_with_index_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(sum_with_index_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 1113:
#
# ===== SYNTAX =====
#   def palindrome_generator(lst)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   palindrome_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   palindrome_generator(lst): looks up variable 'lst', then runs the function body
#   print(palindrome_generator(?)): the inner palindrome_generator() executes FIRST, then print() outputs the result
#   palindrome_generator(?): resolves the value, then runs the function body
#   palindrome_generator(?): resolves the value, then runs the function body
#   palindrome_generator(?): resolves the value, then runs the function body
#   palindrome_generator(?): resolves the value, then runs the function body
#   word == ?: is this True or False? The answer picks which branch
#   word == ?: is this True or False? The answer picks which branch
#   word == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def palindrome_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(palindrome_generator(...))
#       calls palindrome_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over lst
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions palindrome_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(palindrome_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1114:
#
# ===== SYNTAX =====
#   def pair_product_generator(lst)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   pair_product_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: + - *
#
# ===== EVALUATION ORDER =====
#   pair_product_generator(lst): looks up variable 'lst', then runs the function body
#   print(pair_product_generator(?)): the inner pair_product_generator() executes FIRST, then print() outputs the result
#   pair_product_generator(?): resolves the value, then runs the function body
#   pair_product_generator(?): resolves the value, then runs the function body
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   pair_product_generator(?): resolves the value, then runs the function body
#   pair_product_generator(?): resolves the value, then runs the function body
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   len(...) - 1: first get len(...), then get 1, then compute len(...) - 1
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   ? * ?: first get ?, then get ?, then compute ? * ?
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#   i + 1: first get i, then get 1, then compute i + 1
#
# ===== EXECUTION ORDER =====
#   1. Define def pair_product_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(pair_product_generator(...))
#       calls pair_product_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions pair_product_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(pair_product_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1115:
#
# ===== SYNTAX =====
#   def greater_than_previous_generator(lst)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   greater_than_previous_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: - >
#
# ===== EVALUATION ORDER =====
#   greater_than_previous_generator(lst): looks up variable 'lst', then runs the function body
#   print(greater_than_previous_generator(?)): the inner greater_than_previous_generator() executes FIRST, then print() outputs the result
#   greater_than_previous_generator(?): resolves the value, then runs the function body
#   greater_than_previous_generator(?): resolves the value, then runs the function body
#   greater_than_previous_generator(?): resolves the value, then runs the function body
#   greater_than_previous_generator(?): resolves the value, then runs the function body
#   i - 1: first get i, then get 1, then compute i - 1
#   i - 1: first get i, then get 1, then compute i - 1
#   i - 1: first get i, then get 1, then compute i - 1
#   ? > ?: is this True or False? The answer picks which branch
#   ? > ?: is this True or False? The answer picks which branch
#   ? > ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def greater_than_previous_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(greater_than_previous_generator(...))
#       calls greater_than_previous_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greater_than_previous_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greater_than_previous_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1116:
#
# ===== SYNTAX =====
#   def divisible_by_generator(lst, divisor)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   divisible_by_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   divisible_by_generator(lst, divisor): looks up variable 'lst', looks up variable 'divisor', then runs the function body
#   print(divisible_by_generator(?, 25)): the inner divisible_by_generator() executes FIRST, then print() outputs the result
#   divisible_by_generator(?, 5): resolves the value, gets the literal 5, then runs the function body
#   divisible_by_generator(?, 25): resolves the value, gets the literal 25, then runs the function body
#   divisible_by_generator(?, 5): resolves the value, gets the literal 5, then runs the function body
#   divisible_by_generator(?, 5): resolves the value, gets the literal 5, then runs the function body
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   num % divisor: first get num, then get divisor, then compute num % divisor
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_generator(lst, divisor) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to divisor
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(divisible_by_generator(...))
#       calls divisible_by_generator with: lst=[1, 3, 5, 7], divisor=25
#         10.1 For loop over lst
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      divisor = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_generator(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1117:
#
# ===== SYNTAX =====
#   def even_fibonacci_generator(n)
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   even_fibonacci_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   while
#   operators: + * <
#
# ===== EVALUATION ORDER =====
#   even_fibonacci_generator(n): looks up variable 'n', then runs the function body
#   print(even_fibonacci_generator(15)): the inner even_fibonacci_generator() executes FIRST, then print() outputs the result
#   even_fibonacci_generator(5): gets the literal 5, then runs the function body
#   even_fibonacci_generator(15): gets the literal 15, then runs the function body
#   even_fibonacci_generator(5): gets the literal 5, then runs the function body
#   even_fibonacci_generator(5): gets the literal 5, then runs the function body
#   ? + a: first get ?, then get a, then compute ? + a
#   4 * b: first get 4, then get b, then compute 4 * b
#   ? + a: first get ?, then get a, then compute ? + a
#   ? + a: first get ?, then get a, then compute ? + a
#   4 * b: first get 4, then get b, then compute 4 * b
#   4 * b: first get 4, then get b, then compute 4 * b
#
# ===== EXECUTION ORDER =====
#   1. Define def even_fibonacci_generator(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_fibonacci_generator(...))
#       calls even_fibonacci_generator with: n=15
#         9.1 Assign to ?
#         9.2 Assign to count
#         9.3 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_fibonacci_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_fibonacci_generator(15)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1118:
#
# ===== SYNTAX =====
#   def even_index_generator(lst)
#   range() — built-in function call
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   even_index_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   even_index_generator(lst): looks up variable 'lst', then runs the function body
#   print(even_index_generator(?)): the inner even_index_generator() executes FIRST, then print() outputs the result
#   even_index_generator(?): resolves the value, then runs the function body
#   even_index_generator(?): resolves the value, then runs the function body
#   even_index_generator(?): resolves the value, then runs the function body
#   even_index_generator(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def even_index_generator(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_index_generator(...))
#       calls even_index_generator with: lst=[1, 3, 5, 7]
#         9.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_index_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_index_generator(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1119:
#
# ===== SYNTAX =====
#   def even_length_word_generator(sentence)
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   even_length_word_generator() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   even_length_word_generator(sentence): looks up variable 'sentence', then runs the function body
#   print(even_length_word_generator('Python is fun')): the inner even_length_word_generator() executes FIRST, then print() outputs the result
#   even_length_word_generator('This is a simple test sentence'): gets the literal 'This is a simple test sentence', then runs the function body
#   even_length_word_generator('Python is fun'): gets the literal 'Python is fun', then runs the function body
#   even_length_word_generator('This is a simple test sentence'): gets the literal 'This is a simple test sentence', then runs the function body
#   even_length_word_generator('This is a simple test sentence'): gets the literal 'This is a simple test sentence', then runs the function body
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def even_length_word_generator(sentence) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to sentence
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_length_word_generator(...))
#       calls even_length_word_generator with: sentence='Python is fun'
#         9.1 Assign to words
#         9.2 For loop over words
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_length_word_generator, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      sentence = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_length_word_generator('Python is fun')) → function runs first, then print shows the result
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1120:
#
# ===== SYNTAX =====
#   def check_number(n)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: < >
#
# ===== EVALUATION ORDER =====
#   check_number(5): gets the literal 5, then runs the function body
#   check_number(n): looks up variable 'n', then runs the function body
#   print(check_number(15)): the inner check_number() executes FIRST, then print() outputs the result
#   check_number(15): gets the literal 15, then runs the function body
#   n > 0: is this True or False? The answer picks which branch
#   n < 0: is this True or False? The answer picks which branch
#   n < 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_number(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_number(n) — stored for later
#   5. Define def check_number(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_number(...))
#       calls check_number with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_number, check_number, check_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_number(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1121:
#
# ===== SYNTAX =====
#   def largest_of_three(a, b, c)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: >= and
#
# ===== EVALUATION ORDER =====
#   largest_of_three(10, 20, 15): gets the literal 10, gets the literal 20, gets the literal 15, then runs the function body
#   largest_of_three(a, b, c): looks up variable 'a', looks up variable 'b', looks up variable 'c', then runs the function body
#   print(largest_of_three(20, 30, 10)): the inner largest_of_three() executes FIRST, then print() outputs the result
#   largest_of_three(20, 30, 10): gets the literal 20, gets the literal 30, gets the literal 10, then runs the function body
#   a >= b: is this True or False? The answer picks which branch
#   a >= c: is this True or False? The answer picks which branch
#   b >= a: is this True or False? The answer picks which branch
#   b >= c: is this True or False? The answer picks which branch
#   b >= a: is this True or False? The answer picks which branch
#   b >= c: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def largest_of_three(a, b, c) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def largest_of_three(a, b, c) — stored for later
#   5. Define def largest_of_three(a, b, c) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to c
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(largest_of_three(...))
#       calls largest_of_three with: a=20, b=30, c=10
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions largest_of_three, largest_of_three, largest_of_three are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      c = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(largest_of_three(20, 30, 10)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1122:
#
# ===== SYNTAX =====
#   def is_leap_year(year)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: % == != and or
#
# ===== EVALUATION ORDER =====
#   is_leap_year(2024): gets the literal 2024, then runs the function body
#   is_leap_year(year): looks up variable 'year', then runs the function body
#   print(is_leap_year(25)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   is_leap_year(25): gets the literal 25, then runs the function body
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_leap_year(year) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_leap_year(year) — stored for later
#   5. Define def is_leap_year(year) — stored for later
#   6. Assign to year
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_leap_year(...))
#       calls is_leap_year with: year=25
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_leap_year, is_leap_year, is_leap_year are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_leap_year(25)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1123:
#
# ===== SYNTAX =====
#   def check_even_odd(n)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   check_even_odd(7): gets the literal 7, then runs the function body
#   check_even_odd(n): looks up variable 'n', then runs the function body
#   print(check_even_odd(15)): the inner check_even_odd() executes FIRST, then print() outputs the result
#   check_even_odd(15): gets the literal 15, then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def check_even_odd(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def check_even_odd(n) — stored for later
#   5. Define def check_even_odd(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(check_even_odd(...))
#       calls check_even_odd with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_even_odd, check_even_odd, check_even_odd are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check_even_odd(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1124:
#
# ===== SYNTAX =====
#   def divisible_by_3_and_5(n)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   divisible_by_3_and_5(15): gets the literal 15, then runs the function body
#   divisible_by_3_and_5(n): looks up variable 'n', then runs the function body
#   print(divisible_by_3_and_5(15)): the inner divisible_by_3_and_5() executes FIRST, then print() outputs the result
#   divisible_by_3_and_5(15): gets the literal 15, then runs the function body
#   n % 3: first get n, then get 3, then compute n % 3
#   n % 5: first get n, then get 5, then compute n % 5
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_3_and_5(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def divisible_by_3_and_5(n) — stored for later
#   5. Define def divisible_by_3_and_5(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(divisible_by_3_and_5(...))
#       calls divisible_by_3_and_5 with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_3_and_5, divisible_by_3_and_5, divisible_by_3_and_5 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_3_and_5(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1125:
#
# ===== SYNTAX =====
#   def divisible_by_7(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   divisible_by_7() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: + % ==
#
# ===== EVALUATION ORDER =====
#   divisible_by_7(50): gets the literal 50, then runs the function body
#   divisible_by_7(n): looks up variable 'n', then runs the function body
#   print(divisible_by_7(15)): the inner divisible_by_7() executes FIRST, then print() outputs the result
#   divisible_by_7(50): gets the literal 50, then runs the function body
#   divisible_by_7(50): gets the literal 50, then runs the function body
#   divisible_by_7(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   i % 7: first get i, then get 7, then compute i % 7
#   n + 1: first get n, then get 1, then compute n + 1
#   n + 1: first get n, then get 1, then compute n + 1
#   i % 7: first get i, then get 7, then compute i % 7
#   i % 7: first get i, then get 7, then compute i % 7
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_7(n) — stored for later
#   2. Run divisible_by_7(50)
#       with: n=50
#         2.1 For loop over range(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(divisible_by_7(...))
#       calls divisible_by_7 with: n=15
#         8.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_7, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      divisible_by_7(50) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_7(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1126:
#
# ===== SYNTAX =====
#   def is_prime(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   int() — built-in function call
#   for
#   if/elif/else
#   in
#   operators: + % ** == <
#
# ===== EVALUATION ORDER =====
#   is_prime(11): gets the literal 11, then runs the function body
#   is_prime(n): looks up variable 'n', then runs the function body
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(15): gets the literal 15, then runs the function body
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n % i: first get n, then get i, then compute n % i
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n < 2: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_prime(n) — stored for later
#   5. Define def is_prime(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_prime(...))
#       calls is_prime with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1127:
#
# ===== SYNTAX =====
#   def factorial(n)
#   return — exits the function and sends back a value
#   factorial() — built-in function call
#   if/elif/else
#   operators: - * == or
#
# ===== EVALUATION ORDER =====
#   factorial(5): gets the literal 5, then runs the function body
#   factorial(n): looks up variable 'n', then runs the function body
#   print(factorial(15)): the inner factorial() executes FIRST, then print() outputs the result
#   n * factorial(...): first get n, then get factorial(...), then compute n * factorial(...)
#   factorial(15): gets the literal 15, then runs the function body
#   factorial(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n == 0: is this True or False? The answer picks which branch
#   n == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def factorial(n) — stored for later
#   5. Define def factorial(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(factorial(...))
#       calls factorial with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial, factorial, factorial are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1128:
#
# ===== SYNTAX =====
#   def is_palindrome(s)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   is_palindrome('madam'): gets the literal 'madam', then runs the function body
#   is_palindrome(s): looks up variable 's', then runs the function body
#   print(is_palindrome(?)): the inner is_palindrome() executes FIRST, then print() outputs the result
#   is_palindrome(?): resolves the value, then runs the function body
#   s == ?: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_palindrome(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_palindrome(s) — stored for later
#   5. Define def is_palindrome(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_palindrome(...))
#       calls is_palindrome with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_palindrome, is_palindrome, is_palindrome are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_palindrome(?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1129:
#
# ===== SYNTAX =====
#   def count_vowels(s)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   len() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   count_vowels('Hello World'): gets the literal 'Hello World', then runs the function body
#   count_vowels(s): looks up variable 's', then runs the function body
#   print(count_vowels(?)): the inner count_vowels() executes FIRST, then print() outputs the result
#   count_vowels(?): resolves the value, then runs the function body
#   char.lower(...) in vowels: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_vowels(s) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_vowels(s) — stored for later
#   5. Define def count_vowels(s) — stored for later
#   6. Assign to s
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(count_vowels(...))
#       calls count_vowels with: s=[4, 1, 8, 6, 3]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_vowels, count_vowels, count_vowels are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      s = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_vowels(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from s each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1130:
#
# ===== SYNTAX =====
#   def is_perfect_number(n)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   range() — built-in function call
#   comprehension
#   if/elif/else
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   is_perfect_number(6): gets the literal 6, then runs the function body
#   is_perfect_number(n): looks up variable 'n', then runs the function body
#   print(is_perfect_number(15)): the inner is_perfect_number() executes FIRST, then print() outputs the result
#   is_perfect_number(15): gets the literal 15, then runs the function body
#   n % i: first get n, then get i, then compute n % i
#   sum(...) == n: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_perfect_number(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_perfect_number(n) — stored for later
#   5. Define def is_perfect_number(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_perfect_number(...))
#       calls is_perfect_number with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_perfect_number, is_perfect_number, is_perfect_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_perfect_number(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1131:
#
# ===== SYNTAX =====
#   def is_armstrong(n)
#   return — exits the function and sends back a value
#   str() — built-in function call
#   len() — built-in function call
#   sum() — built-in function call
#   int() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ** ==
#
# ===== EVALUATION ORDER =====
#   is_armstrong(153): gets the literal 153, then runs the function body
#   is_armstrong(n): looks up variable 'n', then runs the function body
#   print(is_armstrong(15)): the inner is_armstrong() executes FIRST, then print() outputs the result
#   int(...) ** length: first get int(...), then get length, then compute int(...) ** length
#   is_armstrong(15): gets the literal 15, then runs the function body
#   int(...) ** length: first get int(...), then get length, then compute int(...) ** length
#   total == n: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_armstrong(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Assign to total
#   5. For loop over num_str
#       5.1 Update total += ...
#   6. Return from function
#   7. Define def is_armstrong(n) — stored for later
#   8. Define def is_armstrong(n) — stored for later
#   9. Assign to n
#   10. Assign to result
#   11. Run print(result)
#   12. Run print(is_armstrong(...))
#       calls is_armstrong with: n=15
#         12.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_armstrong, is_armstrong, is_armstrong are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      total = ... (stored in memory)
#      for loop runs its body once per item
#      top-level statement runs
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_armstrong(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from num_str each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1132:
#
# ===== SYNTAX =====
#   def prime_numbers_up_to_n(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   all() — built-in function call
#   int() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + % ** !=
#
# ===== EVALUATION ORDER =====
#   prime_numbers_up_to_n(20): gets the literal 20, then runs the function body
#   prime_numbers_up_to_n(n): looks up variable 'n', then runs the function body
#   print(prime_numbers_up_to_n(15)): the inner prime_numbers_up_to_n() executes FIRST, then print() outputs the result
#   prime_numbers_up_to_n(15): gets the literal 15, then runs the function body
#   n + 1: first get n, then get 1, then compute n + 1
#   num % i: first get num, then get i, then compute num % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   num ** 0.5: first get num, then get 0.5, then compute num ** 0.5
#   ? != 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def prime_numbers_up_to_n(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def prime_numbers_up_to_n(n) — stored for later
#   5. Define def prime_numbers_up_to_n(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(prime_numbers_up_to_n(...))
#       calls prime_numbers_up_to_n with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions prime_numbers_up_to_n, prime_numbers_up_to_n, prime_numbers_up_to_n are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(prime_numbers_up_to_n(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1133:
#
# ===== SYNTAX =====
#   def even_and_greater_than_10(n)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: % == > and
#
# ===== EVALUATION ORDER =====
#   even_and_greater_than_10(12): gets the literal 12, then runs the function body
#   even_and_greater_than_10(n): looks up variable 'n', then runs the function body
#   print(even_and_greater_than_10(15)): the inner even_and_greater_than_10() executes FIRST, then print() outputs the result
#   even_and_greater_than_10(15): gets the literal 15, then runs the function body
#   n % 2: first get n, then get 2, then compute n % 2
#   ? == 0: is this True or False? The answer picks which branch
#   n > 10: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def even_and_greater_than_10(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def even_and_greater_than_10(n) — stored for later
#   5. Define def even_and_greater_than_10(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(even_and_greater_than_10(...))
#       calls even_and_greater_than_10 with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_and_greater_than_10, even_and_greater_than_10, even_and_greater_than_10 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(even_and_greater_than_10(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1134:
#
# ===== SYNTAX =====
#   def find_divisors(n)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: + % ==
#
# ===== EVALUATION ORDER =====
#   find_divisors(12): gets the literal 12, then runs the function body
#   find_divisors(n): looks up variable 'n', then runs the function body
#   print(find_divisors(15)): the inner find_divisors() executes FIRST, then print() outputs the result
#   n + 1: first get n, then get 1, then compute n + 1
#   find_divisors(15): gets the literal 15, then runs the function body
#   n % i: first get n, then get i, then compute n % i
#   n + 1: first get n, then get 1, then compute n + 1
#   n % i: first get n, then get i, then compute n % i
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to n
#   2. For loop over range(...)
#       2.1 If statement (one branch runs)
#   3. Define def find_divisors(n) — stored for later
#   4. Assign to result
#   5. Run print(result)
#   6. Define def find_divisors(n) — stored for later
#   7. Define def find_divisors(n) — stored for later
#   8. Assign to n
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(find_divisors(...))
#       calls find_divisors with: n=15
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_divisors, find_divisors, find_divisors are just stored
#   Execution starts at the non-def lines below:
#      n = ... (stored in memory)
#      for loop runs its body once per item
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(find_divisors(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1135:
#
# ===== SYNTAX =====
#   def multiplication_table(n)
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   multiplication_table() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#   operators: *
#
# ===== EVALUATION ORDER =====
#   multiplication_table(7): gets the literal 7, then runs the function body
#   multiplication_table(n): looks up variable 'n', then runs the function body
#   print(multiplication_table(15)): the inner multiplication_table() executes FIRST, then print() outputs the result
#   multiplication_table(7): gets the literal 7, then runs the function body
#   multiplication_table(7): gets the literal 7, then runs the function body
#   multiplication_table(15): gets the literal 15, then runs the function body
#   n * i: first get n, then get i, then compute n * i
#   n * i: first get n, then get i, then compute n * i
#   n * i: first get n, then get i, then compute n * i
#
# ===== EXECUTION ORDER =====
#   1. Define def multiplication_table(n) — stored for later
#   2. Run multiplication_table(7)
#       with: n=7
#         2.1 For loop over range(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to n
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(multiplication_table(...))
#       calls multiplication_table with: n=15
#         8.1 For loop over range(...)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiplication_table, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      multiplication_table(7) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiplication_table(15)) → function runs first, then print shows the result
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1136:
#
# ===== SYNTAX =====
#   def is_perfect_square(n)
#   return — exits the function and sends back a value
#   int() — built-in function call
#   operators: ** ==
#
# ===== EVALUATION ORDER =====
#   is_perfect_square(25): gets the literal 25, then runs the function body
#   is_perfect_square(n): looks up variable 'n', then runs the function body
#   print(is_perfect_square(15)): the inner is_perfect_square() executes FIRST, then print() outputs the result
#   is_perfect_square(15): gets the literal 15, then runs the function body
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   int(...) ** 2: first get int(...), then get 2, then compute int(...) ** 2
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#
# ===== EXECUTION ORDER =====
#   1. Define def is_perfect_square(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_perfect_square(n) — stored for later
#   5. Define def is_perfect_square(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_perfect_square(...))
#       calls is_perfect_square with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_perfect_square, is_perfect_square, is_perfect_square are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_perfect_square(15)) → function runs first, then print shows the result

"""

"""
Problem 1137:
#
# ===== SYNTAX =====
#   def is_power_of_2(n)
#   return — exits the function and sends back a value
#   operators: - == > and
#
# ===== EVALUATION ORDER =====
#   is_power_of_2(16): gets the literal 16, then runs the function body
#   is_power_of_2(n): looks up variable 'n', then runs the function body
#   print(is_power_of_2(15)): the inner is_power_of_2() executes FIRST, then print() outputs the result
#   is_power_of_2(15): gets the literal 15, then runs the function body
#   n & ?: first get n, then get ?, then compute n & ?
#   n & ?: first get n, then get ?, then compute n & ?
#   n & ?: first get n, then get ?, then compute n & ?
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#   n - 1: first get n, then get 1, then compute n - 1
#
# ===== EXECUTION ORDER =====
#   1. Define def is_power_of_2(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_power_of_2(n) — stored for later
#   5. Define def is_power_of_2(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_power_of_2(...))
#       calls is_power_of_2 with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_power_of_2, is_power_of_2, is_power_of_2 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_power_of_2(15)) → function runs first, then print shows the result

"""

"""
Problem 1138:
#
# ===== SYNTAX =====
#   def second_largest(lst)
#   return — exits the function and sends back a value
#   sorted() — built-in function call
#   set() — built-in function call
#   len() — built-in function call
#   operators: >
#
# ===== EVALUATION ORDER =====
#   second_largest(?): resolves the value, then runs the function body
#   second_largest(lst): looks up variable 'lst', then runs the function body
#   print(second_largest(?)): the inner second_largest() executes FIRST, then print() outputs the result
#   second_largest(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def second_largest(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def second_largest(lst) — stored for later
#   5. Define def second_largest(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(second_largest(...))
#       calls second_largest with: lst=[1, 3, 5, 7]
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions second_largest, second_largest, second_largest are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(second_largest(?)) → function runs first, then print shows the result

"""

"""
Problem 1139:
#
# ===== SYNTAX =====
#   def is_power_of_3(n)
#   return — exits the function and sends back a value
#   if/elif/else
#   while
#   operators: % == > and
#
# ===== EVALUATION ORDER =====
#   is_power_of_3(27): gets the literal 27, then runs the function body
#   is_power_of_3(n): looks up variable 'n', then runs the function body
#   print(is_power_of_3(15)): the inner is_power_of_3() executes FIRST, then print() outputs the result
#   is_power_of_3(15): gets the literal 15, then runs the function body
#   n % 3: first get n, then get 3, then compute n % 3
#   n == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def is_power_of_3(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_power_of_3(n) — stored for later
#   5. Define def is_power_of_3(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_power_of_3(...))
#       calls is_power_of_3 with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_power_of_3, is_power_of_3, is_power_of_3 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_power_of_3(15)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1140:
#
# ===== SYNTAX =====
#   def fibonacci_up_to_n(n)
#   (no return — the function prints directly instead of returning)
#   while
#   operators: + <=
#
# ===== EVALUATION ORDER =====
#   fibonacci_up_to_n(20): gets the literal 20, then runs the function body
#   fibonacci_up_to_n(n): looks up variable 'n', then runs the function body
#   print(fibonacci_up_to_n(15)): the inner fibonacci_up_to_n() executes FIRST, then print() outputs the result
#   fibonacci_up_to_n(15): gets the literal 15, then runs the function body
#   a + b: first get a, then get b, then compute a + b
#
# ===== EXECUTION ORDER =====
#   1. Define def fibonacci_up_to_n(n) — stored for later
#   2. Run fibonacci_up_to_n(20)
#       with: n=20
#         2.1 Assign to ?
#         2.2 While loop
#   3. Assign to n
#   4. Assign to result
#   5. Run print(result)
#   6. Run print(fibonacci_up_to_n(...))
#       calls fibonacci_up_to_n with: n=15
#         6.1 Assign to ?
#         6.2 While loop
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions fibonacci_up_to_n are just stored
#   Execution starts at the non-def lines below:
#      fibonacci_up_to_n(20) → jumps into function, runs body, returns result → printed
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(fibonacci_up_to_n(15)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1141:
#
# ===== SYNTAX =====
#   def count_occurrences(lst, element)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   count_occurrences(?, 2): resolves the value, gets the literal 2, then runs the function body
#   count_occurrences(lst, element): looks up variable 'lst', looks up variable 'element', then runs the function body
#   print(count_occurrences(?, 25)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(?, 25): resolves the value, gets the literal 25, then runs the function body
#   c == element: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def count_occurrences(lst, element) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def count_occurrences(lst, element) — stored for later
#   5. Define def count_occurrences(lst, element) — stored for later
#   6. Assign to lst
#   7. Assign to element
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 3, 5, 7], element=25
#         10.1 Assign to count
#         10.2 For loop over lst
#         10.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_occurrences, count_occurrences, count_occurrences are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      element = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences(?, 25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1142:
#
# ===== SYNTAX =====
#   def is_perfect_square_and_armstrong(n)
#   return — exits the function and sends back a value
#   is_perfect_square() — built-in function call
#   is_armstrong() — built-in function call
#   if/elif/else
#   operators: and
#
# ===== EVALUATION ORDER =====
#   is_perfect_square_and_armstrong(153): gets the literal 153, then runs the function body
#   is_perfect_square_and_armstrong(n): looks up variable 'n', then runs the function body
#   print(is_perfect_square_and_armstrong(15)): the inner is_perfect_square_and_armstrong() executes FIRST, then print() outputs the result
#   is_perfect_square_and_armstrong(15): gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def is_perfect_square_and_armstrong(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def is_perfect_square_and_armstrong(n) — stored for later
#   5. Define def is_perfect_square_and_armstrong(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_perfect_square_and_armstrong(...))
#       calls is_perfect_square_and_armstrong with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_perfect_square_and_armstrong, is_perfect_square_and_armstrong, is_perfect_square_and_armstrong are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_perfect_square_and_armstrong(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1143:
#
# ===== SYNTAX =====
#   def divisible_by_7_and_11(start, end)
#   return — exits the function and sends back a value
#   range() — built-in function call
#   comprehension
#   operators: + % == and
#
# ===== EVALUATION ORDER =====
#   divisible_by_7_and_11(1, 100): gets the literal 1, gets the literal 100, then runs the function body
#   divisible_by_7_and_11(start, end): looks up variable 'start', looks up variable 'end', then runs the function body
#   print(divisible_by_7_and_11(3, 20)): the inner divisible_by_7_and_11() executes FIRST, then print() outputs the result
#   divisible_by_7_and_11(3, 20): gets the literal 3, gets the literal 20, then runs the function body
#   end + 1: first get end, then get 1, then compute end + 1
#   end + 1: first get end, then get 1, then compute end + 1
#   end + 1: first get end, then get 1, then compute end + 1
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_7_and_11(start, end) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def divisible_by_7_and_11(start, end) — stored for later
#   5. Define def divisible_by_7_and_11(start, end) — stored for later
#   6. Assign to start
#   7. Assign to end
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(divisible_by_7_and_11(...))
#       calls divisible_by_7_and_11 with: start=3, end=20
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_7_and_11, divisible_by_7_and_11, divisible_by_7_and_11 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      start = ... (stored in memory)
#      end = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_7_and_11(3, 20)) → function runs first, then print shows the result

"""

"""
Problem 1144:
#
# ===== SYNTAX =====
#   def factorial_while(n)
#   return — exits the function and sends back a value
#   while
#   operators: >
#
# ===== EVALUATION ORDER =====
#   factorial_while(5): gets the literal 5, then runs the function body
#   factorial_while(n): looks up variable 'n', then runs the function body
#   print(factorial_while(15)): the inner factorial_while() executes FIRST, then print() outputs the result
#   factorial_while(15): gets the literal 15, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial_while(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def factorial_while(n) — stored for later
#   5. Define def factorial_while(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(factorial_while(...))
#       calls factorial_while with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial_while, factorial_while, factorial_while are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial_while(15)) → function runs first, then print shows the result
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1145:
#
# ===== SYNTAX =====
#   def reverse_list(lst)
#   return — exits the function and sends back a value
#   reversed() — built-in function call
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   reverse_list(?): resolves the value, then runs the function body
#   reverse_list(lst): looks up variable 'lst', then runs the function body
#   print(reverse_list(?)): the inner reverse_list() executes FIRST, then print() outputs the result
#   reverse_list(?): resolves the value, then runs the function body
#   char + result: first get char, then get result, then compute char + result
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_list(lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def reverse_list(lst) — stored for later
#   5. Define def reverse_list(lst) — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(reverse_list(...))
#       calls reverse_list with: lst=[1, 3, 5, 7]
#         9.1 Assign to result
#         9.2 For loop over lst
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_list, reverse_list, reverse_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(reverse_list(?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round

"""

"""
Problem 1146:
#
# ===== SYNTAX =====
#   def divisible_by_5_or_10(n)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   divisible_by_5_or_10(50): gets the literal 50, then runs the function body
#   divisible_by_5_or_10(n): looks up variable 'n', then runs the function body
#   print(divisible_by_5_or_10(15)): the inner divisible_by_5_or_10() executes FIRST, then print() outputs the result
#   divisible_by_5_or_10(15): gets the literal 15, then runs the function body
#   n % 5: first get n, then get 5, then compute n % 5
#   n % 10: first get n, then get 10, then compute n % 10
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_5_or_10(n) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def divisible_by_5_or_10(n) — stored for later
#   5. Define def divisible_by_5_or_10(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(divisible_by_5_or_10(...))
#       calls divisible_by_5_or_10 with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_5_or_10, divisible_by_5_or_10, divisible_by_5_or_10 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_5_or_10(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1147:
#
# ===== SYNTAX =====
#   def divisible_by_any_number(n, lst)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   divisible_by_any_number(10, ?): gets the literal 10, resolves the value, then runs the function body
#   divisible_by_any_number(n, lst): looks up variable 'n', looks up variable 'lst', then runs the function body
#   print(divisible_by_any_number(15, ?)): the inner divisible_by_any_number() executes FIRST, then print() outputs the result
#   divisible_by_any_number(15, ?): gets the literal 15, resolves the value, then runs the function body
#   n % num: first get n, then get num, then compute n % num
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def divisible_by_any_number(n, lst) — stored for later
#   2. Assign to result
#   3. Run print(result)
#   4. Define def divisible_by_any_number(n, lst) — stored for later
#   5. Define def divisible_by_any_number(n, lst) — stored for later
#   6. Assign to n
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(divisible_by_any_number(...))
#       calls divisible_by_any_number with: n=15, lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions divisible_by_any_number, divisible_by_any_number, divisible_by_any_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(result)
#      n = ... (stored in memory)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(divisible_by_any_number(15, ?)) → function runs first, then print shows the result
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1148:
#
# ===== SYNTAX =====
#   def outer_function()
#   inner_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   outer_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   outer_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   outer_function(): — no arguments needed
#   outer_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def outer_function() — stored for later
#   2. Run outer_function()
#         2.1 Assign to x
#         2.2 Define def inner_function() — stored for later
#         2.3 Run inner_function()
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to x
#   6. Define def inner_function() — stored for later
#   7. Run inner_function()
#         7.1 Run print(x)
#   8. Assign to x
#   9. Define def inner_function() — stored for later
#   10. Run inner_function()
#         10.1 Run print(x)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions outer_function, solve, solve, inner_function, inner_function are just stored
#   Execution starts at the non-def lines below:
#      outer_function() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1149:
#
# ===== SYNTAX =====
#   def test_scope()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   test_scope(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Define def test_scope() — stored for later
#   3. Run test_scope()
#         3.1 Assign to x
#         3.2 Assign to result
#         3.3 Return from function
#   4. Run print(?)
#   5. Define def test_scope() — stored for later
#   6. Define def test_scope() — stored for later
#   7. Assign to x
#   8. Run print(?)
#   9. Assign to x
#   10. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions test_scope, test_scope, test_scope are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      test_scope() → jumps into function, runs body, returns result → printed
#      print(?)
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1150:
#
# ===== SYNTAX =====
#   def modify_global_variable()
#   (no return — the function prints directly instead of returning)
#   modify_global_variable() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   modify_global_variable(): — no arguments needed
#   modify_global_variable(): — no arguments needed
#   modify_global_variable(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def modify_global_variable() — stored for later
#   2. Assign to x
#   3. Run modify_global_variable()
#         3.1 Global
#         3.2 Assign to x
#   4. Run print(x)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Global
#   8. Assign to x
#   9. Global
#   10. Assign to x
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions modify_global_variable, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      modify_global_variable() → jumps into function, runs body, returns result → printed
#      print(x)
#      top-level statement runs
#      x = ... (stored in memory)
#      top-level statement runs
#      x = ... (stored in memory)

"""

"""
Problem 1151:
#
# ===== SYNTAX =====
#   def outer_function()
#   inner_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   outer_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def outer_function() — stored for later
#   2. Run outer_function()
#         2.1 Assign to x
#         2.2 Define def inner_function() — stored for later
#         2.3 Run inner_function()
#         2.4 Assign to result
#         2.5 Return from function
#   3. Define def outer_function() — stored for later
#   4. Define def outer_function() — stored for later
#   5. Assign to x
#   6. Define def inner_function() — stored for later
#   7. Run inner_function()
#         7.1 Nonlocal
#         7.2 Assign to x
#   8. Run print(x)
#   9. Assign to x
#   10. Define def inner_function() — stored for later
#   11. Run inner_function()
#         11.1 Nonlocal
#         11.2 Assign to x
#   12. Run print(x)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions outer_function, outer_function, outer_function, inner_function, inner_function are just stored
#   Execution starts at the non-def lines below:
#      outer_function() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      print(x)
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      print(x)

"""

"""
Problem 1152:
#
# ===== SYNTAX =====
#   def shadowing_example()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   shadowing_example(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Define def shadowing_example() — stored for later
#   3. Run shadowing_example()
#         3.1 Assign to x
#         3.2 Assign to result
#         3.3 Return from function
#   4. Run print(?)
#   5. Define def shadowing_example() — stored for later
#   6. Define def shadowing_example() — stored for later
#   7. Assign to x
#   8. Run print(?)
#   9. Assign to x
#   10. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions shadowing_example, shadowing_example, shadowing_example are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      shadowing_example() → jumps into function, runs body, returns result → printed
#      print(?)
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1153:
#
# ===== SYNTAX =====
#   def example_function(a, b)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   example_function(5, 7): gets the literal 5, gets the literal 7, then runs the function body
#   example_function(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(example_function(20, 30)): the inner example_function() executes FIRST, then print() outputs the result
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   a + b: first get a, then get b, then compute a + b
#   example_function(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def example_function(a, b) — stored for later
#   2. Run example_function(5, 7)
#       with: a=5, b=7
#         2.1 Assign to c
#         2.2 Assign to result
#         2.3 Return from function
#   3. Define def example_function(a, b) — stored for later
#   4. Define def example_function(a, b) — stored for later
#   5. Assign to a
#   6. Assign to b
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(example_function(...))
#       calls example_function with: a=20, b=30
#         9.1 Assign to c
#         9.2 Assign to result
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions example_function, example_function, example_function are just stored
#   Execution starts at the non-def lines below:
#      example_function(5, 7) → jumps into function, runs body, returns result → printed
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(example_function(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1154:
#
# ===== SYNTAX =====
#   def greet(name)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   greet(): — no arguments needed
#   greet('Alice'): gets the literal 'Alice', then runs the function body
#   greet(name): looks up variable 'name', then runs the function body
#   print(greet('Bob')): the inner greet() executes FIRST, then print() outputs the result
#   greet('Bob'): gets the literal 'Bob', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def greet(name) — stored for later
#   2. Run greet()
#       with: 
#         2.1 Assign to result
#         2.2 Return from function
#   3. Run greet('Alice')
#       with: name='Alice'
#         3.1 Assign to result
#         3.2 Return from function
#   4. Define def greet(name) — stored for later
#   5. Define def greet(name) — stored for later
#   6. Assign to name
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(greet(...))
#       calls greet with: name='Bob'
#         9.1 Assign to result
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet, greet, greet are just stored
#   Execution starts at the non-def lines below:
#      greet() → jumps into function, runs body, returns result → printed
#      greet('Alice') → jumps into function, runs body, returns result → printed
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet('Bob')) → function runs first, then print shows the result

"""

"""
Problem 1155:
#
# ===== SYNTAX =====
#   def modify_list(lst)
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   modify_list(my_list): looks up variable 'my_list', then runs the function body
#   modify_integer(my_int): looks up variable 'my_int', then runs the function body
#   modify_list(lst): looks up variable 'lst', then runs the function body
#   print(modify_list(?)): the inner modify_list() executes FIRST, then print() outputs the result
#   modify_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def modify_list(lst) — stored for later
#   2. Assign to my_list
#   3. Run modify_list(my_list)
#       with: lst=my_list
#         3.1 Run lst.append(4)
#         3.2 Assign to result
#         3.3 Return from function
#   4. Run print(?)
#   5. Define def modify_integer(n) — stored for later
#   6. Assign to my_int
#   7. Run modify_integer(my_int)
#       with: n=my_int
#         7.1 Update n += ...
#         7.2 Run print(?)
#   8. Run print(?)
#   9. Define def modify_list(lst) — stored for later
#   10. Define def modify_list(lst) — stored for later
#   11. Assign to lst
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(modify_list(...))
#       calls modify_list with: lst=[1, 3, 5, 7]
#         14.1 Run lst.append(4)
#         14.2 Assign to result
#         14.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions modify_list, modify_integer, modify_list, modify_list are just stored
#   Execution starts at the non-def lines below:
#      my_list = ... (stored in memory)
#      modify_list(my_list) → jumps into function, runs body, returns result → printed
#      print(?)
#      my_int = ... (stored in memory)
#      modify_integer(my_int) → jumps into function, runs body, returns result → printed
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(modify_list(?)) → function runs first, then print shows the result

"""

"""
Problem 1156:
#
# ===== SYNTAX =====
#   def loop_scope()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   loop_scope(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def loop_scope() — stored for later
#   2. Run loop_scope()
#         2.1 For loop over range(...)
#         2.2 Return from function
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def loop_scope() — stored for later
#   4. Define def loop_scope() — stored for later
#   5. For loop over range(...)
#       5.1 Assign to x
#       5.2 Run print(?)
#   6. Run print(?)
#   7. For loop over range(...)
#       7.1 Assign to x
#       7.2 Run print(?)
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions loop_scope, loop_scope, loop_scope are just stored
#   Execution starts at the non-def lines below:
#      loop_scope() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      print(?)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1157:
#
# ===== SYNTAX =====
#   def check_variable()
#   (no return — the function prints directly instead of returning)
#   check_variable() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   try/except
#
# ===== EVALUATION ORDER =====
#   check_variable(): — no arguments needed
#   check_variable(): — no arguments needed
#   check_variable(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_variable() — stored for later
#   2. Run check_variable()
#         2.1 Try block (error handling)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Try block (error handling)
#   6. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_variable, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      check_variable() → jumps into function, runs body, returns result → printed
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1158:
#
# ===== SYNTAX =====
#   def outer_function()
#   inner_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   lambda
#
# ===== EVALUATION ORDER =====
#   outer_function(): — no arguments needed
#   print(outer_function()): the inner outer_function() executes FIRST, then print() outputs the result
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   outer_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def outer_function() — stored for later
#   2. Run outer_function()
#         2.1 Assign to x
#         2.2 Define def inner_function() — stored for later
#         2.3 Return from function
#         2.4 Run inner_function()
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def outer_function() — stored for later
#   4. Run print(outer_function(...))
#         4.1 Assign to x
#         4.2 Define def inner_function() — stored for later
#         4.3 Return from function
#         4.4 Run inner_function()
#         4.5 Assign to result
#         4.6 Return from function
#   5. Define def outer_function() — stored for later
#   6. Define def outer_function() — stored for later
#   7. Assign to x
#   8. Define def inner_function() — stored for later
#   9. Run inner_function()
#         9.1 Assign to y
#         9.2 Run print(?)
#   10. Run print(?)
#   11. Assign to x
#   12. Define def inner_function() — stored for later
#   13. Run inner_function()
#         13.1 Assign to y
#         13.2 Run print(?)
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions outer_function, outer_function, outer_function, outer_function, inner_function, inner_function are just stored
#   Execution starts at the non-def lines below:
#      outer_function() → jumps into function, runs body, returns result → printed
#      print(outer_function()) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      print(?)
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      print(?)

"""

"""
Problem 1159:
#
# ===== SYNTAX =====
#   def add_numbers(a, b)
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   operators: +
#
# ===== EVALUATION ORDER =====
#   add_numbers(3, 5): gets the literal 3, gets the literal 5, then runs the function body
#   add_numbers(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(add_numbers(20, 30)): the inner add_numbers() executes FIRST, then print() outputs the result
#   a + b: first get a, then get b, then compute a + b
#   add_numbers(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def add_numbers(a, b) — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def add_numbers(a, b) — stored for later
#   5. Define def add_numbers(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(add_numbers(...))
#       calls add_numbers with: a=20, b=30
#         10.1 Import operator
#         10.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_numbers, add_numbers, add_numbers are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(add_numbers(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1160:
#
# ===== SYNTAX =====
#   def append_to_list(n, default_list)
#   return — exits the function and sends back a value
#   if/elif/else
#   operators: is
#
# ===== EVALUATION ORDER =====
#   print(append_to_list(1)): the inner append_to_list() executes FIRST, then print() outputs the result
#   print(append_to_list(2)): the inner append_to_list() executes FIRST, then print() outputs the result
#   print(append_to_list(3)): the inner append_to_list() executes FIRST, then print() outputs the result
#   print(append_to_list()): the inner append_to_list() executes FIRST, then print() outputs the result
#   print(append_to_list()): the inner append_to_list() executes FIRST, then print() outputs the result
#   append_to_list(n, default_list): looks up variable 'n', looks up variable 'default_list', then runs the function body
#   print(append_to_list(15, ?)): the inner append_to_list() executes FIRST, then print() outputs the result
#   append_to_list(1): gets the literal 1, then runs the function body
#   append_to_list(2): gets the literal 2, then runs the function body
#   append_to_list(3): gets the literal 3, then runs the function body
#   append_to_list(): — no arguments needed
#   append_to_list(): — no arguments needed
#   append_to_list(15, ?): gets the literal 15, resolves the value, then runs the function body
#   lst  None: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def append_to_list(n, default_list) — stored for later
#   2. Run print(append_to_list(...))
#       calls append_to_list with: n=1
#         2.1 Return from function
#   3. Run print(append_to_list(...))
#       calls append_to_list with: n=2
#         3.1 Return from function
#   4. Run print(append_to_list(...))
#       calls append_to_list with: n=3
#         4.1 Return from function
#   5. Define def append_to_list(lst) — stored for later
#   6. Run print(append_to_list(...))
#       calls append_to_list with: 
#         6.1 Return from function
#   7. Run print(append_to_list(...))
#       calls append_to_list with: 
#         7.1 Return from function
#   8. Define def append_to_list(n, default_list) — stored for later
#   9. Define def append_to_list(n, default_list) — stored for later
#   10. Assign to n
#   11. Assign to default_list
#   12. Assign to result
#   13. Run print(result)
#   14. Run print(append_to_list(...))
#       calls append_to_list with: n=15, default_list=[4, 1, 8, 6, 3]
#         14.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions append_to_list, append_to_list, append_to_list, append_to_list are just stored
#   Execution starts at the non-def lines below:
#      print(append_to_list(1)) → function runs first, then print shows the result
#      print(append_to_list(2)) → function runs first, then print shows the result
#      print(append_to_list(3)) → function runs first, then print shows the result
#      print(append_to_list()) → function runs first, then print shows the result
#      print(append_to_list()) → function runs first, then print shows the result
#      n = ... (stored in memory)
#      default_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(append_to_list(15, ?)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1161:
#
# ===== SYNTAX =====
#   def square_numbers(lst)
#   return — exits the function and sends back a value
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   square_numbers(numbers): looks up variable 'numbers', then runs the function body
#   square_numbers(lst): looks up variable 'lst', then runs the function body
#   print(square_numbers(?)): the inner square_numbers() executes FIRST, then print() outputs the result
#   square_numbers(?): resolves the value, then runs the function body
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def square_numbers(lst) — stored for later
#   2. Assign to numbers
#   3. Assign to result
#   4. Run print(?)
#   5. Define def square_numbers(lst) — stored for later
#   6. Define def square_numbers(lst) — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(square_numbers(...))
#       calls square_numbers with: lst=[1, 3, 5, 7]
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions square_numbers, square_numbers, square_numbers are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(square_numbers(?)) → function runs first, then print shows the result

"""

"""
Problem 1162:
#
# ===== SYNTAX =====
#   def reuse_variable()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   reuse_variable(): — no arguments needed
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#
# ===== EXECUTION ORDER =====
#   1. Define def reuse_variable() — stored for later
#   2. Run reuse_variable()
#         2.1 Assign to x
#         2.2 Assign to x
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def reuse_variable() — stored for later
#   4. Define def reuse_variable() — stored for later
#   5. Assign to x
#   6. Assign to x
#   7. Run print(?)
#   8. Assign to x
#   9. Assign to x
#   10. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reuse_variable, reuse_variable, reuse_variable are just stored
#   Execution starts at the non-def lines below:
#      reuse_variable() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1163:
#
# ===== SYNTAX =====
#   def function_one()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   function_one(): — no arguments needed
#   function_two(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Define def function_one() — stored for later
#   3. Define def function_two() — stored for later
#   4. Run function_one()
#         4.1 Assign to result
#         4.2 Return from function
#   5. Run function_two()
#         5.1 Assign to x
#         5.2 Run print(?)
#   6. Define def function_one() — stored for later
#   7. Define def function_one() — stored for later
#   8. Run print(?)
#   9. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions function_one, function_two, function_one, function_one are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      function_one() → jumps into function, runs body, returns result → printed
#      function_two() → jumps into function, runs body, returns result → printed
#      print(?)
#      print(?)

"""

"""
Problem 1164:
#
# ===== SYNTAX =====
#   def function_one()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   function_one() — built-in function call
#   function_two() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   function_one(): — no arguments needed
#   function_two(): — no arguments needed
#   function_one(): — no arguments needed
#   function_two(): — no arguments needed
#   function_one(): — no arguments needed
#   function_two(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Define def function_one() — stored for later
#   3. Define def function_two() — stored for later
#   4. Run function_one()
#         4.1 Global
#         4.2 Update x += ...
#   5. Run function_two()
#         5.1 Global
#         5.2 Update x -= ...
#   6. Run print(?)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Global
#   10. Update x += ...
#   11. Global
#   12. Update x += ...
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions function_one, function_two, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      function_one() → jumps into function, runs body, returns result → printed
#      function_two() → jumps into function, runs body, returns result → printed
#      print(?)
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1165:
#
# ===== SYNTAX =====
#   def outer_function()
#   inner_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   outer_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#   inner_function(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def outer_function() — stored for later
#   2. Run outer_function()
#         2.1 Assign to x
#         2.2 Define def inner_function() — stored for later
#         2.3 Return from function
#         2.4 Run inner_function()
#         2.5 Assign to result
#         2.6 Return from function
#   3. Define def outer_function() — stored for later
#   4. Define def outer_function() — stored for later
#   5. Assign to x
#   6. Define def inner_function() — stored for later
#   7. Run inner_function()
#         7.1 Nonlocal
#         7.2 Update x += ...
#         7.3 Run print(?)
#   8. Run print(?)
#   9. Assign to x
#   10. Define def inner_function() — stored for later
#   11. Run inner_function()
#         11.1 Nonlocal
#         11.2 Update x += ...
#         11.3 Run print(?)
#   12. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions outer_function, outer_function, outer_function, inner_function, inner_function are just stored
#   Execution starts at the non-def lines below:
#      outer_function() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      print(?)
#      x = ... (stored in memory)
#      inner_function() → jumps into function, runs body, returns result → printed
#      print(?)

"""

"""
Problem 1166:
#
# ===== SYNTAX =====
#   def print_arguments()
#   (no return — the function prints directly instead of returning)
#   print_arguments() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_arguments(1, 2, 3, 'hello', ?): gets the literal 1, gets the literal 2, gets the literal 3, gets the literal 'hello', resolves the value, then runs the function body
#   print_arguments(?): resolves the value, then runs the function body
#   print(print_arguments(25)): the inner print_arguments() executes FIRST, then print() outputs the result
#   print_arguments(1, 2, 3, 'hello', ?): gets the literal 1, gets the literal 2, gets the literal 3, gets the literal 'hello', resolves the value, then runs the function body
#   print_arguments(1, 2, 3, 'hello', ?): gets the literal 1, gets the literal 2, gets the literal 3, gets the literal 'hello', resolves the value, then runs the function body
#   print_arguments(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_arguments() — stored for later
#   2. Run print_arguments(1, 2, 3, 'hello', ?)
#         2.1 For loop over args
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to ?
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(print_arguments(...))
#         8.1 For loop over args
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_arguments, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print_arguments(1, 2, 3, 'hello', ?) → jumps into function, runs body, returns result → printed
#      ? = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_arguments(25)) → function runs first, then print shows the result
#
#   Loop: grabs one item from args each round

"""

"""
Problem 1167:
#
# ===== SYNTAX =====
#   def loop_variable_scope()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   loop_variable_scope(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def loop_variable_scope() — stored for later
#   2. Run loop_variable_scope()
#         2.1 For loop over range(...)
#         2.2 Return from function
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def loop_variable_scope() — stored for later
#   4. Define def loop_variable_scope() — stored for later
#   5. For loop over range(...)
#       5.1 Assign to y
#       5.2 Run print(?)
#   6. Run print(?)
#   7. For loop over range(...)
#       7.1 Assign to y
#       7.2 Run print(?)
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions loop_variable_scope, loop_variable_scope, loop_variable_scope are just stored
#   Execution starts at the non-def lines below:
#      loop_variable_scope() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      print(?)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1168:
#
# ===== SYNTAX =====
#   def global_inside_function()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   global_inside_function(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def global_inside_function() — stored for later
#   2. Assign to x
#   3. Run global_inside_function()
#         3.1 Assign to result
#         3.2 Return from function
#   4. Define def global_inside_function() — stored for later
#   5. Define def global_inside_function() — stored for later
#   6. Run print(?)
#   7. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions global_inside_function, global_inside_function, global_inside_function are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      global_inside_function() → jumps into function, runs body, returns result → printed
#      print(?)
#      print(?)

"""

"""
Problem 1169:
#
# ===== SYNTAX =====
#   def test_mutability()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   test_mutability(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def test_mutability() — stored for later
#   2. Run test_mutability()
#         2.1 Assign to mutable
#         2.2 Assign to immutable
#         2.3 Assign to ?
#         2.4 Assign to immutable
#         2.5 Return from function
#         2.6 Assign to result
#         2.7 Return from function
#   3. Define def test_mutability() — stored for later
#   4. Define def test_mutability() — stored for later
#   5. Assign to mutable
#   6. Assign to immutable
#   7. Assign to ?
#   8. Assign to immutable
#   9. Run print(?)
#   10. Run print(?)
#   11. Assign to mutable
#   12. Assign to immutable
#   13. Assign to ?
#   14. Assign to immutable
#   15. Run print(?)
#   16. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions test_mutability, test_mutability, test_mutability are just stored
#   Execution starts at the non-def lines below:
#      test_mutability() → jumps into function, runs body, returns result → printed
#      mutable = ... (stored in memory)
#      immutable = ... (stored in memory)
#      ? = ... (stored in memory)
#      immutable = ... (stored in memory)
#      print(?)
#      print(?)
#      mutable = ... (stored in memory)
#      immutable = ... (stored in memory)
#      ? = ... (stored in memory)
#      immutable = ... (stored in memory)
#      print(?)
#      print(?)

"""

"""
Problem 1170:
#
# ===== SYNTAX =====
#   def list_comprehension_variable_scope()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   list_comprehension_variable_scope(): — no arguments needed
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Define def list_comprehension_variable_scope() — stored for later
#   2. Run list_comprehension_variable_scope()
#         2.1 Assign to lst
#         2.2 Assign to result
#         2.3 Return from function
#   3. Define def list_comprehension_variable_scope() — stored for later
#   4. Define def list_comprehension_variable_scope() — stored for later
#   5. Assign to lst
#   6. Run print(?)
#   7. Assign to lst
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_comprehension_variable_scope, list_comprehension_variable_scope, list_comprehension_variable_scope are just stored
#   Execution starts at the non-def lines below:
#      list_comprehension_variable_scope() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      print(?)

"""

"""
Problem 1171:
#
# ===== SYNTAX =====
#   def print_variable_in_function(x)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print_variable_in_function(z): looks up variable 'z', then runs the function body
#   print_variable_in_function(x): looks up variable 'x', then runs the function body
#   print(print_variable_in_function(3)): the inner print_variable_in_function() executes FIRST, then print() outputs the result
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   x * 2: first get x, then get 2, then compute x * 2
#   print_variable_in_function(3): gets the literal 3, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def print_variable_in_function(x) — stored for later
#   2. Assign to z
#   3. Run print_variable_in_function(z)
#       with: x=z
#         3.1 Assign to y
#         3.2 Assign to result
#         3.3 Return from function
#   4. Define def print_variable_in_function(x) — stored for later
#   5. Define def print_variable_in_function(x) — stored for later
#   6. Assign to x
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_variable_in_function(...))
#       calls print_variable_in_function with: x=3
#         9.1 Assign to y
#         9.2 Assign to result
#         9.3 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_variable_in_function, print_variable_in_function, print_variable_in_function are just stored
#   Execution starts at the non-def lines below:
#      z = ... (stored in memory)
#      print_variable_in_function(z) → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_variable_in_function(3)) → function runs first, then print shows the result

"""

"""
Problem 1172:
#
# ===== SYNTAX =====
#   def for_loop_variable_scope()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   for_loop_variable_scope(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def for_loop_variable_scope() — stored for later
#   2. Run for_loop_variable_scope()
#         2.1 For loop over range(...)
#         2.2 Return from function
#         2.3 Assign to result
#         2.4 Return from function
#   3. Define def for_loop_variable_scope() — stored for later
#   4. Define def for_loop_variable_scope() — stored for later
#   5. For loop over range(...)
#       5.1 Assign to x
#       5.2 Run print(?)
#   6. Run print(?)
#   7. For loop over range(...)
#       7.1 Assign to x
#       7.2 Run print(?)
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions for_loop_variable_scope, for_loop_variable_scope, for_loop_variable_scope are just stored
#   Execution starts at the non-def lines below:
#      for_loop_variable_scope() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      print(?)
#      for loop runs its body once per item
#      print(?)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1173:
#
# ===== SYNTAX =====
#   def change_global_variable()
#   (no return — the function prints directly instead of returning)
#   change_global_variable() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   change_global_variable(): — no arguments needed
#   change_global_variable(): — no arguments needed
#   change_global_variable(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Define def change_global_variable() — stored for later
#   3. Run change_global_variable()
#         3.1 Global
#         3.2 Assign to x
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Global
#   8. Assign to x
#   9. Global
#   10. Assign to x
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions change_global_variable, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      change_global_variable() → jumps into function, runs body, returns result → printed
#      print(?)
#      top-level statement runs
#      x = ... (stored in memory)
#      top-level statement runs
#      x = ... (stored in memory)

"""

"""
Problem 1174:
#
# ===== SYNTAX =====
#   def outer_function()
#   middle_function() — built-in function call
#   inner_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   outer_function() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   outer_function(): — no arguments needed
#   middle_function(): — no arguments needed
#   middle_function(): — no arguments needed
#   middle_function(): — no arguments needed
#   outer_function(): — no arguments needed
#   outer_function(): — no arguments needed
#   middle_function(): — no arguments needed
#   middle_function(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def outer_function() — stored for later
#   2. Run outer_function()
#         2.1 Assign to x
#         2.2 Define def middle_function() — stored for later
#         2.3 Run middle_function()
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to x
#   6. Define def middle_function() — stored for later
#   7. Run middle_function()
#         7.1 Assign to y
#         7.2 Define def inner_function() — stored for later
#         7.3 Run inner_function()
#   8. Assign to x
#   9. Define def middle_function() — stored for later
#   10. Run middle_function()
#         10.1 Assign to y
#         10.2 Define def inner_function() — stored for later
#         10.3 Run inner_function()
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions outer_function, solve, solve, middle_function, middle_function are just stored
#   Execution starts at the non-def lines below:
#      outer_function() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      middle_function() → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      middle_function() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1175:
#
# ===== SYNTAX =====
#   def print_global_variable(x)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print_global_variable(x): looks up variable 'x', then runs the function body
#   print_global_variable(x): looks up variable 'x', then runs the function body
#   print(print_global_variable(3)): the inner print_global_variable() executes FIRST, then print() outputs the result
#   print_global_variable(3): gets the literal 3, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Define def print_global_variable(x) — stored for later
#   3. Run print_global_variable(x)
#       with: x=x
#         3.1 Assign to result
#         3.2 Return from function
#   4. Define def print_global_variable(x) — stored for later
#   5. Define def print_global_variable(x) — stored for later
#   6. Assign to x
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(print_global_variable(...))
#       calls print_global_variable with: x=3
#         9.1 Assign to result
#         9.2 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_global_variable, print_global_variable, print_global_variable are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print_global_variable(x) → jumps into function, runs body, returns result → printed
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(print_global_variable(3)) → function runs first, then print shows the result

"""

"""
Problem 1176:
#
# ===== SYNTAX =====
#   def factorial_recursive(n)
#   return — exits the function and sends back a value
#   factorial_recursive() — built-in function call
#   if/elif/else
#   operators: - * ==
#
# ===== EVALUATION ORDER =====
#   factorial_recursive(5): gets the literal 5, then runs the function body
#   factorial_recursive(n): looks up variable 'n', then runs the function body
#   print(factorial_recursive(15)): the inner factorial_recursive() executes FIRST, then print() outputs the result
#   factorial_recursive(15): gets the literal 15, then runs the function body
#   n * factorial_recursive(...): first get n, then get factorial_recursive(...), then compute n * factorial_recursive(...)
#   factorial_recursive(n - 1): computes the expression first, then runs the function body
#   n - 1: first get n, then get 1, then compute n - 1
#   n == 1: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def factorial_recursive(n) — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def factorial_recursive(n) — stored for later
#   5. Define def factorial_recursive(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(factorial_recursive(...))
#       calls factorial_recursive with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions factorial_recursive, factorial_recursive, factorial_recursive are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(factorial_recursive(15)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1177:
#
# ===== SYNTAX =====
#   def calculate_square_root(number)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   calculate_square_root(16): gets the literal 16, then runs the function body
#   calculate_square_root(number): looks up variable 'number', then runs the function body
#   print(calculate_square_root(25)): the inner calculate_square_root() executes FIRST, then print() outputs the result
#   calculate_square_root(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_square_root(number) — stored for later
#   3. Assign to result
#   4. Run print(?)
#   5. Define def calculate_square_root(number) — stored for later
#   6. Define def calculate_square_root(number) — stored for later
#   7. Assign to number
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_square_root(...))
#       calls calculate_square_root with: number=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_square_root, calculate_square_root, calculate_square_root are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_square_root(25)) → function runs first, then print shows the result

"""

"""
Problem 1178:
#
# ===== SYNTAX =====
#   def generate_random_number()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   generate_random_number(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Define def generate_random_number() — stored for later
#   3. Assign to random_number
#   4. Run print(?)
#   5. Define def generate_random_number() — stored for later
#   6. Define def generate_random_number() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions generate_random_number, generate_random_number, generate_random_number are just stored
#   Execution starts at the non-def lines below:
#      random_number = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1179:
#
# ===== SYNTAX =====
#   def current_datetime()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   current_datetime(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import datetime
#   2. Define def current_datetime() — stored for later
#   3. Assign to now
#   4. Run print(?)
#   5. Define def current_datetime() — stored for later
#   6. Define def current_datetime() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions current_datetime, current_datetime, current_datetime are just stored
#   Execution starts at the non-def lines below:
#      now = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1180:
#
# ===== SYNTAX =====
#   def current_working_directory()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   current_working_directory(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import os
#   2. Define def current_working_directory() — stored for later
#   3. Assign to cwd
#   4. Run print(?)
#   5. Define def current_working_directory() — stored for later
#   6. Define def current_working_directory() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions current_working_directory, current_working_directory, current_working_directory are just stored
#   Execution starts at the non-def lines below:
#      cwd = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1181:
#
# ===== SYNTAX =====
#   def python_version()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   python_version(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import sys
#   2. Define def python_version() — stored for later
#   3. Assign to version
#   4. Run print(?)
#   5. Define def python_version() — stored for later
#   6. Define def python_version() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions python_version, python_version, python_version are just stored
#   Execution starts at the non-def lines below:
#      version = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1182:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1183:
#
# ===== SYNTAX =====
#   def contains_number(string)
#   return — exits the function and sends back a value
#   bool() — built-in function call
#
# ===== EVALUATION ORDER =====
#   contains_number('Hello123'): gets the literal 'Hello123', then runs the function body
#   contains_number(string): looks up variable 'string', then runs the function body
#   print(contains_number('world')): the inner contains_number() executes FIRST, then print() outputs the result
#   contains_number('world'): gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Define def contains_number(string) — stored for later
#   3. Assign to result
#   4. Run print(?)
#   5. Define def contains_number(string) — stored for later
#   6. Define def contains_number(string) — stored for later
#   7. Assign to string
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(contains_number(...))
#       calls contains_number with: string='world'
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions contains_number, contains_number, contains_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      string = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(contains_number('world')) → function runs first, then print shows the result

"""

"""
Problem 1184:
#
# ===== SYNTAX =====
#   def list_files(directory)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   list_files('.'): gets the literal '.', then runs the function body
#   list_files(directory): looks up variable 'directory', then runs the function body
#   print(list_files(25)): the inner list_files() executes FIRST, then print() outputs the result
#   list_files(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import os
#   2. Define def list_files(directory) — stored for later
#   3. Assign to files
#   4. Run print(?)
#   5. Define def list_files(directory) — stored for later
#   6. Define def list_files(directory) — stored for later
#   7. Assign to directory
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(list_files(...))
#       calls list_files with: directory=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions list_files, list_files, list_files are just stored
#   Execution starts at the non-def lines below:
#      files = ... (stored in memory)
#      print(?)
#      directory = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(list_files(25)) → function runs first, then print shows the result

"""

"""
Problem 1185:
#
# ===== SYNTAX =====
#   def calculate_factorial(number)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   calculate_factorial(5): gets the literal 5, then runs the function body
#   calculate_factorial(number): looks up variable 'number', then runs the function body
#   print(calculate_factorial(25)): the inner calculate_factorial() executes FIRST, then print() outputs the result
#   calculate_factorial(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_factorial(number) — stored for later
#   3. Assign to factorial_result
#   4. Run print(?)
#   5. Define def calculate_factorial(number) — stored for later
#   6. Define def calculate_factorial(number) — stored for later
#   7. Assign to number
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(calculate_factorial(...))
#       calls calculate_factorial with: number=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_factorial, calculate_factorial, calculate_factorial are just stored
#   Execution starts at the non-def lines below:
#      factorial_result = ... (stored in memory)
#      print(?)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_factorial(25)) → function runs first, then print shows the result

"""

"""
Problem 1186:
#
# ===== SYNTAX =====
#   def measure_time()
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   range() — built-in function call
#   operators: + -
#
# ===== EVALUATION ORDER =====
#   measure_time(): — no arguments needed
#   end_time - start_time: first get end_time, then get start_time, then compute end_time - start_time
#   end_time - start_time: first get end_time, then get start_time, then compute end_time - start_time
#   end_time - start_time: first get end_time, then get start_time, then compute end_time - start_time
#   end_time + -start_time: first get end_time, then get -start_time, then compute end_time + -start_time
#
# ===== EXECUTION ORDER =====
#   1. Import time
#   2. Define def measure_time() — stored for later
#   3. Assign to execution_time
#   4. Run print(?)
#   5. Define def measure_time() — stored for later
#   6. Define def measure_time() — stored for later
#   7. Assign to start_time
#   8. Run sum(range(...))
#   9. Assign to end_time
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to start_time
#   13. Run sum(range(...))
#   14. Assign to end_time
#   15. Assign to result
#   16. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions measure_time, measure_time, measure_time are just stored
#   Execution starts at the non-def lines below:
#      execution_time = ... (stored in memory)
#      print(?)
#      start_time = ... (stored in memory)
#      sum(range(...))
#      end_time = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      start_time = ... (stored in memory)
#      sum(range(...))
#      end_time = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1187:
#
# ===== SYNTAX =====
#   def is_leap_year(year)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   is_leap_year(2024): gets the literal 2024, then runs the function body
#   is_leap_year(year): looks up variable 'year', then runs the function body
#   print(is_leap_year(25)): the inner is_leap_year() executes FIRST, then print() outputs the result
#   is_leap_year(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import calendar
#   2. Define def is_leap_year(year) — stored for later
#   3. Assign to result
#   4. Run print(?)
#   5. Define def is_leap_year(year) — stored for later
#   6. Define def is_leap_year(year) — stored for later
#   7. Assign to year
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(is_leap_year(...))
#       calls is_leap_year with: year=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions is_leap_year, is_leap_year, is_leap_year are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_leap_year(25)) → function runs first, then print shows the result

"""

"""
Problem 1188:
#
# ===== SYNTAX =====
#   def get_pi_value()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_pi_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def get_pi_value() — stored for later
#   3. Assign to pi_value
#   4. Run print(?)
#   5. Define def get_pi_value() — stored for later
#   6. Define def get_pi_value() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_pi_value, get_pi_value, get_pi_value are just stored
#   Execution starts at the non-def lines below:
#      pi_value = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1189:
#
# ===== SYNTAX =====
#   def shuffle_list(lst)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   shuffle_list(numbers): looks up variable 'numbers', then runs the function body
#   shuffle_list(lst): looks up variable 'lst', then runs the function body
#   print(shuffle_list(?)): the inner shuffle_list() executes FIRST, then print() outputs the result
#   shuffle_list(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Define def shuffle_list(lst) — stored for later
#   3. Assign to numbers
#   4. Assign to shuffled_numbers
#   5. Run print(?)
#   6. Define def shuffle_list(lst) — stored for later
#   7. Define def shuffle_list(lst) — stored for later
#   8. Assign to lst
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(shuffle_list(...))
#       calls shuffle_list with: lst=[1, 3, 5, 7]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions shuffle_list, shuffle_list, shuffle_list are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      shuffled_numbers = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(shuffle_list(?)) → function runs first, then print shows the result

"""

"""
Problem 1190:
#
# ===== SYNTAX =====
#   def count_occurrences(lst)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   count_occurrences(numbers): looks up variable 'numbers', then runs the function body
#   count_occurrences(lst): looks up variable 'lst', then runs the function body
#   print(count_occurrences(?)): the inner count_occurrences() executes FIRST, then print() outputs the result
#   count_occurrences(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import collections
#   2. Define def count_occurrences(lst) — stored for later
#   3. Assign to numbers
#   4. Assign to occurrences
#   5. Run print(?)
#   6. Define def count_occurrences(lst) — stored for later
#   7. Define def count_occurrences(lst) — stored for later
#   8. Assign to lst
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(count_occurrences(...))
#       calls count_occurrences with: lst=[1, 3, 5, 7]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions count_occurrences, count_occurrences, count_occurrences are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      occurrences = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(count_occurrences(?)) → function runs first, then print shows the result

"""

"""
Problem 1191:
#
# ===== SYNTAX =====
#   def calculate_gcd(a, b)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   calculate_gcd(48, 180): gets the literal 48, gets the literal 180, then runs the function body
#   calculate_gcd(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(calculate_gcd(20, 30)): the inner calculate_gcd() executes FIRST, then print() outputs the result
#   calculate_gcd(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def calculate_gcd(a, b) — stored for later
#   3. Assign to gcd_result
#   4. Run print(?)
#   5. Define def calculate_gcd(a, b) — stored for later
#   6. Define def calculate_gcd(a, b) — stored for later
#   7. Assign to a
#   8. Assign to b
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(calculate_gcd(...))
#       calls calculate_gcd with: a=20, b=30
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions calculate_gcd, calculate_gcd, calculate_gcd are just stored
#   Execution starts at the non-def lines below:
#      gcd_result = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(calculate_gcd(20, 30)) → function runs first, then print shows the result

"""

"""
Problem 1192:
#
# ===== SYNTAX =====
#   def rename_file(old_name, new_name)
#   (no return — the function prints directly instead of returning)
#   rename_file() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   rename_file('old_file.txt', 'new_file.txt'): gets the literal 'old_file.txt', gets the literal 'new_file.txt', then runs the function body
#   rename_file(old_name, new_name): looks up variable 'old_name', looks up variable 'new_name', then runs the function body
#   print(rename_file('world', 'world')): the inner rename_file() executes FIRST, then print() outputs the result
#   rename_file('old_file.txt', 'new_file.txt'): gets the literal 'old_file.txt', gets the literal 'new_file.txt', then runs the function body
#   rename_file('old_file.txt', 'new_file.txt'): gets the literal 'old_file.txt', gets the literal 'new_file.txt', then runs the function body
#   rename_file('world', 'world'): gets the literal 'world', gets the literal 'world', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import os
#   2. Define def rename_file(old_name, new_name) — stored for later
#   3. Run rename_file('old_file.txt', 'new_file.txt')
#       with: old_name='old_file.txt', new_name='new_file.txt'
#         3.1 Run os.rename(old_name, new_name)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to old_name
#   7. Assign to new_name
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(rename_file(...))
#       calls rename_file with: old_name='world', new_name='world'
#         10.1 Run os.rename(old_name, new_name)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions rename_file, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      rename_file('old_file.txt', 'new_file.txt') → jumps into function, runs body, returns result → printed
#      old_name = ... (stored in memory)
#      new_name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(rename_file('world', 'world')) → function runs first, then print shows the result

"""

"""
Problem 1193:
#
# ===== SYNTAX =====
#   def format_current_date()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   format_current_date(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import datetime
#   2. Define def format_current_date() — stored for later
#   3. Assign to formatted_date
#   4. Run print(?)
#   5. Define def format_current_date() — stored for later
#   6. Define def format_current_date() — stored for later
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions format_current_date, format_current_date, format_current_date are just stored
#   Execution starts at the non-def lines below:
#      formatted_date = ... (stored in memory)
#      print(?)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1194:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import json
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1195:
#
# ===== SYNTAX =====
#   def select_random_element(lst)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   select_random_element(elements): looks up variable 'elements', then runs the function body
#   select_random_element(lst): looks up variable 'lst', then runs the function body
#   print(select_random_element(?)): the inner select_random_element() executes FIRST, then print() outputs the result
#   select_random_element(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import random
#   2. Define def select_random_element(lst) — stored for later
#   3. Assign to elements
#   4. Assign to random_element
#   5. Run print(?)
#   6. Define def select_random_element(lst) — stored for later
#   7. Define def select_random_element(lst) — stored for later
#   8. Assign to lst
#   9. Assign to result
#   10. Run print(result)
#   11. Run print(select_random_element(...))
#       calls select_random_element with: lst=[1, 3, 5, 7]
#         11.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions select_random_element, select_random_element, select_random_element are just stored
#   Execution starts at the non-def lines below:
#      elements = ... (stored in memory)
#      random_element = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(select_random_element(?)) → function runs first, then print shows the result

"""

"""
Problem 1196:
#
# ===== SYNTAX =====
#   def round_number(number)
#   return — exits the function and sends back a value
#   round() — built-in function call
#
# ===== EVALUATION ORDER =====
#   round_number(7.5): gets the literal 7.5, then runs the function body
#   round_number(number): looks up variable 'number', then runs the function body
#   print(round_number(25)): the inner round_number() executes FIRST, then print() outputs the result
#   round_number(25): gets the literal 25, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def round_number(number) — stored for later
#   3. Assign to rounded_value
#   4. Run print(?)
#   5. Define def round_number(number) — stored for later
#   6. Define def round_number(number) — stored for later
#   7. Assign to number
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(round_number(...))
#       calls round_number with: number=25
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions round_number, round_number, round_number are just stored
#   Execution starts at the non-def lines below:
#      rounded_value = ... (stored in memory)
#      print(?)
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(round_number(25)) → function runs first, then print shows the result

"""

"""
Problem 1197:
#
# ===== SYNTAX =====
#   def access_value()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   access_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def access_value() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def access_value() — stored for later
#   5. Define def access_value() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions access_value, access_value, access_value are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1198:
#
# ===== SYNTAX =====
#   def add_key_value()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   add_key_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def add_key_value() — stored for later
#   2. Assign to updated_dict
#   3. Run print(?)
#   4. Define def add_key_value() — stored for later
#   5. Define def add_key_value() — stored for later
#   6. Assign to sample_dict
#   7. Assign to ?
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to sample_dict
#   11. Assign to ?
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions add_key_value, add_key_value, add_key_value are just stored
#   Execution starts at the non-def lines below:
#      updated_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      ? = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      ? = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1199:
#
# ===== SYNTAX =====
#   def remove_key()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   remove_key(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_key() — stored for later
#   2. Assign to updated_dict
#   3. Run print(?)
#   4. Define def remove_key() — stored for later
#   5. Define def remove_key() — stored for later
#   6. Assign to sample_dict
#   7. Delete
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to sample_dict
#   11. Delete
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_key, remove_key, remove_key are just stored
#   Execution starts at the non-def lines below:
#      updated_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      top-level statement runs
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      top-level statement runs
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1200:
#
# ===== SYNTAX =====
#   def get_keys()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_keys(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def get_keys() — stored for later
#   2. Assign to keys
#   3. Run print(?)
#   4. Define def get_keys() — stored for later
#   5. Define def get_keys() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_keys, get_keys, get_keys are just stored
#   Execution starts at the non-def lines below:
#      keys = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1201:
#
# ===== SYNTAX =====
#   def get_values()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   get_values(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def get_values() — stored for later
#   2. Assign to values
#   3. Run print(?)
#   4. Define def get_values() — stored for later
#   5. Define def get_values() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_values, get_values, get_values are just stored
#   Execution starts at the non-def lines below:
#      values = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1202:
#
# ===== SYNTAX =====
#   def check_key()
#   return — exits the function and sends back a value
#   operators: in
#
# ===== EVALUATION ORDER =====
#   check_key(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_key() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_key() — stored for later
#   5. Define def check_key() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_key, check_key, check_key are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1203:
#
# ===== SYNTAX =====
#   def get_value(dictionary, key, default)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(get_value(my_dict, 'a')): the inner get_value() executes FIRST, then print() outputs the result
#   print(get_value(my_dict, 'd', 'not found')): the inner get_value() executes FIRST, then print() outputs the result
#   get_value_or_default(): — no arguments needed
#   get_value(dictionary, key, default): looks up variable 'dictionary', looks up variable 'key', looks up variable 'default', then runs the function body
#   print(get_value(?, 'value', 1)): the inner get_value() executes FIRST, then print() outputs the result
#   get_value(my_dict, 'a'): looks up variable 'my_dict', gets the literal 'a', then runs the function body
#   get_value(my_dict, 'd', 'not found'): looks up variable 'my_dict', gets the literal 'd', gets the literal 'not found', then runs the function body
#   get_value(?, 'value', 1): resolves the value, gets the literal 'value', gets the literal 1, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def get_value(dictionary, key, default) — stored for later
#   2. Assign to my_dict
#   3. Run print(get_value(...))
#       calls get_value with: dictionary=my_dict, key='a'
#         3.1 Return from function
#   4. Run print(get_value(...))
#       calls get_value with: dictionary=my_dict, key='d', default='not found'
#         4.1 Return from function
#   5. Define def get_value_or_default() — stored for later
#   6. Assign to result
#   7. Run print(?)
#   8. Define def get_value(dictionary, key, default) — stored for later
#   9. Define def get_value(dictionary, key, default) — stored for later
#   10. Assign to dictionary
#   11. Assign to key
#   12. Assign to default
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(get_value(...))
#       calls get_value with: dictionary={...}, key='value', default=1
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_value, get_value_or_default, get_value, get_value are just stored
#   Execution starts at the non-def lines below:
#      my_dict = ... (stored in memory)
#      print(get_value(my_dict, 'a')) → function runs first, then print shows the result
#      print(get_value(my_dict, 'd', 'not found')) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(?)
#      dictionary = ... (stored in memory)
#      key = ... (stored in memory)
#      default = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(get_value(?, 'value', 1)) → function runs first, then print shows the result

"""

"""
Problem 1204:
#
# ===== SYNTAX =====
#   def clear_dict()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   clear_dict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def clear_dict() — stored for later
#   2. Assign to updated_dict
#   3. Run print(?)
#   4. Define def clear_dict() — stored for later
#   5. Define def clear_dict() — stored for later
#   6. Assign to sample_dict
#   7. Run sample_dict.clear()
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to sample_dict
#   11. Run sample_dict.clear()
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions clear_dict, clear_dict, clear_dict are just stored
#   Execution starts at the non-def lines below:
#      updated_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      sample_dict.clear()
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      sample_dict.clear()
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1205:
#
# ===== SYNTAX =====
#   def copy_dict()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   copy_dict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def copy_dict() — stored for later
#   2. Assign to copied_dict
#   3. Run print(?)
#   4. Define def copy_dict() — stored for later
#   5. Define def copy_dict() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions copy_dict, copy_dict, copy_dict are just stored
#   Execution starts at the non-def lines below:
#      copied_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1206:
#
# ===== SYNTAX =====
#   def merge_dicts()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   merge_dicts(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_dicts() — stored for later
#   2. Assign to merged_dict
#   3. Run print(?)
#   4. Define def merge_dicts() — stored for later
#   5. Define def merge_dicts() — stored for later
#   6. Assign to dict1
#   7. Assign to dict2
#   8. Run dict1.update(dict2)
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to dict1
#   12. Assign to dict2
#   13. Run dict1.update(dict2)
#   14. Assign to result
#   15. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_dicts, merge_dicts, merge_dicts are just stored
#   Execution starts at the non-def lines below:
#      merged_dict = ... (stored in memory)
#      print(?)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      result = ... (stored in memory)
#      print(result)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict1.update(dict2)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1207:
#
# ===== SYNTAX =====
#   def iterate_dict()
#   (no return — the function prints directly instead of returning)
#   iterate_dict() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   iterate_dict(): — no arguments needed
#   iterate_dict(): — no arguments needed
#   iterate_dict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def iterate_dict() — stored for later
#   2. Run iterate_dict()
#         2.1 Assign to sample_dict
#         2.2 For loop over sample_dict.items(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to sample_dict
#   6. For loop over sample_dict.items(...)
#       6.1 Run print(?)
#   7. Assign to sample_dict
#   8. For loop over sample_dict.items(...)
#       8.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions iterate_dict, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      iterate_dict() → jumps into function, runs body, returns result → printed
#      sample_dict = ... (stored in memory)
#      for loop runs its body once per item
#      sample_dict = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from sample_dict.items(...) each round

"""

"""
Problem 1208:
#
# ===== SYNTAX =====
#   def dict_length()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   comprehension
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   dict_length(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def dict_length() — stored for later
#   2. Assign to length
#   3. Run print(?)
#   4. Define def dict_length() — stored for later
#   5. Define def dict_length() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions dict_length, dict_length, dict_length are just stored
#   Execution starts at the non-def lines below:
#      length = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from sample_dict each round

"""

"""
Problem 1209:
#
# ===== SYNTAX =====
#   def reverse_dict()
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   reverse_dict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_dict() — stored for later
#   2. Assign to reversed_dict
#   3. Run print(?)
#   4. Define def reverse_dict() — stored for later
#   5. Define def reverse_dict() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_dict, reverse_dict, reverse_dict are just stored
#   Execution starts at the non-def lines below:
#      reversed_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1210:
#
# ===== SYNTAX =====
#   def create_dict_from_lists()
#   return — exits the function and sends back a value
#   dict() — built-in function call
#   zip() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_dict_from_lists(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def create_dict_from_lists() — stored for later
#   2. Assign to created_dict
#   3. Run print(?)
#   4. Define def create_dict_from_lists() — stored for later
#   5. Define def create_dict_from_lists() — stored for later
#   6. Assign to keys
#   7. Assign to values
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to keys
#   11. Assign to values
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions create_dict_from_lists, create_dict_from_lists, create_dict_from_lists are just stored
#   Execution starts at the non-def lines below:
#      created_dict = ... (stored in memory)
#      print(?)
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1211:
#
# ===== SYNTAX =====
#   def sum_values()
#   return — exits the function and sends back a value
#   sum() — built-in function call
#
# ===== EVALUATION ORDER =====
#   sum_values(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_values() — stored for later
#   2. Assign to total
#   3. Run print(?)
#   4. Define def sum_values() — stored for later
#   5. Define def sum_values() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_values, sum_values, sum_values are just stored
#   Execution starts at the non-def lines below:
#      total = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1212:
#
# ===== SYNTAX =====
#   def filter_dict()
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   filter_dict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def filter_dict() — stored for later
#   2. Assign to filtered_dict
#   3. Run print(?)
#   4. Define def filter_dict() — stored for later
#   5. Define def filter_dict() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions filter_dict, filter_dict, filter_dict are just stored
#   Execution starts at the non-def lines below:
#      filtered_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1213:
#
# ===== SYNTAX =====
#   def max_value()
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   max_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def max_value() — stored for later
#   2. Assign to max_val
#   3. Run print(?)
#   4. Define def max_value() — stored for later
#   5. Define def max_value() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_value, max_value, max_value are just stored
#   Execution starts at the non-def lines below:
#      max_val = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1214:
#
# ===== SYNTAX =====
#   def is_dict_empty()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   is_dict_empty(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def is_dict_empty() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def is_dict_empty() — stored for later
#   5. Define def is_dict_empty() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_dict_empty, is_dict_empty, is_dict_empty are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1215:
#
# ===== SYNTAX =====
#   def remove_value()
#   return — exits the function and sends back a value
#   comprehension
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   remove_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_value() — stored for later
#   2. Assign to updated_dict
#   3. Run print(?)
#   4. Define def remove_value() — stored for later
#   5. Define def remove_value() — stored for later
#   6. Assign to sample_dict
#   7. Assign to sample_dict
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to sample_dict
#   11. Assign to sample_dict
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_value, remove_value, remove_value are just stored
#   Execution starts at the non-def lines below:
#      updated_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1216:
#
# ===== SYNTAX =====
#   def find_key_by_value()
#   return — exits the function and sends back a value
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   find_key_by_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def find_key_by_value() — stored for later
#   2. Assign to key
#   3. Run print(?)
#   4. Define def find_key_by_value() — stored for later
#   5. Define def find_key_by_value() — stored for later
#   6. Assign to sample_dict
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_dict
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions find_key_by_value, find_key_by_value, find_key_by_value are just stored
#   Execution starts at the non-def lines below:
#      key = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1217:
#
# ===== SYNTAX =====
#   def create_defaultdict()
#   return — exits the function and sends back a value
#   defaultdict() — built-in function call
#   dict() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_defaultdict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Define def create_defaultdict() — stored for later
#   3. Assign to default_dict
#   4. Run print(?)
#   5. Define def create_defaultdict() — stored for later
#   6. Define def create_defaultdict() — stored for later
#   7. Assign to sample_dict
#   8. Update ? += ...
#   9. Update ? += ...
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to sample_dict
#   13. Update ? += ...
#   14. Update ? += ...
#   15. Assign to result
#   16. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions create_defaultdict, create_defaultdict, create_defaultdict are just stored
#   Execution starts at the non-def lines below:
#      default_dict = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      top-level statement runs
#      top-level statement runs
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      top-level statement runs
#      top-level statement runs
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1218:
#
# ===== SYNTAX =====
#   def first_key_value()
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   first_key_value(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def first_key_value() — stored for later
#   2. Assign to first_item
#   3. Run print(?)
#   4. Define def first_key_value() — stored for later
#   5. Define def first_key_value() — stored for later
#   6. Assign to sample_dict
#   7. Assign to first_item
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to sample_dict
#   11. Assign to first_item
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions first_key_value, first_key_value, first_key_value are just stored
#   Execution starts at the non-def lines below:
#      first_item = ... (stored in memory)
#      print(?)
#      sample_dict = ... (stored in memory)
#      first_item = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_dict = ... (stored in memory)
#      first_item = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1219:
#
# ===== SYNTAX =====
#   def merge_multiple_dicts()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   merge_multiple_dicts(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_multiple_dicts() — stored for later
#   2. Assign to merged_dict
#   3. Run print(?)
#   4. Define def merge_multiple_dicts() — stored for later
#   5. Define def merge_multiple_dicts() — stored for later
#   6. Assign to dict1
#   7. Assign to dict2
#   8. Assign to dict3
#   9. Assign to merged_dict
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to dict1
#   13. Assign to dict2
#   14. Assign to dict3
#   15. Assign to merged_dict
#   16. Assign to result
#   17. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_multiple_dicts, merge_multiple_dicts, merge_multiple_dicts are just stored
#   Execution starts at the non-def lines below:
#      merged_dict = ... (stored in memory)
#      print(?)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict3 = ... (stored in memory)
#      merged_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      dict1 = ... (stored in memory)
#      dict2 = ... (stored in memory)
#      dict3 = ... (stored in memory)
#      merged_dict = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1220:
#
# ===== SYNTAX =====
#   def list_to_dict()
#   return — exits the function and sends back a value
#   dict() — built-in function call
#
# ===== EVALUATION ORDER =====
#   list_to_dict(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def list_to_dict() — stored for later
#   2. Assign to converted_dict
#   3. Run print(?)
#   4. Define def list_to_dict() — stored for later
#   5. Define def list_to_dict() — stored for later
#   6. Assign to sample_list
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to sample_list
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions list_to_dict, list_to_dict, list_to_dict are just stored
#   Execution starts at the non-def lines below:
#      converted_dict = ... (stored in memory)
#      print(?)
#      sample_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      sample_list = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1221:
#
# ===== SYNTAX =====
#   def create_path()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_path(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def create_path() — stored for later
#   3. Assign to path
#   4. Run print(?)
#   5. Define def create_path() — stored for later
#   6. Define def create_path() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions create_path, create_path, create_path are just stored
#   Execution starts at the non-def lines below:
#      path = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1222:
#
# ===== SYNTAX =====
#   def check_path_exists()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   check_path_exists(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_path_exists() — stored for later
#   3. Assign to exists
#   4. Run print(?)
#   5. Define def check_path_exists() — stored for later
#   6. Define def check_path_exists() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_path_exists, check_path_exists, check_path_exists are just stored
#   Execution starts at the non-def lines below:
#      exists = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1223:
#
# ===== SYNTAX =====
#   def check_if_file()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   check_if_file(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_if_file() — stored for later
#   3. Assign to is_file
#   4. Run print(?)
#   5. Define def check_if_file() — stored for later
#   6. Define def check_if_file() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_if_file, check_if_file, check_if_file are just stored
#   Execution starts at the non-def lines below:
#      is_file = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1224:
#
# ===== SYNTAX =====
#   def check_if_directory()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   check_if_directory(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_if_directory() — stored for later
#   3. Assign to is_directory
#   4. Run print(?)
#   5. Define def check_if_directory() — stored for later
#   6. Define def check_if_directory() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_if_directory, check_if_directory, check_if_directory are just stored
#   Execution starts at the non-def lines below:
#      is_directory = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1225:
#
# ===== SYNTAX =====
#   def get_file_name()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_file_name(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_file_name() — stored for later
#   3. Assign to file_name
#   4. Run print(?)
#   5. Define def get_file_name() — stored for later
#   6. Define def get_file_name() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_file_name, get_file_name, get_file_name are just stored
#   Execution starts at the non-def lines below:
#      file_name = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1226:
#
# ===== SYNTAX =====
#   def get_parent_directory()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_parent_directory(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_parent_directory() — stored for later
#   3. Assign to parent_directory
#   4. Run print(?)
#   5. Define def get_parent_directory() — stored for later
#   6. Define def get_parent_directory() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_parent_directory, get_parent_directory, get_parent_directory are just stored
#   Execution starts at the non-def lines below:
#      parent_directory = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1227:
#
# ===== SYNTAX =====
#   def join_paths()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   float() — built-in function call
#   Fraction() — built-in function call
#   operators: * /
#
# ===== EVALUATION ORDER =====
#   join_paths(): — no arguments needed
#   p1 / p2: first get p1, then get p2, then compute p1 / p2
#   p1 / p2: first get p1, then get p2, then compute p1 / p2
#   p1 / p2: first get p1, then get p2, then compute p1 / p2
#   p1 * ?: first get p1, then get ?, then compute p1 * ?
#   1 / p2: first get 1, then get p2, then compute 1 / p2
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def join_paths() — stored for later
#   3. Assign to joined_path
#   4. Run print(?)
#   5. Define def join_paths() — stored for later
#   6. Define def join_paths() — stored for later
#   7. Assign to p1
#   8. Assign to p2
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to p1
#   12. Assign to p2
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions join_paths, join_paths, join_paths are just stored
#   Execution starts at the non-def lines below:
#      joined_path = ... (stored in memory)
#      print(?)
#      p1 = ... (stored in memory)
#      p2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p1 = ... (stored in memory)
#      p2 = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1228:
#
# ===== SYNTAX =====
#   def get_file_extension()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_file_extension(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_file_extension() — stored for later
#   3. Assign to file_extension
#   4. Run print(?)
#   5. Define def get_file_extension() — stored for later
#   6. Define def get_file_extension() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_file_extension, get_file_extension, get_file_extension are just stored
#   Execution starts at the non-def lines below:
#      file_extension = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1229:
#
# ===== SYNTAX =====
#   def get_file_size()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_file_size(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_file_size() — stored for later
#   3. Assign to file_size
#   4. Run print(?)
#   5. Define def get_file_size() — stored for later
#   6. Define def get_file_size() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_file_size, get_file_size, get_file_size are just stored
#   Execution starts at the non-def lines below:
#      file_size = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1230:
#
# ===== SYNTAX =====
#   def rename_file()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   rename_file(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def rename_file() — stored for later
#   3. Assign to renamed_file
#   4. Run print(?)
#   5. Define def rename_file() — stored for later
#   6. Define def rename_file() — stored for later
#   7. Assign to old_path
#   8. Assign to new_path
#   9. Run old_path.rename(new_path)
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to old_path
#   13. Assign to new_path
#   14. Run old_path.rename(new_path)
#   15. Assign to result
#   16. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions rename_file, rename_file, rename_file are just stored
#   Execution starts at the non-def lines below:
#      renamed_file = ... (stored in memory)
#      print(?)
#      old_path = ... (stored in memory)
#      new_path = ... (stored in memory)
#      old_path.rename(new_path)
#      result = ... (stored in memory)
#      print(result)
#      old_path = ... (stored in memory)
#      new_path = ... (stored in memory)
#      old_path.rename(new_path)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1231:
#
# ===== SYNTAX =====
#   def create_directory()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_directory(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def create_directory() — stored for later
#   3. Assign to created_directory
#   4. Run print(?)
#   5. Define def create_directory() — stored for later
#   6. Define def create_directory() — stored for later
#   7. Assign to p
#   8. Run p.mkdir()
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to p
#   12. Run p.mkdir()
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions create_directory, create_directory, create_directory are just stored
#   Execution starts at the non-def lines below:
#      created_directory = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      p.mkdir()
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      p.mkdir()
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1232:
#
# ===== SYNTAX =====
#   def remove_directory()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: %
#
# ===== EVALUATION ORDER =====
#   remove_directory(): — no arguments needed
#   'Directory %s removed' % p: first get 'Directory %s removed', then get p, then compute 'Directory %s removed' % p
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def remove_directory() — stored for later
#   3. Assign to removed_directory
#   4. Run print(removed_directory)
#   5. Define def remove_directory() — stored for later
#   6. Define def remove_directory() — stored for later
#   7. Assign to p
#   8. Run p.rmdir()
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to p
#   12. Run p.rmdir()
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions remove_directory, remove_directory, remove_directory are just stored
#   Execution starts at the non-def lines below:
#      removed_directory = ... (stored in memory)
#      print(removed_directory)
#      p = ... (stored in memory)
#      p.rmdir()
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      p.rmdir()
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1233:
#
# ===== SYNTAX =====
#   def remove_file()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: %
#
# ===== EVALUATION ORDER =====
#   remove_file(): — no arguments needed
#   'File %s removed' % p: first get 'File %s removed', then get p, then compute 'File %s removed' % p
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def remove_file() — stored for later
#   3. Assign to removed_file
#   4. Run print(removed_file)
#   5. Define def remove_file() — stored for later
#   6. Define def remove_file() — stored for later
#   7. Assign to p
#   8. Run p.unlink()
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to p
#   12. Run p.unlink()
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions remove_file, remove_file, remove_file are just stored
#   Execution starts at the non-def lines below:
#      removed_file = ... (stored in memory)
#      print(removed_file)
#      p = ... (stored in memory)
#      p.unlink()
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      p.unlink()
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1234:
#
# ===== SYNTAX =====
#   def get_absolute_path()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_absolute_path(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_absolute_path() — stored for later
#   3. Assign to absolute_path
#   4. Run print(?)
#   5. Define def get_absolute_path() — stored for later
#   6. Define def get_absolute_path() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_absolute_path, get_absolute_path, get_absolute_path are just stored
#   Execution starts at the non-def lines below:
#      absolute_path = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1235:
#
# ===== SYNTAX =====
#   def check_if_symlink()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   check_if_symlink(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_if_symlink() — stored for later
#   3. Assign to is_symlink
#   4. Run print(?)
#   5. Define def check_if_symlink() — stored for later
#   6. Define def check_if_symlink() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_if_symlink, check_if_symlink, check_if_symlink are just stored
#   Execution starts at the non-def lines below:
#      is_symlink = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1236:
#
# ===== SYNTAX =====
#   def create_symlink()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   create_symlink(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def create_symlink() — stored for later
#   3. Assign to symlink
#   4. Run print(?)
#   5. Define def create_symlink() — stored for later
#   6. Define def create_symlink() — stored for later
#   7. Assign to target
#   8. Assign to symlink
#   9. Run symlink.symlink_to(target)
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to target
#   13. Assign to symlink
#   14. Run symlink.symlink_to(target)
#   15. Assign to result
#   16. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions create_symlink, create_symlink, create_symlink are just stored
#   Execution starts at the non-def lines below:
#      symlink = ... (stored in memory)
#      print(?)
#      target = ... (stored in memory)
#      symlink = ... (stored in memory)
#      symlink.symlink_to(target)
#      result = ... (stored in memory)
#      print(result)
#      target = ... (stored in memory)
#      symlink = ... (stored in memory)
#      symlink.symlink_to(target)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1237:
#
# ===== SYNTAX =====
#   def check_directory_and_files()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   comprehension
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   check_directory_and_files(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_directory_and_files() — stored for later
#   3. Assign to directory_files
#   4. Run print(?)
#   5. Define def check_directory_and_files() — stored for later
#   6. Define def check_directory_and_files() — stored for later
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_directory_and_files, check_directory_and_files, check_directory_and_files are just stored
#   Execution starts at the non-def lines below:
#      directory_files = ... (stored in memory)
#      print(?)
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1238:
#
# ===== SYNTAX =====
#   def get_last_modified_time()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_last_modified_time(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Import time
#   3. Define def get_last_modified_time() — stored for later
#   4. Assign to last_modified_time
#   5. Run print(?)
#   6. Define def get_last_modified_time() — stored for later
#   7. Define def get_last_modified_time() — stored for later
#   8. Assign to p
#   9. Assign to timestamp
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to p
#   13. Assign to timestamp
#   14. Assign to result
#   15. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   def lines are SKIPPED during execution — functions get_last_modified_time, get_last_modified_time, get_last_modified_time are just stored
#   Execution starts at the non-def lines below:
#      last_modified_time = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      timestamp = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      timestamp = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1239:
#
# ===== SYNTAX =====
#   def get_directory_name()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_directory_name(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_directory_name() — stored for later
#   3. Assign to directory_name
#   4. Run print(?)
#   5. Define def get_directory_name() — stored for later
#   6. Define def get_directory_name() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_directory_name, get_directory_name, get_directory_name are just stored
#   Execution starts at the non-def lines below:
#      directory_name = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1240:
#
# ===== SYNTAX =====
#   def check_if_empty()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   len() — built-in function call
#   list() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   check_if_empty(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_if_empty() — stored for later
#   3. Assign to is_empty
#   4. Run print(?)
#   5. Define def check_if_empty() — stored for later
#   6. Define def check_if_empty() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_if_empty, check_if_empty, check_if_empty are just stored
#   Execution starts at the non-def lines below:
#      is_empty = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1241:
#
# ===== SYNTAX =====
#   def iterate_files()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   iterate_files(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def iterate_files() — stored for later
#   3. Assign to files_in_directory
#   4. Run print(?)
#   5. Define def iterate_files() — stored for later
#   6. Define def iterate_files() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions iterate_files, iterate_files, iterate_files are just stored
#   Execution starts at the non-def lines below:
#      files_in_directory = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1242:
#
# ===== SYNTAX =====
#   def get_parent_directory()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#
# ===== EVALUATION ORDER =====
#   get_parent_directory(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_parent_directory() — stored for later
#   3. Assign to parent_directory
#   4. Run print(?)
#   5. Define def get_parent_directory() — stored for later
#   6. Define def get_parent_directory() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_parent_directory, get_parent_directory, get_parent_directory are just stored
#   Execution starts at the non-def lines below:
#      parent_directory = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1243:
#
# ===== SYNTAX =====
#   def check_file_extension()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   check_file_extension(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_file_extension() — stored for later
#   3. Assign to has_extension
#   4. Run print(?)
#   5. Define def check_file_extension() — stored for later
#   6. Define def check_file_extension() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_file_extension, check_file_extension, check_file_extension are just stored
#   Execution starts at the non-def lines below:
#      has_extension = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1244:
#
# ===== SYNTAX =====
#   def get_human_readable_size()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: /
#
# ===== EVALUATION ORDER =====
#   get_human_readable_size(): — no arguments needed
#   size_in_bytes / 1024: first get size_in_bytes, then get 1024, then compute size_in_bytes / 1024
#   size_in_bytes / 1024: first get size_in_bytes, then get 1024, then compute size_in_bytes / 1024
#   size_in_bytes / 1024: first get size_in_bytes, then get 1024, then compute size_in_bytes / 1024
#   size_in_bytes / 1024: first get size_in_bytes, then get 1024, then compute size_in_bytes / 1024
#   size_in_bytes / 1024: first get size_in_bytes, then get 1024, then compute size_in_bytes / 1024
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def get_human_readable_size() — stored for later
#   3. Assign to size
#   4. Run print(?)
#   5. Define def get_human_readable_size() — stored for later
#   6. Define def get_human_readable_size() — stored for later
#   7. Assign to p
#   8. Assign to size_in_bytes
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to p
#   12. Assign to size_in_bytes
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions get_human_readable_size, get_human_readable_size, get_human_readable_size are just stored
#   Execution starts at the non-def lines below:
#      size = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      size_in_bytes = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      size_in_bytes = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1245:
#
# ===== SYNTAX =====
#   def change_working_directory()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: %
#
# ===== EVALUATION ORDER =====
#   change_working_directory(): — no arguments needed
#   'Changed working directory to: %s' % p: first get 'Changed working directory to: %s', then get p, then compute 'Changed working directory to: %s' % p
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Import os
#   3. Define def change_working_directory() — stored for later
#   4. Assign to changed_directory
#   5. Run print(changed_directory)
#   6. Define def change_working_directory() — stored for later
#   7. Define def change_working_directory() — stored for later
#   8. Assign to p
#   9. Run os.chdir(p)
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to p
#   13. Run os.chdir(p)
#   14. Assign to result
#   15. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 2 import(s) first
#   def lines are SKIPPED during execution — functions change_working_directory, change_working_directory, change_working_directory are just stored
#   Execution starts at the non-def lines below:
#      changed_directory = ... (stored in memory)
#      print(changed_directory)
#      p = ... (stored in memory)
#      os.chdir(p)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      os.chdir(p)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1246:
#
# ===== SYNTAX =====
#   def check_conditions()
#   return — exits the function and sends back a value
#   operators: < > and
#
# ===== EVALUATION ORDER =====
#   check_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_conditions() — stored for later
#   5. Define def check_conditions() — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to a
#   11. Assign to b
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_conditions, check_conditions, check_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1247:
#
# ===== SYNTAX =====
#   def check_range()
#   return — exits the function and sends back a value
#   operators: <= >= and
#
# ===== EVALUATION ORDER =====
#   check_range(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_range() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_range() — stored for later
#   5. Define def check_range() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_range, check_range, check_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1249:
#
# ===== SYNTAX =====
#   def main(number)
#   return — exits the function and sends back a value
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   main(number): looks up variable 'number', then runs the function body
#   check_divisibility(): — no arguments needed
#   main(number): looks up variable 'number', then runs the function body
#   main(number): looks up variable 'number', then runs the function body
#   print(main(25)): the inner main() executes FIRST, then print() outputs the result
#   main(25): gets the literal 25, then runs the function body
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   number % 3: first get number, then get 3, then compute number % 3
#   number % 5: first get number, then get 5, then compute number % 5
#   number % 3: first get number, then get 3, then compute number % 3
#   number % 5: first get number, then get 5, then compute number % 5
#   number % 3: first get number, then get 3, then compute number % 3
#   number % 5: first get number, then get 5, then compute number % 5
#   number % 3: first get number, then get 3, then compute number % 3
#   number % 5: first get number, then get 5, then compute number % 5
#
# ===== EXECUTION ORDER =====
#   1. Define def main(number) — stored for later
#   2. Assign to number
#   3. Run main(number)
#       with: number=number
#         3.1 Return from function
#   4. Define def check_divisibility() — stored for later
#   5. Assign to result
#   6. Run print(?)
#   7. Define def main(number) — stored for later
#   8. Assign to number
#   9. Run main(number)
#       with: number=number
#         9.1 Return from function
#   10. Define def main(number) — stored for later
#   11. Define def main(number) — stored for later
#   12. Assign to number
#   13. Assign to result
#   14. Run print(result)
#   15. Run print(main(...))
#       calls main with: number=25
#         15.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, check_divisibility, main, main, main are just stored
#   Execution starts at the non-def lines below:
#      number = ... (stored in memory)
#      main(number) → jumps into function, runs body, returns result → printed
#      result = ... (stored in memory)
#      print(?)
#      number = ... (stored in memory)
#      main(number) → jumps into function, runs body, returns result → printed
#      number = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(main(25)) → function runs first, then print shows the result

"""

"""
Problem 1250:
#
# ===== SYNTAX =====
#   def check_list()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: > and
#
# ===== EVALUATION ORDER =====
#   check_list(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list() — stored for later
#   5. Define def check_list() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list, check_list, check_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1251:
#
# ===== SYNTAX =====
#   def check_leap_year()
#   return — exits the function and sends back a value
#   operators: % == != and or
#
# ===== EVALUATION ORDER =====
#   check_leap_year(): — no arguments needed
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#
# ===== EXECUTION ORDER =====
#   1. Define def check_leap_year() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_leap_year() — stored for later
#   5. Define def check_leap_year() — stored for later
#   6. Assign to year
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to year
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_leap_year, check_leap_year, check_leap_year are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1252:
#
# ===== SYNTAX =====
#   def check_number()
#   return — exits the function and sends back a value
#   operators: % == > and
#
# ===== EVALUATION ORDER =====
#   check_number(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 4: first get num, then get 4, then compute num % 4
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 4: first get num, then get 4, then compute num % 4
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 4: first get num, then get 4, then compute num % 4
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 4: first get num, then get 4, then compute num % 4
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 4: first get num, then get 4, then compute num % 4
#
# ===== EXECUTION ORDER =====
#   1. Define def check_number() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_number() — stored for later
#   5. Define def check_number() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_number, check_number, check_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1253:
#
# ===== SYNTAX =====
#   def check_list_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: == > and
#
# ===== EVALUATION ORDER =====
#   check_list_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list_conditions() — stored for later
#   5. Define def check_list_conditions() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list_conditions, check_list_conditions, check_list_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1254:
#
# ===== SYNTAX =====
#   def check_string_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: > and
#
# ===== EVALUATION ORDER =====
#   check_string_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_string_conditions() — stored for later
#   5. Define def check_string_conditions() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_conditions, check_string_conditions, check_string_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1255:
#
# ===== SYNTAX =====
#   def check_range_conditions()
#   return — exits the function and sends back a value
#   operators: < > and
#
# ===== EVALUATION ORDER =====
#   check_range_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_range_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_range_conditions() — stored for later
#   5. Define def check_range_conditions() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_range_conditions, check_range_conditions, check_range_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1256:
#
# ===== SYNTAX =====
#   def check_multiples()
#   return — exits the function and sends back a value
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   check_multiples(): — no arguments needed
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#
# ===== EXECUTION ORDER =====
#   1. Define def check_multiples() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_multiples() — stored for later
#   5. Define def check_multiples() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_multiples, check_multiples, check_multiples are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1257:
#
# ===== SYNTAX =====
#   def check_voting_eligibility()
#   return — exits the function and sends back a value
#   operators: > and
#
# ===== EVALUATION ORDER =====
#   check_voting_eligibility(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_voting_eligibility() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_voting_eligibility() — stored for later
#   5. Define def check_voting_eligibility() — stored for later
#   6. Assign to age
#   7. Assign to is_citizen
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to age
#   11. Assign to is_citizen
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_voting_eligibility, check_voting_eligibility, check_voting_eligibility are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      age = ... (stored in memory)
#      is_citizen = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      age = ... (stored in memory)
#      is_citizen = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1258:
#
# ===== SYNTAX =====
#   def check_divisibility_all()
#   return — exits the function and sends back a value
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   check_divisibility_all(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#
# ===== EXECUTION ORDER =====
#   1. Define def check_divisibility_all() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_divisibility_all() — stored for later
#   5. Define def check_divisibility_all() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_divisibility_all, check_divisibility_all, check_divisibility_all are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1260:
#
# ===== SYNTAX =====
#   def check_string_length()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: % == > and
#
# ===== EVALUATION ORDER =====
#   check_string_length(): — no arguments needed
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_length() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_string_length() — stored for later
#   5. Define def check_string_length() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_length, check_string_length, check_string_length are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1261:
#
# ===== SYNTAX =====
#   def check_range_inclusive()
#   return — exits the function and sends back a value
#   operators: <= >= and
#
# ===== EVALUATION ORDER =====
#   check_range_inclusive(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_range_inclusive() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_range_inclusive() — stored for later
#   5. Define def check_range_inclusive() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_range_inclusive, check_range_inclusive, check_range_inclusive are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1262:
#
# ===== SYNTAX =====
#   def check_divisible_by_2_and_7()
#   return — exits the function and sends back a value
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   check_divisible_by_2_and_7(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 7: first get num, then get 7, then compute num % 7
#
# ===== EXECUTION ORDER =====
#   1. Define def check_divisible_by_2_and_7() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_divisible_by_2_and_7() — stored for later
#   5. Define def check_divisible_by_2_and_7() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_divisible_by_2_and_7, check_divisible_by_2_and_7, check_divisible_by_2_and_7 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1263:
#
# ===== SYNTAX =====
#   def check_negative_and_divisible()
#   return — exits the function and sends back a value
#   operators: % == < and
#
# ===== EVALUATION ORDER =====
#   check_negative_and_divisible(): — no arguments needed
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 5: first get num, then get 5, then compute num % 5
#
# ===== EXECUTION ORDER =====
#   1. Define def check_negative_and_divisible() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_negative_and_divisible() — stored for later
#   5. Define def check_negative_and_divisible() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_negative_and_divisible, check_negative_and_divisible, check_negative_and_divisible are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1264:
#
# ===== SYNTAX =====
#   def check_digit_string()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: > and
#
# ===== EVALUATION ORDER =====
#   check_digit_string(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_digit_string() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_digit_string() — stored for later
#   5. Define def check_digit_string() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_digit_string, check_digit_string, check_digit_string are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1265:
#
# ===== SYNTAX =====
#   def check_prime_and_greater_than_10()
#   return — exits the function and sends back a value
#   all() — built-in function call
#   range() — built-in function call
#   comprehension
#   operators: % != > and
#
# ===== EVALUATION ORDER =====
#   check_prime_and_greater_than_10(): — no arguments needed
#   num % i: first get num, then get i, then compute num % i
#   num % i: first get num, then get i, then compute num % i
#   num % i: first get num, then get i, then compute num % i
#   num % i: first get num, then get i, then compute num % i
#   num % i: first get num, then get i, then compute num % i
#
# ===== EXECUTION ORDER =====
#   1. Define def check_prime_and_greater_than_10() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_prime_and_greater_than_10() — stored for later
#   5. Define def check_prime_and_greater_than_10() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_prime_and_greater_than_10, check_prime_and_greater_than_10, check_prime_and_greater_than_10 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1266:
#
# ===== SYNTAX =====
#   def check_odd_and_positive()
#   return — exits the function and sends back a value
#   operators: % != > and
#
# ===== EVALUATION ORDER =====
#   check_odd_and_positive(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def check_odd_and_positive() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_odd_and_positive() — stored for later
#   5. Define def check_odd_and_positive() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_odd_and_positive, check_odd_and_positive, check_odd_and_positive are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1267:
#
# ===== SYNTAX =====
#   def check_file_exists_and_size()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: * > and
#
# ===== EVALUATION ORDER =====
#   check_file_exists_and_size(): — no arguments needed
#   100 * 1024: first get 100, then get 1024, then compute 100 * 1024
#   100 * 1024: first get 100, then get 1024, then compute 100 * 1024
#   100 * 1024: first get 100, then get 1024, then compute 100 * 1024
#   100 * 1024: first get 100, then get 1024, then compute 100 * 1024
#   100 * 1024: first get 100, then get 1024, then compute 100 * 1024
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_file_exists_and_size() — stored for later
#   3. Assign to result
#   4. Run print(?)
#   5. Define def check_file_exists_and_size() — stored for later
#   6. Define def check_file_exists_and_size() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_file_exists_and_size, check_file_exists_and_size, check_file_exists_and_size are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1268:
#
# ===== SYNTAX =====
#   def check_list_length_and_last_element()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: == > and
#
# ===== EVALUATION ORDER =====
#   check_list_length_and_last_element(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list_length_and_last_element() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list_length_and_last_element() — stored for later
#   5. Define def check_list_length_and_last_element() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list_length_and_last_element, check_list_length_and_last_element, check_list_length_and_last_element are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1269:
#
# ===== SYNTAX =====
#   def check_range_and_odd()
#   return — exits the function and sends back a value
#   operators: % != <= >= and
#
# ===== EVALUATION ORDER =====
#   check_range_and_odd(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 2: first get num, then get 2, then compute num % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def check_range_and_odd() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_range_and_odd() — stored for later
#   5. Define def check_range_and_odd() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_range_and_odd, check_range_and_odd, check_range_and_odd are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1270:
#
# ===== SYNTAX =====
#   def check_conditions()
#   return — exits the function and sends back a value
#   operators: < > or
#
# ===== EVALUATION ORDER =====
#   check_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_conditions() — stored for later
#   5. Define def check_conditions() — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to a
#   11. Assign to b
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_conditions, check_conditions, check_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1271:
#
# ===== SYNTAX =====
#   def check_range()
#   return — exits the function and sends back a value
#   operators: < > or
#
# ===== EVALUATION ORDER =====
#   check_range(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_range() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_range() — stored for later
#   5. Define def check_range() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_range, check_range, check_range are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1273:
#
# ===== SYNTAX =====
#   def check_divisibility()
#   return — exits the function and sends back a value
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   check_divisibility(): — no arguments needed
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 5: first get num, then get 5, then compute num % 5
#
# ===== EXECUTION ORDER =====
#   1. Define def check_divisibility() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_divisibility() — stored for later
#   5. Define def check_divisibility() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_divisibility, check_divisibility, check_divisibility are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1274:
#
# ===== SYNTAX =====
#   def check_list()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: == > or
#
# ===== EVALUATION ORDER =====
#   check_list(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list() — stored for later
#   5. Define def check_list() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list, check_list, check_list are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1275:
#
# ===== SYNTAX =====
#   def check_leap_year()
#   return — exits the function and sends back a value
#   operators: % == != and or
#
# ===== EVALUATION ORDER =====
#   check_leap_year(): — no arguments needed
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 400: first get year, then get 400, then compute year % 400
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#   year % 4: first get year, then get 4, then compute year % 4
#   year % 100: first get year, then get 100, then compute year % 100
#
# ===== EXECUTION ORDER =====
#   1. Define def check_leap_year() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_leap_year() — stored for later
#   5. Define def check_leap_year() — stored for later
#   6. Assign to year
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to year
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_leap_year, check_leap_year, check_leap_year are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      year = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1276:
#
# ===== SYNTAX =====
#   def check_number()
#   return — exits the function and sends back a value
#   operators: % == > or
#
# ===== EVALUATION ORDER =====
#   check_number(): — no arguments needed
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#
# ===== EXECUTION ORDER =====
#   1. Define def check_number() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_number() — stored for later
#   5. Define def check_number() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_number, check_number, check_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1277:
#
# ===== SYNTAX =====
#   def check_string_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: > or
#
# ===== EVALUATION ORDER =====
#   check_string_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_string_conditions() — stored for later
#   5. Define def check_string_conditions() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_conditions, check_string_conditions, check_string_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1278:
#
# ===== SYNTAX =====
#   def check_conditions()
#   return — exits the function and sends back a value
#   operators: % == <= >= and or
#
# ===== EVALUATION ORDER =====
#   check_conditions(): — no arguments needed
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 7: first get num, then get 7, then compute num % 7
#
# ===== EXECUTION ORDER =====
#   1. Define def check_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_conditions() — stored for later
#   5. Define def check_conditions() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_conditions, check_conditions, check_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1279:
#
# ===== SYNTAX =====
#   def check_multiples()
#   return — exits the function and sends back a value
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   check_multiples(): — no arguments needed
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#   num % 7: first get num, then get 7, then compute num % 7
#   num % 11: first get num, then get 11, then compute num % 11
#
# ===== EXECUTION ORDER =====
#   1. Define def check_multiples() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_multiples() — stored for later
#   5. Define def check_multiples() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_multiples, check_multiples, check_multiples are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1280:
#
# ===== SYNTAX =====
#   def check_voting_eligibility()
#   return — exits the function and sends back a value
#   operators: > or
#
# ===== EVALUATION ORDER =====
#   check_voting_eligibility(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_voting_eligibility() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_voting_eligibility() — stored for later
#   5. Define def check_voting_eligibility() — stored for later
#   6. Assign to age
#   7. Assign to is_citizen
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to age
#   11. Assign to is_citizen
#   12. Assign to result
#   13. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_voting_eligibility, check_voting_eligibility, check_voting_eligibility are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      age = ... (stored in memory)
#      is_citizen = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      age = ... (stored in memory)
#      is_citizen = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1281:
#
# ===== SYNTAX =====
#   def check_divisibility_all()
#   return — exits the function and sends back a value
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   check_divisibility_all(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#
# ===== EXECUTION ORDER =====
#   1. Define def check_divisibility_all() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_divisibility_all() — stored for later
#   5. Define def check_divisibility_all() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_divisibility_all, check_divisibility_all, check_divisibility_all are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1283:
#
# ===== SYNTAX =====
#   def check_string_empty_or_number()
#   return — exits the function and sends back a value
#   any() — built-in function call
#   len() — built-in function call
#   comprehension
#   operators: == or
#
# ===== EVALUATION ORDER =====
#   check_string_empty_or_number(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_empty_or_number() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_string_empty_or_number() — stored for later
#   5. Define def check_string_empty_or_number() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_empty_or_number, check_string_empty_or_number, check_string_empty_or_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1284:
#
# ===== SYNTAX =====
#   def check_negative_or_divisible_by_3()
#   return — exits the function and sends back a value
#   operators: % == < or
#
# ===== EVALUATION ORDER =====
#   check_negative_or_divisible_by_3(): — no arguments needed
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 3: first get num, then get 3, then compute num % 3
#
# ===== EXECUTION ORDER =====
#   1. Define def check_negative_or_divisible_by_3() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_negative_or_divisible_by_3() — stored for later
#   5. Define def check_negative_or_divisible_by_3() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_negative_or_divisible_by_3, check_negative_or_divisible_by_3, check_negative_or_divisible_by_3 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1285:
#
# ===== SYNTAX =====
#   def check_list_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: == > or
#
# ===== EVALUATION ORDER =====
#   check_list_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list_conditions() — stored for later
#   5. Define def check_list_conditions() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list_conditions, check_list_conditions, check_list_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1286:
#
# ===== SYNTAX =====
#   def check_perfect_square_or_cube()
#   return — exits the function and sends back a value
#   round() — built-in function call
#   operators: / ** == or
#
# ===== EVALUATION ORDER =====
#   check_perfect_square_or_cube(): — no arguments needed
#   math.isqrt(...) ** 2: first get math.isqrt(...), then get 2, then compute math.isqrt(...) ** 2
#   round(...) ** 3: first get round(...), then get 3, then compute round(...) ** 3
#   math.isqrt(...) ** 2: first get math.isqrt(...), then get 2, then compute math.isqrt(...) ** 2
#   round(...) ** 3: first get round(...), then get 3, then compute round(...) ** 3
#   math.isqrt(...) ** 2: first get math.isqrt(...), then get 2, then compute math.isqrt(...) ** 2
#   round(...) ** 3: first get round(...), then get 3, then compute round(...) ** 3
#   math.isqrt(...) ** 2: first get math.isqrt(...), then get 2, then compute math.isqrt(...) ** 2
#   round(...) ** 3: first get round(...), then get 3, then compute round(...) ** 3
#   math.isqrt(...) ** 2: first get math.isqrt(...), then get 2, then compute math.isqrt(...) ** 2
#   round(...) ** 3: first get round(...), then get 3, then compute round(...) ** 3
#   num ** ?: first get num, then get ?, then compute num ** ?
#   num ** ?: first get num, then get ?, then compute num ** ?
#   num ** ?: first get num, then get ?, then compute num ** ?
#   num ** ?: first get num, then get ?, then compute num ** ?
#   num ** ?: first get num, then get ?, then compute num ** ?
#   1 / 3: first get 1, then get 3, then compute 1 / 3
#   1 / 3: first get 1, then get 3, then compute 1 / 3
#   1 / 3: first get 1, then get 3, then compute 1 / 3
#   1 / 3: first get 1, then get 3, then compute 1 / 3
#   1 / 3: first get 1, then get 3, then compute 1 / 3
#
# ===== EXECUTION ORDER =====
#   1. Import math
#   2. Define def check_perfect_square_or_cube() — stored for later
#   3. Assign to result
#   4. Run print(?)
#   5. Define def check_perfect_square_or_cube() — stored for later
#   6. Define def check_perfect_square_or_cube() — stored for later
#   7. Assign to num
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to num
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_perfect_square_or_cube, check_perfect_square_or_cube, check_perfect_square_or_cube are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1287:
#
# ===== SYNTAX =====
#   def check_string_type()
#   return — exits the function and sends back a value
#   operators: or
#
# ===== EVALUATION ORDER =====
#   check_string_type(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_type() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_string_type() — stored for later
#   5. Define def check_string_type() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_type, check_string_type, check_string_type are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1288:
#
# ===== SYNTAX =====
#   def check_list_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   sum() — built-in function call
#   operators: == > or
#
# ===== EVALUATION ORDER =====
#   check_list_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list_conditions() — stored for later
#   5. Define def check_list_conditions() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list_conditions, check_list_conditions, check_list_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1289:
#
# ===== SYNTAX =====
#   def check_divisible_by_2_or_5()
#   return — exits the function and sends back a value
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   check_divisible_by_2_or_5(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 5: first get num, then get 5, then compute num % 5
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 5: first get num, then get 5, then compute num % 5
#
# ===== EXECUTION ORDER =====
#   1. Define def check_divisible_by_2_or_5() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_divisible_by_2_or_5() — stored for later
#   5. Define def check_divisible_by_2_or_5() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_divisible_by_2_or_5, check_divisible_by_2_or_5, check_divisible_by_2_or_5 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1290:
#
# ===== SYNTAX =====
#   def check_file_conditions()
#   return — exits the function and sends back a value
#   Path() — built-in function call
#   operators: or
#
# ===== EVALUATION ORDER =====
#   check_file_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Import Path from pathlib
#   2. Define def check_file_conditions() — stored for later
#   3. Assign to result
#   4. Run print(?)
#   5. Define def check_file_conditions() — stored for later
#   6. Define def check_file_conditions() — stored for later
#   7. Assign to p
#   8. Assign to result
#   9. Run print(result)
#   10. Assign to p
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   def lines are SKIPPED during execution — functions check_file_conditions, check_file_conditions, check_file_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      p = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1291:
#
# ===== SYNTAX =====
#   def check_number()
#   return — exits the function and sends back a value
#   operators: < > or
#
# ===== EVALUATION ORDER =====
#   check_number(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_number() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_number() — stored for later
#   5. Define def check_number() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_number, check_number, check_number are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1292:
#
# ===== SYNTAX =====
#   def check_string_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   check_string_conditions(): — no arguments needed
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def check_string_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_string_conditions() — stored for later
#   5. Define def check_string_conditions() — stored for later
#   6. Assign to text
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to text
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_string_conditions, check_string_conditions, check_string_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1293:
#
# ===== SYNTAX =====
#   def check_list_conditions()
#   return — exits the function and sends back a value
#   len() — built-in function call
#   operators: > or
#
# ===== EVALUATION ORDER =====
#   check_list_conditions(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_list_conditions() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_list_conditions() — stored for later
#   5. Define def check_list_conditions() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_list_conditions, check_list_conditions, check_list_conditions are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1294:
#
# ===== SYNTAX =====
#   def check_even_or_divisible_by_3()
#   return — exits the function and sends back a value
#   operators: % == or
#
# ===== EVALUATION ORDER =====
#   check_even_or_divisible_by_3(): — no arguments needed
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#   num % 2: first get num, then get 2, then compute num % 2
#   num % 3: first get num, then get 3, then compute num % 3
#
# ===== EXECUTION ORDER =====
#   1. Define def check_even_or_divisible_by_3() — stored for later
#   2. Assign to result
#   3. Run print(?)
#   4. Define def check_even_or_divisible_by_3() — stored for later
#   5. Define def check_even_or_divisible_by_3() — stored for later
#   6. Assign to num
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to num
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_even_or_divisible_by_3, check_even_or_divisible_by_3, check_even_or_divisible_by_3 are just stored
#   Execution starts at the non-def lines below:
#      result = ... (stored in memory)
#      print(?)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      num = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1296:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Assign to words
#   3. Run print(words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to s
#   7. Assign to words
#   8. Run print(words)
#   9. Assign to s
#   10. Assign to words
#   11. Run print(words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)
#      s = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)
#      s = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)

"""

"""
Problem 1297:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Assign to items
#   3. Run print(items)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      items = ... (stored in memory)
#      print(items)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1298:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: and
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to password
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to password
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to password
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      password = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      password = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      password = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1299:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to emails
#   3. Run print(emails)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to emails
#   8. Run print(emails)
#   9. Assign to text
#   10. Assign to emails
#   11. Run print(emails)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      emails = ... (stored in memory)
#      print(emails)
#      text = ... (stored in memory)
#      emails = ... (stored in memory)
#      print(emails)
#      text = ... (stored in memory)
#      emails = ... (stored in memory)
#      print(emails)

"""

"""
Problem 1300:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to tweet
#   2. Assign to hashtags
#   3. Run print(hashtags)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to tweet
#   7. Assign to hashtags
#   8. Run print(hashtags)
#   9. Assign to tweet
#   10. Assign to hashtags
#   11. Run print(hashtags)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      tweet = ... (stored in memory)
#      hashtags = ... (stored in memory)
#      print(hashtags)
#      tweet = ... (stored in memory)
#      hashtags = ... (stored in memory)
#      print(hashtags)
#      tweet = ... (stored in memory)
#      hashtags = ... (stored in memory)
#      print(hashtags)

"""

"""
Problem 1301:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to domains
#   3. Run print(domains)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to domains
#   8. Run print(domains)
#   9. Assign to text
#   10. Assign to domains
#   11. Run print(domains)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      domains = ... (stored in memory)
#      print(domains)
#      text = ... (stored in memory)
#      domains = ... (stored in memory)
#      print(domains)
#      text = ... (stored in memory)
#      domains = ... (stored in memory)
#      print(domains)

"""

"""
Problem 1302:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to ip
#   2. Assign to pattern
#   3. Assign to match
#   4. Run print(match)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to ip
#   8. Assign to pattern
#   9. Assign to match
#   10. Run print(match)
#   11. Assign to ip
#   12. Assign to pattern
#   13. Assign to match
#   14. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ip = ... (stored in memory)
#      pattern = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      ip = ... (stored in memory)
#      pattern = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      ip = ... (stored in memory)
#      pattern = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1303:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to clean
#   3. Run print(clean)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to clean
#   8. Run print(clean)
#   9. Assign to text
#   10. Assign to clean
#   11. Run print(clean)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      clean = ... (stored in memory)
#      print(clean)
#      text = ... (stored in memory)
#      clean = ... (stored in memory)
#      print(clean)
#      text = ... (stored in memory)
#      clean = ... (stored in memory)
#      print(clean)

"""

"""
Problem 1304:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to s
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to s
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      s = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      s = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1305:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to dates
#   3. Run print(dates)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to dates
#   8. Run print(dates)
#   9. Assign to text
#   10. Assign to dates
#   11. Run print(dates)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      dates = ... (stored in memory)
#      print(dates)
#      text = ... (stored in memory)
#      dates = ... (stored in memory)
#      print(dates)
#      text = ... (stored in memory)
#      dates = ... (stored in memory)
#      print(dates)

"""

"""
Problem 1306:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to color
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to color
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to color
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      color = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      color = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      color = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1307:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to words
#   3. Run print(words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to words
#   8. Run print(words)
#   9. Assign to text
#   10. Assign to words
#   11. Run print(words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)
#      text = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)
#      text = ... (stored in memory)
#      words = ... (stored in memory)
#      print(words)

"""

"""
Problem 1308:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to filename
#   2. Assign to ext
#   3. Run print(ext)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to filename
#   7. Assign to ext
#   8. Run print(ext)
#   9. Assign to filename
#   10. Assign to ext
#   11. Run print(ext)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      filename = ... (stored in memory)
#      ext = ... (stored in memory)
#      print(ext)
#      filename = ... (stored in memory)
#      ext = ... (stored in memory)
#      print(ext)
#      filename = ... (stored in memory)
#      ext = ... (stored in memory)
#      print(ext)

"""

"""
Problem 1309:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to duplicates
#   3. Run print(duplicates)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to duplicates
#   8. Run print(duplicates)
#   9. Assign to text
#   10. Assign to duplicates
#   11. Run print(duplicates)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      duplicates = ... (stored in memory)
#      print(duplicates)
#      text = ... (stored in memory)
#      duplicates = ... (stored in memory)
#      print(duplicates)
#      text = ... (stored in memory)
#      duplicates = ... (stored in memory)
#      print(duplicates)

"""

"""
Problem 1310:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to capitalized
#   3. Run print(capitalized)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to capitalized
#   8. Run print(capitalized)
#   9. Assign to text
#   10. Assign to capitalized
#   11. Run print(capitalized)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      capitalized = ... (stored in memory)
#      print(capitalized)
#      text = ... (stored in memory)
#      capitalized = ... (stored in memory)
#      print(capitalized)
#      text = ... (stored in memory)
#      capitalized = ... (stored in memory)
#      print(capitalized)

"""

"""
Problem 1311:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to time
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to time
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to time
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      time = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      time = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      time = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1312:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to numbers
#   3. Run print(numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to numbers
#   8. Run print(numbers)
#   9. Assign to text
#   10. Assign to numbers
#   11. Run print(numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      text = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)
#      text = ... (stored in memory)
#      numbers = ... (stored in memory)
#      print(numbers)

"""

"""
Problem 1313:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to ip
#   2. Assign to clean_ip
#   3. Run print(clean_ip)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      ip = ... (stored in memory)
#      clean_ip = ... (stored in memory)
#      print(clean_ip)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1314:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to slug
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to slug
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to slug
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      slug = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      slug = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      slug = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1315:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to content
#   3. Run print(content)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to content
#   8. Run print(content)
#   9. Assign to text
#   10. Assign to content
#   11. Run print(content)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      content = ... (stored in memory)
#      print(content)
#      text = ... (stored in memory)
#      content = ... (stored in memory)
#      print(content)
#      text = ... (stored in memory)
#      content = ... (stored in memory)
#      print(content)

"""

"""
Problem 1316:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to html
#   2. Assign to tags
#   3. Run print(tags)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to html
#   7. Assign to tags
#   8. Run print(tags)
#   9. Assign to html
#   10. Assign to tags
#   11. Run print(tags)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      html = ... (stored in memory)
#      tags = ... (stored in memory)
#      print(tags)
#      html = ... (stored in memory)
#      tags = ... (stored in memory)
#      print(tags)
#      html = ... (stored in memory)
#      tags = ... (stored in memory)
#      print(tags)

"""

"""
Problem 1317:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to zip_code
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to zip_code
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to zip_code
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      zip_code = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      zip_code = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      zip_code = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1318:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to abbreviations
#   3. Run print(abbreviations)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to abbreviations
#   8. Run print(abbreviations)
#   9. Assign to text
#   10. Assign to abbreviations
#   11. Run print(abbreviations)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      abbreviations = ... (stored in memory)
#      print(abbreviations)
#      text = ... (stored in memory)
#      abbreviations = ... (stored in memory)
#      print(abbreviations)
#      text = ... (stored in memory)
#      abbreviations = ... (stored in memory)
#      print(abbreviations)

"""

"""
Problem 1319:
#
# ===== SYNTAX =====
#   def solve()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to count
#   3. Run print(count)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to count
#   8. Run print(count)
#   9. Assign to text
#   10. Assign to count
#   11. Run print(count)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      count = ... (stored in memory)
#      print(count)
#      text = ... (stored in memory)
#      count = ... (stored in memory)
#      print(count)
#      text = ... (stored in memory)
#      count = ... (stored in memory)
#      print(count)

"""

"""
Problem 1320:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to clean
#   3. Run print(clean)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to clean
#   8. Run print(clean)
#   9. Assign to text
#   10. Assign to clean
#   11. Run print(clean)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      clean = ... (stored in memory)
#      print(clean)
#      text = ... (stored in memory)
#      clean = ... (stored in memory)
#      print(clean)
#      text = ... (stored in memory)
#      clean = ... (stored in memory)
#      print(clean)

"""

"""
Problem 1321:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to mac
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to mac
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to mac
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      mac = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      mac = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      mac = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1322:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to email
#   3. Assign to username
#   4. Run print(username)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to email
#   9. Assign to username
#   10. Run print(username)
#   11. Import re
#   12. Assign to email
#   13. Assign to username
#   14. Run print(username)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      email = ... (stored in memory)
#      username = ... (stored in memory)
#      print(username)
#      email = ... (stored in memory)
#      username = ... (stored in memory)
#      print(username)
#      email = ... (stored in memory)
#      username = ... (stored in memory)
#      print(username)

"""

"""
Problem 1323:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to num
#   2. Assign to formatted
#   3. Run print(formatted)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to num
#   7. Assign to formatted
#   8. Run print(formatted)
#   9. Assign to num
#   10. Assign to formatted
#   11. Run print(formatted)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      num = ... (stored in memory)
#      formatted = ... (stored in memory)
#      print(formatted)
#      num = ... (stored in memory)
#      formatted = ... (stored in memory)
#      print(formatted)
#      num = ... (stored in memory)
#      formatted = ... (stored in memory)
#      print(formatted)

"""

"""
Problem 1324:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to handles
#   3. Run print(handles)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to handles
#   8. Run print(handles)
#   9. Assign to text
#   10. Assign to handles
#   11. Run print(handles)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      handles = ... (stored in memory)
#      print(handles)
#      text = ... (stored in memory)
#      handles = ... (stored in memory)
#      print(handles)
#      text = ... (stored in memory)
#      handles = ... (stored in memory)
#      print(handles)

"""

"""
Problem 1325:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to emoji_text
#   3. Assign to match
#   4. Run print(match)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to emoji_text
#   9. Assign to match
#   10. Run print(match)
#   11. Import re
#   12. Assign to emoji_text
#   13. Assign to match
#   14. Run print(match)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      emoji_text = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      emoji_text = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      emoji_text = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1326:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to times
#   3. Run print(times)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to times
#   8. Run print(times)
#   9. Assign to text
#   10. Assign to times
#   11. Run print(times)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      times = ... (stored in memory)
#      print(times)
#      text = ... (stored in memory)
#      times = ... (stored in memory)
#      print(times)
#      text = ... (stored in memory)
#      times = ... (stored in memory)
#      print(times)

"""

"""
Problem 1327:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#   2. Assign to text
#   3. Assign to prices
#   4. Run print(prices)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import re
#   8. Assign to text
#   9. Assign to prices
#   10. Run print(prices)
#   11. Import re
#   12. Assign to text
#   13. Assign to prices
#   14. Run print(prices)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      prices = ... (stored in memory)
#      print(prices)
#      text = ... (stored in memory)
#      prices = ... (stored in memory)
#      print(prices)
#      text = ... (stored in memory)
#      prices = ... (stored in memory)
#      print(prices)

"""

"""
Problem 1328:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(set(duplicates)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Assign to duplicates
#   3. Run print(set(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      duplicates = ... (stored in memory)
#      print(set(...))

"""

"""
Problem 1329:
#
# ===== SYNTAX =====
#   def solve()
#   bool() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to hex_num
#   2. Assign to match
#   3. Run print(match)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to hex_num
#   7. Assign to match
#   8. Run print(match)
#   9. Assign to hex_num
#   10. Assign to match
#   11. Run print(match)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      hex_num = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      hex_num = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)
#      hex_num = ... (stored in memory)
#      match = ... (stored in memory)
#      print(match)

"""

"""
Problem 1330:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import re
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1331:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, int)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1332:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, str)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1333:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, float)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1334:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, list)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1335:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, dict)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1336:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, ?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1337:
#
# ===== SYNTAX =====
#   def solve()
#   all() — built-in function call
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to lst
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1338:
#
# ===== SYNTAX =====
#   def solve()
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to items
#   2. Assign to ints
#   3. Run print(ints)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to items
#   7. Assign to ints
#   8. Run print(ints)
#   9. Assign to items
#   10. Assign to ints
#   11. Run print(ints)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      ints = ... (stored in memory)
#      print(ints)
#      items = ... (stored in memory)
#      ints = ... (stored in memory)
#      print(ints)
#      items = ... (stored in memory)
#      ints = ... (stored in memory)
#      print(ints)

"""

"""
Problem 1339:
#
# ===== SYNTAX =====
#   def solve()
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to items
#   2. Assign to strings
#   3. Run print(strings)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to items
#   7. Assign to strings
#   8. Run print(strings)
#   9. Assign to items
#   10. Assign to strings
#   11. Run print(strings)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      strings = ... (stored in memory)
#      print(strings)
#      items = ... (stored in memory)
#      strings = ... (stored in memory)
#      print(strings)
#      items = ... (stored in memory)
#      strings = ... (stored in memory)
#      print(strings)

"""

"""
Problem 1340:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, bool)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1341:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, tuple)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1342:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1343:
#
# ===== SYNTAX =====
#   def greet(name)
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   print(greet('Alice')): the inner greet() executes FIRST, then print() outputs the result
#   print(greet(123)): the inner greet() executes FIRST, then print() outputs the result
#   greet(name): looks up variable 'name', then runs the function body
#   print(greet('Bob')): the inner greet() executes FIRST, then print() outputs the result
#   greet('Alice'): gets the literal 'Alice', then runs the function body
#   greet(123): gets the literal 123, then runs the function body
#   greet('Bob'): gets the literal 'Bob', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def greet(name) — stored for later
#   2. Run print(greet(...))
#       calls greet with: name='Alice'
#         2.1 Return from function
#   3. Run print(greet(...))
#       calls greet with: name=123
#         3.1 Return from function
#   4. Define def greet(name) — stored for later
#   5. Define def greet(name) — stored for later
#   6. Assign to name
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(greet(...))
#       calls greet with: name='Bob'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet, greet, greet are just stored
#   Execution starts at the non-def lines below:
#      print(greet('Alice')) → function runs first, then print shows the result
#      print(greet(123)) → function runs first, then print shows the result
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet('Bob')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1344:
#
# ===== SYNTAX =====
#   def solve()
#   all() — built-in function call
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to nested
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to nested
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to nested
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      nested = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      nested = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      nested = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1345:
#
# ===== SYNTAX =====
#   def solve()
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to mixed
#   2. Assign to nums
#   3. Assign to words
#   4. Run print(nums)
#   5. Run print(words)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to mixed
#   9. Assign to nums
#   10. Assign to words
#   11. Run print(nums)
#   12. Run print(words)
#   13. Assign to mixed
#   14. Assign to nums
#   15. Assign to words
#   16. Run print(nums)
#   17. Run print(words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      mixed = ... (stored in memory)
#      nums = ... (stored in memory)
#      words = ... (stored in memory)
#      print(nums)
#      print(words)
#      mixed = ... (stored in memory)
#      nums = ... (stored in memory)
#      words = ... (stored in memory)
#      print(nums)
#      print(words)
#      mixed = ... (stored in memory)
#      nums = ... (stored in memory)
#      words = ... (stored in memory)
#      print(nums)
#      print(words)

"""

"""
Problem 1346:
#
# ===== SYNTAX =====
#   def multiply(a, b)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   isinstance() — built-in function call
#   comprehension
#   if/elif/else
#   operators: *
#
# ===== EVALUATION ORDER =====
#   print(multiply(2, 3)): the inner multiply() executes FIRST, then print() outputs the result
#   print(multiply(2, 'x')): the inner multiply() executes FIRST, then print() outputs the result
#   multiply(a, b): looks up variable 'a', looks up variable 'b', then runs the function body
#   print(multiply(20, 30)): the inner multiply() executes FIRST, then print() outputs the result
#   a * b: first get a, then get b, then compute a * b
#   multiply(2, 3): gets the literal 2, gets the literal 3, then runs the function body
#   multiply(2, 'x'): gets the literal 2, gets the literal 'x', then runs the function body
#   multiply(20, 30): gets the literal 20, gets the literal 30, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def multiply(a, b) — stored for later
#   2. Run print(multiply(...))
#       calls multiply with: a=2, b=3
#         2.1 Return from function
#   3. Run print(multiply(...))
#       calls multiply with: a=2, b='x'
#         3.1 Return from function
#   4. Define def multiply(a, b) — stored for later
#   5. Define def multiply(a, b) — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(multiply(...))
#       calls multiply with: a=20, b=30
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions multiply, multiply, multiply are just stored
#   Execution starts at the non-def lines below:
#      print(multiply(2, 3)) → function runs first, then print shows the result
#      print(multiply(2, 'x')) → function runs first, then print shows the result
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(multiply(20, 30)) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1347:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(isinstance(x, set)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(isinstance(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(isinstance(...))

"""

"""
Problem 1348:
#
# ===== SYNTAX =====
#   def solve()
#   all() — built-in function call
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to result
#   3. Run print(result)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to d
#   10. Assign to result
#   11. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1349:
#
# ===== SYNTAX =====
#   def solve()
#   all() — built-in function call
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   'a' not in word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to result
#   3. Run print(result)
#   4. Assign to result
#   5. For loop over words
#       5.1 If statement (one branch runs)
#   6. Run print(result)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Assign to d
#   10. Assign to result
#   11. Run print(result)
#   12. Assign to d
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      d = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1350:
#
# ===== SYNTAX =====
#   class FakeFile — defines a new type
#     def read — method of FakeFile
#   operators: and
#
# ===== EVALUATION ORDER =====
#   print(FakeFile()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class FakeFile
#   2. Assign to f
#   3. Run print(?)
#   4. Assign to obj
#   5. Run print(obj)
#   6. Run print(FakeFile(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   Execution starts at the non-def lines below:
#      f = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(FakeFile(...))

"""

"""
Problem 1351:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Run d.pop('b')
#   3. Run print(d)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Run d.pop('b')
#   8. Run print(d)
#   9. Assign to d
#   10. Run d.pop('b')
#   11. Run print(d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      d.pop('b')
#      print(d)
#      d = ... (stored in memory)
#      d.pop('b')
#      print(d)
#      d = ... (stored in memory)
#      d.pop('b')
#      print(d)

"""

"""
Problem 1352:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to value
#   3. Run print(value)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to value
#   8. Run print(value)
#   9. Assign to d
#   10. Assign to value
#   11. Run print(value)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      value = ... (stored in memory)
#      print(value)
#      d = ... (stored in memory)
#      value = ... (stored in memory)
#      print(value)
#      d = ... (stored in memory)
#      value = ... (stored in memory)
#      print(value)

"""

"""
Problem 1353:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run lst.pop()
#   3. Run print(lst)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Run lst.pop()
#   8. Run print(lst)
#   9. Assign to lst
#   10. Run lst.pop()
#   11. Run print(lst)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      lst.pop()
#      print(lst)
#      lst = ... (stored in memory)
#      lst.pop()
#      print(lst)
#      lst = ... (stored in memory)
#      lst.pop()
#      print(lst)

"""

"""
Problem 1354:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run lst.pop(1)
#   3. Run print(lst)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Run lst.pop(1)
#   8. Run print(lst)
#   9. Assign to lst
#   10. Run lst.pop(1)
#   11. Run print(lst)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      lst.pop(1)
#      print(lst)
#      lst = ... (stored in memory)
#      lst.pop(1)
#      print(lst)
#      lst = ... (stored in memory)
#      lst.pop(1)
#      print(lst)

"""

"""
Problem 1355:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to removed
#   3. Run print(removed)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to removed
#   8. Run print(removed)
#   9. Assign to lst
#   10. Assign to removed
#   11. Run print(removed)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      removed = ... (stored in memory)
#      print(removed)
#      lst = ... (stored in memory)
#      removed = ... (stored in memory)
#      print(removed)
#      lst = ... (stored in memory)
#      removed = ... (stored in memory)
#      print(removed)

"""

"""
Problem 1356:
#
# ===== SYNTAX =====
#   while
#
# ===== EVALUATION ORDER =====
#   print(lst.pop()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. While loop
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1357:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Try block (error handling)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to lst
#   6. Try block (error handling)
#   7. Assign to lst
#   8. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      top-level statement runs
#      lst = ... (stored in memory)
#      top-level statement runs
#      lst = ... (stored in memory)
#      top-level statement runs

"""

"""
Problem 1358:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to val
#   3. Run print(val)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to val
#   8. Run print(val)
#   9. Assign to d
#   10. Assign to val
#   11. Run print(val)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      val = ... (stored in memory)
#      print(val)
#      d = ... (stored in memory)
#      val = ... (stored in memory)
#      print(val)
#      d = ... (stored in memory)
#      val = ... (stored in memory)
#      print(val)

"""

"""
Problem 1359:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   try/except
#
# ===== EVALUATION ORDER =====
#   print(d.pop('b')): inner expression resolves first, then print() outputs
#   print(d.pop('b')): inner expression resolves first, then print() outputs
#   print(d.pop('b')): inner expression resolves first, then print() outputs
#   print(d.pop('b')): inner expression resolves first, then print() outputs
#   print(d_2.pop('b')): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Try block (error handling)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to d
#   6. Try block (error handling)
#   7. Assign to d
#   8. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      top-level statement runs
#      d = ... (stored in memory)
#      top-level statement runs
#      d = ... (stored in memory)
#      top-level statement runs

"""

"""
Problem 1360:
#
# ===== SYNTAX =====
#   while
#
# ===== EVALUATION ORDER =====
#   print(d.popitem()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. While loop
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      while loop repeats until condition is False
#
#   Loop: checks condition → if True runs body, then checks again

"""

"""
Problem 1361:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(stack.pop()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to stack
#   2. Run print(stack.pop(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      stack = ... (stored in memory)
#      print(stack.pop(...))

"""

"""
Problem 1362:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(queue.pop(0)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to queue
#   2. Run print(queue.pop(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      queue = ... (stored in memory)
#      print(queue.pop(...))

"""

"""
Problem 1363:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to last
#   3. Run print(last)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to last
#   8. Run print(last)
#   9. Assign to numbers
#   10. Assign to last
#   11. Run print(last)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      last = ... (stored in memory)
#      print(last)
#      numbers = ... (stored in memory)
#      last = ... (stored in memory)
#      print(last)
#      numbers = ... (stored in memory)
#      last = ... (stored in memory)
#      print(last)

"""

"""
Problem 1364:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(d.pop('name')): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Run print(d.pop(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      print(d.pop(...))

"""

"""
Problem 1365:
#
# ===== SYNTAX =====
#   def solve()
#   range() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. For loop over range(...)
#       2.1 Run lst.pop()
#   3. Run print(lst)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. For loop over range(...)
#       7.1 Run lst.pop()
#   8. Run print(lst)
#   9. Assign to lst
#   10. For loop over range(...)
#       10.1 Run lst.pop()
#   11. Run print(lst)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      for loop runs its body once per item
#      print(lst)
#      lst = ... (stored in memory)
#      for loop runs its body once per item
#      print(lst)
#      lst = ... (stored in memory)
#      for loop runs its body once per item
#      print(lst)
#
#   Loop: grabs one item from range(...) each round

"""

"""
Problem 1366:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: +
#
# ===== EVALUATION ORDER =====
#   lst.pop(...) + lst.pop(...): first get lst.pop(...), then get lst.pop(...), then compute lst.pop(...) + lst.pop(...)
#   lst.pop(...) + lst.pop(...): first get lst.pop(...), then get lst.pop(...), then compute lst.pop(...) + lst.pop(...)
#   lst.pop(...) + lst.pop(...): first get lst.pop(...), then get lst.pop(...), then compute lst.pop(...) + lst.pop(...)
#   lst.pop(...) + lst.pop(...): first get lst.pop(...), then get lst.pop(...), then compute lst.pop(...) + lst.pop(...)
#   lst.pop(...) + lst.pop(...): first get lst.pop(...), then get lst.pop(...), then compute lst.pop(...) + lst.pop(...)
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to sum_last_two
#   3. Run print(sum_last_two)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to sum_last_two
#   8. Run print(sum_last_two)
#   9. Assign to lst
#   10. Assign to sum_last_two
#   11. Run print(sum_last_two)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sum_last_two = ... (stored in memory)
#      print(sum_last_two)
#      lst = ... (stored in memory)
#      sum_last_two = ... (stored in memory)
#      print(sum_last_two)
#      lst = ... (stored in memory)
#      sum_last_two = ... (stored in memory)
#      print(sum_last_two)

"""

"""
Problem 1367:
#
# ===== SYNTAX =====
#   def remove_key(d, key)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(remove_key(?, 'a')): the inner remove_key() executes FIRST, then print() outputs the result
#   remove_key(d, key): looks up variable 'd', looks up variable 'key', then runs the function body
#   print(remove_key(10, 'value')): the inner remove_key() executes FIRST, then print() outputs the result
#   remove_key(?, 'a'): resolves the value, gets the literal 'a', then runs the function body
#   remove_key(10, 'value'): gets the literal 10, gets the literal 'value', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_key(d, key) — stored for later
#   2. Run print(remove_key(...))
#       calls remove_key with: d={...}, key='a'
#         2.1 Return from function
#   3. Define def remove_key(d, key) — stored for later
#   4. Define def remove_key(d, key) — stored for later
#   5. Assign to d
#   6. Assign to key
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(remove_key(...))
#       calls remove_key with: d=10, key='value'
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_key, remove_key, remove_key are just stored
#   Execution starts at the non-def lines below:
#      print(remove_key(?, 'a')) → function runs first, then print shows the result
#      d = ... (stored in memory)
#      key = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(remove_key(10, 'value')) → function runs first, then print shows the result

"""

"""
Problem 1368:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to src
#   2. Assign to dst
#   3. Run dst.append(src.pop(...))
#   4. Run print(dst)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to src
#   8. Assign to dst
#   9. Run dst.append(src.pop(...))
#   10. Run print(dst)
#   11. Assign to src
#   12. Assign to dst
#   13. Run dst.append(src.pop(...))
#   14. Run print(dst)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      src = ... (stored in memory)
#      dst = ... (stored in memory)
#      dst.append(src.pop(...))
#      print(dst)
#      src = ... (stored in memory)
#      dst = ... (stored in memory)
#      dst.append(src.pop(...))
#      print(dst)
#      src = ... (stored in memory)
#      dst = ... (stored in memory)
#      dst.append(src.pop(...))
#      print(dst)

"""

"""
Problem 1369:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to actions
#   2. Assign to undo
#   3. Run print(undo)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to actions
#   7. Assign to undo
#   8. Run print(undo)
#   9. Assign to actions
#   10. Assign to undo
#   11. Run print(undo)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      actions = ... (stored in memory)
#      undo = ... (stored in memory)
#      print(undo)
#      actions = ... (stored in memory)
#      undo = ... (stored in memory)
#      print(undo)
#      actions = ... (stored in memory)
#      undo = ... (stored in memory)
#      print(undo)

"""

"""
Problem 1370:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to nested
#   2. Assign to last_sublist
#   3. Run print(last_sublist)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to nested
#   7. Assign to last_sublist
#   8. Run print(last_sublist)
#   9. Assign to nested
#   10. Assign to last_sublist
#   11. Run print(last_sublist)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      nested = ... (stored in memory)
#      last_sublist = ... (stored in memory)
#      print(last_sublist)
#      nested = ... (stored in memory)
#      last_sublist = ... (stored in memory)
#      print(last_sublist)
#      nested = ... (stored in memory)
#      last_sublist = ... (stored in memory)
#      print(last_sublist)

"""

"""
Problem 1371:
#
# ===== SYNTAX =====
#   def print_kwargs()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def print_kwargs() — stored for later
#   2. Run print_kwargs()
#         2.1 Assign to result
#         2.2 Return from function
#   3. Define def print_kwargs() — stored for later
#   4. Define def print_kwargs() — stored for later
#   5. Run print(kwargs)
#   6. Run print(kwargs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_kwargs, print_kwargs, print_kwargs are just stored
#   Execution starts at the non-def lines below:
#      print_kwargs() → jumps into function, runs body, returns result → printed
#      print(kwargs)
#      print(kwargs)

"""

"""
Problem 1372:
#
# ===== SYNTAX =====
#   def display_kwargs()
#   (no return — the function prints directly instead of returning)
#   display_kwargs() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   display_kwargs(): — no arguments needed
#   display_kwargs(): — no arguments needed
#   display_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def display_kwargs() — stored for later
#   2. Run display_kwargs()
#         2.1 For loop over kwargs.items(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. For loop over kwargs.items(...)
#       5.1 Run print(?)
#   6. For loop over kwargs.items(...)
#       6.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions display_kwargs, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      display_kwargs() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from kwargs.items(...) each round

"""

"""
Problem 1373:
#
# ===== SYNTAX =====
#   def count_kwargs()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   count_kwargs(): — no arguments needed
#   print(len(kwargs)): inner expression resolves first, then print() outputs
#   print(len(kwargs)): inner expression resolves first, then print() outputs
#   print(len(kwargs)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define def count_kwargs() — stored for later
#   2. Run count_kwargs()
#         2.1 Assign to result
#         2.2 Return from function
#   3. Define def count_kwargs() — stored for later
#   4. Define def count_kwargs() — stored for later
#   5. Run print(len(...))
#   6. Run print(len(...))
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_kwargs, count_kwargs, count_kwargs are just stored
#   Execution starts at the non-def lines below:
#      count_kwargs() → jumps into function, runs body, returns result → printed
#      print(len(...))
#      print(len(...))

"""

"""
Problem 1374:
#
# ===== SYNTAX =====
#   def kwargs_keys()
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(kwargs_keys()): the inner kwargs_keys() executes FIRST, then print() outputs the result
#   kwargs_keys(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def kwargs_keys() — stored for later
#   2. Run print(kwargs_keys(...))
#         2.1 Return from function
#   3. Define def kwargs_keys() — stored for later
#   4. Define def kwargs_keys() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions kwargs_keys, kwargs_keys, kwargs_keys are just stored
#   Execution starts at the non-def lines below:
#      print(kwargs_keys()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1375:
#
# ===== SYNTAX =====
#   def kwargs_values()
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(kwargs_values()): the inner kwargs_values() executes FIRST, then print() outputs the result
#   kwargs_values(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def kwargs_values() — stored for later
#   2. Run print(kwargs_values(...))
#         2.1 Return from function
#   3. Define def kwargs_values() — stored for later
#   4. Define def kwargs_values() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions kwargs_values, kwargs_values, kwargs_values are just stored
#   Execution starts at the non-def lines below:
#      print(kwargs_values()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1376:
#
# ===== SYNTAX =====
#   def has_id()
#   return — exits the function and sends back a value
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(has_id()): the inner has_id() executes FIRST, then print() outputs the result
#   has_id(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def has_id() — stored for later
#   2. Run print(has_id(...))
#         2.1 Return from function
#   3. Define def has_id() — stored for later
#   4. Define def has_id() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions has_id, has_id, has_id are just stored
#   Execution starts at the non-def lines below:
#      print(has_id()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1377:
#
# ===== SYNTAX =====
#   def get_name()
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(get_name()): the inner get_name() executes FIRST, then print() outputs the result
#   get_name(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def get_name() — stored for later
#   2. Run print(get_name(...))
#         2.1 Return from function
#   3. Define def get_name() — stored for later
#   4. Define def get_name() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions get_name, get_name, get_name are just stored
#   Execution starts at the non-def lines below:
#      print(get_name()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1378:
#
# ===== SYNTAX =====
#   def sum_kwargs()
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   isinstance() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(sum_kwargs()): the inner sum_kwargs() executes FIRST, then print() outputs the result
#   sum_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def sum_kwargs() — stored for later
#   2. Run print(sum_kwargs(...))
#         2.1 Return from function
#   3. Define def sum_kwargs() — stored for later
#   4. Define def sum_kwargs() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sum_kwargs, sum_kwargs, sum_kwargs are just stored
#   Execution starts at the non-def lines below:
#      print(sum_kwargs()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1379:
#
# ===== SYNTAX =====
#   def string_kwargs()
#   return — exits the function and sends back a value
#   isinstance() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(string_kwargs()): the inner string_kwargs() executes FIRST, then print() outputs the result
#   string_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def string_kwargs() — stored for later
#   2. Run print(string_kwargs(...))
#         2.1 Return from function
#   3. Define def string_kwargs() — stored for later
#   4. Define def string_kwargs() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions string_kwargs, string_kwargs, string_kwargs are just stored
#   Execution starts at the non-def lines below:
#      print(string_kwargs()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1380:
#
# ===== SYNTAX =====
#   def greet()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   greet(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def greet() — stored for later
#   2. Run greet()
#         2.1 Assign to result
#         2.2 Return from function
#   3. Define def greet() — stored for later
#   4. Define def greet() — stored for later
#   5. Run print(?)
#   6. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet, greet, greet are just stored
#   Execution starts at the non-def lines below:
#      greet() → jumps into function, runs body, returns result → printed
#      print(?)
#      print(?)

"""

"""
Problem 1381:
#
# ===== SYNTAX =====
#   def update_dict(base)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(update_dict(?)): the inner update_dict() executes FIRST, then print() outputs the result
#   update_dict(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def update_dict(base) — stored for later
#   2. Run print(update_dict(...))
#       calls update_dict with: base={...}
#         2.1 Return from function
#   3. Define def update_dict(base) — stored for later
#   4. Define def update_dict(base) — stored for later
#   5. Run base.update(kwargs)
#   6. Assign to result
#   7. Run print(result)
#   8. Run base.update(kwargs)
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions update_dict, update_dict, update_dict are just stored
#   Execution starts at the non-def lines below:
#      print(update_dict(?)) → function runs first, then print shows the result
#      base.update(kwargs)
#      result = ... (stored in memory)
#      print(result)
#      base.update(kwargs)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1382:
#
# ===== SYNTAX =====
#   def merge_dicts(d1)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(merge_dicts(?)): the inner merge_dicts() executes FIRST, then print() outputs the result
#   merge_dicts(?): resolves the value, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def merge_dicts(d1) — stored for later
#   2. Run print(merge_dicts(...))
#       calls merge_dicts with: d1={...}
#         2.1 Return from function
#   3. Define def merge_dicts(d1) — stored for later
#   4. Define def merge_dicts(d1) — stored for later
#   5. Assign to merged
#   6. Run merged.update(kwargs)
#   7. Assign to result
#   8. Run print(result)
#   9. Assign to merged
#   10. Run merged.update(kwargs)
#   11. Assign to result
#   12. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions merge_dicts, merge_dicts, merge_dicts are just stored
#   Execution starts at the non-def lines below:
#      print(merge_dicts(?)) → function runs first, then print shows the result
#      merged = ... (stored in memory)
#      merged.update(kwargs)
#      result = ... (stored in memory)
#      print(result)
#      merged = ... (stored in memory)
#      merged.update(kwargs)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1383:
#
# ===== SYNTAX =====
#   def even_kwargs()
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   even_kwargs() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#   operators: % == and
#
# ===== EVALUATION ORDER =====
#   even_kwargs(): — no arguments needed
#   even_kwargs(): — no arguments needed
#   even_kwargs(): — no arguments needed
#   v % 2: first get v, then get 2, then compute v % 2
#   v % 2: first get v, then get 2, then compute v % 2
#   v % 2: first get v, then get 2, then compute v % 2
#   v % 2: first get v, then get 2, then compute v % 2
#   v % 2: first get v, then get 2, then compute v % 2
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#   ? == 0: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def even_kwargs() — stored for later
#   2. Run even_kwargs()
#         2.1 For loop over kwargs.items(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. For loop over kwargs.items(...)
#       5.1 If statement (one branch runs)
#   6. For loop over kwargs.items(...)
#       6.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_kwargs, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      even_kwargs() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from kwargs.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1384:
#
# ===== SYNTAX =====
#   def sorted_kwargs()
#   sorted() — built-in function call
#   (no return — the function prints directly instead of returning)
#   sorted_kwargs() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   sorted_kwargs(): — no arguments needed
#   sorted_kwargs(): — no arguments needed
#   sorted_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def sorted_kwargs() — stored for later
#   2. Run sorted_kwargs()
#         2.1 For loop over sorted(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. For loop over sorted(...)
#       5.1 Run print(?)
#   6. For loop over sorted(...)
#       6.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions sorted_kwargs, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      sorted_kwargs() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from sorted(...) each round

"""

"""
Problem 1385:
#
# ===== SYNTAX =====
#   def count_strings()
#   sum() — built-in function call
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   count_strings(): — no arguments needed
#   print(sum(?)): inner expression resolves first, then print() outputs
#   print(sum(?)): inner expression resolves first, then print() outputs
#   print(sum(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define def count_strings() — stored for later
#   2. Run count_strings()
#         2.1 Assign to result
#         2.2 Return from function
#   3. Define def count_strings() — stored for later
#   4. Define def count_strings() — stored for later
#   5. Run print(sum(...))
#   6. Run print(sum(...))
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_strings, count_strings, count_strings are just stored
#   Execution starts at the non-def lines below:
#      count_strings() → jumps into function, runs body, returns result → printed
#      print(sum(...))
#      print(sum(...))

"""

"""
Problem 1386:
#
# ===== SYNTAX =====
#   def check_truthy()
#   (no return — the function prints directly instead of returning)
#   check_truthy() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   check_truthy(): — no arguments needed
#   check_truthy(): — no arguments needed
#   check_truthy(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def check_truthy() — stored for later
#   2. Run check_truthy()
#         2.1 For loop over kwargs.items(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. For loop over kwargs.items(...)
#       5.1 Run print(?)
#   6. For loop over kwargs.items(...)
#       6.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check_truthy, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      check_truthy() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from kwargs.items(...) each round

"""

"""
Problem 1387:
#
# ===== SYNTAX =====
#   def starts_with_a()
#   (no return — the function prints directly instead of returning)
#   starts_with_a() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   starts_with_a(): — no arguments needed
#   starts_with_a(): — no arguments needed
#   starts_with_a(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def starts_with_a() — stored for later
#   2. Run starts_with_a()
#         2.1 For loop over kwargs.items(...)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. For loop over kwargs.items(...)
#       5.1 If statement (one branch runs)
#   6. For loop over kwargs.items(...)
#       6.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions starts_with_a, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      starts_with_a() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from kwargs.items(...) each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1388:
#
# ===== SYNTAX =====
#   def reverse_kwargs()
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(reverse_kwargs()): the inner reverse_kwargs() executes FIRST, then print() outputs the result
#   reverse_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def reverse_kwargs() — stored for later
#   2. Run print(reverse_kwargs(...))
#         2.1 Return from function
#   3. Define def reverse_kwargs() — stored for later
#   4. Define def reverse_kwargs() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions reverse_kwargs, reverse_kwargs, reverse_kwargs are just stored
#   Execution starts at the non-def lines below:
#      print(reverse_kwargs()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1389:
#
# ===== SYNTAX =====
#   def longest_key()
#   return — exits the function and sends back a value
#   max() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(longest_key()): the inner longest_key() executes FIRST, then print() outputs the result
#   longest_key(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def longest_key() — stored for later
#   2. Run print(longest_key(...))
#         2.1 Return from function
#   3. Define def longest_key() — stored for later
#   4. Define def longest_key() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions longest_key, longest_key, longest_key are just stored
#   Execution starts at the non-def lines below:
#      print(longest_key()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1390:
#
# ===== SYNTAX =====
#   def stringify_kwargs()
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(stringify_kwargs()): the inner stringify_kwargs() executes FIRST, then print() outputs the result
#   stringify_kwargs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def stringify_kwargs() — stored for later
#   2. Run print(stringify_kwargs(...))
#         2.1 Return from function
#   3. Define def stringify_kwargs() — stored for later
#   4. Define def stringify_kwargs() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions stringify_kwargs, stringify_kwargs, stringify_kwargs are just stored
#   Execution starts at the non-def lines below:
#      print(stringify_kwargs()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1391:
#
# ===== SYNTAX =====
#   def all_ints()
#   return — exits the function and sends back a value
#   all() — built-in function call
#   isinstance() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(all_ints()): the inner all_ints() executes FIRST, then print() outputs the result
#   all_ints(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def all_ints() — stored for later
#   2. Run print(all_ints(...))
#         2.1 Return from function
#   3. Define def all_ints() — stored for later
#   4. Define def all_ints() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions all_ints, all_ints, all_ints are just stored
#   Execution starts at the non-def lines below:
#      print(all_ints()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1392:
#
# ===== SYNTAX =====
#   def max_numeric()
#   return — exits the function and sends back a value
#   max() — built-in function call
#   isinstance() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(max_numeric()): the inner max_numeric() executes FIRST, then print() outputs the result
#   max_numeric(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def max_numeric() — stored for later
#   2. Run print(max_numeric(...))
#         2.1 Return from function
#   3. Define def max_numeric() — stored for later
#   4. Define def max_numeric() — stored for later
#   5. Assign to nums
#   6. Assign to result
#   7. Run print(result)
#   8. Assign to nums
#   9. Assign to result
#   10. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions max_numeric, max_numeric, max_numeric are just stored
#   Execution starts at the non-def lines below:
#      print(max_numeric()) → function runs first, then print shows the result
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      nums = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1393:
#
# ===== SYNTAX =====
#   def count_none()
#   return — exits the function and sends back a value
#   sum() — built-in function call
#   comprehension
#   operators: is
#
# ===== EVALUATION ORDER =====
#   print(count_none()): the inner count_none() executes FIRST, then print() outputs the result
#   count_none(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def count_none() — stored for later
#   2. Run print(count_none(...))
#         2.1 Return from function
#   3. Define def count_none() — stored for later
#   4. Define def count_none() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions count_none, count_none, count_none are just stored
#   Execution starts at the non-def lines below:
#      print(count_none()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1394:
#
# ===== SYNTAX =====
#   def print_types()
#   type() — built-in function call
#   (no return — the function prints directly instead of returning)
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print_types(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def print_types() — stored for later
#   2. Run print_types()
#         2.1 For loop over kwargs.items(...)
#   3. For loop over kwargs.items(...)
#       3.1 Run print(?)
#   4. For loop over kwargs.items(...)
#       4.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions print_types are just stored
#   Execution starts at the non-def lines below:
#      print_types() → jumps into function, runs body, returns result → printed
#      for loop runs its body once per item
#      for loop runs its body once per item
#
#   Loop: grabs one item from kwargs.items(...) each round

"""

"""
Problem 1395:
#
# ===== SYNTAX =====
#   def kv_pairs()
#   return — exits the function and sends back a value
#   list() — built-in function call
#
# ===== EVALUATION ORDER =====
#   print(kv_pairs()): the inner kv_pairs() executes FIRST, then print() outputs the result
#   kv_pairs(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def kv_pairs() — stored for later
#   2. Run print(kv_pairs(...))
#         2.1 Return from function
#   3. Define def kv_pairs() — stored for later
#   4. Define def kv_pairs() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions kv_pairs, kv_pairs, kv_pairs are just stored
#   Execution starts at the non-def lines below:
#      print(kv_pairs()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1396:
#
# ===== SYNTAX =====
#   def remove_falsy()
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(remove_falsy()): the inner remove_falsy() executes FIRST, then print() outputs the result
#   remove_falsy(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def remove_falsy() — stored for later
#   2. Run print(remove_falsy(...))
#         2.1 Return from function
#   3. Define def remove_falsy() — stored for later
#   4. Define def remove_falsy() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions remove_falsy, remove_falsy, remove_falsy are just stored
#   Execution starts at the non-def lines below:
#      print(remove_falsy()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1397:
#
# ===== SYNTAX =====
#   def has_list()
#   return — exits the function and sends back a value
#   any() — built-in function call
#   isinstance() — built-in function call
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(has_list()): the inner has_list() executes FIRST, then print() outputs the result
#   has_list(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def has_list() — stored for later
#   2. Run print(has_list(...))
#         2.1 Return from function
#   3. Define def has_list() — stored for later
#   4. Define def has_list() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions has_list, has_list, has_list are just stored
#   Execution starts at the non-def lines below:
#      print(has_list()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1398:
#
# ===== SYNTAX =====
#   def even_or_odd_kwargs()
#   len() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   even_or_odd_kwargs(): — no arguments needed
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#   len(...) % 2: first get len(...), then get 2, then compute len(...) % 2
#
# ===== EXECUTION ORDER =====
#   1. Define def even_or_odd_kwargs() — stored for later
#   2. Run even_or_odd_kwargs()
#         2.1 Assign to result
#         2.2 Return from function
#   3. Define def even_or_odd_kwargs() — stored for later
#   4. Define def even_or_odd_kwargs() — stored for later
#   5. Run print(?)
#   6. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions even_or_odd_kwargs, even_or_odd_kwargs, even_or_odd_kwargs are just stored
#   Execution starts at the non-def lines below:
#      even_or_odd_kwargs() → jumps into function, runs body, returns result → printed
#      print(?)
#      print(?)

"""

"""
Problem 1399:
#
# ===== SYNTAX =====
#   def clean_temp_keys()
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(clean_temp_keys()): the inner clean_temp_keys() executes FIRST, then print() outputs the result
#   clean_temp_keys(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def clean_temp_keys() — stored for later
#   2. Run print(clean_temp_keys(...))
#         2.1 Return from function
#   3. Define def clean_temp_keys() — stored for later
#   4. Define def clean_temp_keys() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions clean_temp_keys, clean_temp_keys, clean_temp_keys are just stored
#   Execution starts at the non-def lines below:
#      print(clean_temp_keys()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1400:
#
# ===== SYNTAX =====
#   def uppercase_keys_only()
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(uppercase_keys_only()): the inner uppercase_keys_only() executes FIRST, then print() outputs the result
#   uppercase_keys_only(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def uppercase_keys_only() — stored for later
#   2. Run print(uppercase_keys_only(...))
#         2.1 Return from function
#   3. Define def uppercase_keys_only() — stored for later
#   4. Define def uppercase_keys_only() — stored for later
#   5. Assign to result
#   6. Run print(result)
#   7. Assign to result
#   8. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions uppercase_keys_only, uppercase_keys_only, uppercase_keys_only are just stored
#   Execution starts at the non-def lines below:
#      print(uppercase_keys_only()) → function runs first, then print shows the result
#      result = ... (stored in memory)
#      print(result)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1402:
#
# ===== SYNTAX =====
#   def solve()
#   enumerate() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to fruits
#   2. For loop over enumerate(...)
#       2.1 Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to fruits
#   6. For loop over enumerate(...)
#       6.1 Run print(?)
#   7. Assign to fruits
#   8. For loop over enumerate(...)
#       8.1 Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      fruits = ... (stored in memory)
#      for loop runs its body once per item
#      fruits = ... (stored in memory)
#      for loop runs its body once per item
#      fruits = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from enumerate(...) each round

"""

"""
Problem 1403:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   zip() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to names
#   2. Assign to ages
#   3. Assign to combined
#   4. Run print(combined)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to names
#   8. Assign to ages
#   9. Assign to combined
#   10. Run print(combined)
#   11. Assign to names
#   12. Assign to ages
#   13. Assign to combined
#   14. Run print(combined)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      names = ... (stored in memory)
#      ages = ... (stored in memory)
#      combined = ... (stored in memory)
#      print(combined)
#      names = ... (stored in memory)
#      ages = ... (stored in memory)
#      combined = ... (stored in memory)
#      print(combined)
#      names = ... (stored in memory)
#      ages = ... (stored in memory)
#      combined = ... (stored in memory)
#      print(combined)

"""

"""
Problem 1404:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to original
#   2. Assign to swapped
#   3. Run print(swapped)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to original
#   7. Assign to swapped
#   8. Run print(swapped)
#   9. Assign to original
#   10. Assign to swapped
#   11. Run print(swapped)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      original = ... (stored in memory)
#      swapped = ... (stored in memory)
#      print(swapped)
#      original = ... (stored in memory)
#      swapped = ... (stored in memory)
#      print(swapped)
#      original = ... (stored in memory)
#      swapped = ... (stored in memory)
#      print(swapped)

"""

"""
Problem 1405:
#
# ===== SYNTAX =====
#   def solve()
#   all() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to all_even
#   3. Run print(all_even)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to all_even
#   8. Run print(all_even)
#   9. Assign to numbers
#   10. Assign to all_even
#   11. Run print(all_even)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      all_even = ... (stored in memory)
#      print(all_even)
#      numbers = ... (stored in memory)
#      all_even = ... (stored in memory)
#      print(all_even)
#      numbers = ... (stored in memory)
#      all_even = ... (stored in memory)
#      print(all_even)

"""

"""
Problem 1406:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to nested
#   2. Assign to flattened
#   3. Run print(flattened)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to nested
#   7. Assign to flattened
#   8. Run print(flattened)
#   9. Assign to nested
#   10. Assign to flattened
#   11. Run print(flattened)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      nested = ... (stored in memory)
#      flattened = ... (stored in memory)
#      print(flattened)
#      nested = ... (stored in memory)
#      flattened = ... (stored in memory)
#      print(flattened)
#      nested = ... (stored in memory)
#      flattened = ... (stored in memory)
#      print(flattened)

"""

"""
Problem 1407:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   map() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to uppercase_words
#   3. Run print(uppercase_words)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to uppercase_words
#   8. Run print(uppercase_words)
#   9. Assign to words
#   10. Assign to uppercase_words
#   11. Run print(uppercase_words)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      uppercase_words = ... (stored in memory)
#      print(uppercase_words)
#      words = ... (stored in memory)
#      uppercase_words = ... (stored in memory)
#      print(uppercase_words)
#      words = ... (stored in memory)
#      uppercase_words = ... (stored in memory)
#      print(uppercase_words)

"""

"""
Problem 1408:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   filter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to numbers
#   2. Assign to even_numbers
#   3. Run print(even_numbers)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to numbers
#   7. Assign to even_numbers
#   8. Run print(even_numbers)
#   9. Assign to numbers
#   10. Assign to even_numbers
#   11. Run print(even_numbers)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      numbers = ... (stored in memory)
#      even_numbers = ... (stored in memory)
#      print(even_numbers)
#      numbers = ... (stored in memory)
#      even_numbers = ... (stored in memory)
#      print(even_numbers)
#      numbers = ... (stored in memory)
#      even_numbers = ... (stored in memory)
#      print(even_numbers)

"""

"""
Problem 1409:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to items
#   2. Assign to unique_ordered
#   3. Run print(unique_ordered)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to items
#   7. Assign to unique_ordered
#   8. Run print(unique_ordered)
#   9. Assign to items
#   10. Assign to unique_ordered
#   11. Run print(unique_ordered)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      unique_ordered = ... (stored in memory)
#      print(unique_ordered)
#      items = ... (stored in memory)
#      unique_ordered = ... (stored in memory)
#      print(unique_ordered)
#      items = ... (stored in memory)
#      unique_ordered = ... (stored in memory)
#      print(unique_ordered)

"""

"""
Problem 1410:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Assign to sentence
#   3. Run print(sentence)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to words
#   7. Assign to sentence
#   8. Run print(sentence)
#   9. Assign to words
#   10. Assign to sentence
#   11. Run print(sentence)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      sentence = ... (stored in memory)
#      print(sentence)
#      words = ... (stored in memory)
#      sentence = ... (stored in memory)
#      print(sentence)
#      words = ... (stored in memory)
#      sentence = ... (stored in memory)
#      print(sentence)

"""

"""
Problem 1411:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to reversed_text
#   3. Run print(reversed_text)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to reversed_text
#   8. Run print(reversed_text)
#   9. Assign to text
#   10. Assign to reversed_text
#   11. Run print(reversed_text)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      reversed_text = ... (stored in memory)
#      print(reversed_text)
#      text = ... (stored in memory)
#      reversed_text = ... (stored in memory)
#      print(reversed_text)
#      text = ... (stored in memory)
#      reversed_text = ... (stored in memory)
#      print(reversed_text)

"""

"""
Problem 1412:
#
# ===== SYNTAX =====
#   def solve()
#   Counter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import Counter from collections
#   2. Assign to text
#   3. Assign to char_counts
#   4. Run print(char_counts)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import Counter from collections
#   8. Assign to text
#   9. Assign to char_counts
#   10. Run print(char_counts)
#   11. Import Counter from collections
#   12. Assign to text
#   13. Assign to char_counts
#   14. Run print(char_counts)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      char_counts = ... (stored in memory)
#      print(char_counts)
#      text = ... (stored in memory)
#      char_counts = ... (stored in memory)
#      print(char_counts)
#      text = ... (stored in memory)
#      char_counts = ... (stored in memory)
#      print(char_counts)

"""

"""
Problem 1413:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   chain() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import chain from itertools
#   2. Assign to list1
#   3. Assign to list2
#   4. Assign to list3
#   5. Assign to combined
#   6. Run print(combined)
#   7. Define def solve() — stored for later
#   8. Define def solve() — stored for later
#   9. Import chain from itertools
#   10. Assign to list1
#   11. Assign to list2
#   12. Assign to list3
#   13. Assign to combined
#   14. Run print(combined)
#   15. Import chain from itertools
#   16. Assign to list1
#   17. Assign to list2
#   18. Assign to list3
#   19. Assign to combined
#   20. Run print(combined)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      combined = ... (stored in memory)
#      print(combined)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      combined = ... (stored in memory)
#      print(combined)
#      list1 = ... (stored in memory)
#      list2 = ... (stored in memory)
#      list3 = ... (stored in memory)
#      combined = ... (stored in memory)
#      print(combined)

"""

"""
Problem 1414:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to capitalized
#   3. Run print(capitalized)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to capitalized
#   8. Run print(capitalized)
#   9. Assign to text
#   10. Assign to capitalized
#   11. Run print(capitalized)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      capitalized = ... (stored in memory)
#      print(capitalized)
#      text = ... (stored in memory)
#      capitalized = ... (stored in memory)
#      print(capitalized)
#      text = ... (stored in memory)
#      capitalized = ... (stored in memory)
#      print(capitalized)

"""

"""
Problem 1415:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   zip() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to matrix
#   2. Assign to transposed
#   3. Run print(transposed)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to matrix
#   7. Assign to transposed
#   8. Run print(transposed)
#   9. Assign to matrix
#   10. Assign to transposed
#   11. Run print(transposed)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      matrix = ... (stored in memory)
#      transposed = ... (stored in memory)
#      print(transposed)
#      matrix = ... (stored in memory)
#      transposed = ... (stored in memory)
#      print(transposed)
#      matrix = ... (stored in memory)
#      transposed = ... (stored in memory)
#      print(transposed)

"""

"""
Problem 1416:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Assign to name
#   3. Assign to city
#   4. Run print(name, city)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to data
#   8. Assign to name
#   9. Assign to city
#   10. Run print(name, city)
#   11. Assign to data
#   12. Assign to name
#   13. Assign to city
#   14. Run print(name, city)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      name = ... (stored in memory)
#      city = ... (stored in memory)
#      print(name, city)
#      data = ... (stored in memory)
#      name = ... (stored in memory)
#      city = ... (stored in memory)
#      print(name, city)
#      data = ... (stored in memory)
#      name = ... (stored in memory)
#      city = ... (stored in memory)
#      print(name, city)

"""

"""
Problem 1417:
#
# ===== SYNTAX =====
#   for
#   in
#
# ===== EVALUATION ORDER =====
#   print(dict(grouped)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to items
#   3. Assign to grouped
#   4. For loop over items
#       4.1 Run ?.append(item)
#   5. Run print(dict(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      grouped = ... (stored in memory)
#      for loop runs its body once per item
#      print(dict(...))
#
#   Loop: grabs one item from items each round

"""

"""
Problem 1418:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   groupby() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import groupby from itertools
#   2. Assign to data
#   3. Assign to grouped
#   4. Run print(grouped)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import groupby from itertools
#   8. Assign to data
#   9. Assign to grouped
#   10. Run print(grouped)
#   11. Import groupby from itertools
#   12. Assign to data
#   13. Assign to grouped
#   14. Run print(grouped)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      grouped = ... (stored in memory)
#      print(grouped)
#      data = ... (stored in memory)
#      grouped = ... (stored in memory)
#      print(grouped)
#      data = ... (stored in memory)
#      grouped = ... (stored in memory)
#      print(grouped)

"""

"""
Problem 1419:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to translation
#   3. Assign to result
#   4. Run print(result)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to text
#   8. Assign to translation
#   9. Assign to result
#   10. Run print(result)
#   11. Assign to text
#   12. Assign to translation
#   13. Assign to result
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      translation = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      translation = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      text = ... (stored in memory)
#      translation = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)

"""

"""
Problem 1420:
#
# ===== SYNTAX =====
#   def solve()
#   frozenset() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to fs1
#   2. Assign to fs2
#   3. Assign to mapping
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to fs1
#   8. Assign to fs2
#   9. Assign to mapping
#   10. Run print(?)
#   11. Assign to fs1
#   12. Assign to fs2
#   13. Assign to mapping
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      fs1 = ... (stored in memory)
#      fs2 = ... (stored in memory)
#      mapping = ... (stored in memory)
#      print(?)
#      fs1 = ... (stored in memory)
#      fs2 = ... (stored in memory)
#      mapping = ... (stored in memory)
#      print(?)
#      fs1 = ... (stored in memory)
#      fs2 = ... (stored in memory)
#      mapping = ... (stored in memory)
#      print(?)

"""

"""
Problem 1421:
#
# ===== SYNTAX =====
#   def solve()
#   list() — built-in function call
#   combinations() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import combinations from itertools
#   2. Assign to items
#   3. Assign to pairs
#   4. Run print(pairs)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import combinations from itertools
#   8. Assign to items
#   9. Assign to pairs
#   10. Run print(pairs)
#   11. Import combinations from itertools
#   12. Assign to items
#   13. Assign to pairs
#   14. Run print(pairs)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      items = ... (stored in memory)
#      pairs = ... (stored in memory)
#      print(pairs)
#      items = ... (stored in memory)
#      pairs = ... (stored in memory)
#      print(pairs)
#      items = ... (stored in memory)
#      pairs = ... (stored in memory)
#      print(pairs)

"""

"""
Problem 1422:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(list(queue)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Import deque from collections
#   2. Assign to queue
#   3. Run queue.append(1)
#   4. Run queue.append(2)
#   5. Run queue.append(3)
#   6. Assign to first
#   7. Run print(first, list(...))
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      queue = ... (stored in memory)
#      queue.append(1)
#      queue.append(2)
#      queue.append(3)
#      first = ... (stored in memory)
#      print(first, list(...))

"""

"""
Problem 1423:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to text
#   2. Assign to ?
#   3. Run print(before, after)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to text
#   7. Assign to ?
#   8. Run print(before, after)
#   9. Assign to text
#   10. Assign to ?
#   11. Run print(before, after)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      text = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(before, after)
#      text = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(before, after)
#      text = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(before, after)

"""

"""
Problem 1424:
#
# ===== SYNTAX =====
#   def solve()
#   sorted() — built-in function call
#   itemgetter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import itemgetter from operator
#   2. Assign to people
#   3. Assign to sorted_people
#   4. Run print(sorted_people)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import itemgetter from operator
#   8. Assign to people
#   9. Assign to sorted_people
#   10. Run print(sorted_people)
#   11. Import itemgetter from operator
#   12. Assign to people
#   13. Assign to sorted_people
#   14. Run print(sorted_people)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      people = ... (stored in memory)
#      sorted_people = ... (stored in memory)
#      print(sorted_people)
#      people = ... (stored in memory)
#      sorted_people = ... (stored in memory)
#      print(sorted_people)
#      people = ... (stored in memory)
#      sorted_people = ... (stored in memory)
#      print(sorted_people)

"""

"""
Problem 1425:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import suppress from contextlib
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Nothing runs at the top level — functions are only called externally

"""

"""
Problem 1426:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(any(lst)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(any(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(any(...))

"""

"""
Problem 1427:
#
# ===== SYNTAX =====
#   def solve()
#   sum() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: **
#
# ===== EVALUATION ORDER =====
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#   x ** 2: first get x, then get 2, then compute x ** 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to total
#   3. Run print(total)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to total
#   8. Run print(total)
#   9. Assign to lst
#   10. Assign to total
#   11. Run print(total)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)
#      lst = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)
#      lst = ... (stored in memory)
#      total = ... (stored in memory)
#      print(total)

"""

"""
Problem 1428:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   a & b: first get a, then get b, then compute a & b
#   a & b: first get a, then get b, then compute a & b
#   a & b: first get a, then get b, then compute a & b
#   a & b: first get a, then get b, then compute a & b
#   a & b: first get a, then get b, then compute a & b
#
# ===== EXECUTION ORDER =====
#   1. Assign to a
#   2. Assign to b
#   3. Run print(a & b)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Run print(a & b)
#   9. Assign to a
#   10. Assign to b
#   11. Run print(a & b)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(a & b)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(a & b)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(a & b)

"""

"""
Problem 1429:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   a | b: first get a, then get b, then compute a | b
#   a | b: first get a, then get b, then compute a | b
#   a | b: first get a, then get b, then compute a | b
#   a | b: first get a, then get b, then compute a | b
#   a | b: first get a, then get b, then compute a | b
#
# ===== EXECUTION ORDER =====
#   1. Run print(a | b)
#   2. Define def solve() — stored for later
#   3. Define def solve() — stored for later
#   4. Run print(a | b)
#   5. Run print(a | b)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      print(a | b)
#      print(a | b)
#      print(a | b)

"""

"""
Problem 1430:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#
# ===== EVALUATION ORDER =====
#   print(s.isdigit()): inner expression resolves first, then print() outputs
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Run print(s.isdigit(...))
#   3. Define def main() — stored for later
#   4. Run main()
#         4.1 Pass (placeholder)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      print(s.isdigit(...))
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1431:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to lst
#   6. Run print(?)
#   7. Assign to lst
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      print(?)

"""

"""
Problem 1432:
#
# ===== SYNTAX =====
#   def main()
#   (no return — the function prints directly instead of returning)
#   list() — built-in function call
#   zip() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to zipped
#   3. Run print(zipped)
#   4. Define def main() — stored for later
#   5. Run main()
#         5.1 Pass (placeholder)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      zipped = ... (stored in memory)
#      print(zipped)
#      main() → jumps into function, runs body, returns result → printed

"""

"""
Problem 1433:
#
# ===== SYNTAX =====
#   def solve()
#   defaultdict() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Import defaultdict from collections
#   2. Assign to d
#   3. Update ? += ...
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Import defaultdict from collections
#   8. Assign to d
#   9. Update ? += ...
#   10. Run print(?)
#   11. Import defaultdict from collections
#   12. Assign to d
#   13. Update ? += ...
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Python loads 3 import(s) first
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      top-level statement runs
#      print(?)
#      d = ... (stored in memory)
#      top-level statement runs
#      print(?)
#      d = ... (stored in memory)
#      top-level statement runs
#      print(?)

"""

"""
Problem 1434:
#
# ===== SYNTAX =====
#   for
#   in
#   operators: +
#
# ===== EVALUATION ORDER =====
#   print(dict(grouped)): inner expression resolves first, then print() outputs
#   ?.get(...) + 1: first get ?.get(...), then get 1, then compute ?.get(...) + 1
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Import defaultdict from collections
#   3. Assign to grouped
#   4. For loop over words
#       4.1 Run ?.append(word)
#   5. Run print(dict(...))
#   6. Assign to nested
#   7. For loop over data
#       7.1 Run nested.setdefault(key1, ?)
#       7.2 Assign to ?
#
# ===== EXECUTION FLOW =====
#   Python loads 1 import(s) first
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      grouped = ... (stored in memory)
#      for loop runs its body once per item
#      print(dict(...))
#      nested = ... (stored in memory)
#      for loop runs its body once per item
#
#   Loop: grabs one item from words each round

"""

"""
Problem 1435:
#
# ===== SYNTAX =====
#   def solve()
#   sorted() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   lambda
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to sorted_lst
#   3. Run print(sorted_lst)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to sorted_lst
#   8. Run print(sorted_lst)
#   9. Assign to lst
#   10. Assign to sorted_lst
#   11. Run print(sorted_lst)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      sorted_lst = ... (stored in memory)
#      print(sorted_lst)
#      lst = ... (stored in memory)
#      sorted_lst = ... (stored in memory)
#      print(sorted_lst)
#      lst = ... (stored in memory)
#      sorted_lst = ... (stored in memory)
#      print(sorted_lst)

"""

"""
Problem 1436:
#
# ===== SYNTAX =====
#   with
#
# ===== EVALUATION ORDER =====
#   print(f.read()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. With
#   2. With
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      top-level statement runs
#      top-level statement runs

"""

"""
Problem 1437:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(lst)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1438:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Assign to ?
#   3. Run print(a, b, c)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to lst
#   7. Assign to ?
#   8. Run print(a, b, c)
#   9. Assign to lst
#   10. Assign to ?
#   11. Run print(a, b, c)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      lst = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)
#      lst = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(a, b, c)

"""

"""
Problem 1439:
#
# ===== SYNTAX =====
#   def solve()
#   dict() — built-in function call
#   zip() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to keys
#   2. Assign to values
#   3. Assign to d
#   4. Run print(d)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to keys
#   8. Assign to values
#   9. Assign to d
#   10. Run print(d)
#   11. Assign to keys
#   12. Assign to values
#   13. Assign to d
#   14. Run print(d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      d = ... (stored in memory)
#      print(d)
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      d = ... (stored in memory)
#      print(d)
#      keys = ... (stored in memory)
#      values = ... (stored in memory)
#      d = ... (stored in memory)
#      print(d)

"""

"""
Problem 1440:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Run d.pop('b', None)
#   3. Run print(d)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Run d.pop('b', None)
#   8. Run print(d)
#   9. Assign to d
#   10. Run d.pop('b', None)
#   11. Run print(d)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      d.pop('b', None)
#      print(d)
#      d = ... (stored in memory)
#      d.pop('b', None)
#      print(d)
#      d = ... (stored in memory)
#      d.pop('b', None)
#      print(d)

"""

"""
Problem 1441:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to x
#   6. Run print(?)
#   7. Assign to x
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1442:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   try/except
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Try block (error handling)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to d
#   6. Try block (error handling)
#   7. Assign to d
#   8. Try block (error handling)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      top-level statement runs
#      d = ... (stored in memory)
#      top-level statement runs
#      d = ... (stored in memory)
#      top-level statement runs

"""

"""
Problem 1443:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to t
#   2. Assign to ?
#   3. Run print(x, y)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to t
#   7. Assign to ?
#   8. Run print(x, y)
#   9. Assign to t
#   10. Assign to ?
#   11. Run print(x, y)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      t = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y)
#      t = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y)
#      t = ... (stored in memory)
#      ? = ... (stored in memory)
#      print(x, y)

"""

"""
Problem 1444:
#
# ===== SYNTAX =====
#   operators: !=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(?)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(?)

"""

"""
Problem 1445:
#
# ===== SYNTAX =====
#   def solve()
#   Counter() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Assign to freq
#   3. Run print(freq)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to s
#   7. Assign to freq
#   8. Run print(freq)
#   9. Assign to s
#   10. Assign to freq
#   11. Run print(freq)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      freq = ... (stored in memory)
#      print(freq)
#      s = ... (stored in memory)
#      freq = ... (stored in memory)
#      print(freq)
#      s = ... (stored in memory)
#      freq = ... (stored in memory)
#      print(freq)

"""

"""
Problem 1446:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to lst
#   6. Run print(?)
#   7. Assign to lst
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      print(?)
#      lst = ... (stored in memory)
#      print(?)

"""

"""
Problem 1447:
#
# ===== SYNTAX =====
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(any(?)): inner expression resolves first, then print() outputs
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(any(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(any(...))

"""

"""
Problem 1448:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(lst.index('b')): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(lst.index(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(lst.index(...))

"""

"""
Problem 1449:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to filtered
#   3. Run print(filtered)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to filtered
#   8. Run print(filtered)
#   9. Assign to d
#   10. Assign to filtered
#   11. Run print(filtered)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      filtered = ... (stored in memory)
#      print(filtered)
#      d = ... (stored in memory)
#      filtered = ... (stored in memory)
#      print(filtered)
#      d = ... (stored in memory)
#      filtered = ... (stored in memory)
#      print(filtered)

"""

"""
Problem 1450:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   comprehension
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Assign to inverted
#   3. Run print(inverted)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d
#   7. Assign to inverted
#   8. Run print(inverted)
#   9. Assign to d
#   10. Assign to inverted
#   11. Run print(inverted)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      inverted = ... (stored in memory)
#      print(inverted)
#      d = ... (stored in memory)
#      inverted = ... (stored in memory)
#      print(inverted)
#      d = ... (stored in memory)
#      inverted = ... (stored in memory)
#      print(inverted)

"""

"""
Problem 1451:
#
# ===== SYNTAX =====
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1452:
#
# ===== SYNTAX =====
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1453:
#
# ===== SYNTAX =====
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1454:
#
# ===== SYNTAX =====
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   'a' not in word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Run print(all(...))
#   3. Assign to result
#   4. For loop over words
#       4.1 If statement (one branch runs)
#   5. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      print(all(...))
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1455:
#
# ===== SYNTAX =====
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to s
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      s = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1456:
#
# ===== SYNTAX =====
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   'a' not in word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to lines
#   2. Run print(all(...))
#   3. Assign to result
#   4. For loop over words
#       4.1 If statement (one branch runs)
#   5. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lines = ... (stored in memory)
#      print(all(...))
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1457:
#
# ===== SYNTAX =====
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to row
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      row = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1458:
#
# ===== SYNTAX =====
#   def main()
#   isinstance() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   main(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(all(...))
#   3. Define def main() — stored for later
#   4. Run main()
#         4.1 Assign to lst
#         4.2 Assign to result
#         4.3 For loop over lst
#         4.4 Assign to result
#         4.5 Return from function
#   5. Define def main() — stored for later
#   6. Define def main() — stored for later
#   7. Assign to lst
#   8. Assign to result
#   9. For loop over lst
#       9.1 If statement (one branch runs)
#   10. Run print(result)
#   11. Assign to lst
#   12. Assign to result
#   13. For loop over lst
#       13.1 If statement (one branch runs)
#   14. Run print(result)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions main, main, main are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(all(...))
#      main() → jumps into function, runs body, returns result → printed
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#      lst = ... (stored in memory)
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#
#   Loop: grabs one item from lst each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1459:
#
# ===== SYNTAX =====
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to d
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      d = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1460:
#
# ===== SYNTAX =====
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to files
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      files = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1461:
#
# ===== SYNTAX =====
#   comprehension
#   operators: <=
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to nums
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      nums = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1462:
#
# ===== SYNTAX =====
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to matrix
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      matrix = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1463:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(all(flags)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to flags
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      flags = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1464:
#
# ===== SYNTAX =====
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: in
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   'a' not in word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Run print(all(...))
#   3. Assign to result
#   4. For loop over words
#       4.1 If statement (one branch runs)
#   5. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(all(...))
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1465:
#
# ===== SYNTAX =====
#   comprehension
#   operators: <
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to nums
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      nums = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1466:
#
# ===== SYNTAX =====
#   comprehension
#   operators: and
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to envs
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      envs = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1467:
#
# ===== SYNTAX =====
#   comprehension
#   operators: ==
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to t
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      t = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1468:
#
# ===== SYNTAX =====
#   def is_prime(n)
#   return — exits the function and sends back a value
#   all() — built-in function call
#   range() — built-in function call
#   int() — built-in function call
#   comprehension
#   operators: + % ** != > and
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   is_prime(n): looks up variable 'n', then runs the function body
#   print(is_prime(15)): the inner is_prime() executes FIRST, then print() outputs the result
#   is_prime(15): gets the literal 15, then runs the function body
#   is_prime(x): looks up variable 'x', then runs the function body
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   n % i: first get n, then get i, then compute n % i
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   int(...) + 1: first get int(...), then get 1, then compute int(...) + 1
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#   n ** 0.5: first get n, then get 0.5, then compute n ** 0.5
#
# ===== EXECUTION ORDER =====
#   1. Define def is_prime(n) — stored for later
#   2. Assign to lst
#   3. Run print(all(...))
#   4. Define def is_prime(n) — stored for later
#   5. Define def is_prime(n) — stored for later
#   6. Assign to n
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(is_prime(...))
#       calls is_prime with: n=15
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions is_prime, is_prime, is_prime are just stored
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(all(...))
#      n = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(is_prime(15)) → function runs first, then print shows the result

"""

"""
Problem 1469:
#
# ===== SYNTAX =====
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(all(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(all(...))

"""

"""
Problem 1470:
#
# ===== SYNTAX =====
#   break
#   comprehension
#   for
#   if/elif/else
#   in
#   operators: >=
#
# ===== EVALUATION ORDER =====
#   print(all(?)): inner expression resolves first, then print() outputs
#   'a' not in word: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Assign to passwords
#   2. Run print(all(...))
#   3. Assign to result
#   4. For loop over words
#       4.1 If statement (one branch runs)
#   5. Run print(result)
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      passwords = ... (stored in memory)
#      print(all(...))
#      result = ... (stored in memory)
#      for loop runs its body once per item
#      print(result)
#
#   Loop: grabs one item from words each round
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1471:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to x
#   6. Run print(?)
#   7. Assign to x
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1472:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to a
#   2. Assign to b
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Run print(?)
#   9. Assign to a
#   10. Assign to b
#   11. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)

"""

"""
Problem 1473:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to a
#   2. Assign to b
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Run print(?)
#   9. Assign to a
#   10. Assign to b
#   11. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)

"""

"""
Problem 1474:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to flag
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to flag
#   6. Run print(?)
#   7. Assign to flag
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      flag = ... (stored in memory)
#      print(?)
#      flag = ... (stored in memory)
#      print(?)
#      flag = ... (stored in memory)
#      print(?)

"""

"""
Problem 1475:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: == is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to s1
#   2. Assign to s2
#   3. Run print(?)
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to s1
#   8. Assign to s2
#   9. Run print(?)
#   10. Assign to s1
#   11. Assign to s2
#   12. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      s1 = ... (stored in memory)
#      s2 = ... (stored in memory)
#      print(?)
#      print(?)
#      s1 = ... (stored in memory)
#      s2 = ... (stored in memory)
#      print(?)
#      s1 = ... (stored in memory)
#      s2 = ... (stored in memory)
#      print(?)

"""

"""
Problem 1476:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to a
#   2. Assign to b
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to a
#   7. Assign to b
#   8. Run print(?)
#   9. Assign to a
#   10. Assign to b
#   11. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)

"""

"""
Problem 1477:
#
# ===== SYNTAX =====
#   def do_nothing()
#   (no return — the function prints directly instead of returning)
#   operators: is
#
# ===== EVALUATION ORDER =====
#   do_nothing(): — no arguments needed
#
# ===== EXECUTION ORDER =====
#   1. Define def do_nothing() — stored for later
#   2. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions do_nothing are just stored
#   Execution starts at the non-def lines below:
#      print(?)

"""

"""
Problem 1478:
#
# ===== SYNTAX =====
#   class A — defines a new type
#   def solve()
#   A() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: == is
#
# ===== EVALUATION ORDER =====
#   print(A()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class A
#   2. Assign to a1
#   3. Assign to a2
#   4. Run print(?)
#   5. Run print(?)
#   6. Define def solve() — stored for later
#   7. Define def solve() — stored for later
#   8. Assign to obj
#   9. Run print(obj)
#   10. Run print(A(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a1 = ... (stored in memory)
#      a2 = ... (stored in memory)
#      print(?)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(A(...))

"""

"""
Problem 1479:
#
# ===== SYNTAX =====
#   def identity(x)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   identity(x): looks up variable 'x', then runs the function body
#   print(identity(3)): the inner identity() executes FIRST, then print() outputs the result
#   identity(3): gets the literal 3, then runs the function body
#   identity(obj): looks up variable 'obj', then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Define def identity(x) — stored for later
#   2. Assign to obj
#   3. Run print(?)
#   4. Define def identity(x) — stored for later
#   5. Define def identity(x) — stored for later
#   6. Assign to x
#   7. Assign to result
#   8. Run print(result)
#   9. Run print(identity(...))
#       calls identity with: x=3
#         9.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions identity, identity, identity are just stored
#   Execution starts at the non-def lines below:
#      obj = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(identity(3)) → function runs first, then print shows the result

"""

"""
Problem 1480:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to x
#   6. Run print(?)
#   7. Assign to x
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1481:
#
# ===== SYNTAX =====
#   class X — defines a new type
#   def solve()
#   X() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   print(X()): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Define class X
#   2. Assign to x1
#   3. Assign to x2
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to obj
#   8. Run print(obj)
#   9. Run print(X(...))
#
# ===== EXECUTION FLOW =====
#   Class definitions are processed (methods stored)
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x1 = ... (stored in memory)
#      x2 = ... (stored in memory)
#      print(?)
#      obj = ... (stored in memory)
#      print(obj)
#      print(X(...))

"""

"""
Problem 1482:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to d1
#   2. Assign to d2
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to d1
#   7. Assign to d2
#   8. Run print(?)
#   9. Assign to d1
#   10. Assign to d2
#   11. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      d1 = ... (stored in memory)
#      d2 = ... (stored in memory)
#      print(?)
#      d1 = ... (stored in memory)
#      d2 = ... (stored in memory)
#      print(?)
#      d1 = ... (stored in memory)
#      d2 = ... (stored in memory)
#      print(?)

"""

"""
Problem 1483:
#
# ===== SYNTAX =====
#   def solve()
#   object() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to a, b
#   2. Assert
#   3. Run print('Assertion passed')
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to a, b
#   7. Assert
#   8. Run print('Assertion passed')
#   9. Assign to a, b
#   10. Assert
#   11. Run print('Assertion passed')
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a, b = ... (stored in memory)
#      top-level statement runs
#      print('Assertion passed')
#      a, b = ... (stored in memory)
#      top-level statement runs
#      print('Assertion passed')
#      a, b = ... (stored in memory)
#      top-level statement runs
#      print('Assertion passed')

"""

"""
Problem 1484:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: < is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to x
#   2. Assign to y
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to x
#   7. Assign to y
#   8. Run print(?)
#   9. Assign to x
#   10. Assign to y
#   11. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      print(?)
#      x = ... (stored in memory)
#      y = ... (stored in memory)
#      print(?)

"""

"""
Problem 1485:
#
# ===== SYNTAX =====
#   def greet(name)
#   (no return — the function prints directly instead of returning)
#   greet() — built-in function call
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   if/elif/else
#
# ===== EVALUATION ORDER =====
#   greet('Alice'): gets the literal 'Alice', then runs the function body
#   greet(name): looks up variable 'name', then runs the function body
#   print(greet('Bob')): the inner greet() executes FIRST, then print() outputs the result
#   greet('Alice'): gets the literal 'Alice', then runs the function body
#   greet('Alice'): gets the literal 'Alice', then runs the function body
#   greet('Bob'): gets the literal 'Bob', then runs the function body
#   name  None: is this True or False? The answer picks which branch
#   name  None: is this True or False? The answer picks which branch
#   name  None: is this True or False? The answer picks which branch
#
# ===== EXECUTION ORDER =====
#   1. Define def greet(name) — stored for later
#   2. Run greet('Alice')
#       with: name='Alice'
#         2.1 If statement (one branch runs)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to name
#   6. Assign to result
#   7. Run print(result)
#   8. Run print(greet(...))
#       calls greet with: name='Bob'
#         8.1 If statement (one branch runs)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions greet, solve, solve are just stored
#   Execution starts at the non-def lines below:
#      greet('Alice') → jumps into function, runs body, returns result → printed
#      name = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(greet('Bob')) → function runs first, then print shows the result
#   Branch: only the True branch executes; the rest is skipped

"""

"""
Problem 1486:
#
# ===== SYNTAX =====
#   def check(x)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   print(check(value)): the inner check() executes FIRST, then print() outputs the result
#   check(x): looks up variable 'x', then runs the function body
#   print(check(3)): the inner check() executes FIRST, then print() outputs the result
#   check(value): looks up variable 'value', then runs the function body
#   check(3): gets the literal 3, then runs the function body
#
# ===== EXECUTION ORDER =====
#   1. Assign to SENTINEL
#   2. Define def check(x) — stored for later
#   3. Assign to value
#   4. Run print(check(...))
#       calls check with: x=value
#         4.1 Return from function
#   5. Define def check(x) — stored for later
#   6. Define def check(x) — stored for later
#   7. Assign to x
#   8. Assign to result
#   9. Run print(result)
#   10. Run print(check(...))
#       calls check with: x=3
#         10.1 Return from function
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions check, check, check are just stored
#   Execution starts at the non-def lines below:
#      SENTINEL = ... (stored in memory)
#      value = ... (stored in memory)
#      print(check(value)) → function runs first, then print shows the result
#      x = ... (stored in memory)
#      result = ... (stored in memory)
#      print(result)
#      print(check(3)) → function runs first, then print shows the result

"""

"""
Problem 1487:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to a
#   2. Assign to b
#   3. Assign to a
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to a
#   8. Assign to b
#   9. Assign to a
#   10. Run print(?)
#   11. Assign to a
#   12. Assign to b
#   13. Assign to a
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      a = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      a = ... (stored in memory)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      a = ... (stored in memory)
#      print(?)

"""

"""
Problem 1488:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#   operators: is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to NONE_CONST
#   2. Assign to x
#   3. Run print(?)
#   4. Define def solve() — stored for later
#   5. Define def solve() — stored for later
#   6. Assign to NONE_CONST
#   7. Assign to x
#   8. Run print(?)
#   9. Assign to NONE_CONST
#   10. Assign to x
#   11. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      NONE_CONST = ... (stored in memory)
#      x = ... (stored in memory)
#      print(?)
#      NONE_CONST = ... (stored in memory)
#      x = ... (stored in memory)
#      print(?)
#      NONE_CONST = ... (stored in memory)
#      x = ... (stored in memory)
#      print(?)

"""

"""
Problem 1489:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   return — exits the function and sends back a value
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to value
#   2. Run print(?)
#   3. Define def solve() — stored for later
#   4. Define def solve() — stored for later
#   5. Assign to value
#   6. Run print(?)
#   7. Assign to value
#   8. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      value = ... (stored in memory)
#      print(?)
#      value = ... (stored in memory)
#      print(?)
#      value = ... (stored in memory)
#      print(?)

"""

"""
Problem 1490:
#
# ===== SYNTAX =====
#   def solve()
#   (no return — the function prints directly instead of returning)
#   (no return — the function prints directly instead of returning)
#   operators: == is
#
# ===== EVALUATION ORDER =====
#   expressions evaluate left-to-right, inner calls first
#
# ===== EXECUTION ORDER =====
#   1. Assign to a
#   2. Assign to b
#   3. Run print(?)
#   4. Run print(?)
#   5. Define def solve() — stored for later
#   6. Define def solve() — stored for later
#   7. Assign to a
#   8. Assign to b
#   9. Run print(?)
#   10. Run print(?)
#   11. Assign to a
#   12. Assign to b
#   13. Run print(?)
#   14. Run print(?)
#
# ===== EXECUTION FLOW =====
#   def lines are SKIPPED during execution — functions solve, solve are just stored
#   Execution starts at the non-def lines below:
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      print(?)
#      a = ... (stored in memory)
#      b = ... (stored in memory)
#      print(?)
#      print(?)

"""

"""
Problem 1491:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(nums)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to nums
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      nums = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1492:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(min(values)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to values
#   2. Run print(min(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      values = ... (stored in memory)
#      print(min(...))

"""

"""
Problem 1493:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(words)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to words
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      words = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1494:
#
# ===== SYNTAX =====
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(next(?)): inner expression resolves first, then print() outputs
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to lst
#   2. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      lst = ... (stored in memory)
#      print(next(...))

"""

"""
Problem 1495:
#
# ===== SYNTAX =====
#   comprehension
#   operators: >
#
# ===== EVALUATION ORDER =====
#   print(next(gen)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to gen
#   2. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      gen = ... (stored in memory)
#      print(next(...))

"""

"""
Problem 1496:
#
# ===== SYNTAX =====
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(max(?)): inner expression resolves first, then print() outputs
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to nums
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      nums = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1497:
#
# ===== SYNTAX =====
#   comprehension
#   operators: % ==
#
# ===== EVALUATION ORDER =====
#   print(min(?)): inner expression resolves first, then print() outputs
#   x % 2: first get x, then get 2, then compute x % 2
#
# ===== EXECUTION ORDER =====
#   1. Assign to nums
#   2. Run print(min(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      nums = ... (stored in memory)
#      print(min(...))

"""

"""
Problem 1498:
#
# ===== SYNTAX =====
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(next(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to names
#   2. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      names = ... (stored in memory)
#      print(next(...))

"""

"""
Problem 1499:
#
# ===== SYNTAX =====
#
# ===== EVALUATION ORDER =====
#   print(max(data)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Assign to data
#   2. Run print(max(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      data = ... (stored in memory)
#      print(max(...))

"""

"""
Problem 1500:
#
# ===== SYNTAX =====
#   comprehension
#
# ===== EVALUATION ORDER =====
#   print(next(?)): inner expression resolves first, then print() outputs
#
# ===== EXECUTION ORDER =====
#   1. Run print(next(...))
#
# ===== EXECUTION FLOW =====
#   Execution starts at the non-def lines below:
#      print(next(...))

"""

